
> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Toward Efficient Agents: A Survey of Memory, Tool learning, and Planning》的理解。
![](<images/Toward-Efficient-Agents-01.png>)

> 上海人工智能实验室、复旦大学、中国科学技术大学、上海交通大学等团队在 2026 年 1 月发布了这篇关于高效 Agent 的综述。它关心的不是“Agent 能不能做复杂任务”，而是更现实的问题：当 Agent 接入记忆、工具和规划之后，调用变多、上下文变长、搜索变深、协作变复杂，怎样才能在成本可控的情况下真正跑起来？

## 核心问题

这篇综述的核心问题非常清楚：**如果 Agent 的能力来自记忆、工具学习和规划，那么这些能力带来的额外成本该如何控制**？

传统大模型的效率问题，通常围绕模型大小、推理延迟、生成 token 数展开。但 Agent 不一样。Agent 会反复观察环境、检索记忆、调用工具、执行计划、根据结果重试。也就是说，它的成本不只来自“生成了多少文字”，还来自**记忆读写、工具调用、搜索步骤、重试轮数和多智能体通信**。

所以这篇综述的关键改造，是把 Agent 效率拆成三条主线：**高效记忆、高效工具学习、高效规划**。记忆解决“历史信息如何压缩、管理和取用”；工具学习解决“如何少而准地调用工具”；规划解决“如何在推理深度、搜索宽度和协作开销之间做取舍”。

## 摘要

近几年，大语言模型（Large Language Model, LLM）不断被扩展成具备行动能力的智能体系统（agentic systems）。能力在提升，但效率经常被忽略，而效率恰恰决定 Agent 能不能进入真实部署。

这里把效率放在 Agent 的三个核心组件里讨论：记忆、工具学习和规划。成本不只包括延迟，还包括 token 消耗、执行步数、工具调用次数、重试成本等。虽然不同方法实现方式很不一样，但很多方法会落到相似原则上：通过压缩和管理限制上下文膨胀，通过强化学习奖励减少不必要的工具调用，通过受控搜索提升规划效率。

效率也不是单一指标。可以在固定成本预算下比较任务效果，也可以在相近任务效果下比较成本。更完整的看法，是把效果和成本放在一起看：理想 Agent 不是单纯最强，也不是单纯最省，而是在效果和成本之间更好地取平衡。

## 1 引言

Agent 系统把大语言模型从“回答问题的模型”推向“能持续行动的系统”。它们可以处理软件工程、科学发现、网页操作、长期对话等复杂任务。这里的关键变化，是模型不再一次性生成答案，而是进入一个循环：

```text
输入 -> 记忆上下文 -> 规划决策 -> 工具行动 -> 环境反馈 -> 下一轮
```

这个循环让 Agent 变强，也让它变贵。普通大模型的主要成本来自输入输出 token 和一次推理延迟；Agent 的成本还会随着记忆检索、工具调用、计划搜索、失败重试不断叠加。一个深度研究任务里，搜索 API 可能被调用数百次；一个多智能体系统里，多个 Agent 之间可能反复通信；一个长程任务里，历史上下文可能越积越长，最后变成 token 爆炸。

因此，高效 Agent 不是“小模型 Agent”的同义词。这里的“高效”指的是：**在任务成功率尽量高的前提下，减少 token、延迟、计算、工具调用、搜索步数和通信开销**。这就把 Agent 效率自然分成三条线：

- 高效记忆，压缩历史、管理存储、取回真正有用的信息；
- 高效工具学习，选择合适工具，减少无效调用，让工具调用服务于推理而不是拖慢推理；
- 高效规划，把推理看成受资源约束的控制过程，控制搜索深度、协作范围和重试次数。

图1先把高效 Agent 相关研究按时间和方向放在一起。

![](<images/Toward-Efficient-Agents-02.png>)

> 图1：高效 Agent 研究的发展轨迹。图中按照四个主要分支组织：记忆、工具学习、规划和基准测试。关键工作及其所属机构按照时间顺序排列，用来展示 2023 到 2025 年间该领域的发展和分类。

解释：
- 记忆方向从早期的对话记忆、经验记忆，发展到长期记忆、图记忆、分层记忆和多智能体共享记忆；
- 工具学习方向从能调用工具，走向工具选择、工具调用成本控制和带工具推理优化；
- 规划方向从单 Agent 的搜索、分解、预算控制，扩展到多 Agent 的拓扑优化、通信压缩和协作蒸馏；
- 基准测试方向则开始把 token、延迟、工具调用次数、路径成本等指标纳入评价。
图里真正重要的是：**Agent 效率已经不再是模型压缩问题，而是系统级设计问题**。

## 2 预备知识

### 2.1 Agent 形式化：在部分可观测环境里行动

先把 Agent 写成一个带工具和记忆的**部分可观测马尔可夫决策过程**（Partially Observable Markov Decision Process, POMDP）。形式化写法是：

$$
M = (S, O, A, P, R, \gamma; T, \Psi; M_{\mathrm{mem}}, U, \rho).
$$

这里每个符号对应 Agent 系统里的一块：

- $S$ 是环境真实状态空间，很多时候 Agent 看不到完整状态；
- $O$ 是观察空间，也就是 Agent 实际能看到的信息；
- $A$ 是动作空间，包括语言动作、工具调用、环境交互等；
- $P$ 是状态转移规则，描述动作之后环境如何变化；
- $R$ 是奖励函数，用来衡量动作或轨迹好不好；
- $\gamma$ 是折扣因子，用来平衡当前收益和未来收益；
- $T$ 是工具集合，比如搜索、代码解释器、数据库、API；
- $\Psi$ 是工具接口，规定工具如何被调用、如何返回结果；
- $M_{\mathrm{mem}}$ 是记忆状态空间；
- $U$ 是记忆更新规则，决定新观察、新经验如何写入记忆；
- $\rho$ 是初始记忆分布。

这个形式化的意思很直接：**Agent 不只是一个语言模型，而是一个在环境中循环行动的系统**。语言模型负责生成动作或推理，但**工具、记忆、环境反馈**也都进入了系统状态。

### 2.2 从纯大模型到 Agent：成本来源变复杂了

效率要基于**成本 - 性能权衡**来定义：一是在固定资源开销下对比模型性能，二是在性能持平的前提下对比资源消耗。

纯大模型推理的成本可以粗略写成：

$$
\mathrm{Cost}_{\mathrm{LLM}} \approx \alpha N_{\mathrm{tok}}.
$$

其中 $N_{\mathrm{tok}}$ 是生成的 token 数，$\alpha$ 是单个 token 的成本。这个公式很粗，但足够说明普通大模型的主要成本来自语言生成。

Agent 的成本更复杂：

$$
\mathrm{Cost}_{\mathrm{agent}} \approx \alpha N_{\mathrm{tok}} + I_{\mathrm{tool}} \cdot \mathrm{Cost}_{\mathrm{tool}} + I_{\mathrm{mem}} \cdot \mathrm{Cost}_{\mathrm{mem}} + I_{\mathrm{retry}} \cdot \mathrm{Cost}_{\mathrm{retry}}.
$$

这里 $I_{\mathrm{tool}}$、$I_{\mathrm{mem}}$、$I_{\mathrm{retry}}$ 是指示变量，只要发生工具调用、记忆访问或失败重试，对应成本就会出现。$\mathrm{Cost}_{\mathrm{tool}}$ 是工具调用成本，$\mathrm{Cost}_{\mathrm{mem}}$ 是记忆读写和检索成本，$\mathrm{Cost}_{\mathrm{retry}}$ 是重试带来的额外成本。

**这条公式是全篇的效率底座**。Agent 效率不能只盯着 token 数，还要同时看工具、记忆和重试。一个 Agent 生成文字很少，但如果调用工具 600 次，它仍然可能非常低效；反过来，一个 Agent 多写一点推理过程，但能避免无效工具调用，也可能总体更省。

图2把纯大模型和 Agent 的成本差异放在同一张图里。

![](<images/Toward-Efficient-Agents-03.png>)

> 图2：从大语言模型到 Agent：从独立推理走向带记忆、规划和工具学习的轨迹级推理，同时也引入了额外的成本来源。

解释：普通大模型更像一次性推理，输入问题后生成答案；Agent 则会在多轮轨迹中工作：
- 记忆提供历史上下文，
- 规划决定下一步，
- 工具学习负责选择和调用外部工具。
能力变强的同时，系统也多了几类成本：更长上下文、更长行动轨迹、更多工具调用、更多失败重试。后文的记忆、工具学习、规划三部分，其实都在回答同一个问题：**这些额外能力怎样才能不把系统成本拖垮**。

## 3 高效记忆

记忆是 Agent 效率里最容易被低估的一部分。很多人会把记忆理解成“把历史都存下来”，但这在长程任务里很快失效。历史越长，token 越多；信息越杂，检索越慢；无关记忆越多，模型越容易被干扰。

高效记忆要解决的是三件事：先把信息构造成可存储的形式，再不断管理这些记忆，最后在需要时选出少量真正有用的内容。

图3给出高效记忆的整体生命周期。

![](<images/Toward-Efficient-Agents-04.png>)

> 图3：高效记忆概览。图中把 Agent 记忆生命周期总结为三个阶段：记忆构建，在工作记忆和外部记忆中压缩长交互上下文，以缓解 token 爆炸；记忆管理，通过基于规则、基于大语言模型或混合策略整理和更新不断累积的记忆存储，以控制延迟；记忆访问，决定检索哪些记忆，以及如何把这些记忆整合进模型。

解释：
- 记忆构建阶段，历史交互会被压缩进工作记忆或外部记忆，工作记忆又包括文本记忆和潜在记忆，外部记忆包括条目式记忆、图记忆和分层记忆。
- 记忆管理阶段，系统要不断对记忆做增加、更新、删除或不操作，管理器可以来自规则、大语言模型或二者混合。
- 记忆访问阶段，系统要先从大量记忆里选出相关内容，再通过文本拼接、压缩过滤或潜在状态注入的方式交给模型。
这里的主线不是“记得越多越好”，而是**记忆必须被压缩、筛选、更新和有选择地使用**。

接下来，表1把这些记忆机制对应到具体代表方法。

![](<images/Toward-Efficient-Agents-05.png>)

> 表1：面向效率的记忆机制概览。表格按照这里提出的分类组织，覆盖工作记忆、外部记忆和多智能体记忆。

解释：**工作记忆**主要关注如何压缩当前上下文，**外部记忆**关注如何把历史经验存到模型外部，**多智能体记忆**关注多个 Agent 如何共享、路由或压缩彼此的上下文。这个分类很重要，因为不同记忆方式的效率瓶颈不同：工作记忆怕上下文太长，外部记忆怕检索太慢，多智能体记忆怕通信和同步成本太高。

### 3.1 记忆构建：先把历史变成能用的记忆

长上下文任务和长期交互任务都会遇到同一个问题：原始历史不能一直往提示词里塞。token 会快速增长，相关信息还可能被埋在中间，出现“中间遗忘”（lost in the middle）现象。记忆构建的目标，就是把过去的信息压缩成更可控的表示。

这里的“构建”不是简单保存，而是一次信息重写：把原始交互里的任务状态、用户偏好、错误经验、环境反馈，变成后续能检索、能更新、能放回上下文的记忆单元。这样做节省的不是某一次生成，而是后面每一轮推理的上下文成本。

#### 3.1.1 工作记忆：当前推理里随身携带的信息

**工作记忆**（Working Memory）不是长期档案，更像 Agent 当前任务里“随身携带”的信息。这里分成两类：文本记忆和潜在记忆。

**文本记忆最直观，主流思路是将工作记忆的长度维持在固定区间，通过把历史对话、用户偏好、关键事件、任务状态动态重写、实时压缩实现这一目标**。比如 COMEDY 会从会话里抽取关键事件、用户画像、关系变化，再压缩成后续可用的记忆；MemAgent 和 MEM1 会顺序处理长输入，每次更新一个紧凑状态；AgentFold 则把历史折叠成多尺度摘要，同时保留最近一轮的完整信息。它们的共同思路是：**不保留全部历史，而是保留对下一步有用的状态**。

这种文本工作记忆的优势是可读、可调、容易插入提示词。缺点也很明确：它仍然占 token，而且摘要一旦写错，后续步骤会一直继承这个错误。所以这类方法真正要平衡的是摘要压缩率和信息保真度。压得太轻，省不了多少；压得太狠，关键细节会丢。

**潜在记忆（Latent Memory）更靠近模型内部表示，它不一定把记忆写成自然语言，而是利用隐藏状态、键值缓存（KV cache）或特殊记忆 token 存储信息**。Activation Beacon 把层级键值激活压缩成 beacon tokens；MemoRAG 使用记忆 token 和键值缓存；MemoryLLM 维护固定大小的记忆池；M+ 用两级 GPU/CPU 记忆和检索器降低长上下文服务成本；Memory3 通过稀疏键值缓存降低推理开销；Titans 引入测试时神经长期记忆；MemGen 则按需生成潜在记忆。潜在记忆的优势是更省 token，但解释性和可控性通常弱一些。

潜在记忆的效率点更偏系统层面，将长交互历史压缩为精简隐状态，按需更新，借助注意力机制或简易接口供决策模块调用，让智能体在无需重复加载完整文本轨迹的前提下，使用长周期历史信息。它希望把“必须写进上下文的文字”变成“模型内部可以直接利用的状态”，从而减少显式 token。问题是，潜在状态不像文本摘要那样容易检查。对于真实 Agent 来说，这意味着潜在记忆可能更高效，但调试、审计和纠错难度也更高。

**工作记忆总结**

- 优势：推理过程可直接调用，无外部检索、重复编码带来的延迟与开销；
- 劣势：文本型记忆扩容会提升计算量，潜在记忆会增大显存占用；长上下文易出现信息稀释，导致性能下降；
- 适用场景：文本型记忆适合上下文长度适中、侧重逻辑推理的任务；潜在记忆对延迟敏感、需要复用历史状态的高效场景更友好。

#### 3.1.2 外部记忆：把长期信息放到模型外

**外部记忆**（External Memory）把信息放在模型外部，通常**以文档、条目、向量库、知识图谱或分层存储形式存在**。它的好处是容量大、可更新、可检索；问题是每次检索和整合都会带来额外成本。

**条目式记忆把经验拆成一条条可检索记录**。MemoryBank 记录日常事件和摘要，但会带来较高 token 成本；Expel 从试错中抽取经验；Human-like memory 把情景记忆用于长期交互；SeCom 会做主题一致的分段和压缩；Memory-R1 和 Mem0 会从对话中抽取候选记忆，再决定是否写入；计划缓存（plan caching）会复用过去的计划，减少重复规划。LD-Agent、MemoChat、RMM、Memento、MemInsight、ReasoningBank、A-MEM、ACE、Agent KB 等，也都在围绕“怎样把经验变成可重用条目”做不同设计。

条目式记忆适合存“一个事实、一次经验、一个偏好、一个失败原因”。它的效率来自复用：过去踩过的坑不用再踩，过去写过的计划不用重写，过去确认过的偏好不用重新询问。但条目多了之后会出现重复、冲突和过时，所以它必须和后面的记忆管理、记忆访问一起看。

**图记忆把实体、关系、事件组织成图结构**。GraphReader、KG-Agent、Zep、Mem0g、AriGraph、D-SMART 等方法，用图来表达记忆之间的连接。相比一条条独立文本，图结构更适合处理跨实体、跨事件、跨时间的关联，但构图、更新和路径搜索也会增加复杂度。

图记忆的优势是能把“相关性”显式存出来。比如某个用户、某个项目、某个工具、某次失败之间有联系，图结构比普通向量检索更容易沿关系找到上下文。它节省的是盲目检索成本，但会增加建图和维护成本。

**分层记忆把短期、中期、长期信息放在不同层级**。MemGPT、MemoryOS、MemOS、ReadAgent、HiAgent、H-MEM、LightMem 等都属于这一类。分层的好处是可以按重要性和使用频率分配成本：最近的信息放在更容易访问的位置，长期历史压缩到更远的层级，需要时再调出来。

分层记忆更接近真实系统里的缓存设计。最常用、最紧急的信息放在近处，长期但低频的信息放在远处。这样每次推理不用扫完整个历史，也不用把所有长期记忆都塞进上下文。**分层的核心价值，是用结构减少检索范围**。

**外部记忆总结**

- 优势：模型外部存储，容量近乎无上限，通过精准检索避免上下文溢出；
- 劣势：新增系统开销与检索延迟，易引入检索噪声；
- 适用场景：条目型记忆适配通用长轨迹智能体；图结构记忆适合实体关系、多跳推理任务；层级化记忆适配超长历史、海量语料的粗粒度 + 细粒度检索场景。

### 3.2 记忆管理：记忆不能只增加，还要会更新和删除

如果只会写入、不管理，记忆系统很快会变成垃圾堆。记忆越多，检索越慢；相似记忆越多，冲突越多；过时记忆不删除，后续判断还可能被误导。因此记忆管理要处理增加、更新、合并、删除和保留。

这里的效率问题很实际：记忆写入越积极，短期看信息更完整，长期看系统越臃肿。**记忆管理就是给记忆系统加一个“维护层”，让它知道哪些内容该保留，哪些内容该合并，哪些内容该遗忘**。

#### 3.2.1 基于规则的管理：便宜、稳定，但不够懂任务

基于规则的管理（Rule-based Management）用**固定规则处理记忆**。例如：
- **遗忘曲线规则**：MemoryBank 借鉴艾宾浩斯遗忘曲线，让记忆随时间自然衰减，同时强化重要内容；H-MEM 结合遗忘规则与用户反馈动态调整记忆权重。这类规则可以控制记忆体量、缩短检索时间，但也可能牺牲任务性能。
- **缓冲区淘汰规则**：当定长缓冲区存满后，采用先进先出（FIFO）等规则淘汰内容，MemGPT、MemoryOS 均采用该方案；此类规则常与大语言模型结合，在内容迁移、删除前先做摘要留存。

这种方式的优点是成本低、行为可预测，不需要每次调用大模型判断。问题也明显：规则通常不理解任务语义。某条很久没用的记忆可能正好对当前任务关键，某条很新的记忆也可能完全没价值。

所以基于规则的方法**适合做“底线控制”**：限制容量、定期清理、按时间迁移、按访问频次保留。它能把系统成本压住，但很难独立承担复杂语义判断。

#### 3.2.2 基于大语言模型的管理：更懂内容，但更贵

基于大语言模型的管理（LLM-based Management）**让模型参与记忆操作决策**：
- 一种是**让模型在固定动作里选择**，比如新增、更新、删除或不操作。Memory-R1 和 Mem0 会先检索相似记忆，再由模型决定具体操作；RMM 会判断当前记忆应该合并到已有主题里，还是作为新记忆加入；ExpeL 支持对经验列表执行新增、编辑、强化、削弱等操作，用来修正错误经验或降低过时经验的影响。
- 另一种是**让模型直接生成新的记忆内容**，完成更开放的更新。A-MEM 会在检索相似笔记后，由模型生成关联信息并更新相关内容。

**这种方式更灵活，也更懂任务语义**。它可以判断“这条新信息是旧信息的补充，还是相互冲突，还是根本不重要”。但是它会增加额外调用，带来延迟和费用。**高质量记忆管理本身也会消耗资源**，这就是效率问题的矛盾点。

这里其实形成了一个**小闭环：大模型帮记忆系统变聪明，记忆系统再帮大模型少走弯路**。只有当被节省的后续成本大于管理成本时，这个设计才真正高效。

#### 3.2.3 混合管理：把贵的判断用在关键位置

混合管理（Hybrid Management）试图**在规则和大模型之间取平衡**。简单说，就是**先用便宜规则做触发、筛选或分层，再把复杂判断交给大模型**。

混合管理则更适合真实系统：便宜、稳定的部分交给规则，比如容量控制、时间衰减、分层迁移、相似度过滤；需要语义判断的部分再交给大语言模型，比如判断是否合并、是否冲突、是否值得长期保留。

MemoryOS 和 LightMem 会针对不同记忆层级使用不同策略；LD-Agent 用时间间隔等规则触发记忆整理；MemGPT 把主上下文和外部上下文分开，用队列管理器和函数执行器协调；Agent KB 和 ACE 也会对记忆条目做选择、修剪和整理。

这样既不会让每条记忆都触发模型调用，也能避免纯规则完全不懂内容。不必每条记忆都让大模型判断，而是在最需要语义理解的地方才花成本。

这也是更偏落地的方向：用规则控制频率和范围，用大模型处理少数高价值判断。**混合管理不是折中凑合，而是把不同成本的操作放到合适位置**。

### 3.3 记忆读取：不是找最多，而是找对、放对

**记忆读取（Memory Access）**包括两步：
- 先从海量记忆库中筛选出与当前查询相关的子集；
- 再决定怎样把这些内容交给模型。
这里的核心不是召回越多越好，而是在**检索延迟、token 开销和生成质量**之间取得平衡。检索太少，关键经验可能缺失；检索太多，又会拖慢系统，并把无关内容塞进上下文。

#### 3.3.1 记忆筛选：从语义相似到规则、图结构和工具化检索

最普通的做法是**向量相似度检索**，把当前问题和历史记忆编码成向量，再找最相近的条目。但 Agent 的记忆不只是语义相似，还涉及**时间、重要性、检索频次、角色和任务阶段**等因素。所以后续方法通常会在基础相似度之外，再叠加规则、图结构、工具调用或训练式排序。

**规则增强检索**会在语义相似度之外补充时间、重要性、检索频次等规则打分。Generative Agents 会同时考虑**新近性**和**重要性**；Human-like memory 加入经过时间和召回频率；Agent KB 结合词法匹配与语义排序；LD-Agent 融合语义相似度、主题重叠度和时间衰减做综合打分；MemInsight 依托属性标签辅助筛选和检索。这类方法适合规则明确、预算紧张的场景，因为它不用每次都让大模型完整判断。

**图结构检索**依托图谱邻接关系扩展检索范围，更适合实体关系明显、多跳依赖较强的任务。GraphReader 做粗到细的图阅读；KG-Agent 在知识图谱局部跳转；Zep 维护时间知识图谱；Mem0g 更新图节点；AriGraph 从语义记忆级联到情景记忆；D-SMART 使用 OWL 知识图谱。图检索的优势是能沿着实体和关系找到关联信息，但路径扩展如果不受控，也会增加检索成本。

**基于大语言模型或工具的检索**会把记忆层级、检索逻辑或图操作封装成模型可调用的工具，让模型自己决定什么时候查、查哪里、查多深。MemGPT 把不同记忆层级做成可操作接口，让模型在 token 预算约束下选择操作；ReadAgent、MemoChat 会让模型判断检索时机和检索范围；GraphReader、D-SMART 这类方法则可以借助图操作完成由粗到细的多跳检索。这类方案精度更高，但 token 和延迟开销也更大，更适合低频次、高优先级任务。

**层级化检索**匹配分层记忆结构，先定位大层级，再进入更细粒度的记忆。MemoryOS、ReadAgent、HiAgent、H-MEM、LightMem 等方法，会通过层级结构逐步缩小检索范围。这样比在全部历史里平铺检索更稳，也更适合超大规模记忆库。

还有一类更偏**自适应排序**。RMM、Memento 这类方法会利用历史反馈、记忆价值或学习式排序机制，让系统优先选择更可能有用的记忆。它们适合长期运行、数据分布持续变化的系统，但训练和在线更新本身也会带来额外成本。

**适用场景总结**：规则增强检索适配规则明确、预算紧张的场景；图检索适配实体关系、多跳推理；大语言模型 / 工具检索适配低频次、高精度要求场景；层级检索适配超大规模记忆库；训练式检索适配长期运行、数据分布持续变化的系统。

#### 3.3.2 记忆融合：检索到了，还要放得进去、用得起来

取回记忆后，还要**决定怎样放进模型**。**记忆融合**决定如何使用检索到的内容，通过过滤、压缩、结构化插入或潜在状态注入，降低生成阶段的上下文开销。这里**不是把检索结果原样塞回提示词，而是要把它变成模型更容易利用的形式**。

**文本融合**最常见：**把检索文本精简、重组后注入提示词，形成提示词片段、摘要、清单、策略条目或操作指南**。DC-RS 使用 cheatsheet；Mem0 把记忆做成紧凑记忆块；A-MEM 受 Zettelkasten 启发，把笔记组织成小工作集；ACE 用 playbook bullet 形式放入上下文；计划缓存会把过去可复用计划作为参考；RECOMP 这类检索 - 压缩流水线则会先检索长内容，再压缩成少量核心片段。

**潜在融合**则**把长期信息以压缩隐状态、KV 缓存或内部表示的形式交给模型，避免原始文本反复进入上下文**。MemoryLLM、M+、Memory3、MemoRAG 等方法，会把知识或长上下文转成固定大小的隐状态、记忆池或 KV 条目，推理时直接并入模型状态或注意力缓存。它的好处是长周期信息的开销不会完全随原始文本长度线性增长；代价是实现更复杂，也更难解释和调试。

### 3.4 多智能体记忆：共享能省成本，也会带来同步成本

多智能体系统（Multi-Agent System, MAS）里，早期工作更多关注智能体之间的文字通信，记忆通常只是隐含在对话历史里。后来的工作开始把**多智能体记忆**单独拿出来设计：多个 Agent 不能只是各自把历史塞进提示词，否则很快会出现重复上下文、重复检索和重复推理。

这里可以按原文分成三类：**共享记忆、本地记忆和混合记忆**。三者解决的问题不一样：共享记忆强调跨 Agent 复用，本地记忆强调每个 Agent 保留自己的角色状态，混合记忆则试图在二者之间分工。

（1）**共享记忆**

共享记忆会搭建一个**全局记忆池**，供所有智能体复用，避免多智能体交互轨迹被重复存到每个 Agent 的提示词里，从而减少 token 和推理开销。

- **文本型共享记忆**：MS、G-Memory、RCR-Router、MemIndex、MIRIX 分别通过内容筛选、层级图结构、token 预算路由、索引图谱和模块化记忆管理，优化全局记忆的存储、检索与分发。
- **潜在共享记忆**：LatentMAS、KVComm 共享压缩后的潜在状态或 KV 缓存，实现跨智能体的状态复用，避免重复编码和重复预填充。

共享记忆的**优势**是跨智能体复用有效信息，减少重复工作与重试，提升协同效率；**劣势**是并发写入容易造成数据不一致，如果缺少内容筛选和访问控制，也容易积累噪声、增加检索成本。

（2）**本地记忆**

本地记忆让每个智能体拥有独立的私有记忆空间，检索和更新都在本地完成，并借鉴单智能体记忆里的选择性写入、删减和容量控制策略。AgentNet、DAMCS、Intrinsic Memory Agents 分别为单个智能体设计固定大小记忆模块、分层知识图谱和角色化记忆模板，不把所有内容直接放进全局共享池。

本地记忆的**优势**是轻量化、噪声少，更适配智能体的专属角色和个性化提示；**劣势**是有效信息无法自然跨智能体传播，不同 Agent 容易重复探索同一个问题。

（3）**混合记忆**

混合记忆结合本地私有记忆与全局共享记忆，重点是区分内容的存储位置和检索时机，同时控制冗余。SRMT 把个人记忆向量与全局循环记忆结合起来；Collaborative Memory 使用私有 / 共享双层存储和动态访问图；LEGOMem 构建模块化任务记忆，让协调者保留完整任务记忆，子任务 Agent 保留子任务记忆。这样既保留每个 Agent 的个性化状态，也让关键经验可以被全局复用。

混合记忆的**优势**是同时具备本地记忆的轻量化和共享记忆的知识复用能力；**劣势**是同步、路由和权限控制更复杂，共享存储仍然可能出现不一致和噪声问题。

所以，多智能体记忆的核心不是“共享越多越好”。共享太少会重复劳动，共享太多又会造成通信和检索负担。真正有效的设计，要让关键经验在 Agent 之间流动，同时避免把所有历史都广播给所有人。

### 3.5 讨论：记忆效率的两个取舍

第一是压缩和效果的取舍，压缩越强，token 越省，但信息损失也越大。LightMem 这类方法展示了压缩率和性能之间的关系：过度压缩会让模型失去必要细节，压缩不足又控制不了成本。

第二是在线管理和离线管理的取舍，在线管理可以马上适应新信息，但会增加推理时延迟；离线管理可以把整理成本放到任务外，但适应性更慢。比较现实的方案往往不是二选一，而是把便宜的在线操作和较重的离线整理结合起来。

## 4 高效工具学习

工具学习（Tool Learning）让大语言模型连接搜索、代码解释器、数据库、API、文件系统等外部能力。工具能缩短纯语言推理，也能让 Agent 获得实时信息和可执行能力。但工具不是免费的，复杂任务里工具调用会快速变多，调用结果也会不断拉长上下文。

所以**工具学习的效率有两层含义**：
- 一方面，工具本身可以帮助模型更快解决问题；
- 另一方面，工具使用过程也要被优化，避免无效调用、重复调用和过长轨迹。

图4把高效工具学习拆成工具选择、工具调用和带工具推理三步。

![](<images/Toward-Efficient-Agents-06.png>)

> 图4：高效工具学习包含三个阶段：工具选择，通过检索或分类识别候选工具；工具调用，处理参数填充和执行，并重点考虑成本约束与预算反馈；带工具推理，通过选择性调用和策略优化来优化高效推理轨迹。

解释：工具选择阶段先从工具库中找候选工具，可以用外部检索、多标签分类或把工具编码成词表 token。工具调用阶段负责参数填充、并行调用、成本感知调用、测试时搜索和后训练优化。带工具推理阶段不只关心“会不会调用”，还关心什么时候调用、调用多少次、工具结果怎样进入后续推理。
这里的关键是：**工具学习的效率来自少调用、准调用，以及让工具结果真正改变推理轨迹**。

表2列出这三类工具学习方法的代表工作。

![](<images/Toward-Efficient-Agents-07.png>)

> 表2：代表性高效工具方法总结。表中按照工具选择、工具调用和带工具推理进行分类。

解释：工具选择关注从大量工具里找到可用工具；工具调用关注参数填充、并行执行、成本控制和训练后优化；带工具推理关注把工具调用嵌入推理过程，并通过选择性调用或策略优化减少无效轨迹。这个表的作用，是把“工具使用”从一个笼统能力拆成可以分别优化的阶段。

### 4.1 工具选择：先别把所有工具都塞给模型

工具越多，直接把工具说明全部放进提示词越不现实。**工具选择的目标，是在调用前筛出少量候选工具，减少提示词长度和选择难度**。

工具选择本质上是在做“工具空间压缩”。如果一个 Agent 有上百个 API、插件或代码工具，模型每次都读完整工具说明，不仅 token 贵，而且更容易选错。**先检索或分类出候选工具，相当于把工具调用前的搜索空间缩小**。主流分为三类方案：**外部检索器、多标签分类、词表级检索**。

**（1）外部检索器**
外部检索器是一类常见方案，外部工具检索的优化可从两大方向入手：一是**优化检索器本身**，重构检索流程、强化检索与重排序模块；二是**优化工具侧内容**，精简工具说明与文档，降低检索匹配难度。两种优化方式均可同时提升检索准确率与运行效率。
- 检索器优化：ProTIP 采用对比学习做渐进式工具检索；AnyTool 设计分层检索，缩小搜索范围；
- 工具描述优化：DRAFT 通过自交互迭代精简工具文档；Toolshed、ToolScope 融合检索增强与工具压缩技术，控制候选集规模与提示词长度。

这里的重点是，检索器不是替模型完成任务，而是替模型过滤工具。任务描述和工具描述被映射到相近空间后，模型只需要在少量候选里做选择。效率收益主要来自两点：提示词更短，候选更少。

**（2）多标签分类**
多标签分类把工具选择当作分类问题。TinyAgent **使用小模型做工具选择，以降低主模型负担**；Tool2Vec 把工具和合成使用样例对齐，让模型根据任务表示选择工具。它们的优势是调用前成本低，但训练或构造工具标签需要额外工作。

这类方法**更像一个前置路由器。主模型不必亲自读完全部工具文档，小模型或分类器先给出可能相关的工具标签**。它适合工具集合相对稳定的系统，但如果工具频繁变化，就要不断更新分类器或工具表示。

**（3）词表级检索**
词表式检索**把工具变成模型可以直接生成的特殊 token**。ToolkenGPT、ToolGen 把工具编码进词表；Toolken+ 增加重排和拒绝机制；Chain-of-Tools 在巨大工具池中结合思维链选择工具。这个方向的好处是工具选择和语言生成结合更紧，但需要让模型稳定理解这些工具 token。

词表式方法的想法更激进：既然模型本来就在生成 token，那就把工具也变成可生成对象。这样工具选择可以像生成词一样自然发生。问题是工具不是普通词，一个工具 token 背后有参数、执行条件和失败风险，所以还需要重排、拒绝和调用验证来兜底。

**工具选择总结**

**优势**：三类方案都能从海量工具中筛出少量候选，降低提示词长度和选择难度；其中外部检索器最灵活，对新增工具和未知工具的适配性更强。

**劣势**：外部检索器会引入额外的编码、检索或重排开销；多标签分类通常需要训练分类器，词表级检索通常需要训练工具 token 或构造监督数据，因此适配新工具的成本更高。

**适用场景**：工具集动态变化时，更适合优先使用外部检索器；工具集相对固定、追求极致效率时，可以优先考虑多标签分类或词表级检索。

### 4.2 工具调用：参数、并行、预算和训练都影响效率

选到工具之后，真正麻烦的是如何调用。**工具调用不仅要选对工具，还要填对参数、安排调用顺序、控制预算，并判断是否值得继续调用**。

工具调用之所以要拆得这么细，是因为工具成本往往不是“调用一次”这么简单。参数填错会导致重试，并行没做好会导致等待，预算没控制会导致过度调用，训练目标没设计好会让模型形成“遇事就调工具”的习惯。

**参数填充**是最基础的问题。Toolformer、CoA 等方法让模型在文本中原地填入调用参数，把自然语言推理和工具执行连接起来。
- Toolformer 将工具调用融入思维链；
- CoA 引入符号抽象表示中间结果，推理结束后替换为真实返回内容。

参数错了，工具再强也没用。参数填充影响的是调用成功率。很多工具调用失败不是因为模型不知道该用哪个工具，而是参数格式、字段名、约束条件错了。参数填充做得好，可以直接减少失败重试成本。

**并行工具调用**可以减少串行等待。LLMCompiler、LLM-Tool Compiler、CATP-LLM 等方法会分析依赖关系，把可以并行执行的工具调用提前编排。对于多个互不依赖的 API 或代码执行任务，并行能显著减少墙钟时间。
- LLMCompiler、LLM-Tool Compiler 借鉴编译器思想，拆分任务并并行执行，后者还支持运行时融合相似操作；
- CATP-LLM 结合多分支规划与成本感知强化学习，在预算约束下实现高质量并行调用。

并行调用节省的是时间成本，而不一定节省调用次数。它**适合依赖关系清楚的任务**，比如多个查询可以同时发出、多个代码片段可以并行运行。难点在于识别哪些调用可以并行，哪些调用必须等前一步结果。

**成本感知工具调用**把预算放进决策过程。BTP 把工具调用看成类似背包问题：在预算有限时选择收益最高的调用组合；TROVE 引入可复用的紧凑工具；ToolCoder 把工具调用看成代码生成，让工具使用更结构化。这里的重点是，工具调用不再只是“能不能完成任务”，还要考虑“这次调用值不值”。

“值不值”是高效工具学习的核心判断。便宜工具可以多试几次，昂贵工具就要更谨慎；低收益调用应该被跳过，高收益调用才值得进入轨迹。把预算放进训练或推理过程后，模型才会学会这种取舍。

**推理时扩展和后训练**也会影响工具调用效率：
- 推理时扩展不是直接“省成本”，而是在推理阶段多尝试、多搜索、多比较，用更高的即时计算成本换更好的工具调用路径。ToolChain* 使用 A* 搜索剪掉不优分支，属于在推理时通过搜索寻找更优工具链；
- 后训练优化借助强化学习微调模型，在任务效果与调用开销之间权衡。OTC-PO 在优化中加入工具使用惩罚；ToolOrchestra 设计效率奖励，让模型在训练后阶段学会更短、更准的工具轨迹。

这类方法的共同点是，把工具使用从“动作生成问题”变成“轨迹优化问题”。不是每一步都追求局部正确，而是看整条工具链能否用更少步骤完成任务。

### 4.3 融合工具推理：让工具成为推理的一部分

融合工具推理（Tool-Integrated Reasoning）关心的是完整轨迹，不只是单次工具调用。一个好的 Agent 不应该机械地“想一步、调一次工具”，而应该判断哪些步骤能靠内部知识完成，哪些步骤必须外部验证。

**（1）选择性调用**

选择性调用需要区分场景，优先使用模型内部知识，仅在能力不足时调用外部工具。

TableMind 使用“计划、行动、反思”循环，并结合排序感知策略优化；SMART 在参数化知识和外部工具之间做选择；Agent-FLAN 区分不同数据类型来训练工具能力；ARTIST 使用结果导向的强化学习；ReTool 让自然语言和代码交替出现，并用可验证奖励训练；ToolRL 则把格式正确性和参数正确性纳入奖励。

这些方法都在试图解决一个问题：工具结果怎样进入下一步推理。工具调用如果只是把结果贴回上下文，模型仍然可能忽略、误读或重复调用。融合工具推理要求模型把工具结果当成中间证据，继续规划、验证和修正后续步骤。

**（2）成本感知策略优化**

还有一些方法专门减少不必要调用，依托强化学习优化多轮决策策略，兼顾推理质量与格式规范：$A^2$FM 和 IKEA 在内部检索与外部检索之间选择；AutoTIR 惩罚不必要工具使用；OTC-PO 减少调用次数；SWiRL 过滤冗余动作；PORTool 使用衰减因子控制调用收益。共同目标是：**工具应该在关键时刻出现，而不是把每一步推理都外包出去**。

这个区别很重要。一个 Agent 如果每个简单判断都查工具，表面上很谨慎，实际会非常慢；但如果完全不查工具，又会在需要外部事实和执行结果时犯错。**高效带工具推理，就是让模型学会什么时候相信自己、什么时候必须调用工具**。

### 4.4 讨论：工具学习正在从“会调用”转向“高性价比调用”

早期工具学习更关心模型能不能正确调用工具，现在重点逐渐变成调用成本和效果之间的平衡。工具调用能提升准确率，但每次调用都会带来延迟、费用、上下文膨胀和错误传播风险。

更合理的目标不是盲目减少工具，而是在效果和成本之间找到更好的点。强化学习、预算反馈、成本惩罚、并行编排、调用拒绝机制，都在围绕这个目标展开。

## 5 高效规划

规划（Planning）是 Agent 把目标转化成行动轨迹的能力。传统规划往往假设计算资源足够，而**高效规划把推理看成资源受限的控制问题：什么时候深入思考，什么时候停止搜索，什么时候分解任务，什么时候让多个 Agent 协作**。

图5展示高效规划在**单智能体**和**多智能体**中的两条主线。

![](<images/Toward-Efficient-Agents-08.png>)

> 图5：高效规划概览。它的**目标是在最小化成本的同时最大化任务成功率**。（a）单智能体方法通过优化推理策略（控制、搜索、分解）或通过学习演化（策略、记忆）来提升效率。（b）多智能体方法通过拓扑优化、上下文优化和协作蒸馏来降低开销。

解释：**单智能体侧重控制推理深度**：可以做结构化搜索与任务分解，也可以用预算控制快慢思考，还可以通过学习把过去经验沉淀成更好的策略或记忆。**多智能体侧重控制协作宽度**：减少密集通信，压缩上下文，或者把多 Agent 协作轨迹蒸馏回单 Agent。高效规划的核心不是让 Agent 永远想得更多，而是**在成功率和计算成本之间调度推理资源**。

表3进一步汇总高效规划的代表方法。

![](<images/Toward-Efficient-Agents-09.png>)

> 表3：代表性高效规划方法总结。表中把单智能体方法分为推理时策略（自适应控制、搜索、分解）和基于学习的演化（策略、记忆），同时列出多智能体协作效率方法。

解释：单智能体里，自适应控制决定思考预算，搜索方法控制探索分支，分解方法把复杂任务拆开，学习式方法把过去经验变成更省的策略。多智能体里，重点不再是单个 Agent 想多深，而是怎样减少协作通信、上下文交换和重复讨论。

### 5.1 单智能体规划效率：控制推理深度和搜索成本

单智能体规划效率主要关注 token、延迟和搜索步数。这里的方法分成两类：**推理时策略**和**基于学习的演化**。

单智能体规划的成本通常来自“想太久”和“试太多”。复杂任务需要分解、搜索和反思，但这些操作一旦没有边界，就会变成无限扩展的推理轨迹。所以这里的效率重点，是**给推理过程加控制器**。

**推理时策略不改变模型参数，而是在执行过程中控制规划成本**：

**（1）自适应预算方法**

自适应预算方法会判断任务难度，简单问题用快速思考，复杂问题再进入慢速思考。SwiftSage 这类快慢系统就属于这个方向；预算感知方法会显式把资源约束放进决策；Reflexion 和 ReST 这类失败分析机制则把错误经验用于后续尝试，减少重复犯错。

自适应控制的价值在于，不同任务不该使用同样预算。简单任务如果也走完整反思和搜索流程，就是浪费；困难任务如果预算太少，又会失败。快慢思考、预算感知和失败反思，本质上都是在动态分配推理资源。

**（2）结构化搜索**
结构化搜索让 Agent 在候选行动之间做更有控制的探索。LATS 结合蒙特卡洛树搜索和自我反思；ToolChain* 用 A* 搜索剪掉成本高或前景差的工具链；CATS 在树搜索中加入成本感知剪枝。搜索能提升全局性，但搜索本身很贵，所以剪枝和预算控制是关键。

搜索类方法最容易体现“能力和成本的矛盾”。搜索越广，越可能找到好方案；搜索越广，成本也越高。高效搜索不追求把所有分支都看完，而是通过启发式评分、价值估计或成本剪枝，把计算放到更有希望的分支上。

**（3）任务分解**
任务分解通过拆解降低单次推理难度。ReWOO 使用规划器、工作器、求解器分工；HuggingGPT 把任务路由到不同模型或工具；Alita 借助 MCP 进行头脑风暴和子任务分解。分解的好处是让每一步更简单，风险是子任务之间协调成本上升。

任务分解能降低单步难度，但不是免费午餐。拆得太粗，帮不上忙；拆得太细，子任务管理会变贵。高效分解要找到合适粒度，让每个子任务足够简单，同时不制造过多协调负担。

**基于学习的演化把过去经验变成更好的策略**，它们共同体现一个方向：**把昂贵的在线搜索，尽量转化为可复用的策略、记忆或技能**：

- **策略优化**：通过外部评价函数、偏好学习或过程级奖励，让 Agent 在后续规划中更少走弯路。QLASS 使用 Q 值评论器为动作选择提供外部指导；Planning without Search 通过离线目标条件评论器减少在线搜索成本；ETO 从试错偏好中学习更好的策略；RLTR 和 Planner-R1 使用过程级奖励，对推理序列本身提供反馈，而不是只奖励最终结果。

- **记忆与技能沉淀**：把成功经验、任务结构或可复用动作保存下来，避免每次都从零规划。VOYAGER 维护技能库，把已经学会的操作固化为可复用技能；GAP 通过图结构组织任务分解和并行动作，让后续规划可以复用已有结构，降低重复推理成本。

**单智能体策略总结**

- 优势：自适应控制降低推理开销；结构化搜索提升探索效率；任务分解减少步骤冗余；学习与记忆将单次规划成本分摊至多次任务；
- 劣势：自适应控制可能判断失误；搜索算法引入额外开销；任务分解会放大错误传播；学习与记忆模块增加训练、维护成本。

### 5.2 多智能体协作效率：拓扑优化、压缩上下文、蒸馏协作

多智能体系统能增强推理，但通信成本可能非常高。多个 Agent 之间如果全连接讨论，每轮都交换完整上下文，成本会随 Agent 数量快速增长。它不只要控制每个 Agent 的思考成本，还要控制 Agent 之间的连接方式、消息长度、讨论轮数和最终汇总成本。**高效多智能体规划，核心是拓扑优化、压缩上下文、蒸馏协作 ：**


**（1）拓扑优化**

**拓扑优化**会优化 Agent 之间的连接方式，旨在对通信图进行改造，借助链式、有向无环图（DAG）等结构化拓扑架构，将消息交互的复杂度从平方级 $O(N^2)$ 降至线性 $O(N)$，以此大幅削减海量消息带来的开销。Chain-of-Agents 让上下文线性传递；MacNet 使用有向无环图；GroupDebate 比较稠密和稀疏讨论；MARS 和 S2-MAD 只在观点分歧明显时触发辩论；AgentPrune、AgentDropout、SafeSieve 会剪掉低效边或低价值 Agent。

拓扑优化解决的是“谁该和谁说话”。全连接交流最充分，但也最贵；链式或图式交流更省，但可能损失信息。剪枝和稀疏连接的目标，是保留关键协作路径，删掉低价值通信。

**（2）协议和上下文优化**

**协议和上下文优化压缩通信内容、约束交互格式，避免上下文膨胀**。CodeAgents 用简洁伪代码表达信息；Smurfs 丢弃失败分支；Free-MAD 和 ConsensAgent 改进提示和共识过程；SMAS 终止冗余循环。这些方法都在避免“多 Agent 聊太多”。

上下文优化解决的是“说什么、说多长”。多 Agent 系统里，消息不一定越完整越好。过长消息会浪费 token，也会让接收方抓不住重点。把消息压缩成代码、摘要、共识或关键状态，本质上是在压缩协作上下文。

**（3）协作蒸馏**

协作蒸馏则更进一步：把多 Agent 的交互经验压缩进更小系统，也就是说将多智能体的协同逻辑蒸馏至单智能体模型，显著减少运行时通信开销。MAGDI、D&R 等方法把互动轨迹或辩论轨迹蒸馏给学生模型或单 Agent，使运行时不用每次都启动完整多 Agent 协作。这个方向很适合部署，因为它把高成本协作放到训练或离线阶段消化。

协作蒸馏可以看成“把多人讨论变成一个人的经验”。训练时使用多 Agent 产生高质量轨迹，推理时用更轻的模型复现协作收益。它牺牲了一部分运行时灵活性，但能大幅降低部署成本。

**多智能体策略总结**

- 优势：拓扑剪枝降低通信成本；协议压缩抑制上下文膨胀；知识蒸馏兼顾性能与低推理开销；
- 劣势：链路裁剪、内容压缩可能丢失有效信息；蒸馏过程需要额外训练，推理阶段会弱化多智能体的多样性优势。

### 5.3 讨论：高效规划把计算从在线搜索迁移到结构和学习里

高效规划的共同趋势，是减少无控制的在线思考。能提前分解的，就不要每一步都重新推理；能通过规则或预算控制的，就不要无限搜索；能从历史经验学到的，就不要每次从零探索；能把多 Agent 协作蒸馏掉的，就不要运行时反复开会。

但是局限也很直接：如果预算控制太激进，Agent 会过早停止思考；如果剪枝策略不准，可能删掉真正有用的路径；如果蒸馏后的单 Agent 没学到协作中的关键判断，部署时效果也会下降。

## 6 基准测试

虽然这里讨论的是效率，但评测仍然要坚持**效果优先**。如果一个方法成本很低，却无法完成任务，或者明显牺牲任务质量，那它不算真正高效。更合理的效率评测有两个互补角度：一是**在固定资源预算下比较任务效果**，二是**在效果相近的前提下比较资源消耗**。再进一步，还可以看效果和成本之间的帕累托前沿。

> **帕累托前沿就是效果 - 成本权衡下最值得考虑的一组方案**。

主线评测分三条：记忆、工具学习和规划。它们的共同点是都不能只看“做没做对”，还要看用了多少 token、耗时多久、调用多少次工具、交互多少轮，以及这些成本是否值得。

### 6.1 记忆基准

记忆评测可以分成**效果评测**和**效率评测**。

效果上，主流做法仍然是通过下游任务间接观察记忆有没有帮助，比如 HotpotQA、Natural Questions 这类问答数据集，以及 GAIA 这类需要多轮交互和工具调用的综合智能体基准。与此同时，专门面向记忆能力的基准也在出现，例如 LoCoMo、LongMemEval，它们更直接关注长时记忆、多轮交互记忆和长期上下文保持能力。

效率上，记忆基准开始把成本拆得更细。Evo-Memory 引入**步骤效率**，衡量智能体完成目标需要多少环境交互步数，步数越少，说明记忆机制对推理流程的支撑越精简；StoryBench 关注**运行开销**和 **token 消耗量**，分别看长时序任务的整体运行时长和模型处理的上下文总量；MemBench 更关注记忆读写本身的时间成本，统计单次记忆读取、写入耗时，用来判断某种记忆架构是否在部署时太慢。

综合这些工作，记忆模块的效率指标大致可以归为四类：

- **token 与接口开销**：最常见的是统计总 token 消耗量；有些工作还会把 token 用量折算成实际资金成本，例如规划缓存、ACE 等。
- **时间类指标**：包括整体运行时长、端到端延迟、纯推理耗时、检索耗时、索引耗时等。比如 HiAgent 统计整体运行时长，A-MEM、H-MEM 单独统计检索延迟。
- **硬件资源指标**：主要看 GPU 显存占用，MemoR 等工作还会比较不同上下文长度下的显存变化。
- **交互类指标**：统计大模型调用频次、推理步数等。MemoryOS 会看单轮回复平均调用多少次大模型，ReasoningBank 则把推理步数作为效率判断依据。

这里的重点是：**记忆系统不只是能不能记住，还要看读写、检索、压缩和整合这些操作本身贵不贵**。

### 6.2 工具学习基准

工具学习目前还没有形成完全统一的标准化评测体系。很多基准优先关注任务效果，比如工具是否选对、参数是否填对、调用结果是否能完成任务。但在 Agent 系统里，工具调用往往占据大量交互成本，也是端到端任务成败的关键环节，所以工具效率评测越来越重要。现有基准大致可以分成三类：**工具选择与参数填充基准、基于模型上下文协议的工具学习基准、智能体级工具学习基准**。这些基准通常都可以进一步统计 token 消耗、运行延迟和工具调用轮次。

**工具选择与参数填充基准**覆盖工具构建、工具选择、参数填写和多工具组合调用。SealTools 面向大规模工具生成，包含多工具联用和嵌套调用案例；UltraTool 更关注真实用户场景下的**工具创建**能力。MetaTool 关注是否应该调用工具、应该调用哪一个工具，覆盖工具可靠性异常和多工具联用等场景。选定工具后，参数填充和调用规范就变成关键，BFCL 覆盖真实应用中的多轮多步函数调用，API-Bank 则内置 73 款人工标注工具，更贴近日常对话场景。

现实任务往往不是单工具调用，而是串行、嵌套、多工具协作。NesTools 对嵌套工具调用做了系统分类；τ-Bench 面向零售、航空等场景，τ²-Bench 进一步扩展到电信领域，并加入用户发起工具调用的用例；ToolBench 从 RapidAPI 收集了超过 16000 个接口，但依赖在线真实接口，稳定性和可复现性会受影响；MGToolBench 则对 ToolBench 做更细的人类整理，缩小训练指令和真实用户查询之间的差距。T-Eval 更进一步，把工具使用流程拆成规划、推理、检索等六类能力，避免只看最终成败而看不出到底哪一步出了问题。StableToolBench 则通过虚拟 API 服务器、缓存和大模型仿真提高评测稳定性。

**基于模型上下文协议的工具学习**来自工具调用规范化趋势。随着 Anthropic 推出模型上下文协议（Model Context Protocol, MCP），工具定义和调用开始有更统一的接口规范。MCP-RADAR 除了准确率，还加入工具选择效率、计算资源利用率、执行速度等指标；MCP-Bench 使用“大模型作为裁判”的模式，评分里专门包含**并行性与效率**，看智能体是否减少冗余工具调用、是否利用并行执行机会。

**智能体级工具学习**则更接近真实复杂任务。随着强模型在普通工具基准上越来越难拉开差距，评测重点开始转向：**模型能不能通过迭代调用搜索工具，回答复杂、冷门、需要外部信息的问题**。SimpleQA 评测事实类短答案，覆盖面广、难度高；BrowseComp 延续类似设计，由人工构造答案简短、可核验但很难仅凭内部知识回答的问题，高度依赖网页检索；SealQA 面向检索增强模型，题目以事实查询为主，但网页检索结果往往冲突、冗余或无效，其中 SEAL-0 子集包含 111 道极难问题，顶尖模型准确率也很低。

工具学习的效率指标包括**工具调用次数、token 数、延迟、执行时间、交互轮数和 API 成本**。这个方向的难点是，不同工具的真实成本差异很大：一次本地计算、一次搜索 API、一次数据库查询、一次付费模型调用，不能简单当成同一种动作。

### 6.3 规划基准

规划能力通常**通过下游智能体任务的最终结果间接评估**，常见基准包括 SWE-Bench、WebArena、WebShop。PlanBench 这类早期工作更偏纯大语言模型规划能力，但不能直接覆盖 Agent 场景，因为 Agent 的规划效率不只取决于模型推理本身，还取决于环境交互步数、工具调用次数、搜索路径和失败重试。

最近的规划效率基准开始专门统计这些成本。Jobs 等人基于积木世界（Blocksworld）构建结构化基准，同时看规划和执行能力，效率指标包括端到端执行时长、规划尝试次数、token 消耗量和对应资金成本。TPS-Bench 同时评估规划效果和规划效率，使用 token 用量、端到端运行时长、工具调用轮次作为核心指标，并提出单次成功成本，也就是完成一次有效任务的预期资金开销，把成本和成功率放在一起比较。

CostBench 面向动态任务环境，关注**成本最优型工具规划**。它会给每个工具设定明确使用成本，再用**成本差值**、路径偏差和无效工具调用来判断 Agent 是否真的走了低成本路径。这里的价值在于，它不只问“任务有没有完成”，还问“是不是用合理成本完成”。

除专用基准外，规划研究也形成了一些通用效率指标。最基础的是 **token 消耗量**和**运行时长**；从搜索深度和广度看，SwiftSage 统计任务执行时间步数，Reflexion 统计试错次数，LATS 计算任务成功所需的平均节点数 / 状态数，CATS 统计找到有效方案的平均迭代次数。单次成功成本也被很多后续研究沿用，另一种常见做法是在固定资源预算下比较不同模型的任务表现。

所以规划评测的核心正在从“能不能完成任务”扩展到：**能不能在有限 token、有限时间、有限工具调用和有限失败尝试内完成任务**。

## 7 挑战与未来方向

第一是记忆效率评价还不统一，不同论文对记忆的定义、存储粒度、读写操作和效率指标都不一样。一个方法报告 token 省了多少，另一个方法报告延迟降了多少，二者很难直接比较。未来需要更统一的评测协议，让记忆效果和记忆成本同时可比。

这个问题在记忆方向尤其明显。有人把记忆当成长期用户画像，有人把记忆当成检索文档，有人把记忆当成键值缓存，还有人把多智能体共享上下文也叫记忆。如果没有统一的任务设置和指标，所谓“更高效”很容易只是在某个局部定义下成立。

第二是 Agent 的潜在推理还没有充分发展。潜在空间推理可以减少显式 token，但 Agent 场景比普通推理更复杂，它还要处理工具使用、长期规划、记忆访问和行动验证。只把思考藏到隐藏状态里不够，还要保证这些隐藏状态能和外部工具、环境反馈稳定交互。

这里的难点不只是技术实现，也包括可解释性。Agent 做的是外部行动，如果中间推理都藏在潜在状态里，系统为什么调用某个工具、为什么跳过某条路径、为什么相信某段记忆，就更难追踪。未来如果要让潜在推理进入真实 Agent，必须同时考虑效率、可控性和审计。

第三是部署感知设计仍然不足。很多方法在论文任务里有效，但真实部署时还要考虑模型并发、工具 API 价格、系统稳定性、失败恢复、日志审计和工程复杂度。尤其是多智能体系统，到底是真多模型协作更划算，还是单模型扮演多角色更划算，需要端到端成本评估。

这也是 Agent 论文和 Agent 产品之间最容易断开的地方。论文里多一次工具调用、多一轮辩论、多一段记忆检索，可能只是一个实验设置；部署里这些都会变成真实延迟、账单和故障点。高效 Agent 需要从一开始就把运行环境纳入设计，而不是最后才补成本分析。

第四是效率不能只看 token。真实成本还包括延迟、内存、计算资源、API 价格、工具执行时间、工程维护成本和可靠性风险。后续研究如果只报准确率和 token，很难判断方法是否真的适合落地。

比如一个方法 token 很少，但需要频繁访问外部数据库；另一个方法 token 稍多，但能一次性完成任务。只看 token 会误判二者的真实效率。更完整的报告应该同时给出任务成功率、总 token、工具调用次数、平均延迟、失败重试次数、资源消耗和价格估计。

**高效 Agent 的下一步，应该是把效果、成本、可靠性和部署复杂度放在同一张表里看**。这比单纯追求更长链路、更大工具库、更多 Agent 更重要。

## 8 结论

这篇综述把 Agent 效率从一个泛泛的问题拆成了三条具体路线：**记忆、工具学习和规划**。记忆负责让历史信息可压缩、可管理、可访问；工具学习负责让外部能力被少而准地调用；规划负责让推理、搜索和协作在预算内完成。

最有价值的地方在于，它没有把效率简化成“少生成 token”。**Agent 的成本来自完整系统：上下文、记忆、工具、搜索、重试、通信、部署。只优化其中一项，往往会把成本转移到另一项**。

但局限也很直接：高效 Agent 还缺少统一评价框架，很多方法只在局部环节展示优势，跨任务、跨工具、跨部署环境的可比性仍然不足。未来真正有用的 Agent，不只是会做复杂任务，还要能在真实资源约束下稳定、便宜、可控地完成任务。
