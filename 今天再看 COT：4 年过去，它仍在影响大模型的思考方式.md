> 大家好，这里是Goodnote（好评笔记）。本文详细介绍我们对论文《Chain-of-Thought Prompting Elicits Reasoning in Large Language Models》的理解。

![](<images/L1-2.-CoT（Chain-of-Thought）-01.png>)

> 谷歌大脑团队 2022 年发表于 NeurIPS 的研究《Chain-of-Thought Prompting Elicits Reasoning in Large Language Models》，提出了一种简单却极具革命性的解决方案 —— 思维链提示（Chain-of-Thought Prompting），通过引导大模型进行“分步推理”，显著提升其在复杂任务上的表现。

## 一、核心痛点：大模型 “规模够大，推理不行”

近年来，大语言模型在参数规模上持续突破，在众多语言任务中展现出接近人类的能力。但一个反直觉的现象始终存在：即便模型规模已十分庞大，它们在多步推理任务上的表现依然不尽人意。

无论是算术题、常识推理，还是符号状态跟踪，模型往往在需要 “分步骤思考” 的问题上败下阵来。这一现象揭示了两个关键结论：

- 单纯扩大模型参数规模，并不能自动带来可靠的推理能力；
- 现有的 prompting 方式，可能并没有正确地激活模型潜在的能力。

论文正是从这一核心痛点出发，提出了一个看似简单却影响深远的解决思路。

## 二、现有推理方法的局限

在 CoT 提出之前，解决语言模型推理问题主要依赖两条路线，但均存在难以逾越的短板：

### 1. 监督式推理学习（Rationale Learning）

- 核心逻辑：通过人工或程序生成中间推理过程（如数学题的分步演算、逻辑题的推导步骤），再用这些带推理链的数据集对模型进行微调。
- 关键局限：

  - 成本极高：构建高质量的推理过程数据集远比标注简单输入 - 输出对复杂，需投入大量人力或算力；
  - 任务依赖性强：为某一任务定制的推理数据集无法迁移到其他任务，通用性差，每类推理问题都需单独标注。

### 2. 传统少样本提示（Few-shot Prompting）

- 核心逻辑：无需训练模型，仅在提示中提供少量 “问题 - 答案” 示例，让模型通过上下文学习任务模式。
- 关键局限：

  - 推理任务表现拉胯：在算术、常识、符号推理等需要多步推导的任务上效果极差，往往直接给出错误答案；
  - 规模增益有限：即使扩大模型参数规模，性能提升也不显著，无法突破推理能力的天花板。

### 3. 基于局限性提出的核心假设

基于以上局限性，作者提出了一个核心假设：模型 “懂推理” 却 “不会表达”？

作者抛出了一个颠覆性假设：大型语言模型或许已经通过预训练习得了解决复杂问题的潜在推理能力，只是传统方法没有提供正确的引导，导致能力无法被激活 —— 就像一个会解题但不爱写步骤的学生，直接给出答案时容易因疏忽出错。

比如问食堂有 23 个苹果，用了 20 个又买了 6 个，还剩多少个，传统提示下的模型可能直接给出 27 这样的离谱结果；但如果让模型一步步说出思考过程，答案往往会变得准确 —— 这就是思维链提示的魔力。

## 三、CoT 的核心定义与实现方式

### 1. 基本定义

CoT 是一种基于少样本提示的改进技术，核心特征是：在提示的示例中，不仅包含 “输入 - 输出” 对，还显式添加从问题到答案的中间自然语言推理步骤，要求模型在生成最终答案前，先模仿生成这一系列推理过程。

简单来说，提示的结构从 “问题→答案” 升级为 “问题→分步推理→答案”，通过示例演示 “如何拆解问题、逐步推导”，引导模型学会 “分步思考”。

### 2. 与传统提示（传统少样本提示（Few-shot Prompting））的关键差异

在图 1 中给出了传统提示词和思维链提示词的对比示例，用于说明提示结构差异，以及推理过程。

![](<images/L1-2.-CoT（Chain-of-Thought）-02.png>)

> 图 1：思维链提示（Chain-of-Thought Prompting）使大型语言模型能够处理复杂的算术推理、常识推理和符号推理任务。思维链推理过程已用高亮标注。

- 左侧（传统提示，其实是传统少样本提示（Few-shot Prompting））：面对 “食堂有 23 个苹果，用掉 20 个又买 6 个” 的问题，模型直接给出 “27” 的错误答案，没有任何推理过程；
- 右侧（CoT 提示）：模型先推导 “原有 23 个，用掉 20 个剩 3 个，再买 6 个共 9 个”，再给出正确答案，推理过程与人类解题逻辑一致。

更重要的是，作者强调：CoT 中的推理过程不是对答案的 “事后解释”，而是生成答案的 “前置计算步骤”—— 模型必须通过推理过程的推导，自然得到最终答案，而非先确定答案再补写步骤。**思维链提示**和**传统少样本提示**的对比如下：

| 对比维度 | 传统少样本提示 | Chain-of-Thought Prompting（思维链提示） |
|-|-|-|
| 示例结构 | 问题 → 直接答案 | 问题 → 中间推理步骤 → 答案 |
| 模型行为 | 跳跃式生成答案 | 逐步推导后输出答案 |
| 核心依赖 | 模型对答案的统计映射 | 模型对推理逻辑的模仿与执行 |

### 3. 核心实现方式

这种方法的核心是**在提示中加入少量带推理过程的示例**。比如问 “罗杰有 5 个网球，又买了 2 罐，每罐 3 个，现在有多少个？”：

- 标准提示示例：仅给出 “答案是 11”；
- **CoT 提示示例：补充 “罗杰一开始有 5 个球，2 罐每罐 3 个就是 6 个，5 加 6 等于 11，答案是 11”。**

通过这样的示范，AI 就能学会模仿这种分步推理的方式。

简单来说，思维链提示就是**让 AI 在给出最终答案前，先 "自言自语" 地说出中间推理步骤**。就像我们解数学题时会在草稿纸上一步步演算，AI 通过输出 "因为... 所以... 然后..." 这样的推理链条，能更清晰地梳理逻辑，避免一步到位的错误。

## 四、CoT 的四大核心优势

1. **分解复杂问题**：把多步骤任务拆成小问题，为需要更多思考的任务分配更多计算资源，就像解复杂方程要分步求解一样；
2. **提升可解释性**：能看到 AI 的思考路径，知道它是如何得出答案的，方便排查逻辑漏洞，而不是面对一个黑箱结果；
3. **适用范围广**：不仅能解数学题，还能处理常识推理、符号操作等多种任务，只要是人类需要分步思考的问题都适用；
4. **无需复杂改造**：不用微调模型，只要在提示中加入推理示例，现成的大模型就能具备更强的推理能力。

## 五、实测效果：三大任务全面突破

谷歌的研究团队在算术、常识、符号推理三大类任务上做了系统测试，结果十分显著：

![](<images/L1-2.-CoT（Chain-of-Thought）-03.png>)

> 图 3：算术推理、常识推理和符号推理基准测试的⟨输入、思维链、输出⟩三元组示例。思维链部分已高亮标注。完整提示见附录 G。

### 1. 算术推理：准确率翻倍

在 GSM8K、SVAMP、ASDiv 等 5 个数学应用题数据集上，思维链提示让大模型的表现实现质的飞跃：

- 540B 参数的 PaLM 模型在 GSM8K 上的准确率从 17.9% 飙升至 56.9%；
- 175B 参数的 GPT-3 则从 15.6% 提升到 46.9%；
- 问题越复杂，提升效果越显著 —— 对于单步计算的简单题，性能提升较小；而对于多步推理的难题，准确率甚至能翻倍。

在 GSM8K 基准测试上，540B 参数的 PaLM 模型用 8 个思维链示例，准确率直接超越了专门微调过的 GPT-3，成为当时的最先进水平。

更有趣的是，思维链的效果和模型规模强相关。只有参数达到 1000 亿左右的大模型，才能真正发挥思维链的作用 —— 小型模型虽然能输出流畅的推理过程，但逻辑往往漏洞百出，反而不如直接给答案。而且问题越复杂，思维链的优势越明显：对于单步计算的简单题，提升可能很小，但对于需要多步推理的难题，性能甚至能翻倍。

### 2. 常识推理：突破认知局限

在 CSQA、StrategyQA 等常识推理任务中，思维链提示帮助模型更好地调用背景知识：

- 540B 参数的 PaLM 在 StrategyQA 上的准确率达到 75.6%，超越了此前的最先进水平（69.4%）；
- 在体育理解任务上，准确率高达 95.4%，超过了普通体育爱好者的水平（84%）。

比如判断 “足球运动员能否在橄榄球锦标赛中接球”，思维链会让 AI 先分析运动员的项目属性，再判断赛事类型，最后得出结论；而标准提示可能凭直觉给出错误答案。

### 3. 符号推理：实现长度泛化

在 “最后一个字母拼接” 和 “硬币翻转” 两个符号任务中，思维链提示不仅让模型在同类问题上表现优异，还实现了对更长序列的泛化：

- 模型仅学习 2 个单词的字母拼接示例，就能正确处理 3 个、4 个单词的拼接任务；
- 面对比示例更多的硬币翻转步骤，也能准确判断最终状态。

## 六、关键洞察与消融实验

### 1. 关键洞察：模型规模是前提

研究发现一个重要规律：思维链推理是模型规模扩大后 “涌现” 的能力。

- 只有当模型参数达到约 100B 时，思维链提示才能带来显著收益；
- 对于参数小于 10B 的小型模型，即使生成了流畅的推理链，逻辑也往往存在漏洞，甚至会降低性能。

这背后的原因在于，小型模型缺乏足够的语义理解、符号映射和算术运算能力，无法支撑连贯的分步推理。而扩大模型规模后，这些基础能力得到提升，思维链才能真正发挥作用 ——62B 参数的 PaLM 模型存在的语义理解和步骤缺失错误，在升级到 540B 后大多得到了修复。

### 2. 消融实验：为什么思维链有效？

为了探究思维链提示的核心价值，研究团队设计了三组消融实验：

1. 仅输出等式——自然语言推理是语义理解的关键：让模型只给出数学等式而不解释推理过程，结果在复杂数据集上效果不佳，说明自然语言推理步骤对理解问题语义至关重要；
2. 仅增加计算量——无效长度无法替代有效推理：让模型输出与推理步骤长度相当的无意义字符，性能与标准提示几乎一致，证明单纯增加计算量无法替代有效推理；
3. 答案后附思维链——顺序推理是生效的核心前提：在仅在答案后提供思维链提示（效果远不如推理在前的主方法），说明顺序推理对引导模型思考的重要性。

> 隐含的第四种（就是主方法 COT，不是消融组）推理在前 → 答案在后（Standard Chain-of-Thought），是所有对照实验的参照点，性能提升的唯一成功配置。

这些消融实验表明，思维链的价值不在于增加计算量（第一组和第二组）或激活知识库（第三组，大模型的 “知识库” 本质就是预训练阶段从海量文本中习得的各类相关知识），而在于通过自然语言的分步推理，帮助模型梳理逻辑、分解问题，这正是人类解决复杂任务的核心方式。

## 七、局限性与使用建议

### 1. 局限性

尽管思维链提示效果显著，但仍存在一些局限：

1. 仅对大模型有效，小型模型难以生成逻辑连贯的推理链；
2. 推理链可能存在逻辑错误，导致 “越思考越错”；
3. 手动编写思维链示例需要一定成本，大规模应用需自动化生成；
4. 模型是否真正 “理解” 推理过程，仍是一个开放性问题。

### 2. 实际使用建议

如果想在实际应用中使用思维链，有几个小建议：

1. 针对复杂推理任务使用，比如数学题、逻辑分析、多步骤决策等，简单任务没必要画蛇添足；
2. 论文实验使用了少量思维链示例（如 8 个）即可显著提升性能，实践中也无需堆叠过多的示例。
3. 推理步骤要简洁明了，不用太复杂的表述，思维链的价值不在于“写得长”，而在于是否真实参与了问题求解；

## 八、未来展望

思维链提示的发现，让我们看到了大模型推理能力的巨大潜力。它证明了大模型不仅能学习语言的表面规律，还能通过适当引导，模仿人类的分步推理过程。

未来，随着技术发展，可能会出现自动生成思维链、多轮交互优化推理路径等更高级的方法，让 AI 的思考越来越接近人类。

> 其实就我们四年后的26年来看，作者的预测非常准确了。包括React、Skills 等一系列辅助AI思考的模式层出不穷。

对于我们普通人来说，现在就可以用思维链提示来提升 AI 的回答质量。下次再让 AI 解复杂问题时，不妨先给它一个带推理过程的示例，看看它会不会给你一个更准确的答案 —— 你会发现，原来 AI 也能像人一样 “认真思考”。