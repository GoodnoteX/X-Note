> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对 Earendil 文章《What is a Harness?》的理解。

> Pi Agent 团队最近发布这篇入门文章，用攀岩安全带解释 Agent harness。Harness 主要由系统 prompt、工具、智能体循环和转换层组成。还有一个很实际的问题：**模型往往由 AI 公司控制，Harness 却可以由用户自己拥有、修改和运行**。中立开源的 Harness 可以切换模型，工具、工作流和会话记录仍然保留在用户自己的环境里。

来源：[What is a Harness?](https://earendil.com/posts/what-is-a-harness/)


Harness 在《剑桥词典》中有两层含义：作为名词，它指固定人或物体的安全带；作为动词，它表示控制并利用某种力量。Harness，可以想成攀岩穿的安全带。

攀岩安全带通过锁扣和绳索提供保护，装备环还能挂上粉袋、岩塞工具和快挂。换到不同地形时，安全带可以继续使用，只需调整携带的装备。杂技演员和树艺师也会使用类似结构。

![Royal Robbins 在 El Capitan 攀岩](<images/Harness-16.-Earendil-What-is-a-Harness-01.png>)

照片中，Royal Robbins 正在攀登 El Capitan，腰间的安全带挂满了攀登工具。这张照片由 Tom Frost 拍摄。安全带负责保护他，也让工具随手可用。Agent harness 与它在结构和功能上有相似之处，会把模型、指令、工具和执行框架接在一起，还可以根据任务和用户需求调整。

## Agent Harness（Agent Harnesses）

可以用 `Agent = Model + Harness` 简单理解二者的关系。模型依靠训练得到的参数和知识理解任务，**Agent harness 则是一套为 AI 模型提供运行环境的软件**。它让模型可以在具体界面中接收请求、使用工具并持续完成任务。

Agent harness 早期的典型应用是编程。随着 AI Agent 进入更多工作，Harness 也开始出现在各种 Agent 的核心。软件工程师常常在终端里直接使用 Pi；OpenClaw 可以通过 iMessage、聊天应用或电子邮件交互；Earendil 的 Lefos 主要围绕电子邮件构建（夹带私货）。**无论入口是终端、聊天应用还是电子邮件，Harness 通常都要处理同样四件事：系统 prompt、工具、智能体循环和转换层。**

### 1. 系统 Prompt（System Prompt）

多数 AI 模型在训练过程中已经形成一套内嵌规则与行为指南。Claude Opus 4.5 曾因为一份公开流传的“灵魂文档（soul document）”受到关注，其中解释了模型如何理解自己以及应当如何行动。

Harness 里的系统 prompt 也会提供行为指导，但它没有那么深地嵌入模型。它更像新员工入职时拿到的说明：新员工还没有将公司的具体工作方式内化，但知道执行当前工作时应当遵循这些要求。

系统 prompt 会与每次用户 prompt 一起放进对话，告诉模型自己正在什么 Harness 中工作，又该怎样完成任务。有了这些说明，模型才能按照当前 Harness 的要求行动。

### 2. 工具（Tools）

工具是由代码实现、可以被模型调用的能力。Harness 要向模型说明每个工具能做什么，也要把真正执行任务的代码准备好。网页搜索、编写并执行代码、撰写电子邮件，都是典型工具。

工具描述需要让模型理解它能完成什么。Harness 通常不会把每个工具的调用时机和用法全部写死，而是提供工具、清楚描述能力，再让模型根据用户请求和当前状态决定何时以何种方式调用。**模型负责决策，Harness 负责提供可调用的能力与执行环境。**

### 3. 智能体循环（Agentic Loops）

当模型已经获得系统 prompt 和一组工具后，智能体循环决定它如何持续工作。假设这个 Harness 通过电子邮件接收任务，可用工具包括 `WebSearch`、`WriteCode` 和 `ComposeEmail`。用户要求它比较附近小学的排名与测试成绩，并给出建议。

整个执行过程可以拆成：

1. 模型先理解用户 prompt，利用预训练知识判断“小学”“附近区域”和用户可能关心的排名指标。
2. 模型构造搜索请求，调用 `WebSearch` 获取最新数据。
3. 搜索结果回到对话上下文后，模型将它们与初始请求对照。数据不正确或不充分时，它会自行发起新一轮搜索。
4. 模型收集到所需数据后，调用 `WriteCode` 完成计算、整理结果和生成电子表格。电子表格本身也可以通过代码生成和格式化。
5. 模型再次将电子表格与用户的初始 prompt 比较。如果仍不满足需求，执行路径会回到搜索或数据处理。
6. 模型判断材料足够后，调用 `ComposeEmail` 回顾发现、撰写摘要与建议，并把电子表格作为附件加入邮件。
7. 模型检查最终产物，确认工作完成后关闭循环。用户随后收到包含结论、建议和附件的电子邮件。

模型会查看一轮工具调用拿回了什么，再决定要不要继续调用。Harness 保留初始请求和工具结果，模型便能将两者对照，决定继续搜索、处理数据，还是结束任务。

### 4. 转换层（Translation Layer）

转换层使同一套 Harness 能够与不同 AI 模型配合。某些 Harness 还会在同一个智能体循环里选择多个模型，让各自擅长不同任务的模型负责对应工作。

有了转换层，同一套 Harness 可以切换到 Anthropic、OpenAI 或开源模型，用户也可以按照单任务成本评估模型的性价比。以学校调研邮件为例，同一个请求可以分别交给 OpenAI 模型、Anthropic 模型和开源模型，再比较三份结果的质量与成本。所有答案仍保留在同一套 Harness 里，无需分散到三个厂商应用中。

**用户可以自己选择模型**。当 Harness 在用户的本地电脑上运行时，会话副本也能留在本地。这些长期积累的对话，会成为人与机器长期往来的记录，不必被锁定在某一家 AI 公司的应用内。

## 把 Harness 变成自己的工具（Making a Harness Yours）

普通用户很难真正拥有和改造 AI 模型，Harness 却可以。它像攀岩安全带一样，能够按照使用者的工作方式调整。

Pi 是一个尽量少做预设的 Agent harness。它的系统 prompt 较短，默认工具很少。用了一段时间后，用户可以修改系统 prompt，也可以按照自己的工作流编写扩展，再把扩展分享给其他人。

Pi 用户之间已经分享了超过 5,000 个扩展。Pi 免费、开源，可以直接运行在用户自己的笔记本电脑上。**工具装在自己的机器上，怎么改、怎么用都由用户决定。**

## 中立开源 Harness 让用户保留自主权（Neutral Open Source Harnesses as Tools of Agency）

早期流行的 Agent harness 未必开源，也未必保持模型中立。Claude Code 是最早走红的 Agent harness。它让用户在本地电脑上使用 Claude 模型编程，并没有把与模型厂商无关的转换层作为主要目标。

随后出现了更多免费开源的 Agent harness，包括 OpenClaw、OpenCode、Hermes 和 Pi。Pi 选择保持模型中立，让用户能够自由选择能力与模型。这套能力也可以继续面向更广泛的人群，让更多人获得选择权和自主权。

人们担心大型 AI 公司的权力和影响越来越大，一些人甚至可能选择完全避开 AI。软件和开放协议可以用来缓解分歧、减少无知，并带来更持久的快乐与理解。实现这件事不能靠回避已经存在的技术。使用这些技术时，人需要保持清醒，牢牢握住控制权。所谓“harnessing AI”的最终含义，就是**让人掌握工具，别让工具反过来掌握人**。
