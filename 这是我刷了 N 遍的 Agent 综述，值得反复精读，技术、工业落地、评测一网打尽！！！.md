> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Empowering Real-World: A Survey on the Technology, Practice, and Evaluation of LLM-driven Industry Agents》的理解。
![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-01.png>)
> 这篇由哈尔滨工业大学（深圳）与华为团队完成的综述，聚焦一个很现实的问题：大语言模型 Agent 的研究已经很多，但它们到底如何进入工业场景，变成可靠的生产力？这里给出的核心答案，是用一个 L1 到 L5 的工业 Agent 能力成熟度框架，把技术、实践和评估三件事串起来看：底层技术看记忆、规划、工具使用；行业实践看流程执行、交互式问题解决、端到端自治、协作智能和适应性社会系统；评估则要同时覆盖基础能力和行业任务。

## 核心问题

这篇论文的核心问题非常清楚：**如果大语言模型 Agent 想从通用研究走向真实工业场景，只靠“能推理、能调用工具、能多轮对话”是否够用**？

答案是不够。真实工业场景不是一个干净的 benchmark，也不是一次性问答。金融、医疗、制造、软件工程、城市治理这些领域都有自己的知识体系、工具链、风险边界、流程约束和合规要求。一个 Agent 想在这些场景里真正有用，必须同时回答三个问题：它的核心技术能力发展到哪一级？它能承担什么类型的行业任务？它该如何被可靠评估？

**关键改造是把工业 Agent 看成一个能力成熟度问题**。从 L1 到 L5，Agent 不只是“越来越强”，而是在记忆、规划和工具使用三个维度上不断升级；这些技术升级又对应到不同的行业应用形态，最后还要落到可评估、可部署、可治理的真实系统里。

## 摘要

随着大语言模型的发展，能够推理、规划并执行复杂任务的 Agent 已经成为人工智能的重要方向。但通用 Agent 要真正进入产业场景，还要面对更复杂的问题：行业任务往往知识密集、流程复杂、风险更高，不能只依赖通用能力直接落地。

这篇综述围绕大语言模型驱动的工业 Agent，梳理了技术基础、行业应用和评估方法三条主线。技术上，工业 Agent 主要依赖记忆、规划和工具使用：记忆负责保存和利用历史信息，规划负责把目标拆成可执行路径，工具使用让 Agent 能连接外部系统、数据库、程序接口和专业工具。

在应用层面，工业 Agent 的能力被组织成五个成熟度层级：从流程执行系统，到交互式问题解决系统，再到端到端自治系统、协作智能系统和适应性社会系统。这个划分的意义在于，它把 Agent 在行业中的角色讲清楚了：有的系统只是把自然语言转换成 SQL 或 CAD，有的系统能辅助决策，有的系统则开始承担完整任务流程，甚至支持多 Agent 协作和复杂系统仿真。

评估部分进一步强调，工业 Agent 不能只用通用推理题来判断好坏，还要同时看基础能力和行业能力。基础能力包括记忆、规划和工具使用，行业能力则覆盖 Web 操作、软件工程、金融、医疗、科学研究和工程设计等具体场景。

整体来看，**工业 Agent 的核心不是单点算法突破，而是技术能力、行业知识、评估体系和治理机制能否形成闭环**。

## I 引言

大语言模型（Large Language Model, LLM）已经具备很强的语言理解、生成和推理能力，但它们本质上仍然更像“静态预测模型”：输入一段文本，输出一段文本。它们不天然具备长期记忆，也不能主动和外部世界交互，更不能稳定完成需要多步执行、环境反馈和工具协作的复杂任务。

Agent 的出现，就是为了解决这个断层。把大语言模型作为“大脑”，再接上记忆模块、规划模块和工具调用接口，模型就不只是回答问题，而是可以感知环境、制定计划、执行动作，并从交互中调整后续行为。

但从通用 Agent 走向工业 Agent，中间还有一层更难的现实约束。工业场景通常具有三个特点：

- **知识密度高**：医疗、金融、制造等行业都有大量专业知识和隐性经验；
- **流程复杂**：真实任务往往跨系统、跨工具、跨角色，不是单步问答；
- **风险更高**：错误可能带来经济损失、安全事故、合规问题或医疗风险。

因此，工业 Agent 不是把一个通用聊天机器人接进企业系统就完事了。它更像一个部署在具体业务环境中的自治或半自治系统，需要结合行业知识、专业工具和安全规则，解决真实世界问题。

这里和已有综述的区别也很清楚。已有综述通常分别讨论记忆、规划、工具学习、多 Agent、科学发现或金融交易等单一方向；这篇综述想补上的，是一个把**技术演化、行业应用和能力评估**放在一起看的框架。

贡献可以概括为三点：

- 提出工业 Agent 的五级能力成熟度框架；
- 把记忆、规划、工具使用三条技术线和 L1-L5 的应用能力对应起来；
- 系统梳理工业实践和评估基准，并指出真实落地中的关键挑战。

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-02.png>)

> 图1：工业 Agent 的框架。

解释：图1是整篇论文的总览图。上方从 Level 1 到 Level 5 给出工业 Agent 的五级成熟度：
- L1 是流程执行系统，主要做自然语言到形式语言转换和信息处理；
- L2 是交互式问题解决系统，重点是交互增强和决策支持；
- L3 是端到端自治系统，进入数字工程、科学发现和具身智能；
- L4 是协作智能系统，面向业务执行和复杂系统仿真；
- L5 是适应性社会系统，开始涉及自主目标、价值演化和环境共演化。
下方把这五级能力映射到三条技术主线：**记忆从上下文记忆发展到演化记忆**，**规划从线性规划发展到自主目标规划**，**工具使用从指令驱动发展到工具创造**。**这张图真正重要的地方，是把“技术能力”和“行业价值形态”绑定起来了**。

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-03.png>)

> 图2：工业 Agent 的分类体系。

解释：图2把整篇综述涉及的内容展开成一棵分类树。最上层分成三块：技术、实践和评估。
- 技术部分继续分成记忆、规划、工具使用，每一类下面都列出从低级到高级的能力层次和代表工作。
- 实践部分对应 L1-L5 的行业形态，从流程执行系统到适应性社会系统。
- 评估部分分成基础能力评估和行业领域评估，前者看记忆、规划、工具使用，后者看通用行业场景和具体行业场景。
它不是简单文献列表，而是把“哪些技术支撑哪些应用、哪些基准评估哪些能力”放在同一张地图里。

## II 工业 Agent 的技术基础

工业 Agent 的技术基础被拆成三条线：记忆、规划和工具使用。可以把它们理解成 Agent 的三个核心能力：

- 记忆回答“我知道什么、记得什么、能从过去学到什么”；
- 规划回答“我要怎么把目标拆成可执行路径”；
- 工具使用回答“我能调用外部世界的哪些能力来完成任务”。

这三者不是并列摆放的模块，而是共同决定 Agent 能力上限的系统结构。一个没有长期记忆的 Agent，很难做跨会话任务；一个不会规划的 Agent，很难处理长流程；一个不能可靠用工具的 Agent，很难进入真实工业系统。

### A 记忆机制：从短期上下文到群体演化记忆

记忆机制让 Agent 超越一次性上下文，能够编码、存储和检索历史信息。对工业 Agent 来说，记忆不只是“记住聊天记录”，而是决定它能不能持续适应一个业务场景。

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-04.png>)

> 图3：工业 Agent 中记忆机制的演化。

解释：图3把记忆机制的升级压缩成三段：从瞬时记录和被动检索开始，到主动学习与经验内化，再到集体记忆的出现。下面同时列出真实部署中的挑战，包括错误传播、过时谬误、隐私问题，以及安全可控。这里的重点是：记忆越强，Agent 越能持续学习；但记忆越强，也越容易把错误、隐私和偏见长期保存下来。

#### 1 从瞬时记录到被动检索

L1 阶段的记忆主要是上下文记忆（Contextual Memory），本质上就是工作记忆。Agent 依赖大语言模型的上下文窗口，临时保存当前任务中的输入、推理过程和最近交互。ReAct 中保留推理链和行动轨迹，就是这种瞬时记忆的典型形式。

这种记忆的好处是简单，能够维持单轮或短流程任务的一致性。问题也明显：**任务结束就容易丢失，窗口长度有限，信息多了还会互相干扰**。LongChat、Memory Sandbox、RecAgent、MemGPT 等工作都在尝试扩展或管理这种短期记忆，但它们并没有改变瞬时记忆“临时、易失、依赖上下文窗口”的本质。

这些方法的侧重点不完全一样。LongChat 更关注长上下文，让模型能处理更完整的长交互；Memory Sandbox 则强调人工管理记忆，把不相关信息从提示中移除，减少上下文污染；RecAgent 面向推荐场景维护短期记忆缓存；MemGPT 把最近交互放进工作上下文或闪存式记忆里，用来维持当前任务状态。它们都在增强“当前任务里的记忆”，但还没有真正形成跨任务、可长期积累的经验系统。

到了 L2，记忆开始变成被动检索记忆（Passive Retrieval Memory）。Agent 不再只靠上下文窗口，而是连接外部知识库、数据库、网页、百科或行业文档。检索增强生成（Retrieval-Augmented Generation, RAG）就是这一阶段的核心技术：任务来了，系统先检索相关知识，再把知识放进上下文辅助回答。

这类方法在行业场景里很常见。ChatDoctor 把医学知识接入问答过程，ChatDB 面向数据库交互，把对话和数据库查询结合起来；MemoryBank 更强调长期对话中的用户记忆，RET-LLM 和 Memory Decoder 则把检索或记忆解码机制接到模型生成过程中。它们的共同点是：**知识主要存在模型外部，Agent 通过检索把相关信息临时取出来使用**。

这一阶段的关键变化是 **Agent 从“无状态执行器”变成“会查资料的助手”**。例如医疗问答可以检索医学知识库，数据库问答可以生成 SQL 精确查询，游戏 Agent 可以查 Minecraft Wiki，科学 Agent 可以调用化学工具或计算工具。限制也很清楚：**Agent 只是更会查，不等于真正学会了这些知识**。它的能力边界仍然受外部知识库质量和检索算法精度限制。

#### 2 主动学习与经验内化

L3 阶段的记忆开始进入主动学习。**Agent 不只是读取外部信息，而是从自己的任务轨迹中总结经验**。这里最关键的变化，是**记忆从“信息存储”变成“经验提炼”**。

**从交互中主动学习**的经验来源可以有两类：
- **一类来自单次任务执行中的观察**，比如 Generative Agents 记录模拟世界中的行动，Voyager 记录在 Minecraft 中成功执行过的可复用代码。
- 另一类更有价值，**来自多次任务尝试中的成功和失败**。Reflexion 会让 Agent 分析失败轨迹，生成语言反思，再存入记忆；Retroformer 进一步训练反思模型，从多次实验中抽取更有效的信息；ExpeL、Synapse 这类方法会检索类似成功轨迹，用过去案例指导新任务。

TiM、MemoChat、SCM 这类工作也在推动记忆从“存储信息”转向“整理经验”。TiM 会把信息抽取成实体关系并存入结构化数据库，MemoChat 会把对话片段总结成可索引的主题，SCM 则设计记忆控制器，判断什么时候应该执行写入操作。它们说明一个变化：Agent 不能只把历史原样塞回上下文，而要把历史压缩成当前任务真的能用的经验。

**这一阶段的 Agent 不只是信息消费者，也开始成为经验生产者**。它会把一堆原始交互记录，压缩成更高层的行动准则。

**经验内化**还有另一种路线：把外部知识写进模型内部。
- 传统做法是**领域微调**，例如 Character-LLM 会把角色特征写进模型参数，医疗领域的 Huatuo、DoctorGLM、Radiology-GPT 分别面向中文医学知识、医疗数据和放射影像数据增强模型，金融领域的 InvestLM 则面向投资能力做适配；
- 更细粒度的参数修改方法是**知识编辑**。它也可能涉及参数更新，但目标不是全量适配一个领域，而是对模型中的特定事实、人格或局部知识进行定向修改，代表技术包括 MEND、KnowledgeEditor、MAC、PersonalityEdit 等。

> 这里的 MEND（Model Editor Networks with Gradient Decomposition，基于梯度分解的模型编辑网络）可以理解成一种“定点改知识”的方法。普通微调是拿一批新数据继续训练模型，让模型整体更适合某个领域；MEND 则是先看模型在某条错误知识上的梯度，判断如果要把这条知识改对，参数大概应该往哪里动，然后用一个小型编辑网络把这个更新方向修正得更克制、更局部。这样做的目的不是重新训练整个模型，而是**尽量只改目标知识，同时少影响其他原本正确的能力**。

KnowledgeEditor 和 MEND 类似，也会训练轻量编辑网络来预测参数更新；MAC 更偏在线参数化记忆适配，让模型在新交互中快速调整；PersonalityEdit 则把编辑对象从事实知识扩展到人格特征，尝试按大五人格等心理学维度修改 Agent 的角色表现。这样看，经验内化不只是在“补知识”，也包括把角色、能力和行为倾向写进模型内部。

**微调是“整体训练模型更适合某个领域”，知识编辑是“定点修改模型里某些具体知识或行为”**。它们代表从“外部存储知识”到“培养内部能力”的转变。

不过，参数化内化通常不够可控，也不容易解释。因此非参数记忆管理仍然很重要，这里可以拆成写入、整理和读取三步：
- 写入负责把原始信息存进记忆；
- 整理负责总结、抽象、去噪和压缩；
- 读取负责按当前任务检索最相关的经验。
Mem0 这类系统进一步把记忆做成可扩展的记忆中心，甚至用实体关系图表示跨会话、跨时间点的信息。

#### 3 集体记忆的出现

L4 及以上，Agent 从单体系统走向多 Agent 协作，**记忆也开始变成分布式共享记忆**。多个 Agent 要协同完成大目标，就必须共享需求文档、代码库、接口规范、历史对话和错误记录。ChatDev、MetaGPT、AutoGen 等框架都体现了这一点。

Memory-R1 这类方法更直接地把记忆作为可共享、可推理的系统资源来设计；AutoGen 让多个 Agent 在对话协作中共享中间状态；MetaGPT 把软件团队里的产品经理、架构师、工程师等角色流程化；ChatDev 则把软件开发拆成多个角色之间的协作对话。这里的记忆已经不只是某个 Agent 的私人上下文，而是整个协作组织的公共工作区。

共享记忆的价值在于避免信息孤岛。多个角色如果各自只看自己的上下文，很容易产生认知偏差或重复工作；共享记忆提供了一个统一认知空间，让协作围绕同一套事实推进。

L5 的记忆更进一步，接近**进化型记忆或文化记忆**。这里的记忆不只是跨 Agent 共享，也会跨时间积累和传承，记录群体成功策略、失败教训和共同价值。Generative Agents 中信息在虚拟社会中传播，Aivilization 构建更大规模的虚拟社会，WarAgent 记录国家之间的长期对话，这些都可以看成演化记忆的早期形态。【说实话，到这里我看不懂了】

ChatHaruhi 和 RoleLLM 这类工作则更偏角色智能体构建，关注 Agent 如何在持续互动中保持身份、性格、说话方式和行为习惯。它们和 Aivilization、WarAgent 这类社会仿真工作共同指向一个更远的问题：当记忆不再只是个体经历，而是被群体长期继承、传播和改写时，Agent 系统会不会形成类似“文化”的稳定模式。

#### 4 真实世界中的记忆挑战

记忆强了，并不等于系统一定更可靠。恰恰相反，记忆会带来新的风险。

1. **错误传播**。如果早期记忆里有错误，后续任务可能不断复用这些错误经验，把小问题放大成长期偏差。
2. **过时经验干扰**。工业环境变化很快，旧流程、旧政策、旧数据可能不再适用，但 Agent 仍然可能把它当成有效经验。
3. **隐私风险**，记忆模块保存用户交互和业务数据，如果没有访问控制和删除机制，就可能被提示注入或记忆抽取攻击利用。

所以真实工业 Agent 的记忆系统不能只追求“记得多”，还要能判断什么该写入、什么该删除、什么该隔离、什么需要审计。

### B 规划能力：从线性推理到自主目标规划

规划决定 Agent **如何把抽象目标拆成可执行动作序列**。工业场景里的**规划能力直接关系到自治程度、可靠性和任务复杂度**。

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-05.png>)


> 图4：工业 Agent 中规划能力的演化。

解释：图4把规划演化分成三段：从线性推理到多轮交互，再到反思、迭代和深度探索，最后扩展到协作规划和自主目标。下方列出真实部署中的挑战：可靠性与不确定性管理、知识和工具深度融合、安全可信、部署实现，以及对规划机制本身的理论理解。这里的重点是：规划越往后，越不只是“列步骤”，而是要面对动态环境、失败恢复、多 Agent 协商和目标生成。

#### 1 从线性推理到多轮交互

L1 的规划是**线性规划**，本质上是开环的。典型方法是思维链（Chain-of-Thought, CoT）：让模型写出中间推理步骤，从而提高多步问题的正确率。Zero-shot-CoT 用一句 “Let’s think step by step” 触发推理，Plan-and-Solve 则把“先规划、再执行”显式拆开。

线性规划能处理很多逻辑题和多步推理题，但它默认环境静态、初始计划正确。**一旦执行过程中出现偏差，它没有天然的反馈修正机制**。

L2 的规划进入**反应式规划**。ReAct 是关键节点：模型交替进行推理、行动和观察，执行过程中可以根据工具或环境反馈调整下一步。DEPS、ProgPrompt、PoT、PAL、LLM+P、LLM+PDDL、LLM+ASP 等方法进一步把自然语言规划接到程序执行、机器人函数或符号规划器上，提高计划的可执行性和可靠性。

这一阶段的**优势是能“边做边改”，但问题是视野通常偏局部**。它擅长应对变化，却不一定擅长长周期权衡和全局搜索。

#### 2 全局规划：反思、迭代和深度探索

L3 阶段，Agent 进入**端到端自治系统**，规划必须面对复杂、动态、不确定的任务。规划不再只是写一条步骤链，而要**具备非线性探索、失败恢复和持续优化能力**。

第一条路线是**把推理结构从链扩展成树或图**。Tree-of-Thought（ToT）把多个候选推理路径组织成树，让 Agent 可以探索、评估、剪枝和回溯。Graph-of-Thought（GoT）进一步把推理结构推广成图，让不同路径的中间结果可以合并、复用和转换。

第二条路线是**把搜索算法引入规划**。LLM-MCTS 和 RAP 把大语言模型作为启发式评估或世界模型的一部分，用蒙特卡洛树搜索在候选路径之间平衡探索和利用。**这让 Agent 从贪心式下一步决策，转向能够比较未来可能性的前瞻式规划**。

第三条路线是**反思和自我修正**。Reflexion 让 Agent 分析失败轨迹并写成语言经验；Self-Refine 让模型围绕当前输出反复生成、反馈和修改；CRITIC 借助外部工具验证并批判当前输出；LEMA 则把错误推理样本加工成纠错数据，用于微调模型。它们共同说明：规划不是一次性生成，而可以变成持续改进的闭环。

#### 3 协作规划和自主目标设定

L4 之后，规划从单个 Agent 扩展到多个 Agent。**核心问题从“我该怎么做”变成“我们该怎么分工协作”**。HuggingGPT 用一个大语言模型协调多个模型完成多模态任务，CodeEdu、RIDAS、Aime 等系统进一步展示了多 Agent 在教育、通信网络和通用任务中的协作潜力。

L5 则进入更远的设想：**Agent 不只是执行目标，也能提出目标**。城市规划、社会仿真、自我演化导航等工作都在探索这种方向。这里已经触及更根本的问题：机器能否形成自己的愿景？如果能，它的目标如何和长期价值对齐？【那人类干什么？？？】

#### 4 规划在真实世界中的难点

真实世界的规划难点主要集中在五个方面。

1. **不确定性**：环境会变化，反馈会延迟，工具会失败，任务条件会更新。
2. **安全可信**：Agent 规划能力越强，攻击面也越大，例如记忆或工具输出中可能隐藏延迟触发的恶意指令。
3. **部署难**：复杂规划框架往往成本高、延迟大，很难直接放进嵌入式系统、企业流程或实时控制环境。
4. **理论理解不足**：我们还没有真正弄清楚大语言模型到底学到了什么规划原语，以及它们如何组合成复杂策略。
5. **知识和工具融合**：未来规划不能只靠文本推理，而要能利用结构化知识、数据库、业务工具和外部系统。

### C 工具使用：从调用工具到创造工具

工具使用是 Agent 区别于传统语言模型的第三条关键线。没有工具，Agent 只是封闭的文本系统；有了工具，它才能访问数据库、调用 API、浏览网页、执行代码、控制软件，甚至操作物理设备。


![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-06.png>)

> 图5：工业 Agent 中工具使用能力的演化。

解释：图5把工具使用分成三个阶段：从指令驱动到目标驱动，从端到端自治系统中的工具编排，再到协作执行和环境改造。图里还列出工具使用的挑战：安全可靠、底层实现和应用深化。它强调的不是“工具越多越好”，而是 Agent 要逐步学会选择工具、组合工具、处理工具失败，最后甚至创造新工具。

#### 1 从指令驱动到目标驱动

L1 的工具使用是固定的、隐式的**指令式调用**。比如 CoT 可以被看成把“推理”当成一种隐式工具，PAL 则让模型生成代码并交给解释器执行。此时工具是预设的，模型并不真正知道自己在选择工具。

L2 进入**目标驱动工具调用**。Toolformer 让模型学习何时、何地、如何调用 API；ReAct 提供了“思考—行动—观察”的工具交互范式；WebGPT、WebShop、ToolLLM、Gorilla 等工作把工具规模扩展到浏览器、网页环境和成千上万个真实 API。

这一阶段的 Agent 已经从“只会想”变成“会使用工具做事”，但它仍然受限于预定义工具库。工具库里没有的能力，它很难凭空补上。

#### 2 端到端自治系统中的工具编排

L3 阶段，任务往往需要**多个工具协同**完成。一个工具解决不了整个问题，Agent 必须学会把多个简单工具组织成工具链，主要有以下3个特点：

1. **工具链规划**：ART、Chameleon、ToolChain、ToolNet 等方法关注多步工具调用、工具空间搜索和工具图结构。它们要解决的是：面对大量工具，Agent 怎么知道该先用哪个、再用哪个、失败后怎么换路。
2. **异常自修复**：真实工具调用经常失败：API 不可用、参数填错、返回异常、权限不足。CRITIC 这类方法通过外部工具反馈来验证和修正输出；软件工程里的 LibLMFuzz、BugScope 等工作则展示了 Agent 如何利用工具链自动分析、发现并修复错误。
3. **行业专用工具工具包**：工业 Agent 不能只会通用搜索和代码执行，还要接入医疗影像模型、化学工具、形式化证明环境、金融数据库、企业系统接口等专业工具。**工具使用从通用能力走向行业专用工具箱，是 Agent 创造行业价值的前提**。

#### 3 从协作执行到环境改造

L4 的工具使用变成**多 Agent 协作执行。多个 Agent 共享工具集，围绕一个大目标分工协作**。HuggingGPT 是早期例子，它用 ChatGPT 协调 Hugging Face 上的多个模型；CodeEdu、GasAgent、IM-Chat、FlowForge 等工作则进入教育、智能合约、注塑行业知识转移和多 Agent 工作流设计。

L5 的工具使用指向**工具创造**。**Agent 不只是调用工具，而是在发现能力缺口后自己创建新工具**。Auto-GPT、BabyAGI 展示了开放目标下串联工具的雏形，CREATOR 则更明确地让模型在解题过程中识别能力缺口并生成新工具。

这一步很重要，因为它意味着 **Agent 不再只是适应环境，而开始主动扩展自身能力、改造环境**。

#### 4 工具使用的真实挑战

工具使用的风险主要集中在三类。

1. **安全和可靠性**，Agent 连接真实 API 后，错误调用可能直接影响业务系统。工具选择、执行和结果整合的每一步都可能被攻击或出错。
2. **底层实现**，如何把工具表示进模型、如何让模型稳定理解工具参数、如何让旧模型适配新工具，仍然是开放问题。
3. **行业应用真实性**，很多工具调用环境仍是简化模拟，和真实企业系统、医疗系统、金融系统差距很大。Agent 在模拟环境里能跑通，不代表在真实系统里可靠。

## III 工业 Agent 的应用实践

技术演化最终要落到应用形态上。这里按照 L1 到 L5，把工业 Agent 的实践分成五类：
1. **L1 流程执行系统**：翻译指令、执行固定流程；
2. **L2 交互式问题求解系统**：人机协同，作为智能助手；
3. **L3 端到端自主系统**：独立完成领域复杂全流程任务；
4. **L4 协同智能系统**：多智能体集群协作，处理大型业务与系统仿真；
5. **L5 自适应社会系统**：自主生成目标、与环境共生演化（现阶段以理论探索为主）。

### A 流程执行系统

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-07.png>)


> 图6：流程执行系统和交互式问题解决系统。

解释：图6对应 L1 和 L2。
- L1 的重点是**把自然语言转换成形式表达**，并从复杂信息中抽取结构化数据；
- L2 则**从单轮处理走向交互式执行**，包括软件/网页操作增强，以及知识探索和决策支持。
图里真正想表达的是：早期工业 Agent 的价值不是完全自治，而是降低专业系统使用门槛、提高人类执行和决策效率。

L1 的 Agent 是流程执行系统，**核心价值是可靠执行人的明确指令**。它主要做两件事：把自然语言转成机器可执行的形式语言，以及从大量非结构化信息中抽取结构化数据。

#### 1 从自然语言到形式语言

数据库交互中的 Text-to-SQL 是典型例子。用户用自然语言提问，系统生成 SQL 查询。HydraNet、SQL-PaLM、DIN-SQL 等工作分别从列排序、少样本提示、指令微调、执行反馈和问题分解等角度提升复杂查询能力。到了行业场景，还会出现面向特定会计标准或金融场景的定制系统。

工业设计里的 Text-to-CAD 也是同一类问题。用户描述产品，系统生成三维模型或 CAD 脚本。CADFusion、Text2CAD、CadQuery、CAD-Coder、FlexCAD 等方法都在降低专业设计软件的使用门槛。

这里的共同逻辑是：**把人类自然语言中的意图，翻译成专业系统能执行的形式表达**。

#### 2 结构化信息抽取与处理

另一类 L1 应用是从表单、票据、病历、网页或业务文档中抽取结构化信息。LayoutLM 和 LayoutXLM 通过联合建模文本、布局和视觉信息，提高富文档理解能力。医疗电话调查、垃圾邮件检测等任务也可以用大语言模型增强传统信息处理流程。

这一阶段的 Agent 更像“自动化执行器”。它们很有用，但还没有真正进入复杂自治。

### B 交互式问题解决系统

L2 的 Agent **从命令执行器变成协作助手**。它们一方面可以操作软件、网页和移动端界面，另一方面可以结合知识库和工具为人提供决策支持。

#### 1 交互执行与增强

GUI 和 Web Agent 是代表方向。UFO、CogAgent、SeeClick、WebVoyager、WebAgent 等系统让 Agent 能理解用户意图，并把意图转化为桌面、移动 App 或网页上的一系列操作。Rollback、LASER、Language Agent Tree Search 等方法引入回退和搜索机制，提高复杂交互任务成功率。

这些工作释放的是数字劳动力：Agent 可以替人完成重复、繁琐、跨界面的操作。

#### 2 知识探索与决策支持

在金融、科学和医疗场景中，L2 Agent 更像顾问。金融 Agent 可以分析市场动态和策略；ChemCrow 可以调用化学工具辅助合成任务；医学 Agent 可以通过多 Agent 辩论、检索和迭代诊断支持复杂问答。

这类系统通常不完全替代人，而是增强人的判断。它们的价值在于整合知识、调用工具、给出解释，但最终高风险决策仍需要人类参与。


### C 端到端自治系统

L3 的 Agent 开始接收高层目标，并独立完成一个领域内的复杂任务。它们不再只是“帮人点按钮”或“帮人查资料”，而是更接近一个能执行完整流程的自治系统。

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-08.png>)


> 图7：端到端自治系统。

解释：图7对应 L3。端到端自治系统不再只是执行局部操作或辅助决策，而**是围绕高层目标独立完成更完整的任务流程**。图中列出的三个方向是数字工程、科学发现和具身智能。它们分别对应数字世界、科学探索和物理世界中的复杂任务。

#### 1 自治数字工程

**软件工程是 L3 Agent 最成熟的领域之一**。AutoDev、SWE-Dev、LocAgent、PentestGPT、HackSynth、OptiGuide 等工作覆盖软件开发、代码定位、渗透测试和供应链优化。数字工程适合 Agent 的原因很直接：代码、测试、日志、接口和执行结果都在数字环境里，反馈清晰，试错成本相对低。

#### 2 自治科学发现

**科学 Agent 的目标是把文献阅读、实验设计、代码实现、结果分析甚至论文写作自动化**。AI Scientist-v2、LLMatDesign、AgentDrug、LIDDiA、ORGANA、ARChemist 等系统进入材料发现、药物优化、化学实验和机器人实验室。

这类工作展示了 Agent 作为“AI 科学家”的潜力，但真实科学发现仍然需要非常强的领域知识、实验反馈和可靠验证。

#### 3 具身智能

**具身智能让 Agent 进入物理世界**。GITM、PaLM-E、Embodied Chain-of-Thought、AdaPlanner、TaPA 等工作**让模型结合视觉、语言、动作和环境反馈完成任务**。这里的**困难比数字世界大很多，因为物理环境有噪声、延迟、摩擦、光照、传感器误差和不可控变化**。

### D 协作智能系统


![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-09.png>)

> 图8：协作智能系统和适应性社会系统。

解释：图8对应 L4 和 L5：
- L4 主要是协作智能系统，包括智能制造、供应链和金融服务等业务执行，也包括交通、城市和社会系统仿真。
- L5 则进一步进入适应性社会系统，强调自主目标生成、演化价值对齐、系统与环境共生演化，以及涌现社会结构。
也就是说，**L4 关注“多个 Agent 怎么协作完成复杂业务”，L5 关注“Agent 群体能否像社会一样长期自组织和演化”**。


L4 的重点**从单个 Agent 转向组织级协作**。多个 Agent 具备不同角色，围绕业务流程或系统仿真协同工作。

#### 1 协作业务执行

在制造、供应链和金融中，**多 Agent 可以模拟企业内部不同角色**：分析师、策略师、风险管理者、库存管理者、生产协调者等。TradingAgents、FinCon、HedgeAgents、InvAgent、MADTwin 等工作展示了多角色协作如何改善复杂业务任务。

这里的关键不是某个 Agent 更聪明，而是**组织结构和协作流程本身成为智能的一部分**。

#### 2 复杂系统仿真

L4 还可以构建高保真数字实验室，**用于模拟交通、城市、经济、社会等复杂系统**。CoMAL、CoLLMLight、CitySim、AgentSociety 等系统用多 Agent 模拟个体行为，再观察宏观系统动态。

这种能力对政策制定、城市治理、供应链调度和风险推演很重要，因为很多问题不能直接在真实世界里大规模试错。

### E 适应性社会系统

L5 是论文中的**远景层级**。它不是当前已经成熟的系统，而是一种**对未来工业 Agent 的设想**：Agent 不只是执行人类目标，也能够生成目标、演化价值、改造环境，并在群体互动中形成组织、规范和文化。

这里有四个核心特征：

- 自主目标生成：系统不再被动等待人类输入目标，而能基于观察和预测提出长期目标；
- 演化价值对齐：价值和决策标准不是固定规则，而会在长期互动中调整；
- 系统与环境共生演化：Agent 改变环境，环境反过来塑造 Agent；
- 涌现社会结构：多个 Agent 的互动中可能自发形成组织、规范和文化。

L5 仍然非常早期，也带来深层问题：如果 Agent 能自主生成目标，如何保证它的目标仍然安全、可控、符合人类长期利益？

## IV 工业 Agent 的评估

工业 Agent 真正部署之前，必须被可靠评估。评估不仅衡量技术进展，也决定模型优化、安全保证和行业信任。

![](<images/赋能现实场景：LLM-驱动的行业Agent-——-技术、实践与评估综述-10.png>)

> 图9：工业 Agent 的评估。

解释：图9把评估分成两大块：
- **基础能力评估**：基础能力包括记忆、规划和工具使用；
- **行业实践评估**：行业实践包括通用领域和具体领域。
具体领域又覆盖 Web 与 GUI 交互、软件和硬件工程、金融经济、医疗、科学研究、工程设计和其他专业领域。这个划分很重要，因为**一个 Agent 在基础能力上强，不代表它能直接进入行业**；反过来，**一个行业任务表现好，也不一定说明它具备通用智能**，也可能在另一个行业很差。

### A 基础能力评估

#### 1 记忆能力评估

记忆评估关注四类能力：信息检索是否准确，跨任务学习是否有效，长距离上下文是否能保持，冲突信息是否能解决。MemoryAgentBench、FindingDory、3DMem-Bench、QuALITY、QMSum、LoCoMo、LifelongAgentBench 等基准分别覆盖多轮交互、具身记忆、长文本理解、长期对话和终身学习。

这里的趋势是从静态问答走向动态交互。真实 Agent 的记忆不是“能背多少”，而是能否在长周期任务中持续整合信息、更新经验、避免冲突。

#### 2 规划能力评估

规划评估从数学逻辑推理扩展到长周期交互决策。GSM8K、HotpotQA、MATH、StrategyQA 等基准评估多步推理和知识整合；TextAtari、FlowBench、NATURAL PLAN 等更关注长程决策、工作流规划和真实任务安排。

反思、修改和反馈学习也成为规划评估重点。Self-Reflection Benchmark、Reflection-Bench、MINT、AdaPlanner、LLF-Bench 都在衡量 Agent 能否从失败或反馈中改进。

更形式化的评估则使用 PDDL-to-NL、ACPBench 等方法，把自然语言规划和符号规划任务连接起来，观察大语言模型与经典规划器之间的差距。

#### 3 工具使用能力评估

工具使用评估关注工具选择、调用、组合和鲁棒性。BFCL、ToolBench、ToolAlpaca、API-Bank 等基准评估函数调用和 API 调用能力；Seal-Tools、StateEval、ComplexFuncBench、NESTFUL、ToolSandbox 等进一步加入多步、嵌套、状态依赖和动态重规划。

工具评估正在从“能不能调用一个 API”走向“能不能在复杂状态下稳定执行一串工具链”。这更接近真实工业任务。

### B 行业实践评估

行业评估不能只看通用能力，还要看 Agent 是否理解业务流程、行业知识和风险边界。

#### 1 通用领域评估

GAIA、AgentBench、OSWorld、TheAgentCompany、CRMArena、SOP-Bench 等基准评估跨行业任务、企业流程、办公自动化、客户关系管理、安全风险和标准操作流程。它们提供了从通用认知能力到商业可用性的中间层。

#### 2 具体领域评估

Web 和 GUI 评估关注真实网页、企业软件、动态界面和安全可信。WebArena、VisualWebArena、WebVoyager、WorkArena、MMInA、ST-WebAgentBench 等都在推动 Agent 从静态网页任务走向真实开放网络环境。

软件和硬件工程评估以 SWE-bench 为代表，关注真实代码仓库中的 issue 修复、测试生成、漏洞检测、部署和硬件设计验证。

金融评估强调交易策略、实时市场、风险控制和压力测试。FinRL Contests、DeepFund、FinArena、InvestorBench、Risk-Engineering Framework 等基准都在强调金融 Agent 不能只看收益，还要看风险、鲁棒性和信息泄漏。

医疗评估覆盖临床推理、诊断、治疗规划、影像分析、护理观察、医疗计算和监管合规。MedAgentBoard、ClinicalAgent Bench、AgentClinic、MedChain、MedSentry、Cancer-Myth 等基准共同指向一个结论：**医疗 Agent 的评估必须把安全、公平和专业可靠性放在核心位置**。

科学研究和工程设计评估则关注 AI 科学家、AI 工程师能否完成实验设计、代码实现、论文分析、计算复现、材料发现、化学工作流、有限元分析、遥感理解等任务。

### C 现有评估基准的局限

当前评估体系仍然有几个明显问题。

1. **真实性和可复现性的矛盾**，越接近真实世界，环境越动态、随机、不可控；越可复现，环境往往越像简化模拟。Agent 在模拟里表现好，不代表上线后也可靠。
2. **评估成本和效率的矛盾**，开放式、多步任务最可靠的评估仍然依赖人类专家，但成本高、速度慢。用更强模型当评委可以提高效率，却会引入偏见、不一致和幻觉。
3. **安全沙箱带来的性能开销**，真实工具调用必须隔离，虚拟机或容器越安全，评估效率可能越低。
4. **知识时效问题**，金融、医疗、法律等领域的知识更新很快，基准很难和最新行业规则同步。
5. **隐私和合规**，真实行业数据很难开放，合成数据又可能丢失关键细节，导致评估和真实部署之间仍有差距。

## V 讨论

### A 知识和经验之间的差距

一个很重要的观察是：Agent 在软件工程、数据库交互、网页浏览这些数字原生领域进展很快，但在物理世界和社会领域进展慢得多。

原因在于数字世界的规则更显式。代码有语法，API 有文档，网页有 DOM，执行结果可以快速反馈。Agent 可以通过大量低成本交互学习。

物理和社会领域则充满隐性经验。老工程师判断设备故障，医生根据患者状态调整诊断，外交谈判中的语境判断，这些经验很难完全写成文本或数据。反馈也常常延迟、模糊、成本高。

所以未来的关键问题不是简单“再喂更多数据”，而是能不能设计新的 Agent 架构，让它通过**少量高质量人类**互动学习隐性经验。

> 这里不只是传统意义上的 RLHF。RLHF 主要通过人类偏好反馈优化模型输出，反馈者通常依据标注规范判断哪个回答更好；但这里强调的是**少量高质量专家**互动，注意是高质量专家。工业 Agent 更需要专家在真实任务中纠错、示范操作轨迹，并参与实际协作，让 Agent 从中学习隐性经验，把专业判断、流程经验和风险意识沉淀进记忆、规划和工具使用机制里。

### B 仿真环境的重要性

Agent 的能力不只来自模型本身，也来自它能交互的环境。代码解释器、浏览器 DOM、API 接口，本质上都是高保真数字仿真器：规则明确、反馈即时、试错成本低。

这解释了为什么数字领域 Agent 发展更快。Agent 的“思考—行动—观察”闭环依赖环境反馈，没有环境，行动没有意义，推理也容易空转。

物理世界的难点在于仿真和现实之间存在巨大差距。再先进的机器人仿真器，也难以完全模拟空气阻力、地面摩擦、光照变化和传感器噪声。

**未来工业 Agent 的上限，很大程度上取决于我们能否构建足够真实、可扩展、可交互的数字孪生环境**。

### C 能力和任务之间的不对称

一个 Agent 的**核心能力有短板，不一定影响所有任务**。比如一个网页抓取 Agent 可能几乎不需要长期记忆或复杂规划，只靠短期上下文也能表现很好。

但在开放、动态、长周期任务中，一个小短板就可能导致整体失败。任何环节缺失，都可能导致任务失败。

这说明工业实践有两条路线：
- 一条是把复杂任务拆成许多窄任务，让专门 Agent 各司其职；
- 另一条是继续构建更均衡的通用自治系统。
短期看，前者更现实；长期看，后者才接近通用工业智能。

### D 自主演化的囚徒困境

从 L3 的自我修正到 L5 的自主目标生成，理想中的 Agent 会不断从环境中学习、适应和进化。但**自主性越强，控制问题越尖锐**。

一方面，我们希望 Agent 在开放环境中探索，产生意外突破；另一方面，我们又担心它演化出不可理解或有害的行为。

这里至少有三个具体风险：
1. **目标漂移**：指 Agent 在长期优化中可能误解原始目标；
2. **沙箱悖论**：指真正的演化需要真实环境，但安全要求又把它限制在沙箱中；
3. **价值对齐**：如何让智能体长期遵循人类主流价值观与行业规范。
这不只是技术问题，也涉及治理框架、人机协作和社会制度设计。

### E 组织和流程集成阻力

工业 Agent 落地的难点不只在模型，技术之外，**企业现有 IT 架构、业务流程、组织架构**成为落地阻力：
- 企业内部往往有旧系统、私有软件、数据孤岛和不统一的接口。Agent 要接进去，本身就是系统集成工程。
- 组织变化：员工需要从传统执行者变成 Agent 的管理者和协作者，流程、权限、绩效和责任边界都要重塑。技术方案可以设计，但打破部门墙、数据墙和流程惯性，是一个慢而复杂的组织过程。

所以工业 Agent 的成功不只取决于模型能力，还取决于企业有没有能力推进深层组织变革。

## VI 结论

这篇综述最有价值的地方，是没有把工业 Agent 简单写成“通用 Agent + 行业知识”，而是提出了一个更清晰的成熟度框架：**Agent 的工业化落地，需要同时看技术能力、应用形态和评估体系**。

从技术上看，记忆、规划和工具使用共同决定 Agent 的能力边界。记忆让 Agent 能长期学习，规划让 Agent 能处理复杂目标，工具使用让 Agent 能进入真实系统。

从应用上看，L1 到 L5 展示了从流程执行到适应性社会系统的演化路径。当前最成熟的是数字原生场景，尤其是软件工程、网页操作、数据分析和信息处理；物理世界和社会系统仍然困难很多。

从评估上看，现有 benchmark 已经覆盖很多能力和行业，但仍然存在真实性、复现性、成本、安全、时效和隐私问题。**工业 Agent 真正要解决的，不是“能不能在测试集上更高”，而是能不能在真实复杂环境里稳定、可信、可治理地工作**。

但是局限也很直接：这是一篇综述，框架很完整，但很多 L5 讨论还处在远景层面；不同层级之间的边界也不是绝对清晰，很多系统会同时具备多个层级的特征。更现实的理解是：L1-L5 不是严格阶梯，而是一张帮助我们判断 Agent 工业成熟度的地图。

整体来看，这篇论文适合用来搭建“工业 Agent”知识框架。它把过去分散在记忆、规划、工具、应用和评估里的工作放到同一张图里，让我们更容易看清：大语言模型 Agent 想走向真实世界，下一步真正要补的不是某一个单点能力，而是可靠系统、行业知识、仿真环境、评估标准和组织落地能力的整体闭环。
