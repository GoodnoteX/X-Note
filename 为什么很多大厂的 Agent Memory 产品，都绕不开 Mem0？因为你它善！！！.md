> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Mem0: Building Production-Ready AI Agents with Scalable Long-Term Memory》的理解。

![](<images/Pasted image 20260619084208.png>)


> 这篇 2025 年的论文把 Agent 记忆问题从“能不能存历史”推进到“能不能在生产环境里低延迟、低成本、可更新地使用长期记忆”。核心方法是 **Mem0** 和它的图记忆版本 **Mem0g**：前者把对话压缩成可检索、可更新的自然语言记忆，后者进一步把实体和关系组织成图结构，用来处理更复杂的关系和时间问题。顺便说一下，Mem0 是众多 Agent Memory 平台的底层实现，市面上的Memory 产品多多少少都会参考 Mem0。

## 核心问题

这篇论文的核心问题非常直接：**如果 Agent 需要跨多轮、跨会话和长时间与用户互动，只靠大模型固定上下文窗口是否够用**？

答案肯定是不够的。上下文窗口再长，本质上仍然是在每次推理时把一大段历史塞回模型里。它可以延缓遗忘，但不能真正解决长期记忆的三个问题：哪些信息值得留下，旧信息和新信息冲突时怎么更新，需要回答问题时又该取出哪些记忆。

Mem0 的关键改造，是**把长期记忆做成一个独立的工程模块**：**先从新对话中抽取重要事实，再和已有记忆对齐、更新、删除或忽略，最后在回答问题时只检索最相关的记忆**。Mem0g 则进一步把记忆从“文本事实列表”升级成“实体关系图”，让 Agent 能更好处理人物、事件、时间和关系链。

## 摘要

大型语言模型已经能生成流畅、连贯的回答，但固定上下文窗口会限制它在多会话长期对话中的一致性。Mem0 解决的是这个问题：**可从持续对话中动态抽取、整合并检索对话中的关键长期信息，让 Agent 不必每次都重读完整历史**。

在 LOCOMO 长期对话记忆基准上，Mem0 和 Mem0g 被拿来和六类基线比较：已有记忆增强系统、不同切块大小和检索数量的检索增强生成、全文上下文方案、开源记忆方案、专有模型记忆系统，以及专门的记忆管理平台。结果显示，Mem0 在外部大模型评判指标上相对 OpenAI 记忆方案提升约 26%，Mem0g 的总体得分比基础 Mem0 约高 2%。更关键的是效率：**Mem0 相比全文上下文方案把 p95 延迟降低 91%，同时节省超过 90% 的 token 成本**。

这篇工作的价值不只在于分数更高，而在于它把长期记忆做成了一个更接近真实部署的系统：记忆要能抽取、更新、检索，还要够快、够便宜、能及时生效。

> **P95（95 分位延迟）** 是**服务性能、接口响应速度**领域最常用的**时延统计指标**，专用来衡量系统**最差情况下的响应表现**，不是平均速度。
>  **P95 延迟 = 把所有耗时排好后，前 95% 的请求都快于这个数值，只剩最后 5% 的请求比它更慢。**
>  全上下文长尾最慢 5% 请求耗时 17.117s，Mem0 对应长尾仅 1.440s，降低 91%。


## 1 引言

先从人类记忆讲起。人类之所以能维持长期关系和持续沟通，是因为我们会记住过去互动、用户偏好、事件线索，也会随着新信息修正旧判断。Agent 如果没有类似机制，就会出现很常见的问题：用户已经说过自己不吃某类食物，下次询问推荐时系统却完全忘记；或者已经建立过的事实，在后续对话里被新回答推翻。

大模型 Agent 的进步很快，但它们仍然依赖固定上下文窗口。窗口长度从 128K 到 200K，甚至更长，确实能容纳更多历史，但这只是把“遗忘”推迟了。**上下文窗口不是长期记忆**。它**不能自动决定哪些信息重要，不能自然处理记忆更新，也不能保证在未来问题里取出正确片段**。

图 1 用一个饮食偏好的例子把这个问题讲得很直观。
![](<images/Pasted image 20260617203958.png>)

> 图1：AI Agent 中记忆重要性的示意。左侧没有持久记忆时，系统会在不同会话之间忘记关键用户信息，例如素食和不吃奶制品的偏好，导致给出不合适的推荐。右侧有有效记忆时，系统能在多次交互中保持这些饮食偏好，从而给出符合此前约束的建议。

解释：没有记忆的系统只能看到当前会话，因此后续推荐会和用户早先说过的偏好冲突。有记忆的系统会把“素食”“不吃奶制品”这类信息保存下来，下一次回答时重新取出。这里要强调的不是饮食推荐本身，而是 **Agent 的长期一致性来自可持续更新和调用的记忆机制**。

现有长期记忆方案大致有几种方向：有的直接把历史对话塞进长上下文，有的把历史切块做检索增强生成，有的维护摘要、观察记录或图结构记忆。这些方法各有价值，但真实部署时会遇到两个硬约束：第一，质量要够好；第二，延迟和 token 成本不能爆炸。Mem0 就是在这个背景下提出的，它试图**在回答质量和生产效率之间找到更好的平衡**。新型记忆架构**Mem0**，可动态捕捉、梳理、调取对话关键信息；并在此基础上推出**图记忆版 Mem0**，利用图结构刻画对话元素间的复杂关联。

## 2 方法

Mem0 系列包含两个架构：
- 基础版 **Mem0** 使用自然语言形式的密集记忆，把对话中值得保留的事实抽取出来，并通过新增、更新、删除、忽略四种操作维护记忆库。
- 增强版 **Mem0g** 使用图记忆，把实体作为节点、关系作为边，用结构化方式保存人物、地点、事件、偏好和时间线。

这两个版本的共同目标是：**不要把完整对话历史当作记忆，而是把历史加工成更小、更准确、更容易检索的长期信息表示**。

### 2.1 Mem0：把新对话变成可维护的自然语言记忆

Mem0 按增量方式运行，适合持续进行的对话。每来一组新的消息对，就判断里面有没有值得进入长期记忆的信息。这里的消息对写成 $(m_{t-1}, m_t)$，其中 $m_t$ 是当前消息，$m_{t-1}$ 是前一条消息，通常可以理解为一次完整的用户和助手交互。

它不是只看这两条消息，而是同时看两类上下文：

- **对话摘要** $S$，从数据库取出，代表更长历史里的整体语义；
- **近期消息序列** $\{m_{t-m}, m_{t-m+1}, ..., m_{t-2}\}$，其中 $m$ 控制最近窗口大小，用来保留还没有被摘要完全吸收的细节。实验中设置参考历史消息数量 $m$=10。

于是，**记忆抽取**的输入可以写成：

$$
P=(S,\{m_{t-m},...,m_{t-2}\},m_{t-1},m_t)
$$

这里的 $P$ 是给大模型的综合提示信息：既有全局摘要，也有最近上下文，还有当前新交互。抽取函数 $\phi$ 由大模型实现，它会从 $P$ 中抽取候选记忆集合：

$$
\phi(P)=\Omega=\{\omega_1,\omega_2,...,\omega_n\}
$$

其中 $\Omega$ 是候选记忆集合，$\omega_i$ 是第 $i$ 条候选事实。可以把它理解成“这一轮对话里可能值得长期保存的信息”。

图 2 是 Mem0 的主流程图。

![](<images/Pasted image 20260618231301.png>)

> 图2：Mem0 系统的架构总览，展示了抽取阶段和更新阶段。**抽取阶段**处理当前消息和历史上下文，生成新的候选记忆。**更新阶段**将这些候选记忆与相似的已有记忆进行比较，并通过工具调用机制选择合适操作。数据库作为中心存储，一方面为处理过程提供上下文，另一方面保存更新后的记忆。

解释：流程先从数据库取出历史摘要和近期消息，再把它们和新消息一起交给大模型抽取候选记忆。候选记忆不会直接写入数据库，而是先去检索语义相近的已有记忆。随后，大模型通过工具调用选择操作：新增、更新、删除或不处理。这样做的好处是，记忆库不会无脑增长，也能在新信息和旧信息冲突时保持一致。

Mem0 的**更新和检索阶段**是整个方法最关键的部分。对每条候选记忆 $\omega_i$，系统先用向量嵌入从数据库里找出前 $s$ 条语义最相近的已有记忆，然后把候选记忆和这些相似记忆一起交给大模型判断。大模型需要在四个操作里选一个：

- **新增（ADD）**，当候选事实在已有记忆中没有等价信息时，创建新记忆；
- **更新（UPDATE）**，当候选事实补充了已有记忆，使它更完整时，更新旧记忆；
- **删除（DELETE）**，当候选事实和旧记忆冲突时，移除被新信息推翻的旧记忆；
- **不操作（NOOP）**，当候选事实已经存在，或者不值得进入长期记忆时，不做修改。

![](<images/Pasted image 20260617220909.png>)
这里没有再训练一个单独分类器，而是直接利用大模型对语义关系的判断能力。附录里的算法 1 把这个逻辑写成伪代码：先判断候选事实和已有记忆是否语义相似，如果完全不相似就新增；如果冲突就删除；如果补充信息量更高就更新；否则不处理。

这套设计解决了长期记忆里最容易被低估的问题：**记忆不是只会增加，还必须会更新、会删除、会忽略**。如果只有“保存历史”，记忆库会迅速变成噪声仓库；如果不能删除冲突事实，Agent 之后的回答就会反复踩旧信息的坑。

### 2.2 Mem0g：把记忆组织成实体关系图

Mem0g 在 Mem0 的基础上加入图结构。它把记忆表示成有向带标签图：

$$
G=(V,E,L)
$$

其中：
- $V$ 是节点集合，表示实体，例如人名、地点、事件、物品、偏好；
- $E$ 是边集合，表示实体之间的关系，例如“住在”“喜欢”“拥有”“发生于”；
- $L$ 是标签集合，用来给节点分配语义类型，例如人物、城市、事件。

每个实体节点 $v \in V$ 包含三类信息：

- **实体类型**，例如人物、地点、事件；
- **嵌入向量** $e_v$，用于表示该实体的语义；
- **元数据**，包括创建时间戳 $t_v$ 等信息。

关系则写成三元组：

$$
(v_s,r,v_d)
$$

其中:
- $v_s$ 是源实体;
- $v_d$ 是目标实体;
- $r$ 是连接两者的关系标签。
比如“用户 Alice 住在 San Francisco”，就可以表示为“ Alice（$v_s$） --lives_in（$r$）--> San Francisco（$v_d$） ”这样的关系。

图 3 展示了 Mem0g 如何从对话文本生成图记忆，并在新信息到来时更新图。

![](<images/Pasted image 20260617222152.png>)

> 图3：Mem0g 的图记忆架构，展示了实体抽取和更新阶段。**抽取阶段**使用大模型把对话消息转换成实体和关系三元组。更新阶段在把新信息整合进已有知识图谱时，使用冲突检测和冲突解决机制。实验中配置为使用前 10 条消息和 10 条相似记忆；大模型操作使用 GPT-4o-mini；向量数据库使用密集嵌入。

解释：**Mem0g 先从消息里抽取实体，再生成实体之间的关系三元组**。新三元组进入图之前，会先和已有图节点做相似度匹配：
- 如果没有相似节点，就创建新节点；
- 如果已有对应节点，就复用旧节点并添加新关系。
- 遇到冲突关系时，系统不会简单物理删除旧关系，而是由大模型判断哪些关系应该过时，并把它们标记为无效，这样后续仍然可以做时间相关推理。

Mem0g 的**抽取过程**分成两步：
- 第一步是 **实体抽取**，从文本中找出值得长期表示的信息单元，识别核心实体并标注类型；例如人物、地点、物体、概念、事件、属性。
- 第二步是 **关系生成**，判断这些实体之间是否存在有意义的连接，并给连接打上关系标签。

接下来是**记忆更新与检索阶段**。这部分决定图记忆能不能长期保持一致，也决定回答问题时到底怎么从图里取信息。

**图更新逻辑**先处理新关系三元组。针对每个新的三元组，系统会计算源实体和目标实体的嵌入向量，再到已有图库中检索语义相似的节点。根据节点是否已经存在，Mem0g 会选择创建两个新节点、只创建其中一个新节点，或者复用已有节点，然后把新的关系挂载到图里。这样可以避免同一个实体被反复创建成多个孤立节点。

- 更关键的是**冲突处理**。新信息进入图时，可能会和旧关系冲突，比如用户曾经住在 A 城市，后来搬到了 B 城市。Mem0g 会用**冲突检测机制**找出可能冲突的旧关系，再由大模型驱动的**更新解析器**判断哪些旧关系应该失效。这里不是把旧关系物理删除，而是把它标记为无效，因为旧关系本身仍然有时间价值：它能帮助系统回答“过去发生过什么”“什么时候发生变化”这类问题。底层图数据库采用 **Neo4j**。

**双模式检索**负责把图记忆取出来用：第一种是**实体导向检索**：先识别问题中的核心实体，在图库中匹配对应节点，再沿着这些节点的入边和出边扩展，取出相关子图作为上下文。第二种是**语义三元组检索**：把整段问题编码成嵌入向量，再和图库中关系三元组的文本表示做相似度匹配，筛选出高于阈值的内容，并按相似度排序。
- 双检索模式的设计适合处理两类问题：
	- 第一类是**明确围绕实体的问题**，比如“某人住在哪里”“某件事发生在哪天”。系统可以先识别查询里的关键实体，再在图里找到对应节点，沿着入边和出边扩展相关子图。
	- 第二类是**更宽泛的语义问题**，系统会把整个查询编码成向量，再和图里的关系三元组文本编码做相似度匹配，取出最相关的三元组。

所以 Mem0g 的优势不是“所有任务都比 Mem0 强”，而是 **当问题需要关系、时间顺序和多实体连接时，图结构能提供更清楚的检索路径**。这也解释了后面实验里它在时间推理上更强，但在单跳和多跳问题上不一定超过基础 Mem0。

## 3 实验设置

### 3.1 数据集：LOCOMO 长期对话记忆

实验使用 **LOCOMO** 数据集。这个数据集**专门评估对话系统的长期记忆能力**，包含 10 段长对话，每段平均约 600 轮、26000 个 token，并分布在多个会话中。对话内容是两个人讨论日常经历和过去事件。

每段长对话后面配有平均约 200 个问题和标准答案，问题分为四类：
- **单跳问题**，答案通常来自某一轮对话里的单个事实；
- **多跳问题**，需要综合多个会话或多个事实；
- **时间问题**，需要判断事件顺序、时间点或持续时间；
- **开放域问题**，需要结合对话记忆和更开放的背景知识。

原始数据集还有对抗问题，用来测试系统能否识别不可回答问题，但这里没有纳入评估，因为这类问题缺少标准答案。

### 3.2 评估指标：不只看回答对不对，也看能不能部署

**质量指标**包括 **F1**、**BLEU-1** 和 **大模型裁判分数（LLM-as-a-Judge, J）**。F1 和 BLEU-1 沿用传统词汇相似度指标作为参考，都偏词面重合，容易被“看起来相似但事实错误”的回答误导。比如标准答案是 Alice 三月出生，模型答成 Alice 七月出生，词面上仍然有不少重叠，但关键事实已经错了。

因此这里补充使用**大模型裁判分数 $J$，让一个更强的模型综合问题、标准答案和生成答案，判断回答是否在事实准确性、相关性、完整性和上下文适配性上正确**。为了降低随机性，每个方法在整个数据集上独立运行 10 次，并报告均值和标准差。

**部署指标**主要看两个方面。第一是 **token 消耗**，也就是回答问题时检索出来、作为上下文送入模型的 token 数。对记忆系统来说，这些 token 是检索到的记忆；对检索增强生成来说，这些 token 是检索到的原始文本块。第二是 **延迟**，分为检索延迟和总延迟：前者只看取回记忆或文本块的时间，后者还包括大模型生成答案的时间。

这组指标的设置很重要。长期记忆系统不能只追求分数，如果每次都要读几万 token、等十几秒，真实产品里就很难用。

### 3.3 基线方法：六类对照

对比对象分成六类。

第一类是已有 LOCOMO 基准，包括 LoCoMo、ReadAgent、MemoryBank、MemGPT 和 A-Mem。这些方法代表了长期对话记忆的不同路线，例如短期/长期记忆分层、阅读摘要、人格和事件记忆、类似操作系统的内外部记忆管理等。

第二类是开源记忆方案，主要是 LangMem。实验里用 GPT-4o-mini 作为语言模型，使用 text-embedding-small-3 作为嵌入模型。

第三类是 **检索增强生成（Retrieval-Augmented Generation, RAG）**。这里把完整对话历史当作文档集合，切成 128、256、512、1024、2048、4096、8192 个 token 的块，再检索前 $k$ 个最相似文本块。实验只设 $k=1$ 或 $k=2$，因为再增加检索块基本就接近把平均 26000 个 token 的完整对话都覆盖掉了，失去了选择性检索的意义。

第四类是 **全文上下文方案**，直接把完整对话历史送进大模型上下文窗口。这是一个很强但很贵的参照：信息最完整，但 token 成本和延迟最高。

第五类是专有模型系统，即 OpenAI 的 ChatGPT 记忆功能。由于没有外部 API 可以选择性检索 OpenAI 记忆，实验采用的是把整段 LOCOMO 对话灌入单个聊天会话，让系统生成带时间戳、人物名和对话文本的记忆，再用这些完整记忆作为回答上下文。

第六类是记忆服务平台 Zep。它是面向 AI Agent 的记忆管理平台，实验中保留时间戳信息，以便回答时间相关问题。

## 4 评估结果、分析与讨论

### 4.1 跨记忆系统的性能对比

表 1 是核心质量结果，覆盖四类问题和三个指标。

![](<images/Pasted image 20260618232635.png>)

> 表1：不同记忆增强系统在 LOCOMO 数据集不同问题类型上的性能对比。评估指标包括 F1、BLEU-1 和大模型裁判分数 $J$，数值越高代表表现越好。A-Mem* 表示重新运行 A-Mem 并将温度设为 0 后得到的大模型裁判分数。Mem0g 表示加入图记忆的增强架构。加粗表示该指标下所有方法中的最佳结果，向上箭头表示越高越好。

解释：
- 单跳问题里，Mem0 的 F1、BLEU-1 和 $J$ 都最高，说明自然语言密集记忆足够处理“从某一轮对话找单个事实”的任务。
- 多跳问题里，Mem0 仍然在 F1 和 $J$ 上领先，说明它能把分散在多个会话中的信息取出来并合成回答。
- 开放域问题里，Zep 的 F1 和 $J$ 略高，Mem0g 的 BLEU-1 最好且 $J$ 很接近 Zep。
- 时间问题里，Mem0g 的 F1 和 $J$ 最高，说明图结构在事件顺序和关系判断上确实有优势。

**单跳问题**的关键结论是：**当答案只依赖单个事实片段时，Mem0 的文本记忆已经足够强，图结构不一定带来额外收益**。表里 Mem0 的 $J$ 为 67.13，高于 Mem0g 的 65.71，也高于 OpenAI、LangMem 和 Zep。

**多跳问题**需要跨会话整合信息。Mem0 的 $J$ 为 51.15，高于 Mem0g 的 47.19。这个结果有点反直觉，但也说明图结构不是无条件更好：**多跳问题未必都需要显式实体关系图，有时更直接的自然语言记忆反而检索和整合更有效**。

**开放域问题**里，Zep 略微领先，$J$ 为 76.60；Mem0g 为 75.71，只差 0.89 个百分点。这里可以看到外部知识和记忆平台的优势，但 Mem0g 已经非常接近，说明关系化记忆能帮助开放域检索保持较高事实精度。

**时间问题**最能体现 Mem0g 的价值。Mem0g 的 $J$ 为 58.13，F1 为 51.55，均为最佳。时间推理依赖事件顺序、相对关系和持续时间，图结构能更清楚地表达“谁和谁在什么时候发生了什么关系”。OpenAI 在这类问题上分数很低，原因是即使用提示明确要求抽取带时间戳的记忆，生成出来的大多数记忆仍然缺少时间戳。

### 4.2 跨问题类型分析

不同问题类型对应不同的记忆结构需求。单跳问题像“查一个事实”，重点是能不能快速定位相关记忆；多跳问题像“把几条分散事实拼起来”，重点是检索覆盖和语义整合；时间问题则要求系统保留事件顺序和关系变化。

Mem0 与 Mem0g 针对不同任务各有所长，整体性能全面超越所有基线方案。

- Mem0 的优势来自它的 **自然语言密集记忆**（稠密纯文本记忆）。这些记忆比原始对话短得多，但仍保留了足够语义，检索速度快，检索延迟最低，噪声少，适合单跳和多跳等常规推理任务，兼顾精度与速度，适合通用交互场景；
- Mem0g 的优势来自 **显式关系建模**。它会把实体和关系拆开，并保留关系的有效性变化，因此更适合时间和关系密集的问题。

这也给真实 Agent 系统一个很实用的启发：**没有一种记忆表示适合所有任务**。如果任务多是事实召回，自然语言记忆更划算；如果任务经常涉及人物关系、事件顺序和时间变化，图记忆更有价值。

### 4.3 和 RAG、全文上下文的对比

表 2 把 RAG、全文上下文、现有记忆方案、Mem0 和 Mem0g 放到一起比较，同时看质量和延迟。

![](<images/Pasted image 20260618233811.png>)

> 表2：不同基线方法和所提方法的性能对比。延迟指标给出检索时间和总时间的 p50 中位数、p95 第 95 百分位数，单位为秒；检索时间表示取回记忆或文本块所需时间，总时间表示生成完整回答所需时间。总体大模型裁判分数 $J$ 表示在整个 LOCOMO 数据集上的回答质量。

解释：RAG 的总体 $J$ 最好约为 60.97，Mem0 达到 66.88，Mem0g 达到 68.44。全文上下文最高，为 72.90，但代价是总 p95 延迟达到 17.117 秒。Mem0 的总 p95 延迟只有 1.440 秒，Mem0g 为 2.590 秒。也就是说，全文上下文质量略高，但生产成本明显更重；Mem0 系列牺牲少量质量，换来显著更低的延迟和 token 开销。

**RAG** 的问题在这里很清楚。它检索的是固定长度原始文本块，块太小可能丢上下文，块太大又会带入大量无关噪声。即使调不同块大小和 $k$ 值，最强 RAG 的 $J$ 也只到约 61%。Mem0 和 Mem0g 则先**把对话历史变成精炼记忆，再检索这些记忆**。**相比“从原始历史里捞文本块”，先抽取关键事实再检索，能减少噪声，也能降低上下文长度**。

**全文上下文**仍然是一个强基线，因为它没有丢信息。但这条路线不适合长期生产场景：对话越长，每次回答要读的内容越多，延迟和成本都会上升。**Mem0 的价值**正是在这里体现出来：**它不是追求超过全文上下文的绝对质量，而是在接近质量的同时，把部署成本压下来**。

### 4.4 延迟分析

图 4 把质量分数和延迟放在同一组图里，便于看“快”和“准”之间的取舍。

![](<images/Pasted image 20260618234443.png>)

> 图4：不同记忆方法的延迟分析。两个子图展示了从表 2 中选出的多种方法在大模型裁判分数和延迟上的对比。子图 a 关注**答案生成前的搜索或检索延迟**，子图 b 关注**包含大模型推理在内的总延迟**。两个图都同时叠加各方法的 $J$ 分数，用来整体观察准确性和效率。

解释：搜索延迟上，Mem0 最低，p50 为 0.148 秒，p95 为 0.200 秒；Mem0g 因为有图结构检索，延迟更高，但仍低于多数现有记忆方案。总延迟上，Mem0 的 p50 为 0.708 秒、p95 为 1.440 秒；Mem0g 的 p50 为 1.091 秒、p95 为 2.590 秒。LangMem 的检索和总延迟都很高，全文上下文没有检索延迟，但总延迟很高，因为每次都要读完整历史。

Mem0 的低延迟来自选择性记忆检索：它检索的是已经压缩过的关键记忆，而不是完整对话或大块文本。Mem0g 增加了实体关系图，搜索会更复杂一些，但换来了总体最高的记忆系统 $J$ 分数 68.44。**Mem0 更偏低延迟，Mem0g 更偏关系和时间推理，两者是互补关系**。

### 4.5 记忆系统开销：token 和构建时间

长期记忆系统不只在回答时**消耗资源**，构建和维护记忆本身也有成本。这里比较的是每个系统把一段长对话物化成长期记忆时，平均需要多少 token，以及记忆构建后能不能及时用于查询。

Mem0 每段对话平均约占 7K token。Mem0g 因为多了图节点和关系，约占 14K token。Zep 的图记忆超过 600K token，原因是它在每个节点缓存完整抽象摘要，同时还在边上存事实，导致图里存在大量冗余。作为参照，直接把原始完整对话放进模型平均也只有约 26K token。

更麻烦的是**构建时间**。实验中，Zep 新增记忆后立即检索，往往不能正确回答；过几个小时后重新搜索，相同查询会好很多。这说明它的图构建依赖多个异步大模型调用和后台处理流程，不适合要求即时生效的实时应用。Mem0g 的图记忆构建即使在最差情况下也能在一分钟内完成，用户可以更快利用刚写入的新记忆。

这里的结论很实用：**长期记忆的效率不只是检索快不快，还包括记忆写入后多久能用、存储里有没有大量冗余、构建过程会不会卡住业务链路**。

## 5 结论与未来工作

Mem0 和 Mem0g 共同解决固定上下文窗口的长期记忆限制。Mem0 通过动态抽取、整合和检索紧凑记忆，在单跳和多跳推理上表现强，并且延迟很低。Mem0g 通过图记忆增强实体和关系表达，在时间问题和开放域问题上更有优势。

在 LOCOMO 上，Mem0 系列在多个问题类型上相比已有最佳方法取得相对提升：单跳、多跳、时间推理都有明显收益；相比全文上下文方案，Mem0 的 p95 延迟降低超过 91%，兼顾推理精度与响应速度。这说明它不是单纯追求更高分，而是在做一个更适合生产部署的长期记忆系统。

未来方向主要有几类：
1. 第一，优化 Mem0g 的图操作，降低图记忆带来的额外延迟。
2. 第二，探索层级记忆，把自然语言记忆的效率和图记忆的关系表达结合起来。
3. 第三，设计更强的记忆整合机制，让系统更像人类一样吸收新信息、修正旧信息。
4. 第四，把这套记忆框架扩展到对话以外的任务，例如过程推理和多模态交互。

## Appendix（附录）

因为Mem0的影响非常大，所以这篇论文的附录值得补一下。它把实验到底怎么问、怎么判、怎么更新记忆、基线到底是什么讲得更清楚。尤其是 Mem0 这种生产级记忆系统，很多效果并不只来自“有一个记忆库”，还来自提示词如何约束答案、算法如何处理新增和冲突、基线各自采用什么记忆思路。

### A 提示词

附录 A 先给出提示词。这里不是随便展示 prompt，而是在补充三个关键问题：**评测时怎样判答案对错，Mem0 怎样基于记忆回答问题，Mem0g 怎样把图关系也纳入回答**。

#### 1 大模型裁判提示词：把答案判成正确或错误

大模型裁判提示词参考了 Packer 等人在 2023 年提出的提示词元素。**裁判模型的任务很明确：给定一个问题、标准答案和模型生成答案，判断生成答案是 `CORRECT` 还是 `WRONG`**。

这里的评测对象不是普通问答，而是长期对话记忆问题。问题通常是在问“一个用户应该根据过去对话记得另一个用户的什么信息”。标准答案往往很短，例如用户上次去夏威夷买了什么，标准答案可能只是“贝壳项链”。因此，裁判提示词要求评判时不能过度机械地追求完全字面匹配，只要生成答案触及同一主题，就可以判为正确。

时间问题也有类似的宽松规则。标准答案可能是具体日期、月份或年份，而生成答案可能写成“上周二”“下个月”这类相对时间。提示词要求裁判关注它们是否指向同一个时间段，而不是只看格式是否一致。比如 “May 7th” 和 “7 May” 格式不同，但日期相同，就应该判为正确。

输出格式也被严格限制：裁判先可以做一句简短解释，但最终必须只返回 `CORRECT` 或 `WRONG`，并且用 JSON 形式返回，键名是 `label`。这样做是为了避免评测脚本被混合输出打断。

这部分对实验很重要。Mem0 的主要质量指标之一是大模型裁判分数 $J$，如果裁判提示词过松或过严，都会影响最终结果。这里的设计倾向是：**只要回答在语义上正确，就不要因为措辞、长度或时间格式差异而误判**。

对应的提示词中文直译如下：

```text
你的任务是将一个问题的答案标记为 “CORRECT” 或 “WRONG”。你会获得以下数据：
（1）一个问题（由一个用户向另一个用户提出），
（2）一个 “gold”（标准）答案，
（3）一个生成答案，你需要将这个生成答案评分为 CORRECT/WRONG。

这个问题的目的，是询问一个用户基于他们之前的对话，应该知道另一个用户的某些信息。标准答案通常会是一个简洁、较短的答案，其中包含被引用的话题，例如：

Question: Do you remember what I got the last time I went to Hawaii?
Gold answer: A shell necklace

生成答案可能会长得多，但你在评分时应该宽松一些——只要它触及同一个话题，就应该被计为 CORRECT。

对于时间相关的问题，标准答案会是一个具体日期、月份、年份等。生成答案可能会长得多，或者使用相对时间引用（比如 “last Tuesday” 或 “next month”），但你在评分时应该宽松一些——只要它指向同一个日期或时间段，就应该被计为 CORRECT。即使格式不同（例如 “May 7th” 和 “7 May”），只要是同一个日期，就认为是 CORRECT。

现在是真正的问题：

Question: {question}
Gold answer: {gold_answer}
Generated answer: {generated_answer}

首先，用一句简短的话解释你的推理，然后以 CORRECT 或 WRONG 结尾。不要在你的回答中同时包含 CORRECT 和 WRONG，否则会破坏评测脚本。

只返回 JSON 格式的标签，键名为 "label"。
```

#### 2 Mem0 回答生成提示词：只根据两位说话人的记忆作答

Mem0 的回答生成提示词把模型设定成一个“智能记忆助手”。它能访问两位说话人的记忆，这些记忆带有时间戳，可能和当前问题相关。

提示词对回答过程有几条很强的约束：

- **同时分析两位说话人的记忆**，不能只看其中一方；
- **特别注意时间戳**，因为很多 LOCOMO 问题依赖时间顺序；
- 如果问题问的是某个具体事件或事实，要在记忆中找直接证据；
- 如果记忆之间有矛盾，优先采用**最新记忆**；
- 如果问题里有“去年”“两个月前”这样的相对时间，要根据记忆时间戳换算成具体日期、月份或年份；
- 只关注两位说话人的记忆内容，不要把记忆里提到的角色名和真实说话人混淆；
- 最终答案要非常短，最好少于 5 到 6 个词。

它还要求模型按步骤思考：先找和问题相关的记忆，再检查时间戳和内容，接着寻找明确的日期、地点或事件。如果答案需要换算相对时间，就展示换算过程；最后给出简短、具体、不含模糊时间表达的答案。

这个提示词其实暴露了 Mem0 评测的一个核心难点：**长期记忆问答不是简单检索到一段文本就结束，还要处理说话人、时间戳、矛盾记忆和相对时间换算**。Mem0 把记忆压缩成事实后，回答阶段仍然需要强约束，才能避免模型自己补充不存在的信息。

模板中的输入占位符包括：

- `{speaker_1_user_id}` 和 `{speaker_2_user_id}`，表示两位说话人的用户标识；
- `{speaker_1_memories}` 和 `{speaker_2_memories}`，表示各自检索到的记忆；
- `{question}`，表示需要回答的问题。

也就是说，Mem0 的回答不是直接把完整对话塞给模型，而是把两位用户相关记忆取出来，再让模型在这些记忆里找证据。

对应的提示词中文直译如下：

```text
你是一个智能记忆助手，任务是从对话记忆中检索准确信息。

# CONTEXT:
你可以访问一段对话中两位说话人的记忆。这些记忆包含带时间戳的信息，可能与回答问题相关。

# INSTRUCTIONS:
1. 仔细分析两位说话人提供的所有记忆
2. 特别注意时间戳，以确定答案
3. 如果问题询问某个具体事件或事实，请在记忆中寻找直接证据
4. 如果记忆中包含矛盾信息，优先采用最新的记忆
5. 如果问题涉及时间引用（例如 “last year”“two months ago”等），请根据记忆时间戳计算实际日期。例如，如果 2022 年 5 月 4 日的一条记忆提到 “went to India last year”，那么这次旅行发生在 2021 年。
6. 一定要把相对时间引用转换成具体日期、月份或年份。例如，基于记忆时间戳，把 “last year” 转换成 “2022”，或者把 “two months ago” 转换成 “March 2023”。回答问题时忽略原来的相对引用表达。
7. 只关注两位说话人的记忆内容。不要把记忆中提到的角色名与创建这些记忆的真实用户混淆。
8. 答案应少于 5-6 个词。

# APPROACH (Think step by step):
1. 首先，检查所有包含与问题相关信息的记忆
2. 仔细检查这些记忆的时间戳和内容
3. 寻找能够回答问题的明确日期、时间、地点或事件
4. 如果答案需要计算（例如转换相对时间引用），请展示你的计算过程
5. 仅基于记忆中的证据，形成一个精确、简洁的答案
6. 再次检查你的答案是否直接回应了所问问题
7. 确保你的最终答案是具体的，并避免模糊的时间引用

Memories for user {speaker_1_user_id}:
{speaker_1_memories}

Memories for user {speaker_2_user_id}:
{speaker_2_memories}

Question: {question}
Answer:
```

#### 3 Mem0g 回答生成提示词：在文本记忆之外加入图关系

Mem0g 的回答生成提示词基本沿用 Mem0，但多了一类输入：**图关系记忆**。也就是说，**除了两位用户的文本记忆，还会提供两位用户对应的关系信息**。

Mem0g 的步骤里多了一步：分析知识图谱关系，理解用户的知识上下文。它的输入占位符也因此多了：

- `{speaker_1_graph_memories}`，表示第一位用户的图关系记忆；
- `{speaker_2_graph_memories}`，表示第二位用户的图关系记忆。

这正好对应正文里的 Mem0g 设计。Mem0g 不只是保存一句句自然语言记忆，还把实体和关系组织成图。回答问题时，模型既能看文本记忆，也能看关系三元组，因此更适合处理“谁和谁有什么关系”“事件先后顺序是什么”“某个偏好如何随时间变化”这类问题。

这里也能解释为什么 Mem0g 在时间推理上更强：时间题往往不是单条事实，而是关系、顺序、变化共同决定答案。提示词让模型显式检查图关系，相当于把图结构真正接到了回答阶段，而不是只在存储阶段存在。

对应的提示词中文直译如下：

```text
（与前一个提示词相同）

# APPROACH (Think step by step):
1. 首先，检查所有包含与问题相关信息的记忆
2. 仔细检查这些记忆的时间戳和内容
3. 寻找能够回答问题的明确日期、时间、地点或事件
4. 如果答案需要计算（例如转换相对时间引用），请展示你的计算过程
5. 分析知识图谱关系，以理解用户的知识上下文
6. 仅基于记忆中的证据，形成一个精确、简洁的答案
7. 再次检查你的答案是否直接回应了所问问题
8. 确保你的最终答案是具体的，并避免模糊的时间引用

Memories for user {speaker_1_user_id}:
{speaker_1_memories}

Relations for user {speaker_1_user_id}:
{speaker_1_graph_memories}

Memories for user {speaker_2_user_id}:
{speaker_2_memories}

Relations for user {speaker_2_user_id}:
{speaker_2_graph_memories}

Question: {question}
Answer:
```

#### 4 OpenAI ChatGPT 提示词：让专有记忆系统生成记忆条目

最后给出用于 OpenAI ChatGPT 记忆基线的提示词。它要求 ChatGPT 从一段对话中抽取相关信息，为每个被提到的用户创建记忆条目，并把这些记忆连同时间戳一起存入知识库，以便未来引用和个性化交互。

这个提示词后面接了一个具体对话片段，格式包含时间戳、日期、说话人和对话内容。例如对话发生在 2023 年 5 月 8 日下午，Caroline 和 Melanie 讨论近况、工作、孩子，以及参加 LGBTQ 支持小组等经历。

这个基线的关键在于，OpenAI 记忆功能不像 Mem0 那样提供外部可控的选择性检索接口。因此实验只能先把完整 LOCOMO 对话灌入 ChatGPT，让它生成带时间戳和用户信息的记忆，再把生成出来的完整记忆作为回答上下文。这也是为什么正文里说这个基线“被给予了所有记忆上下文”，而不是只给问题相关记忆。

对应的提示词中文直译如下：

```text
你能否从这段对话中抽取相关信息，并为每个被提到的用户创建记忆条目？请把这些记忆连同所提供的时间戳一起存入你的知识库，以供未来引用和个性化交互使用。

（2023 年 5 月 8 日下午 1:56）Caroline: Hey Mel! Good to see you! How have you been?
（2023 年 5 月 8 日下午 1:56）Melanie: Hey Caroline! Good to see you! I’m swamped with the kids & work. What’s up with you? Anything new?
（2023 年 5 月 8 日下午 1:56）Caroline: I went to a LGBTQ support group yesterday and it was so powerful.
...
```

### B 算法

算法 1，名字是“记忆管理系统：更新操作”。这段算法对应正文里 Mem0 的更新阶段，核心是把候选事实和已有记忆库进行比较，然后决定新增、更新、删除还是不操作。

![](<images/Pasted image 20260617220909.png>)

算法输入有两个：

- $F$，检索出来或抽取出来的事实集合；
- $M=\{m_1,m_2,\dots,m_n\}$，已有记忆库。

输出是更新后的记忆库 $M'$。

整个过程可以分成两层。第一层是主过程 `UpdateMemory(F, M)`，它遍历每个事实 $f \in F$，先调用 `ClassifyOperation(f, M)` 判断应该执行哪一种操作，然后再真正修改记忆库。

四种操作对应的含义如下。

**第一，ADD 新增**。如果候选事实和已有记忆没有语义相似内容，就生成一个唯一 ID，把这条事实作为新记忆加入 $M$。这对应“新信息还不存在”。

**第二，UPDATE 更新**。如果候选事实能补充已有记忆，就先找到相关旧记忆 $m_i$。算法还会比较信息量：只有当新事实的信息量大于旧记忆时，才用新事实替换旧记忆，并标记为更新。这样可以避免低质量或信息更少的新句子覆盖掉更完整的旧记忆。

**第三，DELETE 删除**。如果候选事实和已有记忆冲突，就找到被新事实推翻的旧记忆，并把它从记忆库中移除。这对应用户偏好、状态或事实发生变化的场景。

**第四，NOOP 不操作**。如果候选事实已经存在，或者和长期记忆无关，就不修改记忆库。

第二层是 `ClassifyOperation(f, M)`。它的判断顺序是：

1. 如果候选事实和已有记忆不语义相似，返回 `ADD`；
2. 如果候选事实和已有记忆冲突，返回 `DELETE`；
3. 如果候选事实增强了已有记忆，返回 `UPDATE`；
4. 否则返回 `NOOP`。

这段算法把 Mem0 的记忆管理思想讲得很清楚：**长期记忆不是追加日志，而是带有语义判断的动态维护过程**。新增解决新知识进入，更新解决信息变丰富，删除解决事实冲突，不操作解决冗余和无关内容。如果没有这四类操作，记忆系统很容易要么不断膨胀，要么保留过时事实。

这里也能看出 Mem0 和普通 RAG 的差别。RAG 通常只是把历史切块、嵌入、检索，不会主动判断一条新信息应该覆盖旧信息还是删除旧信息。Mem0 则把“记忆库维护”显式纳入系统流程。

### C 选定基线方法

这部分展开了部分代表性基线。正文实验部分为了避免太长，只列了基线类别；附录则解释这些基线各自的记忆机制。按原文顺序包括 LoCoMo、ReadAgent、MemoryBank、MemGPT 和 A-Mem。

#### 1 LoCoMo：短期摘要、长期观察和时间事件图

LoCoMo 的记忆管线用于维持长对话的一致性。它把记忆分为**短期记忆**和**长期记忆**。

每个对话会话结束后，Agent 会生成会话摘要，作为短期记忆，用来压缩这一段交互的关键信息。同时，每一轮对话会被转成“观察（observations）”，也就是关于说话人人格、生活事件和事实状态的陈述。这些观察会被存入长期记忆，并保留它们来自哪一轮对话的引用。

生成新回答时，LoCoMo 会同时使用最近会话摘要和选择性检索到的长期观察。除此之外，它还加入了一个**时间事件图**，跟踪不同会话之间因果相关的生活事件。这样，模型回答时可以同时参考当前对话、用户画像、检索记忆和会话间发生的事件变化。

LoCoMo 的优势是结构比较完整：有摘要、有事实观察、有时间事件图。但它的复杂度也更高，而且在本文实验里多个指标仍落后于 Mem0 系列。

#### 2 ReadAgent：模仿人类读长文本的三阶段流程

ReadAgent 解决的是长文本超过大模型上下文窗口的问题。它的思路是模仿人类阅读：不是一次记住所有原文，而是先分页、再概括、最后按需回看。

第一步是 **Episode Pagination**，把长文本按更自然的认知边界切分，而不是按固定长度硬切。第二步是 **Memory Gisting**，给每个片段生成简洁摘要，保留核心意思并显著减少 token。第三步是 **Interactive Lookup**，回答问题时先看这些摘要，再策略性地取回最相关的原文片段做细读。

这个流程让模型可以处理远超原始上下文窗口的文档。附录里说它能管理比正常上下文窗口长约 20 倍的文档。它的核心优势是用摘要获得全局理解，再用交互式查找保留必要细节。

不过，ReadAgent 更像长文档阅读系统，不完全等同于对话 Agent 的长期记忆系统。它强调如何读长文本，而 Mem0 强调如何把持续对话转成可维护的长期记忆。

#### 3 MemoryBank：存储、检索、更新和遗忘

MemoryBank 为大模型增加长期记忆，核心包含三部分：**记忆存储、记忆检索、记忆更新**。

记忆存储部分保存详细对话日志、层级事件摘要和不断演化的用户人格画像。新交互发生时，记忆检索部分使用双塔稠密检索模型，从历史里找出上下文相关的信息。记忆更新部分引入类似人类的遗忘机制：被反复调用的记忆会增强，长期不用的记忆会自然衰减。

这个机制很适合 AI 陪伴类场景，因为它不仅要记住事实，还要逐渐形成用户画像，使回答更个性化、更自然。

MemoryBank 和 Mem0 的共同点是都重视“记忆不是静态存储”。区别在于，MemoryBank 强调遗忘和用户画像演化，Mem0 则更强调生产环境里的抽取、合并、更新、检索和低延迟。

#### 4 MemGPT：把上下文窗口当成操作系统里的内存

MemGPT 使用类操作系统的设计来解决上下文窗口限制。它把模型能直接看到的上下文类比成 **RAM**，把外部存储类比成 **disk**。

它的主上下文（main context）受限于模型上下文窗口，里面包含系统指令、最近对话历史和可被模型修改的工作记忆。外部上下文（external context）则存放模型当前看不到的无限量信息，包括完整对话历史和档案数据。

当模型需要主上下文之外的信息时，可以通过函数调用搜索、检索或修改不同记忆层级的内容，相当于把需要的信息“换入”有限上下文窗口，把暂时不需要的信息放到外部存储中。

MemGPT 的亮点是让模型自己管理记忆资源，形成一种“近似无限上下文”的效果。它适合长对话、超长文档管理和多跳信息检索。但相对 Mem0，它更强调模型主动管理内外部上下文，而 Mem0 更强调把长期记忆抽取为紧凑事实并低成本检索。

#### 5 A-Mem：让记忆像笔记网络一样演化

A-Mem 是一种面向大模型 Agent 的智能体记忆系统。它把每次交互做成带结构属性的笔记，包括关键词、上下文描述和标签，这些属性由大模型生成。

当新记忆被创建时，A-Mem 会用语义嵌入检索相关旧笔记，然后再让大模型根据相似性和共享属性建立有意义的链接。更重要的是，它有**记忆演化机制**：当新相关记忆加入时，旧笔记的上下文信息和属性会被动态细化。

这样一来，记忆结构不是静态列表，而是会持续演化的互联笔记网络。检索时，A-Mem 通过语义相似度找到相关历史上下文，辅助 Agent 后续交互。

A-Mem 和 Mem0 有一个相似点：都强调记忆会随新信息变化。区别在于，A-Mem 更像“自组织笔记网络”，Mem0 更像“面向生产部署的记忆管理管线”。前者重视记忆之间的连接和演化，后者更重视低延迟、低 token、可控更新和稳定检索。

## 总结

Mem0 这篇论文真正有价值的地方，是把 Agent 记忆讲得很工程化：**记忆不是把聊天记录存起来，而是把历史信息变成可抽取、可更新、可检索、可部署的决策依据**。

Mem0 的自然语言记忆适合快速、低成本的事实召回和多跳整合；Mem0g 的图记忆适合关系、时间和复杂上下文。两者的关系不是谁替代谁，而是面向不同任务需求的两种记忆表示。

局限也很直接：Mem0g 仍有图操作延迟，记忆抽取和更新依赖大模型判断，图记忆是否稳定优于文本记忆也取决于任务类型。也就是说，Mem0 给出了一个很强的生产级方向，但长期记忆仍然不是“装上数据库就完成了”，关键还在于记什么、怎么改、何时取、用多少成本取。
