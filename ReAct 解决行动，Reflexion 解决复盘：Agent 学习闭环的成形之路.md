> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Reflexion: Language Agents with Verbal Reinforcement Learning》的理解。

![](<images/L3-1.-Reflexion-01.png>)

<callout emoji="💡">
来自 Northeastern University、MIT 和 Princeton University 的这项工作，提出了 Reflexion 这样一种“语言式强化学习”框架。它想解决的问题很现实：大语言模型（Large Language Model, LLM）已经可以作为智能体去调用工具、执行动作、写代码、查资料，但它们失败之后通常只是重新生成，并不会真正把失败经验沉淀下来。Reflexion 的核心贡献，是不更新模型权重，而是把失败反馈转成自然语言反思，写入记忆，再让下一轮尝试带着这段经验继续行动。
</callout>

> 来自 Northeastern University、MIT 和 Princeton University 的这项工作，提出了 Reflexion 这样一种“语言式强化学习”框架。它想解决的问题很现实：大语言模型（Large Language Model, LLM）已经可以作为智能体去调用工具、执行动作、写代码、查资料，但它们失败之后通常只是重新生成，并不会真正把失败经验沉淀下来。Reflexion 的核心贡献，是不更新模型权重，而是把失败反馈转成自然语言反思，写入记忆，再让下一轮尝试带着这段经验继续行动。

## 核心问题

这篇论文的核心问题非常清楚：**如果一个大语言模型智能体在环境中失败了，它能不能像人一样总结“我刚才错在哪里”，然后在下一次尝试中利用这段经验**？

传统强化学习（Reinforcement Learning, RL）通常依赖大量交互样本和模型参数更新。对于大语言模型智能体来说，这条路很重：模型太大，微调成本高，任务反馈又常常只是“成功/失败”这样的稀疏信号。Reflexion 的回答是：**不一定要更新权重，也可以更新上下文**。也就是说，把失败信号转成一段可读、可解释、可复用的文字经验，让它成为下一轮推理和行动的条件。

<callout emoji="✅">
**Reflexion 的关键改造，是把强化学习里的奖励信号变成语言反馈，把参数更新变成记忆更新**。这让智能体在多轮试错中具备一种轻量级的“经验积累”能力：第一次失败后生成反思，第二次带着反思再行动，之后继续评估、反思、修正。
</callout>

## 摘要

大语言模型正在被越来越多地用作目标驱动的智能体，去和外部环境交互，比如游戏环境、编译器和应用程序接口（Application Programming Interface, API）。难点在于，这类智能体很难像传统强化学习那样快速、低成本地从试错中学习，因为**传统强化学习往往需要大量训练样本和昂贵的模型微调**。

<callout emoji="📌">
Reflexion 提供了一条不同路径：**不是通过更新模型权重来强化智能体，而是通过语言反馈来强化智能体**。智能体会根据任务反馈信号生成文字形式的自我反思，并把这些反思保存在情节记忆（episodic memory）中。之后再遇到同一个任务或同类任务时，这些反思会作为上下文进入提示词，诱导智能体做出更好的决策。
</callout>

这里的**反馈形式**很灵活，既可以是一个分数，也可以是一段自由文本；反馈来源也不固定，可以来自外部环境，也可以来自模型自己的判断。三类任务一起证明了它的适用范围：顺序决策、代码生成和语言推理。最亮眼的结果是在 HumanEval 代码生成基准上，Reflexion 达到 91% 的 pass@1，超过此前 GPT-4 的 80%。

## 1 引言

先从大语言模型智能体的发展现状讲起。推理-行动协同方法（ReAct）、机器人可供性落地方法（SayCan）、工具使用学习方法（Toolformer）、多模型工具编排方法（HuggingGPT）、生成式智能体（generative agents）和浏览辅助问答方法（WebGPT）等工作已经展示出一种趋势：**大语言模型不只是生成文本，还可以生成能够被环境执行的“动作”**。例如在网页里搜索、在游戏里移动、调用工具、运行代码，都是把语言模型放进外部环境后的典型能力。

问题也随之出现：这些智能体大多靠上下文示例来“教会”模型怎么行动。任务一复杂，单次提示里给几个例子就不够了。传统强化学习可以通过梯度下降让策略慢慢变好，但大语言模型参数量太大，直接更新权重既费算力也费时间。于是这里转向一个更轻的方向：**既然模型本身已经会读懂语言，那么能不能把失败经验也写成语言，直接放进下一次上下文里**？

Reflexion 就是这个思路。它**把环境给出的二值反馈或标量反馈，转换成文本摘要，再把摘要加入下一轮智能体的上下文**。这样一来，反思文本就像一种“语义梯度”（semantic gradient）：它不是数学意义上的梯度，不会更新参数，但会给模型一个明确的改进方向，比如“刚才应该先找台灯，再找杯子”“刚才把两个职业都当成交集了”“刚才的代码只检查了括号数量，没有检查顺序”。

生成有效的自省反馈存在两大难点：一是精准定位模型错误（即信用分配问题），二是产出具备可落地改进思路的总结文本。Reflexion 试图让语言模型自己把失败轨迹压缩成一段可执行的经验。它探索了三类反馈来源：

- 环境给出的简单成功/失败信号，
- 针对常见失败模式写好的启发式规则，
- 以及模型自评或自生成单元测试这样的内部评估。

![](<images/L3-1.-Reflexion-02.png>)

> 图1：Reflexion 适用于 4.1 节的决策任务、4.3 节的编程任务，以及 4.2 节的推理任务。

解释：图1把 Reflexion 的工作方式放在三类任务里展示。任务输入，智能体生成的行动过程，随后进入评估环节：决策任务可以收到环境里的成功/失败反馈或启发式判断，编程任务可以通过自生成单元测试发现失败，推理任务可以通过答案是否匹配来判断正确性。评估之后产生反思文本，反思文本再影响下一轮尝试。图里真正想强调的不是某一个任务，而是同一个闭环可以跨任务复用：**行动失败 -> 评估失败原因 -> 写成语言经验 -> 下一轮带着经验重试**。

和传统强化学习相比，Reflexion 的优势主要有四点。

- 第一，它很轻，不需要微调大语言模型。
- 第二，语言反馈比单一奖励分数更细，可以直接告诉智能体哪类动作要改。
- 第三，反思文本形成了显式、可解释的情节记忆。
- 第四，它能为下一轮行为提供更明确的提示。

但是局限也很直接：它依赖模型自评或启发式规则的质量，也不能保证任务一定成功。

## 2 相关研究

相关研究主要分成两条线：一条是推理和决策，另一条是编程。

**在推理和决策方向**，自我改进类方法已经有不少先例。Self-Refine 通过“生成 -> 反馈 -> 修改”的循环改善单次生成结果，**但它主要面向单次生成任务，不处理多步环境交互**。自动提示优化也会用语言反馈去改写提示词，但同样更偏单轮生成。还有一些方法会训练批评器模型，或者在多个推理分支之间做搜索和选择。这些方法都在尝试让模型不要一次性输出后就结束。

Reflexion 的差异在于，**它把这种自我修正放到了智能体长期交互里**。这里不是只改当前答案，而是**把失败经验写进记忆，使下一轮尝试能够利用之前的轨迹教训**。它和 ReAct 也有直接关系：ReAct 让模型交替生成推理和动作，**而 Reflexion 可以套在 ReAct 外层，让 ReAct 失败后产生语言反思，再用反思指导下一次 ReAct 轨迹**。

在编程方向，过去很多工作已经使用测试驱动开发（test-driven development）或调试机制。AlphaCode 会在隐藏测试上评估多个生成结果；CodeT 使用模型生成的单元测试给代码打分；Self-Debugging 和 CodeRL 利用代码执行环境提供的反馈修复程序。Reflexion 对这条线的补充是：**不仅要知道测试失败，还要把失败原因转换成自然语言反思，从“发现错误”进一步走到“解释错误，并用解释指导下一次代码生成**”。

## 3 Reflexion：通过语言反思进行强化

Reflexion 可以拆成**三个模型**：

- 行动者（Actor，记为 $M_a$）；
- 评估器（Evaluator，记为 $M_e$）；
- 自我反思模型（Self-Reflection，记为 $M_{sr}$）。

行动者负责生成文本和动作，评估器负责判断这次尝试做得怎么样，自我反思模型负责把反馈和行动过程整理成语言经验。

这三个模块合起来就是一个循环。行动者先在环境中行动，留下一整段行动过程；评估器判断这次尝试好不好；如果失败，自我反思模型读取行动过程和反馈，生成反思文本；这段反思进入记忆，下一轮行动者再带着记忆重新尝试。

![](<images/L3-1.-Reflexion-03.png>)

> 图2：（a）Reflexion 的框架图。（b）Reflexion 的强化算法。

解释：

- 图2左边展示了信息流：行动者根据当前环境观察、短期行动记录和长期经验记忆生成动作；环境返回观察或奖励；评估器利用外部反馈或内部反馈判断这次尝试是否成功；自我反思模型把失败过程和反馈转成反思文本；反思文本写入长期记忆。
- 右边是算法流程：初始化三个模型和策略，生成初始行动过程，评估这次尝试，生成初始反思，把反思放进记忆，然后在多轮试验中不断尝试、评估、反思、追加记忆，直到评估器认为任务通过或达到最大尝试次数。

### 行动者：把大语言模型当作策略

行动者建立在大语言模型之上，根据状态观察生成文本和动作。这里借用了强化学习里的“策略”概念：策略决定在某个状态下采取什么动作。Reflexion 中的策略可以写成：

$$\pi_\theta(a_i \mid s_i), \quad \theta = \{M_a, mem\}$$

其中，$s_i$ 是第 $i$ 步的状态，$a_i$ 是行动者在该状态下生成的动作或文本，$\pi_\theta$ 是当前策略，$\theta$ 由行动者模型 $M_a$ 和记忆 $mem$ 共同决定。这个写法很关键，因为 Reflexion 里真正会变化的是上下文里的记忆。模型权重不变，但 $mem$ 会随着试错不断改变，所以同一个模型下一轮看到的“经验”不同，行动也会跟着变。

行动者可以采用不同的生成方式。这里使用了思维链提示（Chain-of-Thought, CoT）和推理-行动协同方法（ReAct）。CoT 更适合纯推理式问答，ReAct 更适合需要边思考边调用环境动作的任务。Reflexion 并不绑定某一个行动者结构，它更像一个外层学习循环。

### 评估器：判断这次行动有没有成功

评估器接收行动者生成的行动过程，**输出一个能反映任务表现的分数或判断**。难点在于，**大语言模型智能体做的事情往往是语言、搜索、代码、工具调用，很难像棋类游戏那样写出精确打分函数**。所以 Reflexion 采用了多种评估方式。

- 在推理任务中，可以用精确匹配（Exact Match, EM）判断最终答案是否等于标准答案。
- 在顺序决策任务中，可以用手写启发式规则识别幻觉或低效规划，比如连续执行同一个动作、反复收到同样反馈，或者动作步数过长。
- 在编程任务中，可以让模型生成单元测试，再通过解释器或编译器执行测试，得到更扎实的反馈。

可以把评估器理解成 Reflexion 的“错误触发器”。**它不一定要完整解释哪里错了，但至少要告诉系统这次尝试是否值得反思；如果还能提供一些错误线索，自我反思模型就更容易写出有用经验**。

### 自我反思模型：把很少的反馈写成语言经验

自我反思模型同样是一个大语言模型。它读取三类信息：成功/失败这种很少的反馈、当前失败过程、已有记忆，然后生成一段更细的反馈。单独看“失败”两个字几乎没有信息量；但把它和完整行动过程放在一起，模型就有机会推断“哪一步导致后面错了”。

例如在多步决策任务中，如果某个动作 $a_i$ 导致后续动作 $a_{i+1}$ 和 $a_{i+2}$ 都走偏，反思文本可以写出“我不应该在这里选择 $a_i$，而应该选择 $a'_i$，这样后续会得到 $a'_{i+1}$ 和 $a'_{i+2}$”。这段话会被存入记忆，在下一次到达相似状态时影响行动选择。

<callout emoji="📝">
**自我反思的本质，是把失败轨迹压缩成一条面向未来的操作建议**。它不是简单复述“失败了”，而是要能告诉模型下一轮该避免什么、该优先尝试什么、该怎样重新规划。
</callout>

### 记忆：当前行动过程和长期经验同时进入上下文

Reflexion 区分短期记忆和长期记忆：

- 短期记忆就是当前试验中的行动记录，包括动作、观察和中间反馈；
- 长期记忆是自我反思模型生成的经验文本。

这种设计很像人类处理任务的方式：当前几步的细节要记住，过去几次失败留下的教训也要记住。对于大语言模型来说，这两类记忆最终都会进入上下文。当前行动记录告诉模型“现在发生了什么”，长期反思告诉模型“之前踩过什么坑”。

为了避免上下文过长，记忆容量会被限制为最多 $\Omega$ 条经验，通常设置为 1 到 3 条。这个设置也提醒我们，Reflexion 的记忆不是无限数据库，而是一个滑动窗口。它能做轻量经验累积，但还不是完整的长期知识管理系统。

### Reflexion 流程：从一次失败到下一次改进

整个流程可以按试验轮次理解。第一次试验中，行动者与环境交互，生成轨迹 $\tau_0$。评估器给出奖励：

$$r_t = M_e(\tau_t)$$

其中，$\tau_t$ 是第 $t$ 次试验的行动过程，$M_e$ 是评估器，$r_t$ 是任务相关奖励或通过信号。对于语言模型来说，$r_t$ 本身信息太少，所以自我反思模型继续读取 $\{\tau_t, r_t\}$，生成语言经验：

$$sr_t = M_{sr}(\tau_t, r_t, mem)$$

这里的 $sr_t$ 是第 $t$ 次试验后的反思文本。之后系统把 $sr_t$ 追加到 $mem$ 中，下一轮行动者就由“模型本身 + 新记忆”共同影响。这样，Reflexion 就把强化学习里的“根据奖励更新策略”，改成了“根据奖励写下经验，再用经验改变下一轮上下文”。

## 4 实验

实验看三类任务：ALFWorld 顺序决策、HotPotQA 多跳问答、HumanEval/MBPP/LeetcodeHardGym 代码生成。三类任务的反馈来源不一样，正好对应 Reflexion 的核心设定：把环境反馈、答案反馈或代码执行反馈，转成下一轮能用的反思经验。

### 4.1 顺序决策：ALFWorld

ALFWorld 是文本交互环境，智能体要在家居场景里完成找物体、移动物体、操作物体等多步任务。这里用 ReAct 作为行动者，在 134 个环境中测试 Reflexion。自评方式有两种：一种是让大语言模型判断失败类型，另一种是手写规则，比如反复执行同一动作、动作步数过多，就触发反思。

![](<images/L3-1.-Reflexion-04.png>)

> 图3：（a）ALFWorld 在 134 个任务上的表现，展示使用启发式规则（Heuristic）和 GPT 二分类作为自评技术时，已解决任务的累计比例。（b）按失败原因对 ALFWorld 轨迹进行分类。

解释：图3的重点是 Reflexion 能让 ReAct 在多轮尝试中继续变好，而 ReAct 单独使用很快停滞。使用简单启发式时，ReAct + Reflexion 最终完成 130/134 个任务。主要原因是反思文本能把长失败过程压缩成“下次该怎么找、哪里别再犯错”的自我提示，尤其能减少拿错物体、以为自己已经拿到物体这类幻觉错误。

### 4.2 推理：HotPotQA

HotPotQA 是维基百科多跳问答数据集。这里既测 Reflexion + CoT 的纯推理能力，也测 Reflexion + ReAct 的检索加推理能力。评估方式很直接：答案精确匹配就算对，答错后把失败过程写成反思文本，再让下一轮继续尝试。

![](<images/L3-1.-Reflexion-05.png>)

> 图4：思维链提示（CoT）和 ReAct。Reflexion 在 100 个 HotPotQA 问题上提升了搜索、信息检索和推理能力。（a）Reflexion ReAct 与 Reflexion CoT 的对比。（b）用于纯推理能力测试的 Reflexion CoT（GT）。（c）Reflexion 与情节记忆消融的对比。

随机多试几次不等于学习，单独的 CoT 或 ReAct 很难自然纠错；加入 Reflexion 后，模型能从答错的轨迹里写出下一轮可用的反思。尤其在给定标准上下文的 CoT 设置中，Reflexion 还能提升 14%，并且比只塞入历史记录的情节记忆版本多提升 8%。**关键原因是反思文本直接写出了错因和下一轮怎么改**。

### 4.3 编程

编程任务使用 MBPP、HumanEval 和新引入的 LeetcodeHardGym。这里的反馈比问答更扎实：模型可以生成单元测试，再用解释器或编译器检查代码。Reflexion 的作用，是把测试失败、编译错误这些执行反馈，改写成下一轮代码修改时能用的反思。

结论很清楚，Reflexion 在 HumanEval Python 上达到 91.0，超过 GPT-4 的 80.1；在 HumanEval Rust、MBPP Rust、Leetcode Hard Python 上也有提升。例外是 MBPP Python，Reflexion 低于 GPT-4，这说明代码任务的提升很依赖测试质量。

内部测试如果放过错误代码，也就是假阳性（FP）偏高，智能体就会提前提交错解。相比之下，假阴性（FN）没那么糟，因为模型还有机会通过反思发现测试本身可能有问题。

测试生成和自然语言反思要一起用。只有测试、没有反思，模型容易盲目修；只有反思、没有测试，又缺少可靠反馈。完整 Reflexion 最好，核心原因是它把执行反馈变成了下一次代码修改的自然语言计划。

## 5 局限

Reflexion 本质上是在用自然语言帮助智能体一点点改进行动方式。这种方式很轻，但也会卡住：如果智能体的反思一直围绕错误方向打转，或者任务需要非常多样、很有创造性的探索，Reflexion 不一定能跳出来。

长期记忆也是一个限制。这里使用的是最大容量受限的滑动窗口，通常只保留最近 1 到 3 条经验。这样的设计简单、轻量，但还撑不起真正复杂的长期经验管理。未来可以扩展成向量嵌入数据库或传统 SQL 数据库，让智能体能找回更久之前的经验。

代码生成还有更具体的限制。测试驱动开发依赖输入输出映射足够明确，但并不是所有函数都适合这样评估。非确定性生成函数、会调用外部 API 的非纯函数、依赖硬件规格的函数，以及并行或并发行为，都很难用简单单元测试稳定覆盖。

## 6 更广泛影响

大语言模型正在越来越多地与互联网、软件、机器人和人类交互。Reflexion 这类方法可能提升智能体自动化能力和工作效率，但也会放大误用风险。一个能从失败中学习的智能体，如果被用于不安全目标，同样会更有效地完成不安全行为。

另一方面，传统强化学习常被批评为黑箱：策略怎么变好、为什么选择某个动作，很难解释。**语言式强化学习提供了一种更容易检查的路径。因为反思文本是写出来的，人可以看到智能体准备如何解释失败、下一步打算怎么做。尤其在工具调用场景中，可以在真正执行动作前先审查它的意图**。

## 7 结论

Reflexion 展示了一条很朴素但有力的路线：让大语言模型智能体通过语言反思从过去错误中学习。它不需要更新模型权重，而是把失败经验写进记忆，让下一次行动带着这段经验继续尝试。

在决策、推理和编程任务中，Reflexion 都能带来明显提升。它的核心价值不是“让模型多生成几次”，而是**让每一次失败都变成下一轮能用上的经验**。未来还可以把传统强化学习中更成熟的价值学习、离策略探索等思想，改写成自然语言形式，继续扩展这种语言强化范式。

## 总结

Reflexion 最值得记住的一点是：**它把“学习”从参数空间搬到了语言记忆空间**。传统强化学习通过奖励更新策略参数，Reflexion 则根据奖励写下反思文本，再让反思文本改变下一轮上下文。

这让它特别适合当前大语言模型智能体：模型很强，但微调很贵；任务反馈往往很少，但模型能读懂语言。Reflexion 把二者接了起来。它当然不是万能的，反思质量、评估质量、记忆容量和探索多样性都会限制它。但作为 L3 全局规划里的“反思驱动迭代优化”代表方法，它清楚地说明了一件事：**智能体失败后不应该只是重来，而应该把失败变成可复用的经验**。