> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《LongHorizon-Harness: Advancing Long-Horizon Agents for Real-World Tasks》的理解。

![](<images/Harness-14.-LongHorizon-Harness-01.png>)

> 阿里 DreamX Team 在 2026 年提出 **LongHorizon-Harness**，把长时程 Agent 的主要问题重新定义为**任务状态管理**：执行过程可以很长，也可以多次失败，但跨轮次保存下来的内容必须是独立审计确认过的环境事实。整个系统围绕**管理—执行—审计（Manage–Execute–Audit, MEA）** 循环运行：管理器维护任务状态并决定下一步，执行器在全新上下文中完成一个有边界的子任务，审计器只读检查环境，再把可信结果交回管理器。Claude Code、Codex CLI 和 OpenClaw 原有的内部 Agent 循环保持不变，LongHorizon-Harness 只在外层增加一套可审计的长任务控制结构。

## 核心问题

这项工作要解决的问题很清楚：**当 Agent 需要连续工作几十分钟甚至数小时时，怎样避免早期错误不断累积，防止尚未完成的任务被误判为完成，同时控制持续增长的上下文，让后续决策建立在可信的任务状态上**？

常见 Harness 会在同一个持续增长的上下文里完成三件事：执行任务、记住进度、判断自己是否完成。问题也由此出现：执行细节越来越多，真正的任务状态越来越难找；如果 Agent 把一次“看起来完成”误判成“已经完成”，这个错误还会进入后续规划。

LongHorizon-Harness 的关键改造是：**把持久任务状态移到执行上下文之外，执行器的自我报告只能作为待检查线索，只有审计器从真实环境中确认的事实才能写入并更新任务状态**。每轮执行结束后，冗长轨迹被丢弃，留下的是紧凑、可追溯的审计报告。

## 摘要

大语言模型 Agent 正在承担需要持续推理、工具调用和多次修改的长时程任务。现有 Harness 往往把任务执行、任务状态和完成评估放在同一段不断增长的上下文里，导致状态难以追踪，错误的自我评估也可能成为下一步决策的依据。

LongHorizon-Harness **把长时程执行改写成任务状态管理问题，并通过 MEA 循环实现**：

- **管理器（Manager）** 维护显式任务状态，根据已经审计的事实选择下一项子任务；
- **执行器（Executor）** 在新的、受预算限制的上下文中执行子任务；
- **审计器（Auditor）** 使用只读工具检查执行后的环境，确认实际发生了什么。

MEA 之外还有一层 **Agent 适配器（AgentAdapter）**。它负责把 Claude Code、Codex CLI、OpenClaw 等现有 Agent 后端接入上述三个角色，不改变后端原有的内部执行循环。

**长任务能力由模型与 Harness 共同决定：模型负责完成当前步骤，Harness 负责保存可信进度、隔离执行上下文，并让任务从失败处接着推进**。

这里先把单次长上下文和可审计状态转换放在一起比较。

![](<images/Harness-14.-LongHorizon-Harness-02.png>)

> 图1：左侧展示 LongHorizon-Harness 在不同基准和模型上的长时程执行提升。在保持模型与执行后端相同的情况下，WeaveBench 通过率从 51.8% 提升到 80.7%，Terminal-Bench 2.1 从 69.7% 提升到 77.2%，OSWorld 2.0 的完整任务成功率提高到原来的 3 倍；提升也能从 Qwen 3.7-Plus 迁移到 Claude Opus 4.7。
> 右侧展示经过审计的状态转换：管理器根据审计事实重新规划下一项子任务，全新上下文中的执行器负责操作环境，只读审计器确认环境中实际发生的变化。跨轮次只保留审计报告，Claude Code、Codex 等后端可以互换承担不同角色。

解释：左侧可以看到，同一个模型换一套 Harness，最终能完成的任务数量会明显变化。右侧解释了这种变化从哪里来：单一会话既执行又判断自己，很容易把错误进度继续带下去；MEA 把“做事”和“确认做成了”分开，每一轮都从已经确认的状态重新出发。

# 1 引言

大语言模型已经从对话模型变成自主 Agent 的决策核心，覆盖软件工程、通用助手、科学发现、计算机操作和多模态交互。任务也随之变长：Agent 需要反复推理、调用工具、观察环境、修改方案，有时还会跨越多个上下文窗口或会话。

METR 的测量显示，先进 Agent 可完成任务的时间跨度大约每七个月翻一倍，近期模型的增长速度又加快到约四个月翻一倍。前沿编码 Agent 已经可以在同一项目上持续工作数小时。**能够运行得更久，不等于能够可靠地完成更长任务**。

单看每个动作，许多长任务并不难。难的是让大量互相依赖的动作一直朝同一目标推进。这里有三个反复出现的问题：

- **错误累积与目标漂移**：早期的一次错误会改变后续判断，动作越多，偏离原目标的可能性越大；
- **上下文退化（Context Rot）**：历史不断增长后，相关信息更难被准确找到和使用，超过某个上下文使用阈值后，Agent 表现可能明显下降；
- **任务状态丢失**：Agent 没有持续维护“还要满足哪些要求、已经完成哪些动作、生成了哪些产物、从环境中确认了哪些事实”，后续步骤便无法从准确状态继续。

模型侧的扩展和训练能提升编码、推理与 Agent 能力；Harness 侧的 Claude Code、Codex CLI、OpenClaw 则负责组织提示词、工具、上下文和多步执行。现有 Harness 已经支持规划、任务拆解、工具调用和隔离子 Agent，但仍有两个结构性问题。

- 第一，**任务执行和任务状态管理共享同一段不断增长的上下文**。Agent 一边操作，一边从大量过程记录中维护全局进度，执行越长，状态越难追踪。

- 第二，**任务执行和完成判断仍然耦合在一起**。执行子任务的 Agent 同时判断自己是否完成，一次错误判断可能被写进任务状态，并成为后续行动的前提。

LongHorizon-Harness 把完整任务改造成一系列**经过独立审计的任务状态转换**：

1. 管理器读取当前任务状态，定义一个目标明确、依赖清楚、带验收条件的子任务；
2. 执行器只在当前轮的全新上下文中执行这个子任务；
3. 审计器从环境中独立确认改变、完成项和缺口；
4. 管理器根据审计结果更新状态，再开始下一轮；
5. 执行器的原始交互轨迹在本轮结束后丢弃，跨轮次只保留紧凑的任务状态和审计证据。

还有一个工程选择很重要：**LongHorizon-Harness 保留现有 Agent 后端的内部执行逻辑**。AgentAdapter 只控制输入上下文、工具权限、执行预算和返回报告，因此 Claude Opus、GPT、Qwen 等模型，以及 Claude Code、Codex CLI、OpenClaw、Hermes Agent 等 Harness 都可以接入。

具体做了三件事：

- 把单条、持续增长的执行轨迹改为显式维护任务状态，只有经过独立验证的事实才能写入状态；
- 用 MEA 循环分开任务管理、环境修改和结果验证，每轮执行都使用新的上下文；
- 在混合 GUI/CLI、桌面工作流和纯命令行任务上验证了这套结构，并观察到跨模型、跨后端的稳定提升。

# 2 方法

## 2.1 总览：把长任务拆成经过审计的状态转换

给定一个长时程任务 $T$ 和计算机环境，系统会把任务分成数量不固定的多轮执行，不再依赖一个持续增长的会话。跨轮次持续存在的只有两部分：

- **任务状态**，记录要求、产物、事实及其当前状态；
- **审计报告**，记录这些状态所依据的环境证据。

执行器的完整点击、命令、观察和推理轨迹不会带到下一轮。

第 $i$ 轮开始时：

- $S_i$ 是当前任务状态；
- $e_{i-1}\in\mathcal{E}$ 是执行前的环境状态，$\mathcal{E}$ 表示所有可能环境状态的集合；
- $V_{i-1}=(v_1,\ldots,v_{i-1})$ 是此前积累的审计报告；
- $c_i$ 是管理器为当前轮生成的子任务契约；
- $o_i$ 是执行器完成操作后给出的执行报告；
- $v_i$ 是审计器检查新环境 $e_i$ 后生成的审计报告。

一轮中的信息流是：管理器生成 $c_i$，执行器把环境从 $e_{i-1}$ 改成 $e_i$ 并返回 $o_i$，审计器只读检查 $e_i$ 并返回 $v_i$，管理器再把可信结果写进 $S_{i+1}$。

完整结构如下。

![](<images/Harness-14.-LongHorizon-Harness-03.png>)

> 图2：LongHorizon-Harness 通过**反复执行管理—执行—审计循环处理长时程任务** $T$。管理器读取任务状态 $S_i$，构造子任务契约 $c_i$，其中包含目标、验收标准、边界约束和相关历史证据；如果缺少用户信息或授权，也可以进入询问路径。管理器选择 GUI 或 CLI 执行器，执行器在全新、受预算限制的上下文中接收 $c_i$，执行子任务、修改环境，并返回未经验证的执行报告 $o_i$。审计器参考 $o_i$，使用只读工具独立检查执行后的环境，生成审计报告 $v_i$。下一轮开始前，管理器根据 $v_i$ 更新任务状态。

解释：三个角色拿到的信息和权限不同。管理器能看到任务状态，却完全不能碰环境；执行器可以读写环境，但只收到当前契约需要的信息；审计器可以查看环境，不能修改被检查的结果。本轮的执行过程最终收拢成报告 $o_i$，原始轨迹随后丢弃。审计报告 $V_1,V_2,\ldots$ 会继续保留，作为后续轮次可以读取的持久证据。

循环在四种情况下结束：

- 已审计状态满足原始任务；
- 剩余要求无法通过当前允许的子任务继续推进；
- 需要用户提供信息或授权；
- 达到最大轮数预算。

## 2.2 管理器：只根据可信状态决定下一步

管理器拥有持久任务状态，负责决定任务怎样继续。它能看到原始任务 $T$、当前任务状态和全部审计报告，**但没有任何直接环境接口**：不能查看应用界面、读取工作区内容、调用 GUI/CLI 工具，也不能修改环境。

第 $i$ 轮结束后，管理器执行：

$$
(S_{i+1},q_{i+1},c_{i+1})=\Phi_{\mathrm{mgr}}(T,S_i,V_i).
$$

其中：

- $\Phi_{\mathrm{mgr}}$ 是管理器的状态更新与决策过程；
- $T$ 是完整原始任务，确保局部执行不会脱离全局目标；
- $S_i$ 是本轮开始前的任务状态；
- $V_i=(v_1,\ldots,v_i)$ 包含截至当前轮的全部审计报告；
- $S_{i+1}$ 是吸收最新审计结果后的任务状态；
- $q_{i+1}$ 是下一步控制决定，只能取 `execute`、`done`、`blocked` 或 `ask`；
- $c_{i+1}$ 是下一轮子任务契约，只有 $q_{i+1}=\texttt{execute}$ 时才会生成。

### 2.2.1 任务状态怎样组织

任务状态采用结构化记录，共有三类：

- **要求（Requirement）**：从原始任务中拆出的目标或限制条件；
- **产物（Artifact）**：执行过程中创建或修改的文件、截图、程序或其他输出；
- **事实（Fact）**：后续轮次需要使用的环境信息，例如某文件已生成、某界面处于特定状态、某测试结果已经确认。

每条记录都带有 `completed`、`pending`、`blocked` 或 `untrusted` 状态，并保留支持当前判断的审计证据引用。

初始任务状态 $S_1$ 从任务 $T$ 中构建，所有要求都先标记为待完成。每轮结束后，管理器把 $v_i$ 中经过验证的发现合并到状态里。**执行器说“我完成了”不会直接改变持久状态；只有干净的审计证据才能把一条记录改成已完成**。

### 2.2.2 下一项子任务怎样产生

管理器把更新后的 $S_{i+1}$ 与原始任务 $T$ 对照，选择一个当前可以推进的未解决目标，并检查它的依赖和前置条件。生成的契约 $c_{i+1}$ 包含：

- 当前轮的直接目标；
- 可以检查的验收标准；
- 不能越过的边界约束；
- 执行和审计需要使用的任务状态记录；
- 与当前目标有关的历史审计报告。

管理器还会根据主要环境变化选择 GUI 或 CLI 执行器。只有已审计状态满足完整任务，且不存在未处理的完整性问题时，才返回 `done`；没有允许的动作可以继续推进时返回 `blocked`；需要用户信息或授权时返回 `ask`；其他情况继续 `execute`。

## 2.3 执行器：在新的上下文中完成一个有边界的子任务

执行器是唯一可以有意修改环境的角色。第 $i$ 轮中，它会收到原始任务 $T$、当前任务状态 $S_i$、子任务契约 $c_i$，以及契约明确引用的历史审计报告。它不会收到与当前契约无关的历史记录。这一轮执行可以写成：

$$
(e_i,o_i)=\Phi_{\mathrm{exec}}(T,S_i,c_i; e_{i-1}).
$$

其中：

- $\Phi_{\mathrm{exec}}$ 是执行器的内部 Agent 循环；
- $T$ 是原始任务，用来保留整体目标；
- $S_i$ 是当前任务状态；
- $c_i$ 是这一轮必须完成的子任务契约；
- 分号后的 $e_{i-1}$ 是执行器实际作用的旧环境状态；
- $e_i$ 是执行操作后的新环境状态；
- $o_i$ 是执行报告，记录做过的动作、得到的状态、创建或修改的产物以及遇到的问题。

$o_i$ 只能描述执行器认为发生了什么，**不能证明契约已经完成**。

### 2.3.1 为什么每轮使用全新上下文

每次调用执行器，系统都会建立一个新的、受预算限制的执行会话，只放入当前轮需要的信息。执行器看不到之前轮次的原始轨迹，但可以在本轮内部多次进行规划、操作、观察和修正。

本轮结束后：

- 原始交互轨迹和内部推理被丢弃；
- 执行报告 $o_i$ 被送去审计；
- 后续执行器从已经审计的任务状态继续，无须阅读上一轮越来越长的过程记录。

丢掉的是冗长的执行过程，已经确认的进度仍会保存下来。两类信息从这里开始分开管理。

### 2.3.2 GUI 与 CLI 的权限边界

两类执行器面对不同的环境接口：

- **GUI 执行器**可以截图、点击、滚动和输入文字，负责应用界面和桌面状态变化；
- **CLI 执行器**可以运行命令、编辑文件、编写代码和执行测试，负责工作区、进程和程序状态变化。

Harness 只暴露当前角色被分配的工具。管理器根据子任务需要选择接口，未被显式配置的能力对执行器不可见。

### 2.3.3 怎样接入现有 Agent 后端

AgentAdapter 接收契约、角色对应的环境接口和执行预算，再把 Claude Code、Codex CLI 或 OpenClaw 启动成一次有边界的执行会话。后端内部原有的规划和工具循环保持不变；LongHorizon-Harness 控制的是：

- 输入哪些上下文；
- 暴露哪些工具；
- 允许哪些环境权限；
- 一轮最多运行多久；
- 结束时必须返回什么报告。

## 2.4 审计器：执行报告只是线索，环境证据才决定完成状态

执行器结束后，审计器接收原始任务、当前任务状态、子任务契约、契约引用的历史审计报告和执行报告。它不会看到执行器的完整交互轨迹或内部推理。

审计过程写成：

$$
v_i=\Phi_{\mathrm{aud}}(T,S_i,c_i,o_i; e_i).
$$

其中：

- $\Phi_{\mathrm{aud}}$ 是独立审计过程；
- $T$ 是原始任务；
- $S_i$ 是执行前的任务状态；
- $c_i$ 是当前子任务的目标、验收标准和边界；
- $o_i$ 是执行器给出的未验证报告；
- 分号后的 $e_i$ 是审计器实际检查的执行后环境；
- $v_i$ 是审计报告，会追加到持久审计历史，并在下一轮前交给管理器。

### 2.4.1 独立检查环境

审计器在新的上下文中运行。它可以根据 $o_i$ 找到相关文件、窗口、日志、进程或输出位置，但必须自己检查这些对象，再把实际状态与契约中的目标、验收标准和边界约束比较。

- GUI 审计器通过以观察为目的的界面操作检查应用和屏幕状态；
- CLI 审计器通过不会修改状态的命令检查文件、元数据、日志、进程、测试结果和工作区。

结论必须来自环境证据，不能来自执行器的完成声明。

### 2.4.2 只读权限和完整性保护

审计器可以改变观察视角，例如打开不同窗口或查看不同位置，但不能修改与任务有关的环境状态。它不能创建、编辑、覆盖、移动或删除受保护产物，也不能运行会改变状态的命令。

Harness 会在审计期间监控工作区和产物状态。只要检测到审计器造成了任务相关修改，就会记录为**完整性违规**，这份报告也不能支持任何“已完成”任务记录。这样可以避免审计器在检查过程中顺手修复问题，再把自己改出来的结果当成执行成功。

### 2.4.3 审计报告包含什么

审计报告 $v_i$ 包含三类结果：

1. **完成状态**：根据验收标准给出 `complete`、`incomplete` 或 `blocked`；
2. **完整性状态**：根据工作区变化、产物有效性与来源、删除约束给出 `clean`、`suspect` 或 `violation`；
3. **任务状态更新**：列出已经验证的事实、对应证据和仍未解决的缺口。

审计器可以建议怎样更新要求、产物和事实记录，最终是否写入 $S_{i+1}$ 由管理器决定。因此，$o_i$ 始终是未验证的执行摘要，$v_i$ 才能作为管理器更新持久任务状态的依据。

# 3 实验

## 3.1 实验设置

### 对比配置

主要实验使用 Qwen 3.7-Plus，也用 Claude Opus 4.7 检查不同模型下是否仍然有效。匹配实验比较两种配置：

- 原始 Claude Code 评测框架；
- LongHorizon-Harness，执行器后端仍然使用 Claude Code。

除特别说明外，管理器、执行器和审计器使用同一个基础模型，避免把模型能力差异误认为任务状态管理的收益。

### 实现预算

- 每轮执行器最多运行 1800 秒；
- 管理器和审计器每次最多运行 300 秒；
- MEA 最多执行 $N_{\max}=25$ 轮。

### 三个评测基准

- **WeaveBench** 包含 114 个需要在同一工作流中协调 GUI 与 CLI 的任务，报告完整通过率、114 个任务平均分和八个领域的通过率；
- **OSWorld 2.0** 包含 108 个桌面工作流任务，人类完成时间中位数约为 1.6 小时，报告完整任务成功率和平均部分得分；
- **Terminal-Bench 2.1** 聚焦真实、困难的命令行任务，使用 Harbor 的 Docker 后端，每个任务独立运行三次并取平均结果。

## 3.2 主要结果

WeaveBench 先检验混合 GUI/CLI 长工作流。

![](<images/Harness-14.-LongHorizon-Harness-04.png>)

> 表1：WeaveBench 结果。灰色行是已有官方结果，不同模型使用各自报告的最佳思考模式；除 GPT-5.5 和 Claude Opus 4.7 外，其余模型只使用 OpenClaw 评测。黑色行是 Qwen 3.7-Plus 的匹配实验。本文实验在任务虚拟机中使用 root 权限，官方结果使用普通用户账户，因此灰色行只作为参考，不能视为严格匹配对比。PR 表示 114 个任务的完整通过率，Overall 表示平均得分，其余八列是八个任务领域的通过率。

解释：在相同 Qwen 3.7-Plus 模型和 Claude Code 执行后端下，完整通过率从 51.8% 提升到 80.7%，平均分从 0.702 提升到 0.835，八个领域全部提高。模型和执行后端都没变，因此这组匹配对比主要检验的就是新增的任务状态管理与独立审计层。

OSWorld 2.0 检验更长的桌面操作流程。

![](<images/Harness-14.-LongHorizon-Harness-05.png>)

> 表2：OSWorld 2.0 结果。灰色行是批量动作和单动作设置下的官方结果，黑色行是使用 Qwen 3.7-Plus 和混合工具设置的 LongHorizon-Harness。Binary 表示最终得分为 1 的完整成功任务占比，Partial 表示 108 个任务的平均得分；粗体标出最佳官方结果。

解释：Qwen 3.7-Plus 的完整成功率从 2.8% 提升到 8.3%，平均部分得分从 21.5 提升到 35.2。前者说明完整跑通的任务达到原来的三倍，后者说明即使没有完全完成，也能满足更多任务要求。

Claude Opus 4.7 的子集实验检查方法是否只是在补偿较弱模型。

![](<images/Harness-14.-LongHorizon-Harness-06.png>)

> 表3：OSWorld 2.0 的 34 个任务子集结果，两组配置都使用 Claude Opus 4.7。单动作设置的完整成功率和平均部分得分分别为 20.6 与 55.8，LongHorizon-Harness 的混合设置分别达到 35.3 与 66.9。

解释：更强的 Claude Opus 4.7 也得到明显提升，说明模型能力和任务状态管理提供的是互补收益。模型决定一轮里能产生多好的动作与方案，Harness 则负责验证和保存每轮的完成结果，让任务能够在这些可信结果上继续向前推进。

数字有一处不一致：摘要和引言写的是 Claude Opus 4.7 从 20.0% 提升到 34.3%，表3和后面的分析写的是 20.6% 提升到 35.3%。

纯命令行任务上的结果排除了“收益只来自 GUI/CLI 路由”的可能。

![](<images/Harness-14.-LongHorizon-Harness-07.png>)

> 图3：Terminal-Bench 2.1 结果。LongHorizon-Harness 保留 Claude Code 作为执行器后端，把成功率从 69.7% 提升到 77.2%。带星号的数值来自外部报告。

解释：由于实验预算有限，这里只自行运行了三种配置：Qwen 3.7-Plus 与 Claude Code 基线、使用同一模型和执行后端的 LongHorizon-Harness，以及使用 GPT-5.6 Luna 和 Codex 执行后端的 LongHorizon-Harness；其余数字来自官方排行榜或对应模型的官方报告。匹配配置中，成功率提升了 7.5 个百分点；换成 GPT-5.6 Luna 和 Codex 后达到 83.1%。Terminal-Bench 完全通过命令行完成，不需要视觉感知或 GUI/CLI 切换，因此这组结果说明，显式任务状态、受限上下文和独立审计对一般长时程执行也有效。

## 3.3 分析

### 3.3.1 性能提升要付出多少 token

OSWorld 2.0 的成本和效果被放在同一坐标系里。

![](<images/Harness-14.-LongHorizon-Harness-08.png>)

> 图4：OSWorld 2.0 上的成本—性能前沿。左侧比较完整任务成功率，右侧比较平均部分得分，横轴均为每个任务的平均输出 token。彩色曲线是不同推理强度下的官方结果，标记大小表示推理强度；蓝色星形是使用 Qwen 3.7-Plus 的 LongHorizon-Harness，虚线箭头表示它相对官方单动作 Qwen 3.7-Plus 的提升。

解释：完整成功率从 2.8% 提升到 8.3%，平均部分得分从 21.5 提升到 35.2，但平均输出 token 也从 28.9K 增加到 104K。LongHorizon-Harness 把固定模型推到了更高的任务效果点，同时付出了更多执行和验证成本。

额外成本主要由哪个角色产生，可以继续拆开看。

![](<images/Harness-14.-LongHorizon-Harness-09.png>)

> 图5：不同角色的 token 消耗。图中比较 LongHorizon-Harness 的管理器、执行器和审计器与相应基线的平均每任务 token，分段标签表示各角色所占比例。所有角色都使用 Qwen 3.7-Plus。WeaveBench 和 Terminal-Bench 2.1 统计总 token，OSWorld 2.0 因官方只提供输出 token，所以只比较输出 token。

解释：管理器在三个基准中只占 2.8%、2.0% 和 8.1%，显式维护任务状态本身开销不大；审计器占 19.4%、24.8% 和 38.1%，独立验证是主要新增成本。总成本也不是固定倍数：WeaveBench 是基线的 2.3 倍，OSWorld 2.0 的输出 token 是 3.6 倍，Terminal-Bench 2.1 反而少 24% 且成功率更高。需要多少轮执行和恢复，取决于模型与任务。

### 3.3.2 哪些任务最受益

不同类别的收益差异很大。

![](<images/Harness-14.-LongHorizon-Harness-10.png>)

> 图6：不同任务类型上的性能提升。蓝色表示 LongHorizon-Harness，灰色表示相同模型的基线；左侧是 WeaveBench 领域，中间是 OSWorld 2.0 能力标签，右侧是 Terminal-Bench 2.1 类别。每个面板最右侧给出绝对分数变化，蓝色连线表示提升，红色连线表示下降；括号中的数字是相应类别中的 OSWorld 任务数或 Terminal-Bench 轨迹数。

解释：WeaveBench 的设计和空间/3D 任务分别提升 60 和 50 个百分点；OSWorld 在流式交互、需要用户参与和跟随教程的任务上提升较大；Terminal-Bench 的系统管理和游戏类任务提升明显，部分短分析任务提升较小甚至下降。共同点是，收益大的任务通常需要长期保存、检查和修改多个互相依赖的环境状态。如果瓶颈是视觉、数学、编码或算法等单步基础能力，审计只能发现错误和触发重试，不能替模型补出它没有的能力。

### 3.3.3 Agent 能力属于“模型 + Harness”整个系统

17 个 WeaveBench 游戏任务进一步比较不同模型与 Harness 的组合。

![](<images/Harness-14.-LongHorizon-Harness-11.png>)

> 表4：WeaveBench 游戏子集 17 个任务的逐任务得分和 token 消耗。CC 表示 Claude Code，LH 表示以 Claude Code 为后端的 LongHorizon-Harness，Tok 表示每个任务的总 token，单位为百万；粗体标出同一基础模型下更高的得分。

解释：Claude Opus 4.7 的平均分从 0.680 提升到 0.809，Qwen 3.7-Plus 从 0.524 提升到 0.733。Qwen 基线得分不超过 0.04 的六个任务，加入 LongHorizon-Harness 后全部恢复到 0.30～0.92，说明它的主要作用之一是救回原本会以近乎完全失败结束的执行轨迹。Qwen 加 LongHorizon-Harness 的 0.733 还超过了 Opus 加基础 Claude Code 的 0.680，说明评估 Agent 时不能只看基础模型。成本也随模型能力变化：Qwen 从 10.7M 增至 34.3M token，Opus 却从 16.5M 降到 11.1M。这组相反的成本变化提示，更强模型可能用更少的审计—重规划轮次满足契约，较弱模型则需要更多执行和恢复。

## 3.4 案例研究

这里比较同一个 Qwen 3.7-Plus 在基础 Claude Code 和 LongHorizon-Harness 下的成对轨迹。四个案例分别观察：卡住后能否恢复、看似完成时能否识别违规、修复前证据能否保留，以及已经验证的进度能否跨轮次延续。

### 3.4.1 交互卡住后从缺口继续

![](<images/Harness-14.-LongHorizon-Harness-12.png>)

> 图7：从卡住的交互中恢复。案例是 WebRTC 联播层审计任务 WEB_task_16。上半部分中，Claude Code 基线发现 Wireshark 的“Decode As”对话框没有响应，却仍对同一交互重试超过 400 步，最终得分 0.59。下半部分中，LongHorizon-Harness 把尚未收集的证据缺口写进任务状态，后续执行轮次补齐图表和数据包级证据，得分达到 0.92。

解释：基线其实已经发现问题，但失败只存在于当前执行上下文，模型只能继续现场补救。MEA 把“这个交互失败了”和“还缺哪些证据”变成跨轮次状态，下一位执行器直接处理缺口，不必接着重复失效点击。

### 3.4.2 看起来完成，不等于满足流程要求

![](<images/Harness-14.-LongHorizon-Harness-13.png>)

> 图8：审计表面上的完成状态。DOC_task_2 要求统一标题样式。上半部分中，Claude Code 基线直接修改文档 XML，并在视觉上看似正确后结束；由于没有遵守必须通过 LibreOffice 操作的要求，得分为 0。下半部分中，LongHorizon-Harness 按规定使用 GUI 应用标题样式并重新检查文档，审计器随后解析 XML，确认全部 15 个标题的最终样式，得分为 0.89。

解释：只看截图不够，执行器说“已经完成”也不算证据。规定的 LibreOffice 操作方式会作为契约约束，审计器再独立检查最终文档状态和产物依据。这样可以分开验证“看起来正确”“底层内容正确”以及“满足任务约束”。

### 3.4.3 修复之前的证据也属于任务状态

![](<images/Harness-14.-LongHorizon-Harness-14.png>)

> 图9：保留修复前证据。DOC_task_4 要求修复 Calc 中的 VLOOKUP 公式，同时保留修复前后的证据。上半部分中，Claude Code 基线记录了初始公式错误，却在完成全部修复前证据之前修改表格，导致最终文件与证据序列不一致，得分为 0.45。下半部分中，LongHorizon-Harness 把缺失的修复前证据保留为待完成要求，先记录它，再修改环境，并审计完整的九张截图序列，得分为 0.87。

解释：如果修复前状态没有被显式记入任务要求，一旦环境被修改，这部分证据就无法重建。持久任务状态让管理器先安排取证，再允许执行器修复，从而保持前后证据的时间顺序一致。

### 3.4.4 从已经确认的进度继续

![](<images/Harness-14.-LongHorizon-Harness-15.png>)

> 图10：从已经验证的进度继续。WEB_task_10 是 Lighthouse 性能优化任务。上半部分中，Claude Code 基线完成核心优化，却卡在 DevTools 证据收集阶段，没有生成全部交付物，得分为 0.53。下半部分中，LongHorizon-Harness 保存已经验证的优化状态，把剩余证据要求交给后续轮次，并审计最终截图和元数据，得分为 0.85。

解释：基线需要在同一会话里继续操作 DevTools、追踪交付物、收集证据并判断进度，反复失败后耗尽预算。LongHorizon-Harness 把已经确认完成的优化记录在任务状态里，后续执行器只处理截图、性能轨迹和元数据，不必重新理解或重做前面的优化过程。

# 4 结论

LongHorizon-Harness 用 MEA 循环把任务状态管理与环境交互分开：已经确认的进度会明确记录在任务状态中；每项子任务都在新的上下文中执行；跨轮次只携带独立确认过的结果。

WeaveBench、OSWorld 2.0 和 Terminal-Bench 2.1 覆盖混合 GUI/CLI、专业桌面流程和纯命令行环境，Qwen 3.7-Plus 与 Claude Opus 4.7 上都观察到提升。**长时程 Agent 的能力是模型与 Harness 的共同产物**：模型决定单轮能够提出和执行什么方案，Harness 负责拆分任务、验证结果、从失败处恢复，并保存每轮已经确认的进度，直到整个任务完成。

最后可以记住三句话：

- **执行报告只是一条待查线索，环境证据才能进入任务状态**；
- **执行上下文可以丢弃，可信任务状态必须持续存在**；
- **审计能够提高长任务可靠性，但不能替代模型缺失的基础能力**。
