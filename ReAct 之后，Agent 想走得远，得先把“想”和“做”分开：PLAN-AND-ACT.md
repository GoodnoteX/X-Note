> 大家好，这里是Goodnote（好评笔记）。本文详细介绍我们对论文《PLAN-AND-ACT: Improving Planning of Agents for Long-Horizon Tasks》的理解。

![](<images/L2-5.-Plan-and-Act-01.png>)

> 加州大学伯克利分校与国际计算机科学研究所等机构在 2025 年提出的研究《PLAN-AND-ACT: Improving Planning of Agents for Long-Horizon Tasks》提出了一种**高效且可扩展的智能体架构**。该框架通过**显式分离“高层规划（Planner）”与“底层执行（Executor）”模块**，让大语言模型在长程任务中能够更好地**处理抽象策略推理与具体环境交互之间的差异**。这种分离不仅突破了传统**单模型同时兼顾规划和执行所面临的能力瓶颈**，还配合了一套**可规模化的合成数据生成方法**，解决了规划训练数据稀缺的问题，从而显著提升了智能体在复杂交互式任务中的**连贯性、适应性和任务成功率**。

## 摘要

大语言模型（LLMs）虽在简单任务中表现出色，但在处理复杂、多步骤的长程任务时，往往难以平衡高层目标与底层细节。由于 **LLM 并非原生为规划而设计，生成精准的执行方案仍是核心挑战**。

为解决这一问题，我们提出了PLAN-AND-ACT框架——一种将显性规划融入大语言模型智能体的全新架构，并通过创新的合成数据生成方法，引入了一种**可扩展的规划生成优化方案**。PLAN-AND-ACT包含两个核心模块：

- **规划器（PLANNER）** 模型：负责战略层。生成结构化的高层规划，明确通往用户目标的路径。
- **执行器（EXECUTOR）** 模型：负责操作层。将高层规划拆解并转化为适配具体环境的底层指令。

为解决规划器训练难的问题，该研究引入了一套可扩展的**规划生成优化方案**：

1. **轨迹标注：** 为真实的执行轨迹自动标注可行的规划方案。
2. **数据扩充：** 通过多样化的合成示例进行样本增强，显著提升模型的泛化能力。

在典型的长程规划场景——**网页导航**任务中，PLAN-AND-ACT 展现了极强的竞争力。

## 1 引言

### 长程任务智能体的“规划”困境与突破

大语言模型（LLMs）正进化为能够与外部环境交互的**语言智能体**。然而，在处理复杂、多步骤的长程任务时，**智能体面临着严峻的“规划能力”瓶颈**。

**核心痛点：为什么 LLM 难以胜任长程任务？**

即便是订机票这样简单的任务，对智能体而言也存在三大挑战：

1. 抽象到具体的断层：智能体难以将用户的高层目标（如“帮我订一张去纽约的机票”）拆解为具体、可执行的步骤（如“打开航空公司官网”“输入出行日期”等）。
2. 执行一致性缺失： 随着任务流程变长、复杂度提升，模型容易“断片”，忘记已完成进度或后续计划。
3. 环境动态性偏差： 现实环境瞬息万变，模型缺乏实时修正规划的灵活性。

高质量、能体现有效规划策略的训练数据稀缺，进一步加剧了这些挑战。

现有方法已证明，将高层规划与底层执行分离能提升任务完成的准确性（见图1）。但核心问题在于，大语言模型并未经过专门训练，无法精准生成这类规划方案和底层操作指令，这也是本文的核心研究方向。

![](<images/L2-5.-Plan-and-Act-02.png>)

> **图1 PLAN-AND-ACT框架示意图**。首先，规划器大语言模型对初始用户查询进行处理，生成一份分步的初始规划方案（详见3.1节）。随后，该规划方案被传递至执行器大语言模型，执行器依据此规划生成相应操作，与外部环境进行交互。环境反馈会同时回传给执行器和规划器：执行器可根据反馈生成后续操作，而当需要生成新的规划方案时，规划器也会依据该反馈进行调整。现有研究已证实，这种将高层规划与底层执行相分离的方式能够提升任务完成的准确性。但核心挑战在于，大语言模型并未经过专门训练，无法精准生成此类规划方案和底层操作指令，这也是本文致力于解决的关键问题。

现有方法主要面临**单一模型架构的“平衡困境”**（难以同时兼顾高层推理与底层操作）以及**强化学习方案的“高敏感度”**（奖励函数难设计且训练不稳定），导致智能体在复杂任务中容易偏离目标、行为逻辑不连贯。

### PLAN-AND-ACT：显性规划的解耦架构

为让大语言模型**更可靠地完成长程任务的规划工作**，提出PLAN-AND-ACT框架。与ReAct系列方法[35,13,55]等传统方案（依赖单一模型直接将用户查询映射为操作序列）不同，PLAN-AND-ACT借鉴了LLMCompiler的架构设计，由规划器和执行器两个模块组成（见图1），实现规划与执行的显性分离：

#### 双模块协同机制

- 规划器（PLANNER）： 战略大脑。负责将用户请求拆解为一系列结构化的高层步骤规划，明确核心流程。
- 执行器（EXECUTOR）： 执行手脚。负责将高层规划翻译为适配具体环境（如 DOM 树操作、点击、输入）的底层动作。

#### 可扩展的合成数据生成流水线

针对规划数据稀缺的难题，我们设计了一套高效、可扩展的训练数据生成方案，无需人工标注的自动化训练方案：

- 锚定规划生成（Anchored Plan Generation）： 逆向分析成功的操作轨迹，利用 LLM 回溯生成对应的高层规划，确保“规划”与“实操”严密对齐。
- 数据合成扩充： 以初始规划为种子，通过数据合成生成更多样化的示例，显著提升模型的泛化能力。

LLMCompiler 的核心区别在于，作者提出了可扩展的微调方法，支持对规划器和执行器组件分别进行微调。

### 主要贡献与研究成果

本文的主要贡献如下：

1. **架构范式转移：** 成功将规划与执行解耦，支持对两个组件分别进行针对性的微调。
2. **数据和训练范式：为高效训练规划器模型，设计了一套合成数据生成流水线**，支持在**无额外真实标注数据的场景下生成规划器训练数据**。首先，利用大语言模型分析成功的操作轨迹（点击、输入等操作序列），通过**锚定规划生成**方法生成对应的高层规划，确保规划方案与实际可执行的操作相匹配（详见4.2节）；其次，以初始规划为种子数据，通过数据合成扩充数据集，生成更多样化的规划示例（详见4.3节）。这种全面的方法能够在真实规划示例稀缺的情况下，构建高质量的训练数据。
3. \*\*性能刷新 SOTA：\*\*以网页导航为长程任务场景，在WebArena-Lite[25]环境中验证了本方法的有效性，取得了53.94%的当前最优任务成功率。

## 2 相关研究

### 2.1 作为网页智能体的语言智能体

随着大语言模型（LLM）的普及，网页智能体已成为核心应用场景。它们通过图形用户界面（GUI）或 API 实现网页浏览与操作。PLAN-AND-ACT 框架在这一快速发展的领域中，通过架构简化与数据创新确立了其独特地位。

#### 1. 架构范式：极简双智能体 vs. 复杂多智能体

虽然现有研究（如 [54,60,43,34]）也采用了分层规划思路，但 PLAN-AND-ACT 在复杂度控制上具有显著优势：

- **现有复杂方案：**

  - **AgentOccam [54]：** 采用树状结构的“生成式规划”，将规划深度融入操作空间。
  - **WebPilot [60]：** 架构臃肿，包含多达 6 个 不同的智能体。
  - **AdaPlanner [43]：** 设计了复杂的规划内外优化器及技能发现模块，在规划出错时进行重规划。
  - **ADaPT [34]：** 在执行失败时通过递归分解来拆解任务。而本方法的双智能体架构仅在每一步执行后进行重规划即可
- **PLAN-AND-ACT 优势：** 上述方法均依赖大量提示工程提升性能，而PLAN-AND-ACT在运行时仅采用简洁的规划-执行结构（双智能体框架）。在运行时仅需简洁的“规划-执行”结构，通过每一步后的实时重规划（Re-planning）即可完成任务，摆脱了对大规模提示工程（Prompt Engineering）的依赖。提供了一种简单、系统化的高质量训练数据生成方法，可用于训练适用于网页任务的大语言模型。

#### 2. 训练与数据：系统化生成 vs. 闭源模型驱动

目前多数高性能网页导航方法高度依赖 GPT-4o 等闭源模型及其提示驱动（Prompt-based）能力。PLAN-AND-ACT 则开辟了另一条路径：

- **微调开源模型：** 提供了一套简单、系统化的高质量训练数据生成方法，旨在赋予开源大语言模型原生的网页任务处理能力。
- **规划数据构建（关键差异）：**

  - 现有轨迹收集技术（如 DigiRL、WebRL、AutoWebGLM、NNetNav）侧重于纯操作轨迹，且往往依赖外部模拟器。
  - PLAN-AND-ACT 突破：不仅能够生成轨迹，更专注于规划数据（Planning Data）的构建，且无需模拟器即可实现合成规划数据的生成，与现有轨迹收集技术形成高度互补。

#### 3. 跨领域联系与技术补充

- **机器人领域借鉴：** 虽然机器人与具身智能体领域也采用分层规划的方式进行任务拆解，但该领域同样缺乏面向开源大语言模型的合成数据生成框架。
- **多维能力增强：** 本框架与以下现有能力增强路径完全兼容：

  - **底层理解**：在HTML数据上进行预训练，以提升模型对网页结构与内容的理解能力。
  - **视觉感知**：利用多模态视觉-语言模型（VLMs），增强模型对视觉信息的理解与处理能力。
  - **迭代进化**：通过强化学习（RL）在交互过程中持续迭代，不断提升模型的决策与执行性能。

## 2.2 合成数据生成：从指令增强到轨迹回溯

合成数据已成为提升大语言模型（LLM）性能的核心技术。在网页智能体领域，该技术正从通用指令扩充转向复杂的**在线轨迹收集**。

### 1. 技术背景：Self-Instruct 的延续

- **核心脉络**：继承了 **Self-Instruct [49]** 和 **Alpaca [45]** 的思路，利用模型自主生成与扩充数据来弥补真实数据的不足。

### 2. 网页智能体的轨迹收集策略

研究者主要利用 **WebArena** 等可复用环境，通过 **在线策略（On-policy）** 获取训练样本：

- **数据融合与补充**：如 Patel [32] 融合真实与合成数据；Yang [55] 针对失败任务生成多轮轨迹以补充成功样本。
- **交互与回溯**：**NNetscape Navigator [28]** 采用“交互优先”技术，先探索网站再回溯标注指令；**WebRL [35]** 则利用 Self-Instruct 生成查询，并通过奖励模型（ORM）进行自动化标注。

### 3. “失败”作为种子

- **针对性生成**：WebRL 与 Lee [24] 等研究一致，将**失败轨迹**作为种子数据，反向生成更具针对性的用户指令，从而收集更高价值的执行轨迹。

## 3 系统架构

正如此前的 AdaPlanner 和 AgentOccam 所指出的，**构建高效智能体的核心挑战，是平衡高层推理与底层执行的需求**。当单一模型需要同时完成长程规划和为规划各环节执行多项底层操作时，会面临巨大的认知负荷，进而导致决策效果不佳或行为不一致。这一挑战在长程任务中尤为突出——智能体需要在数十步操作中维持连贯的策略，并根据环境变化做出调整。

为解决这一核心问题，本框架将上述职责拆分给两个专业化组件（见图2）：

- **规划器**专注于策略决策；
- **执行器**专门负责落实决策。

这种拆分让每个组件都能在其核心任务上**发挥优势**：

- 规划器可专注于高层策略推理，无需陷入具体的实现细节；
- 执行器则可专注于将抽象规划转化为具体操作。

![](<images/L2-5.-Plan-and-Act-03.png>)

> **图 2 PLAN-AND-ACT 框架示意图**：给定初始用户查询后，规划器（3.1 节）将其拆解为高层规划方案并传递给执行器（3.2 节），执行器以该规划为指导执行相应操作。当操作完成且网页 HTML 发生变化时，规划器会结合环境中的这些变化，动态生成新的规划方案（3.3 节）。

本框架可**适配各类结构化决策环境**，本文重点聚焦于网页智能体场景——网页环境具有动态性和复杂性，包含多样化的操作类型，且多为长程任务，能充分验证框架的有效性。在网页任务中，采用HTML作为网页环境的文本表征，作为智能体的观察空间，执行步骤如下。

1. **规划器**接收用户查询（如“关注这个GitHub项目的贡献量榜首开发者”），并将其拆解为清晰、可执行的步骤（如“进入贡献者板块”→“识别并关注贡献量榜首开发者”）；
2. **执行器**则根据这些步骤，在网页环境中执行精准操作，如点击特定链接、在搜索框中输入内容等。

### 3.1 规划器

规划器的核心任务是将模糊的用户查询转化为结构化规划方案。它像是一位经验丰富的指挥官，在行动前先绘制出清晰的路线图。

- 职能描述：通过承担复杂的推理和拆解工作，为后续执行提供指导，同时保留操作灵活性。
- 案例演示：

  - 任务：“关注 GitHub 项目贡献量榜首开发者”。
  - 拆解方案：
  
    1. 进入贡献者板块。
    2. 识别并关注贡献量榜首开发者。

### 3.2 执行器

执行器专注于将规划方案中的每一个抽象步骤，翻译成环境中的具体操作。

- **工作流程**：以 HTML 作为输入观察空间，输出锚定在环境中的具体动作（如点击、输入、调用工具）。
- **垃圾回收机制**：执行器在完成每一步操作后，会自动执行“垃圾回收”，清理冗余的 HTML 等无用数据。这一设计保证了长程任务中上下文窗口的整洁，避免了信息过载。

> 更多规划器-执行器的操作轨迹示例见附录A.1。

### 3.3 动态重规划

传统的静态规划在处理动态网页（如搜索结果变化、交易波动）或执行失败（如搜索无结果）时，往往会因过于僵化而失效。PLAN-AND-ACT 引入了\*\*动态重规划（Dynamic Replanning）\*\*机制：

- **运行逻辑**：规划器不再一成不变。在执行器每完成一次操作后，规划器会结合当前环境状态、历史方案、已执行操作，实时生成最新的规划。
- **优势 1：信息实时捕获**  
以“关注榜首开发者”为例：初始规划无法预知开发者的身份；当执行器点击“贡献者”链接并识别出具体人名后，规划器会将该身份信息实时融入下一步规划中。
- **优势 2：隐式记忆管理**  
由于规划方案会随着进度携带最新的上下文，框架无需额外设计复杂的存储模块，便能天然解决长程任务中的记忆丢失难题。

更多动态重规划的详细示例见附录A.2。这一设计契合本架构的核心理念：规划器作为推理和决策的“控制中心”，而执行器仅负责将规划转化为适配环境的操作。

### 3.4 思维链推理

当前的规划器和执行器均直接生成规划方案和操作指令，而近期研究表明，思维链（CoT）提示和推理时缩放技术Zero-shot-CoT、Deepseek- r1能够通过引导模型生成中间的分步推理过程，大幅提升性能。因此，在让规划器和执行器分别生成规划和操作之前，我们先引导二者生成思维链推理轨迹，以进一步提升任务完成效果。

## 4 合成数据生成

在 WebArena-Lite 基准测试中，开源模型的初始成功率仅为 9.85%，即便引入架构优化也仅提升至 14.21%。这一数据的背后揭示了一个残酷的现实：**大语言模型在原生状态下，并未经过长程规划任务的专门训练，针对网页任务的规划能力尤为薄弱**，这一问题同时影响规划器和执行器：

- **规划器（Planner）层面的匮乏**：如果在预训练阶段从未见过特定网站的结构或复杂查询的操作逻辑，模型根本无法凭空生成精准的步骤拆解。
- **执行器（Executor）层面的生疏**：模型几乎没有接受过“输入指令+复杂 HTML -> 输出精准网页操作”的针对性训练。

研究证明，若大语言模型在预训练阶段未针对网页导航等专业领域的规划/执行任务进行训练，仅靠 Prompt Engineering（提示工程）无法解决根本问题。  
虽然上下文示例（Few-shot）能应对简单任务，但在面对非平凡、高难度的长程查询时，模型的逻辑链条依然极易断裂。要实现质变，必须深入底层，对模型进行微调（Fine-tuning），以提升规划器和执行器的性能。

微调需要两类极其专业的数据配对：

- 规划数据：规划器需要“用户查询-对应规划拆解”的配对数据
- 执行数据：执行器则需要“规划步骤HTML输入-目标网页操作输出”的配对数据

这类数据目前并不存在，人工收集虽可行，但成本高、耗时久——标注人员需要为每个步骤撰写操作规划，再完成对应的执行标注。为此，作者提出一种替代方案：一种可扩展的高质量合成训练数据生成方法。该方法**基于大语言模型的标注流水线，通过处理现有操作轨迹生成对应的结构化规划**，具体流程见图3。

![](<images/L2-5.-Plan-and-Act-04.png>)

> 图 3 合成数据生成流程

1. 在**操作轨迹生成阶段（4.1 节）**，将训练数据中的用户查询输入教师大语言模型，由其生成合成用户指令；随后，演示行为大语言模型在网页中尝试执行该查询。
2. 轨迹生成完成后，利用结果监督奖励模型（ORM）筛选出成功的操作轨迹。**在锚定规划生成阶段（4.2 节）**，教师大语言模型接收上述成功轨迹，生成合成高层规划，并将规划的每个步骤与轨迹中的具体操作进行锚定关联。
3. 在**合成规划扩充阶段（4.3 节）**，从训练数据中采样规划方案并输入教师大语言模型，由其生成新的合成规划方案。

### 4.1 操作轨迹生成

![](<images/L2-5.-Plan-and-Act-05.png>)

为执行器积累数据的第一步是**获取海量的操作轨迹**。本框架采用了一种**演进式的生成流水线**：

1. **查询扩充**：采用 Alpaca 风格 [45] 的生成模式。从训练数据中随机采样真实用户查询作为“种子”，利用 LLM 衍生出大量相似的新查询。
2. **自动化过滤**：通过 LLM 预先剔除网页智能体无法完成的无效查询。
3. **环境演示**：将有效指令交给演示智能体（Demonstration Agent），让其在真实网页环境中尝试执行并记录轨迹。
4. **结果监督（ORM）**：利用奖励模型对收集到的轨迹进行评分，仅保留最终成功的样本，确保训练数据的纯净度。

### 4.2 锚定规划生成

![](<images/L2-5.-Plan-and-Act-06.png>)

这是本框架的核心创新点。为规划器生成数据面临一个难题：**教师模型由于无法实时访问环境，直接生成的规划往往与实际网页操作“脱节”**。

**突破局限：从“预判”转为“反推”**

与其让模型凭空想象规划，不如利用 LLM 的 **上下文学习（In-context Learning）** 能力，让其根据已成功的轨迹进行“溯源”：

- **逻辑反向推导**：将 4.1 节生成的成功操作轨迹输入给教师模型。
- **高层抽象化**：提示模型分析这些底层操作序列，从中总结出连贯、合理的结构化高层规划。

**环境锚定（Anchoring）**  
为了确保规划方案不悬浮，引入了锚定机制：

- **职责分配**：提示模型将轨迹中的每一个底层具体操作，精准地归属到对应的规划步骤中。
- **效果**：这种“底层动作 + 高层逻辑”的强制绑定，保证了生成的规划方案既具备准确性，又具备极强的可执行性。

#### 4.2.1 动态重规划的合成数据生成

由于许多复杂任务（如“分析搜索结果并选择条目”、“查询最新订单”）**高度依赖执行过程中的实时观察**，静态规划无法应对环境的变化，均需要**动态重规划**。

- **核心挑战**：初始规划无法预见执行中产生的动态结果。
- **生成算法**：向教师大语言模型提供**原始规划数据**以及执行至重规划节点的**操作轨迹**。
- **目标**：训练模型学会基于动态反馈实时调整后续规划方案。

详细提示词见附录A.9。

#### 4.2.2 思维链推理的合成数据生成

针对部分开源模型缺乏推理能力的现状，利用算法为规划和执行过程补齐“逻辑链条”：

- **规划推理（Planning CoT）**：要求教师模型在最终输出规划前，先生成详细的任务拆解逻辑。
- **操作推理（Acting CoT）**：提供原始规划、实际轨迹和预期正确操作，引导模型反推并补全该决策背后的推理过程。

### 4.3 合成规划扩充

传统的“环境模拟+反向推导”模式存在严重的**数据产出失衡**：一条包含 8 个步骤的成功轨迹能为执行器提供 8 个训练样本，但仅能为规划器生成 1 个规划方案。

**1. 规模化扩充：解决数据量瓶颈**

- **技术手段**：借鉴 Alpaca 风格，以现有的“查询-规划”配对样本为种子，通过 GPT-4o 进行批量生成。
- **效率突破**：在 **1 小时内** 成功扩充了 **10,000 个** 配对样本，显著提升了数据的多样性。
- **实际效果**：不仅解决了微调中的过拟合问题，更覆盖了广泛的应用场景，有效提升了模型的泛化能力。

相关提示词见附录A.5。

**2. 针对性规划扩充：自适应的“补短板”策略**  
虽然大规模生成提升了多样性，但由于缺乏自适应性，无法精准覆盖模型的弱点。

- **失败模式识别**：在预留验证集上测试模型，锁定其性能瓶颈和失败模式（Fail Modes）。
- **精准采样与分类**：利用大语言模型对训练数据进行分类，筛选出与失败网站、失败节点高度相关的样本作为种子。
- **定向生成**：针对特定弱点额外生成 **5,000 个** 合成样本。
- **优势**：这种受“自适应课程学习”启发的针对性扩充，实现了对生成过程的显式控制，显著提升了模型在疑难任务中的实战性能。

相关分类和生成提示词分别见附录A.7和A.8。

## 5 实验结果

为了验证 PLAN-AND-ACT 框架的真实效能，作者在多个维度设置了详尽的实验方案，确保结果的可复现性与权威性。

### 5.1 实验设置

**实验环境**

- **核心测试集**：**WebArena-Lite**（165个用例）。它涵盖了 Reddit、GitLab、内容管理系统（CMS）等真实网站，采用二元成功指标（只有 0 和 1），计算效率高且极具挑战性。
- **泛化测试集**：为了验证模型的广度，我们还在完整的 **WebArena** 以及基于真实动态网页的 **WebVoyager** 数据集上进行了全面评估。

**模型**：

采用了多模型协作的策略，充分发挥不同模型的专长：

- **核心组件**：采用 **LLaMA-3.3-70B-Instruct** 作为规划器与执行器的基础模型。在重规划实验中，利用 **LoRA** 技术进行高效微调。
- **教师与生成角色**：

  - **GPT-4o**：负责生成高层用户查询与结构化规划数据。
  - **DeepSeek-R1-Distill-Llama-70B**：利用其强大的推理能力，为 3.4 节生成\*\*思维链（CoT）\*\*轨迹。
- **辅助工具**：使用 WebRL-Llama-3.1-70B 作为行为模型，并配合 ORM-Llama-3.1-8B 奖励模型进行自动化轨迹筛选。
- **跨架构验证**：同时在 **LLaMA-3.1-8B** 和 **QWQ-32B** 上验证了框架的普适性。

**基准模型**：  
为了体现 PLAN-AND-ACT 的优越性，作者选择了极具代表性的对手：

- **零样本对比**：未微调的 LLaMA-3.3-70B 原始模型。
- **传统微调方案**：采用 ReAct 风格提示词在 WebArena 训练集上微调的模型。
- **SOTA 级对手**：
- **WebRL**：目前 WebArena-Lite 基准测试的领先模型。
- **闭源霸主**：GPT-4-Turbo、GPT-4o。
- **垂直领域强敌**：WebPilot、AgentOccam、NNetNav、AutoWebGLM 等。

**超参数**：

- **数据合成比率**：在生成流水线中，以 **5 个原始种子**数据为基底，通过大模型衍生出 **10 个** 高质量合成样本。
- **评估标准**：严格采用**成功率（Success Rate）**，任务必须 100% 完成才计分。

训练规划器和执行器模块的超参数见表5。

### 5.2 静态规划器实验结果

![](<images/L2-5.-Plan-and-Act-07.png>)

表1展示了实验结果，列代表执行器（LLaMA-3.3-70B）的不同版本：

- 第一列为基础执行器（未微调）；
- 第二列为仅在WebArena-lite训练数据（1113个样本）上训练的执行器；
- 第三列为在WebArena-lite训练数据+4.1节生成的923个合成操作轨迹上训练的执行器。

**无规划器**：各执行器仅用 ReAct 提示训练、无规划器时，翻倍操作轨迹数据仅让性能微增 0.61%，提升效果极有限，这也成为我们聚焦规划生成、优化规划器的核心原因。

**基础规划器**：为各执行器搭配未微调的基础规划器后，仅基础执行器性能有提升，训练后的执行器表现未超基准；因规划器无特定网站的锚定训练数据，生成的非最优规划反而干扰了执行器。

**微调后的规划器**：仅用 WebArena-lite 的 1113 个规划样本微调规划器后，训练后的执行器性能未提升；核心问题是规划器对训练数据过拟合，无法为新任务生成通用规划方案。

**结合数据扩充的微调规划器**：为规划器逐步采用合成数据扩充策略后，性能呈持续提升趋势：先添加合成轨迹，再生成 10000 个合成查询 - 规划配对样本，最后补充 5000 个针对性样本，每一步扩充都让各执行器的表现得到改善。

从这些结果可以看出，经过合理训练的规划器能持续提升各版本执行器的性能：即便是基础执行器，搭配规划器后成功率也从9.85%提升至29.63%。这验证了本文的核心假设：**显性规划**能够有效衔接用户的高层意图与底层操作。

数据生成扩充对性能的提升效果尤为显著，每一次数据集扩充都能带来性能提升，其中添加10000个直接生成的规划样本后，成功率提升约10个百分点，效果最为突出；基于失败分析的针对性示例则为性能带来了4-5个百分点的最终提升，体现了针对性数据生成的价值。

### 5.3 动态重规划实验结果

尽管静态规划表现优异，但面对“分析动态内容”等不确定任务时仍显吃力。为此，本文引入了**动态调整与深度推理机制**。  
在3.3节中的假设一致：静态规划器会将部分复杂推理任务推给执行器，而执行器本应仅负责将抽象规划锚定为具体操作。更多具体示例见附录A.2。

#### 5.3.1 重规划实验结果

通过引入**动态重规划能力**，规划器可以在每步执行后修正方案。

- **性能飞跃**：成功率在静态规划基础上**大幅提升 10.31%**，达到 **53.94%**。
- **超越最强对手**：这一结果比此前的最优模型 WebRL-3.1-70B 高出 **4.84%**。
- **规划的“降维打击”**：令人惊讶的是，即使搭配**完全未微调的基础执行器**，仅靠高质量的动态规划，成功率也达到了 44.24%。这再次验证了：**只要路带得对，哪怕“腿”没练过，也能走得远。**

#### 5.3.2 思维链实验结果

在规划与执行中加入 CoT 推理后，性能再次实现跨越：

- **创纪录表现**：在 WebArena-Lite 上创下 **57.58%** 的新纪录。
- **小模型逆袭**：实验证明，**8B 的小模型**在搭配思维链后，其性能竟能与**未搭配思维链的 70B 大模型**旗鼓相当。这说明逻辑推理轨迹比单纯的模型规模更能决定任务成败。

### 5.4 WebArena实验结果

在包含数百个复杂任务的完整 WebArena 数据集评估中，PLAN-AND-ACT 表现出了极强的鲁棒性。实验结果显示，本方法的性能与目前市面上绝大多数顶尖研究相当，甚至在多个维度上表现更优，证明了该框架从模拟器到全量测试的稳定性。

### 5.5 WebVoyager实验结果

WebVoyager 是基于**真实、动态网页**的基准测试，相比模拟器更具挑战性。

- **无数据困境的突破**：在缺乏原生训练数据的情况下，我们利用 GPT-4o 收集轨迹，并由 QWQ-32B 完成了大规模的合成规划与思维链标注。
- **双项夺冠**：

  - **8B 模型**：超越了所有现有的开源模型。
  - **32B 模型**：超越了所有现有研究（包含闭源方案在内的纯文本任务），刷新了该基准测试的最优纪录。

## 6 结论

研究证实，将高层推理（规划器） 与底层执行（执行器） 进行解耦，确实能有效提升用户查询与实际操作的匹配度，显著增强任务执行的一致性，以及智能体对动态网页环境的适应能力。然而，这一思路面临一个核心挑战：原生大语言模型并不擅长生成精准的规划，尤其在网页这种超出其预训练分布的复杂环境中，这一短板更为突出。为此，本研究提出PLAN-AND-ACT框架，旨在通过可扩展的合成数据生成技术，补齐大模型在复杂长程任务中的规划短板。

本方法最突出的核心优势体现在数据生成的**可扩展性与极致高效性**。传统的 WebRL 等方法通常依赖于与环境的深度交互来进行在线策略数据收集，这种方式往往因需要实际执行每一条网页操作轨迹而变得异常耗时。相比之下，PLAN-AND-ACT 的合成数据生成流水线展现了惊人的生产力：利用 GPT-4o 模型，仅需 1 小时即可生成 15,000 个高质量的合成训练示例，而通过环境交互收集同等规模的数据则可能需要数天甚至数周的时间。这种高效的可扩展性使得我们仅通过在合成数据上进行简单的有监督微调（SFT），便能使模型达到与当前最优 SOTA 模型相当的性能水平。

实验结果表明，PLAN-AND-ACT在网页导航任务的WebArena-Lite基准测试中，性能显著超越现有方法。通过合成数据生成、规划扩充和失败案例针对性优化的组合策略，本文证明了本框架能持续提升任务成功率。值得注意的是，动态重规划机制通过根据实时观察结果调整执行策略，进一步增强了模型的鲁棒性。

本研究仅聚焦于优化规划组件，而执行器仍采用标准的WebArena-Lite风格设计，这一设计充分证明了本方法的潜力——即便无需复杂的执行策略，仅通过优化规划环节即可实现性能提升。这种模块化设计意味着，未来的研究可在保留本框架高效规划优势的基础上，通过为执行器引入思维链推理等技术，进一步提升整体性能。

除网页导航外，本模块化框架在各类需要长程决策的数字环境中均具有广阔的应用前景。未来的研究将探索融合多模态输入、基于强化学习的规划优化和增强记忆的推理技术，进一步提升智能体的能力。

### 局限性

本方法的一个主要缺陷是，4.1节的操作轨迹生成依赖能够成功完成网页任务的基准模型。尽管4.3节的合成数据生成流水线能通过充足的训练数据缓解这一问题，但对于WebVoyager这类无训练数据的数据集，该流水线仍需依赖基础模型收集轨迹。

此外，当前框架在执行器完成**每一步操作后**都进行动态重规划（3.3节），这一设计效率较低，会降低性能。未来的研究可通过两种方式解决这一问题：由执行器判断是否需要重规划，或由规划器将任务分配给不同的子智能体。