
> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《HarnessDev: Can LLMs Create and Evolve Their Own Agent Harness?》的理解。

![](<images/Harness-17.-HarnessDev-Cover.png>)

> 字节跳动 Seed、新加坡科技设计大学、佐治亚理工学院、M-A-P 和 TokenWave.AI 团队在 2026 年 9 月提出 **HarnessDev**。这是一套评测基准，用来检查大模型能否创建并持续改进自己的 Harness，并没有提出一种新的 Harness 架构或训练算法。
> 模型从一个几乎没有任务能力的种子程序出发，创建 Harness，再根据执行反馈持续修改代码。部分领域的成绩已经接近或超过选定的人工构建系统，代码和搜索任务仍有差距。实验发现，模型能够通过后续修改改善 Harness，但这些收益迁移到未见任务或其他执行模型后往往会减小，说明当前模型还不具备稳定、通用的 Harness 演化能力。


## 核心问题

大模型会写代码，是否就能写好自己运行时依赖的 Harness？进一步说，**它能不能从失败记录中找到系统问题，把修复写进代码，让之后的任务持续受益**？

HarnessDev 分别评测**创建（Creation）** 和**演化（Evolution）**：先从弱种子搭建完整系统，再利用反馈维护和改进它。交付物必须是能运行、能检查、能跨任务复用的代码。评测还会更换执行模型、使用开发者没见过的任务，检查改进是否只对当前模型和题目有效。

## 摘要

Agent 的能力越来越依赖模型参数之外的执行系统。同一模型换一套 Harness，任务表现就可能明显变化，而既有评测通常先选定 Harness，再比较最终成绩，对模型能否开发 Harness 本身关注较少。

**HarnessDev 同时评测创建与演化**，使用**下游任务表现**和**执行 token 成本**衡量交付质量。创建实验覆盖六个模型、四个领域、五个基准，共 2,207 个不同任务实例，隐藏的评测任务不进入开发过程。

生成的 Harness 在代码、搜索与研究任务上仍落后于成熟人工系统，在写作和机器学习实验中则有接近或超过所选参考系统的结果。不同 Harness 的**执行 token 成本也有很大差异**。演化能产生有用修改，但收益不稳定，对未见任务的迁移有限；固定执行模型后的对比也表明，**Harness 的改进效果很依赖实际运行它的模型**。

## 1 引言

### Harness，会直接影响 Agent 能做成什么

代码助手、数据分析助手、浏览器 Agent 和研究系统都需要把模型输出变成真实动作。Harness 负责组织这个过程，包括**执行循环、工具调用、上下文管理、失败恢复和结果验证**。

同样的模型权重，配上不同执行系统，任务表现也会变化。GPT-5 在 Terminal-Bench 2.1 中使用 Terminus 2 时解决率为 35.2%，换成 Codex CLI 后为 49.6%。Harness 怎样组织执行，会直接影响模型能否完成任务。

随着 Agent 进入不同业务，定制 Harness 的需求还会增加。系统上线后也要随着需求和失败案例持续维护，因此值得问：模型能承担多少 Harness 工程师的工作？

### 现有评测通常把 Harness 当成固定配置

大多数 Agent 评测会先选定 Harness，再比较模型完成下游任务的成绩。这样便于控制实验条件，却没有评测模型能否开发 Harness 本身。近期工作开始研究 Harness 表示、自动 Agent 设计，以及让 Agent 构建或改进其他 Agent 系统，但模型能否**从头创建并持续改进一套可运行、可长期复用的 Harness**，仍缺少系统检验。

要回答这个问题，需要把写 Harness 的**开发模型**和运行 Harness 的**执行模型**分开，记录模型使用的开发环境，同时检查下游表现、跨执行模型迁移、与人工系统的差距、修改造成的能力退化，以及执行成本。

### 修改 Harness，比普通代码编辑更难判断

修改独立程序时，目标由外部给定，改完后通常可以直接测试。修改 Harness 会改变模型以后如何读取信息、规划任务、调用工具和处理失败，一处改动可能影响后续所有任务。

模型需要从执行轨迹中看出自己反复失败在哪里，再判断问题出在工具协议、上下文组织还是结束条件，最后把修复写进系统。修复还要能帮助后续任务，不能只解决眼前的一个案例。

当前前沿模型已经能够修改多文件代码库，并处理真实的软件修复任务，说明相关能力已经出现。缺少的是一个专门的基准，用来检查这些能力能否支持完整的 Harness 开发。

### HarnessDev：评测模型能否创建和维护执行系统

HarnessDev 把评测对象从单次任务答案换成了**可运行的 Harness**。它包含两个阶段：

- **创建（Creation）**：模型从一个刻意设计得很弱、但能够运行的种子程序出发，面向一类新任务写出完整 Harness；
- **演化（Evolution）**：模型从已经创建的 Harness 出发，根据下游任务的执行反馈继续修改和维护它。

前者对应写出第一版系统，后者对应系统投入使用后的持续维护。两个阶段交付的都是能够跨任务保存、检查和复用的代码。

![图1：HarnessDev 的创建与演化](<images/Harness-17.-HarnessDev-01.png>)

> 图1：HarnessDev 覆盖 Harness 开发的两个阶段。在创建阶段，开发者从一个弱但可运行的种子和少量开发案例出发，构建完整 Harness。在演化阶段，它利用下游执行反馈，继续改进自己持久保存的 Harness。两个阶段评测的都是能够跨任务保留和复用的可运行基础设施，而非一次性的任务输出。

解释：传统任务评测会预先固定执行系统，再给 Agent 的答案评分。HarnessDev 让模型先写出初始版本 $H_0$，之后继续修改代码。图中的上下文压缩、重试、记忆和验证表示 Harness 可以增加的能力，并不表示每个参测模型都成功实现了这些功能。**修改必须进入可运行代码，并在后续任务中真正发挥作用**。

### 如何判断生成的 Harness 是否真的变好了

这类评测很容易出现误判。Harness 可能记住了开发的案例，只适合创造它的模型；新修改也可能提高一项能力，却把原来正常的功能改坏。反馈集涨分还可能来自针对基准的特殊处理，换一批任务后就失效。

HarnessDev 因此同时检查两个维度：

- **能力**：冻结 Harness 后，在开发者没有见过的下游任务上测最终表现；
- **效率**：统计冻结版本执行下游任务时消耗的模型 token。

### 主要发现

创建实验中，模型已经能够从弱种子写出可运行的 Harness，但不同领域的结果相差很大。在由创建它的模型负责执行时，生成的 Harness 在短篇写作上达到所选人工系统的参考成绩，部分模型在机器学习实验中超过参考成绩；搜索和研究任务的差距最大，代码任务也仍明显落后。不同 Harness 的执行 token 成本相差很大，消耗更多 token 并不保证成绩更好。

演化实验中，模型可以根据执行反馈做出有用的局部修改，但连续版本的成绩经常上下波动。开发阶段观察到的提升到了未见任务上会变小，也更不稳定。更换执行模型还会改变初始成绩，以及后续修改是否有效。当前模型已经能够改进 Harness 的局部机制，**跨未见任务和其他执行模型的稳定演化仍未解决**。

## 2 背景

### 部署一个 Agent，比交出任务答案多了什么？

多数基准开始评测时，任务、评分方式和执行环境都已经准备好。真实部署恰恰有大量工作发生在这之前。

**驻场交付工程师（Forward-Deployed Engineer，FDE）** 是一个典型例子。这类工程师会深入客户的业务环境，把通用模型接入客户的数据格式和工作流程，并处理具体的合规要求。例如，日志只能保存一段时间，就要重新调整数据接入流程；跨地区合同处理出错，就要沿着整条流程查出问题发生在哪里。

这类工作不以一次演示成功为终点。系统需要真正投入使用、持续稳定运行，使用中遇到的新问题还要继续变成代码修复和功能需求。

让模型承担这类部署工作，至少要先补齐三样东西：

1. **明确目标**：把比较模糊的业务需求写成具体目标、约束和验收标准；
2. **准备反馈方法**：先有测试、评分规则和执行记录，才能判断系统做得怎么样、修改后有没有进步；
3. **搭建执行系统**：把工具、上下文、状态、生命周期和验证接口接起来，并随着需求变化继续维护。

**HarnessDev 只研究第三项。任务目标和反馈方法已经准备好，模型负责搭建执行系统**，再根据运行结果继续改进。如何澄清业务需求、如何从零设计反馈，不在这次评测范围内。

相关工作的分工也不一样：**Aspire** 研究如何把模糊的真实部署需求变成可以持续改进的具体能力，**S3Gym** 研究如何评价并复用 Agent 的交互经验，HarnessDev 则专门检查模型能否构建和维护执行系统。

## 3 基准设计

### 3.1 总览：开发者、Harness 和执行者分别做什么

提交物是一个**相关代码参数设置被冻结后反复使用的 Harness**。冻结指评测这个版本时不再修改代码，避免把开发过程和任务执行混在一起。

整个流程写成：

$$
(L_C,D)\rightarrow H,\qquad
(H,L_E,x)\rightarrow y\xrightarrow{J}\mathrm{score}.
\tag{1}
$$

其中：

- $L_C$：**开发者大语言模型**，负责创建或修改 Harness；下标 $C$ 表示 creator；
- $D$：**开发环境**，提供读文件、编辑代码、测试和调试能力；它也决定开发时能够获得哪些信号；
- $H$：开发完成的**可运行 Harness**，包含执行循环、工具、上下文管理、持久状态、生命周期控制和验证机制；
- $L_E$：**执行者大语言模型**，在 $H$ 冻结后进入其中完成任务；下标 $E$ 表示 executor；
- $x$：一个下游任务；
- $y$：执行后交出的任务结果；
- $J$：**评测器**，按对应基准的规则给结果评分；
- $\mathrm{score}$：最终得分；箭头表示开发、执行和评分三个阶段之间的传递关系。

先由 $L_C$ 在 $D$ 中写出 $H$，再把 $H$、执行模型 $L_E$ 和任务 $x$ 组合起来得到结果 $y$，最后交给 $J$。**开发环境帮助模型编写代码，最终评测的是写出的 Harness**，两者的用途不同。

两项研究问题分别是：

- **RQ1，创建**：从弱种子和少量开发案例出发，能否构建泛化到未见任务的有效 Harness？
- **RQ2，演化**：从自己创建的 Harness 出发，能否通过执行反馈继续改进，同时保住原本有效的行为？

除了得分，还会分析修改了什么、如何使用反馈，以及修改是否能迁移到新任务和其他执行模型。

### 3.2 开发设置

整个开发过程的关系是：

$$
\underbrace{H_{\mathrm{seed}}}_{\text{研究团队提供的公共弱种子}}
\xrightarrow{\text{创建阶段}}
\underbrace{H_0}_{\text{模型创建的第一版完整 Harness}}
\xrightarrow{\text{演化阶段}}
H_1,H_2,\ldots,H_n.
$$

$H_{\mathrm{seed}}$ 是创建阶段的输入，由研究团队提前提供，不是模型创建的产物。模型在创建阶段把它开发成完整 Harness $H_0$；进入演化阶段后，再根据执行反馈把 $H_0$ 修改成后续版本。创建和演化都允许模型修改工作区，差别在于从什么代码开始，以及模型能够看到哪些反馈。

![表1：两个开发阶段的输入与输出](<images/Harness-17.-HarnessDev-02.png>)

> 表1：Harness 开发设置。创建阶段构建新的 Harness；演化阶段利用下游执行反馈改进该 Harness。

解释：创建阶段给的是规格说明、1～3 个案例和弱种子；演化阶段给的是自己的初始版本与指定反馈集。后者的交付还包括经过配对评测的冻结候选，以及开发者自己选定的最终版本。

#### 弱种子：能接上环境，但不会替模型解题

公共种子 $H_{\mathrm{seed}}$ 是**所有开发者模型进入创建阶段时的统一起点**，还不能算完整 Harness。它是一层可运行的兼容程序，会解析任务和模型配置，暴露允许使用的底层工具，并写出结果、轨迹、日志和任务产物。

它提供的只是被动能力，例如读写文件、搜索路径、执行命令、调用模型接口，以及记录产物。**工具什么时候用、怎么组合使用，都要由开发者补出的控制逻辑决定**。

种子不提供以下能力：

- Agent 执行循环、任务拆解和工具选择策略；
- 上下文管理与持久任务状态；
- 验证器、重试、失败恢复和停止规则。

它最多做一次连通性探测，不尝试完成任务；未经修改时只留下空白或不完整产物，五个下游基准均为零分。因此，非零成绩需要来自开发者新增的执行逻辑。

如果从空仓库开始，模型会花大量时间处理命令行和文件格式；如果直接提供成熟 Agent，又会把待评测的规划与验证结构一并给出。因此，弱种子只解决接入问题，任务策略留给模型设计。

图2说明哪些能力预先提供，哪些能力必须自己开发。

![图2：弱种子与开发环境](<images/Harness-17.-HarnessDev-03.png>)

> 图2：弱种子 Harness 及其开发环境。种子仅固定输入与审计输出的封装，并暴露尚未被编排的被动基础操作；未经修改时不执行任务工作。开发反馈公开可见，隐藏任务、答案和正式评测分数仍不可访问。

解释：任务、模型配置、工作区和输出位置通过统一接口传入。种子只读取输入、解析任务、按需探测连接并记录状态，控制层仍然缺失。开发者可以反复“改代码—运行公开案例—查看日志与分数”，但不能通过这个循环接触隐藏评测内容。

#### 完整 Harness：六类控制能力要真正参与执行

完整 Harness 是**创建阶段需要交出的第一版成品 $H_0$**，也是演化阶段继续修改的起点。它需要把工具和模型调用组织成一套系统。图3用六类控制能力概括它的责任。

![图3：控制层与最终交付物](<images/Harness-17.-HarnessDev-04.png>)

> 图3：从控制层到可评分产物。开发者需要实现六类控制模块 E/T/C/S/L/V，颜色与图1一致。完成的 Harness 通过统一、可审计的输出报告运行过程，并交付与领域对应、可被评分器读取的最终产物。

解释：六类能力共同决定任务怎样推进，并不要求必须写成六个独立类：

- **执行 E（Execution）**：组织执行循环、规划、调度和停止条件；
- **工具 T（Tools）**：定义工具接口、选择工具、约束输入输出并处理调用错误；
- **上下文 C（Context）**：安排任务、代码、日志、历史和约束如何进入模型输入；
- **状态 S（State）**：保存当前目标、假设、进度、尝试、失败和产物状态；
- **生命周期 L（Lifecycle）**：处理工具调用前后的钩子、失败与超时、恢复和最终收尾；
- **验证 V（Verification）**：运行测试与检查，调用评测器验证产物，并记录执行轨迹。

统一审计输出包括 `result.json`、`trajectory.jsonl`、`response.md` 和运行日志。前者记录结果，轨迹文件逐条记录动作与观察，其他文件保留回复及运行信息。但**统一记录格式不等于所有领域交同一种答案**：

- 代码任务需要真实仓库修改与补丁；
- 数据与机器学习任务需要可评分提交和指标；
- 写作任务需要最终面向用户的文字；
- 搜索任务需要答案及支持答案的证据。

领域对应的收尾逻辑负责把运行结果整理成这些产物。只生成日志和结果 JSON，却没完成实际工作，仍然拿不到任务分数。

#### 创建：利用少量案例进行开发，再冻结进行评测

创建阶段要做的是**把公共弱种子 $H_{\mathrm{seed}}$ 开发成第一版完整 Harness $H_0$**。开发者获得弱种子、任务类型说明、工具与权限约束、简短设计教程，以及 **1～3 个开发案例**。可以根据这些案例反馈修改代码，但看不到人工系统实现，也看不到隐藏评测集。

开发结束后冻结 $H$，再执行正式评测。这里测的是从有限开发信号中建立可复用系统的能力。

#### 演化：同一版本必须完成两套正式评测

演化阶段要做的是**从 $H_0$ 出发，根据执行反馈继续修改，得到 $H_1,H_2,\ldots,H_n$**。它固定使用 **100 道 SWE-Pro 任务和全部 89 道 Terminal-Bench 任务**提供开发反馈。

评测控制器按以下规则运行：

1. 先在两套反馈任务上评测 $H_0$，建立起点；
2. 后续候选按代码提交版本冻结，同一个提交必须完整运行 100 道 SWE-Pro 和 89 道 Terminal-Bench；
3. 两部分都完成后，候选才进入正式版本轨迹；同一提交的基础设施修复结果合并处理，探测、未完成运行、停止运行和无效实例不计入正式轨迹；
4. $H_0$ 之后最多允许 **10 次完整配对评测**；两次计入预算的配对评测之间最多做两次小规模探测，每次固定使用两套基准各自的前五道题；
5. 小规模探测只用于诊断。结束时必须声明一个非 $H_0$、且完成正式配对评测的提交作为最终版本。

随后，再把每个正式版本放到 **630 道未参与反馈的 SWE-Pro 任务**上评测。分数只在整个开发过程结束后获得，不返回给开发者，也不影响选版本。

**演化曲线上的 189 道题是反馈集，不是隐藏测试集**。保留集（held-out set）在这里指从开发循环中隔离的任务，即使这些题来自公开基准，只要开发者在本次开发中不能接触，仍属于这一评测意义下的保留集。

### 3.3 领域和下游基准

创建覆盖四个领域、五个基准，演化目前只覆盖代码 Harness。

![表2：下游评测覆盖](<images/Harness-17.-HarnessDev-05.png>)

> 表2：创建阶段的下游评测覆盖。创建使用 SWE-bench Pro 公开划分中的全部 731 个实例。演化从同一公开划分中选取 100 道反馈任务和 630 道保留任务，并使用全部 89 道 Terminal-Bench 任务提供反馈。

解释：代码领域占了两个基准，因此“四个领域”和“五个基准”并不冲突。五套任务共 2,207 个不同实例，演化复用其中的任务，不再重复计数。

各基准关注的能力不同：

- **SWE-bench Pro，简称 SWE-Pro**：731 个软件工程任务，衡量真实代码修改能否解决问题；
- **Terminal-Bench 2.1**：89 个终端环境任务，衡量环境中的实际任务成功情况；
- **MLE-bench**：75 个机器学习工程任务，使用奖牌成绩衡量提交质量；
- **EQ-Bench3**：46 个写作任务，按评分准则评价文字质量；
- **BrowseComp**：1,266 个搜索研究任务，使用答案准确率。

> 任务数量沿用 v1：创建集为 731 道 SWE-Pro，演化反馈集与保留集分别为 100 和 630 道。两者之和为 730，正文没有在此解释余下一个实例的去向，不应自行补成某种筛除原因。

成熟开源系统可以作为人工构建参考，也可以提供开发环境，但这两种身份需要分别指定。被列入候选系统不意味着同时承担所有角色；具体版本、角色和许可应以基准发布配置为准。

### 3.4 评测协议

#### 两种执行设置：自己运行，还是统一换一个模型

每个分数都来自标准运行环境中的冻结 Harness。同一次对比固定执行模型和评分器，尽量把变化归到 Harness 上。

- **同模型执行（Self-Eval）**：令 $L_E=L_C$，写 Harness 的模型也是运行它的模型。参与实验的 Opus 4.8、GPT-5.5、Gemini 3.1 Pro、DeepSeek V4 Pro、Qwen 3.7 Max 和 Seed 2.0 Pro，都会分别运行自己创建的 Harness。结果体现模型、Harness 及二者适配的整体表现；
- **统一模型执行（Unified-Eval）**：所有生成的 Harness 都交给同一个执行模型，本次使用 Gemini 3.1 Pro，用于控制执行模型差异，并观察 Harness 更换执行模型后的兼容性。

这里的 Self-Eval **不表示模型自己给答案打分**，评分依然由 $J$ 完成。它只说明开发者和执行者使用同一个模型。

统一执行可以减少模型能力差异带来的干扰，但不能完全消除模型与 Harness 的相互影响。提示词、工具协议、预算和停止规则仍可能更适合某一模型。

演化的正式反馈分数用于修改和最终版本选择；所有轨迹结束后，才用隔离任务检查每个冻结版本的泛化。人工构建系统则是外部参考，不是第三种执行设置，也不是绝对能力上限。

#### 约束与审计：声称成功不能得分

提交的 Harness 不允许：

- 硬编码任务答案，或根据任务标识、文件名白名单、已知答案推导补丁；
- 访问隐藏测试、答案、补丁、私有评分器内部实现或被隔离的正式评测反馈；
- 绕过提供的厂商中立模型接口，换成自己的模型访问路径。

这些约束通过两层机制检查：

- **评分路径独立**：Harness 自报成功不参与计分；SWE-Pro 看任务工作区留下的真实代码差异，Terminal-Bench 看最终环境状态；
- **保留可审计证据**：冻结源代码、执行轨迹、结果和指标产物一起保存，以便检查代码实际做过什么。

报告中的所有交付代码和运行产物都经过审计，没有发现通过禁止路径得分的情况，也没有因此排除运行。这个结果只描述本次审计观察，不能当成生成代码的普遍安全保证。

### 3.5 指标

HarnessDev 从**任务效果**和**执行成本**两个方面评价冻结后的 Harness：

- **任务表现**：沿用各基准自己的指标，不能把写作评分和代码成功率当成同一种测量；
- **执行成本**：统计执行模型在评测时消耗的 token，可报告总量及每题平均量，**不包含开发者创建或修改 Harness 的 token**。

创建阶段比较弱种子、生成系统与可用人工参考；演化阶段比较同一执行模型和评分器下，后续版本相对 $H_0$ 的变化。成本和效果分别呈现，不强行合成一个“性价比分”。

## 4 实验

实验依次检验创建质量、演化效果和执行模型迁移，同时检查代码是否真的参与运行，以及开发者是否有效利用反馈。

### 4.1 实验设置

- **开发者模型**：Opus 4.8、GPT-5.5、Gemini 3.1 Pro、DeepSeek V4 Pro、Qwen 3.7 Max、Seed 2.0 Pro；
- **开发环境**：GPT-5.5 使用 Codex 0.144.3，其余使用 Claude Code 2.1.177；模型通过官方接口或 OpenRouter 模型接口聚合服务访问；
- **创建重复次数**：每个开发者—基准组合独立创建并评测三个 Harness，报告 **avg@3，即三次独立创建的平均成绩**，并非选最好的一次；
- **统一执行模型**：Gemini 3.1 Pro。Gemini 自己的同模型执行结果可以复用为统一执行对照，不算新增实验单元。

人工参考来自公开系统结果，**不是把所有人工 Harness 都接到同一模型后的严格配对实验**。因此，后面的差距体现与选定成熟系统的距离，不能单独归因于 Harness 代码质量。

资源条件也会限制系统表现。机器学习任务在独立容器中使用一块 80GB A800 GPU，任务总时限为 10 小时，其中 9.5 小时供 Agent 使用，剩余半小时留给固定评分器，最多 500 步。演化代码任务同样最多 500 步，每题时限两小时。

### 4.2 Harness 创建

#### 总体表现：能从零分起步，但领域差距明显

**模型能够从零分的弱种子写出真正能完成任务的 Harness，但效果很看任务类型，整体上仍未达到成熟人工系统的水平**。

![表3：同模型执行下的创建成绩](<images/Harness-17.-HarnessDev-06.png>)

> 表3：RQ1，同模型执行下的 Harness 创建。除另有说明外，每个开发者独立构建并评测三个 Harness，得分为 avg@3。各基准使用原生指标；tok. 表示每个 Harness 的平均执行 token 总量，单位为百万；Avg. 是 SWE-Pro、Terminal-Bench、EQ-Bench3 和 BrowseComp 四项成绩的非加权平均。MLE-bench 覆盖 33 个实际实验单元和 2,475 条结果。人工行是系统级参考，星号表示本实验未重新运行的外部结果，破折号表示数据不可用。

解释：**最好的整体分数仍低于选定人工系统参考，但并非每个领域都落后**。Opus 的四项平均为 67.8，人工参考为 86.2；写作接近参考，机器学习实验中 Opus 与 Gemini 的奖牌率为 32.9 和 32.4，高于参考的 24.0。搜索差距最大，代码也仍有差距。这里的 Avg. 不含 MLE-bench，不能称为“五项平均”。

数据任务失败分析把 77.8% 的失败归因于 Harness 缺陷，说明执行模型能力并非唯一瓶颈。

#### 固定执行模型：排名会变，同一开发者的不同成品也会变

**统一改用 Gemini 后，开发者排名和部分 Harness 的成绩都会变化，说明结果同时受到 Harness 设计、执行模型能力及二者适配程度的影响**。

![表4：统一 Gemini 执行下的创建成绩](<images/Harness-17.-HarnessDev-07.png>)

> 表4：RQ1，固定 Gemini 执行者下的 Harness 创建。所有开发者创建的 Harness 均由 Gemini 3.1 Pro 执行；Gemini 行复用同模型执行对照，不新增实际实验单元。各得分为 avg@3。代码任务中带 ‡ 的单元各包含一次第三副本运行崩溃；事后排除该副本，Opus 的 SWE-Pro 均值为 49.1，DeepSeek 的 SWE-Pro 和 Terminal-Bench 均值分别为 43.8、57.3。GPT-5.5 的 EQ-Bench3 三次平均为 46.5，排除第一次零分 Harness 后为 69.7。其余定义同表3。

解释：正式平均保留失败副本，括注的排除结果只是敏感性分析，不能替代正式成绩。Qwen、Seed、DeepSeek 在若干数据和搜索设置中受益于 Gemini，而 Opus、GPT-5.5 往往更适合自己的执行模型。一次生成结果不足以代表开发能力。

一个具体失败是，Opus 代码 Harness 围绕原执行模型硬编码了 120 步限制，换成 Gemini 后某个副本几乎失效；数据任务中过严的停止规则也会产生类似问题。**程序能换模型运行，不等于预算和控制逻辑也适合新模型**。

图4把不同指标统一换算为相对人工参考的比例。

![图4：与人工构建参考系统的距离](<images/Harness-17.-HarnessDev-08.png>)

> 图4：RQ1，与人工构建参考系统的距离。每个开发者的同模型执行成绩按对应基准的人工参考归一化，虚线表示参考成绩，柱旁标签为原始得分。人工参考来自不同 Harness—模型组合，未在统一执行模型下配对控制，因此这里只展示与选定成熟系统的距离。超过 100% 表示超过该外部参考，不表示超过人类能力。

解释：横轴回答“达到参考系统成绩的多少比例”，不是任务成功率本身。机器学习实验中部分柱子超过参考线，代码和搜索没有达到参考线。这个对比支持领域差异，不支持“自动生成 Harness 已经整体超过人工工程”的判断。

#### 执行成本：消耗更多 token 不保证更好

**在机器学习任务上，不同 Harness 的执行成本相差约 19 倍，但得分没有随 token 消耗同步提高**。成本—效果关系可以用图9进一步看清。

![图9：创建阶段的成本与表现](<images/Harness-17.-HarnessDev-09.png>)

> 图9：RQ1 的成本与表现。纵轴为下游成绩，横轴为执行 token，采用对数刻度。同模型执行用实心圆表示，统一 Gemini 执行用空心方块表示，虚线为可用的人工构建参考。各点对应表3和表4的 avg@3；Gemini 是固定执行对照，两种标记重合，呈现方框内有圆点。相近质量的成本可能接近相差一个数量级：MLE-bench 中 GPT-5.5 用 29.3M token 达到 19.1 的奖牌率，DeepSeek V4 用 208.4M 达到 19.6。这里直接报告成绩与 token 的取舍，不合并成单一成本调整分数。

解释：两个系统结果接近，消耗却可以相差数倍。横轴是每个 Harness 在该基准上的执行 token 总量，以三个成品平均，不是每题成本，更不包括开发 Harness 的消耗。对数刻度表示越向右成本越高，但相同间距对应倍数变化。

#### 实现行为：代码多、类齐全，都不等于系统好用

**代码规模和模块声明都不能直接代表 Harness 有效，相关机制还要真正进入执行路径，并在任务中发挥作用**。

六个开发者呈现出不同修改方式：Opus 经常重写执行栈；GPT-5.5 倾向加入一个较大的单体 Agent；DeepSeek、Qwen、Seed 扩展工具、上下文和状态等模块；Gemini 更多在原运行器上局部修改。

表5比较代码修改量与成绩。

![表5：创建阶段代码修改量](<images/Harness-17.-HarnessDev-10.png>)

> 表5：冻结的 RQ1 代码产物修改规模。文件数量和净新增代码行数按每个开发者的三个独立产物求和，中位数与范围按单个产物统计。只计算开发者负责的 harness/，排除运行器注入的底层运行支持。性能为同模型执行 avg@3。

解释：**修改规模不能可靠预测表现**。18 个代码产物净增 17,111 行；Gemini 总计只增加 1,006 行，却取得最高的 Terminal-Bench 成绩 68.8。增加了多少代码，并不能说明解决了多少问题。

结构审计进一步区分“有声明”和“真在执行”：

- **基础循环较完整**：18 个代码 Harness 都有显式执行循环，工具、生命周期、验证能力完整的分别为 13、13、15 个；
- **状态保存明显不足**：11 个定义了 `State` 状态类，但只有一个暴露状态保存接口，只有一个实现周期性检查点；26,679 条任务轨迹中没有发现检查点事件。检查点就是把当前进度存下来，以便之后恢复；
- **验证偏表面**：2,325 次已执行数据任务中出现 441 个退化提交，没有被 Harness 检测出来，说明格式检查仍不足以保证内容有效。

图5按实际实现与执行证据呈现六类能力。

![图5：Harness 架构的实现与执行证据](<images/Harness-17.-HarnessDev-11.png>)

> 图5：跨领域、跨阶段的 Harness 架构证据。六列分别为执行循环、工具策略、上下文管理、状态与记忆、生命周期与恢复、结果验证。各领域先按实际可用的独立 Harness 数量归一化，再聚合 RQ1 的代码、数据、写作、搜索和 RQ2 主实验；Seed 没有 RQ2 轨迹，因此只包含四个 RQ1 领域。机制进入主执行路径或在正式运行中触发，才获得完整权重；仅有代码声明、配置或临时状态，只获得部分权重。颜色表示机制证据密度，不表示得分、显著性或因果效应。

解释：状态与记忆一列普遍较浅，呼应保存与恢复机制没有真正运行的问题。其他列颜色较深也不意味着该能力一定有效，只能说明有更多实现或运行证据，效果还要结合任务结果判断。

未运行的代码并非个别现象：代码领域 108 个组件实例中，72 个实际触发，18 个只有部分证据，18 个从未观察到，后者均涉及状态与记忆。写作中有 124/587 个功能被确认是不会执行的代码，数据领域另有 36 个机制处在不会触发的路径上。小的实现错误还可能让整条工具调用链失效。

自测次数与得分的相关性只有 0.13～0.26，且不显著；修改调用次数的相关性达到 0.57。相关性不能证明因果，单纯增加测试次数也不能保证提高成绩。开发者还需要**读懂失败原因，做出针对性修改，再验证效果**。

#### 执行模型迁移：接口兼容之后，还有行为兼容

**同一个 Harness 换成 Gemini 执行后，成绩可能提高，也可能大幅下降；能够接入新模型，只说明接口兼容，控制逻辑未必适合新模型**。

图6直接比较同一个开发者的 Harness 换执行模型后的变化。

![图6：固定 Gemini 后的迁移表现](<images/Harness-17.-HarnessDev-12.png>)

> 图6：固定 Gemini 执行者下的 Harness 可移植性。实心标记为同模型执行，空心标记为对应开发者 Harness 的固定 Gemini 结果，标签是统一执行减去同模型执行的差值，虚线为外部人工构建系统参考。所有面板使用表3和表4的 avg@3；两个包含崩溃副本的代码单元，其排除后敏感性均值见表4图注。

解释：空心点相对实心点右移表示换成 Gemini 后提高，左移表示下降。Qwen 在 BrowseComp 和 MLE-bench 分别提高 17.6、12.9 个百分点；Opus 的 SWE-Pro 从 69.3 降到 33.0，写作从 84.6 降到 74.2。迁移方向取决于具体 Harness，不能只看开发模型排名。

Opus 搜索 Harness 更换执行模型后，重复查询率从 10.1% 升至 88.2%，说明它的去重、复查和结束规则与原模型存在较强适配。**可迁移的 Harness 还需要提示词、工具协议、预算和停止规则共同兼容新模型**。

### 4.3 Harness 演化

#### 总体结果：反馈集成绩提高，未见任务上的收益更有限

**演化通常能提高开发过程中可见的反馈集成绩，但迁移到未见任务或固定执行模型后，收益会明显变小，也更不稳定**。

五条同模型执行轨迹的最终版本都提高了可见反馈配对成绩，在保留任务上也有改善，最大为 Opus 的 4.44 个百分点。固定 Gemini 后，四条迁移轨迹只有 Opus 在保留任务上提高，其余三条退步。

这说明演化能产生有用修改，但收益往往围绕当前执行模型或反馈任务，尚不稳定。

#### 评测方式：反馈得分与泛化得分分开算

**反馈集成绩用于修改代码和选择版本，630 道保留任务只用于事后检查泛化，开发者在演化过程中看不到这部分成绩**。

反馈成绩分别报告两个基准，并计算等权配对分数：

$$
\bar P_t=\frac{1}{2}\left(P_t^{\mathrm{SWE100}}+P_t^{\mathrm{Term89}}\right).
\tag{2}
$$

- $t$：完成正式评测的冻结版本序号；
- $P_t^{\mathrm{SWE100}}$：该版本在 100 道 SWE-Pro 反馈任务上的百分制成绩；
- $P_t^{\mathrm{Term89}}$：同一版本在 89 道 Terminal-Bench 反馈任务上的百分制成绩；
- $\bar P_t$：两项成绩的算术平均，横线表示这里取了平均；
- $\frac12$：给两个基准相同权重，不是按 100 和 89 的题目数量加权。

先分别算两套题的成绩，再相加除以二。这一分数供开发反馈和版本比较，**不包含 630 道保留任务的成绩**。

实验运行五条同模型执行轨迹、四条固定 Gemini 轨迹，保持对应开发者、开发环境和初始 Harness 相同。共形成 **73 个正式版本、64 次相邻版本切换**。每条轨迹结束后，再对每个版本测试隔离的 630 道 SWE-Pro，分数不参与开发与选版本。

表6把两种结果放在一起。

![表6：反馈收益与保留集泛化](<images/Harness-17.-HarnessDev-13.png>)

> 表6：RQ2 反馈集收益与冻结后的 630 题保留集泛化。反馈配对成绩是 SWE-Pro-100 和 Terminal-Bench-89 百分制成绩的非加权平均；保留集列只使用之后运行的 SWE-Pro-630。最终差距等于该轨迹最佳保留集分数减去开发者声明版本的保留集分数。Gemini 对照只列一次。

解释：**反馈上涨不保证保留集上涨**。例如固定 Gemini 的 GPT-5.5 轨迹，反馈配对成绩提高约 2.4 个百分点，保留集却下降 10.32 个百分点。$H_{\mathrm{dec}}$ 表示开发者声明的最终版本；“最终差距”越大，表示这条轨迹其实曾出现更好的保留集版本，却没有被选中。

#### 修改与反馈使用：定位具体失败比扩建架构更关键

**多数修改集中在执行流程、工具和失败恢复，状态与独立验证器很少被改；开发者对具体失败案例的检查也不充分**。

最终版本相对初始版本，修改文件数的中位数为 8，新增和删除行数的中位数分别为 476、38。64 次正式切换中，58 次改变执行或控制流程，37 次改变工具，17 次改变恢复逻辑，16 次改变上下文，只有 4 次改变状态，没有一次修改独立验证器；这些类别可以重叠。

表7记录九条轨迹实际改了什么。

![表7：九条演化轨迹的代码修改](<images/Harness-17.-HarnessDev-14.png>)

> 表7：九条 RQ2 轨迹的代码修改规模。“正式切换”只统计相邻正式版本；累计差异比较 $H_0$ 与每条轨迹声明的最终版本。修改重点描述该次轨迹中观察到的行为，不代表对应开发模型的一般属性。

解释：改进常集中在结束前检查、工具消息处理、超时恢复和产物跟踪。**删除和回退也可能有用**：DeepSeek 在固定 Gemini 运行时的一次重写中，因为上下文压缩破坏了工具消息配对，后来回退了大量修改。修改规模本身不能可靠预测收益。

九条轨迹中，八条至少完成过一次“读结果—改代码—重新评测—选版本”的闭环，但**失败诊断仍是薄弱环节**：专用轨迹接口总共只调用两次，各轨迹明确检查的案例只覆盖反馈集的 0.5%～40.2%。开发者更常依赖自写脚本和小规模探测，而这些结果可能与完整测试不一致。GPT-5.5 一个候选在五道 Terminal 探测题上全部通过，完整集合却只有约 58.4%。

图7展示正式版本随修改如何变化。

![图7：反馈集上的演化曲线](<images/Harness-17.-HarnessDev-15.png>)

> 图7：SWE-Pro 与 Terminal-Bench 上的反馈集演化轨迹。上方为同模型运行，下方为固定 Gemini 运行；SWE-Pro 使用 100 道反馈任务，Terminal-Bench 使用 89 道反馈任务。$H_0$ 是冻结的 RQ1 Harness，$H_i$ 是之后完成两个基准正式评测的冻结提交，星号表示开发者声明的版本。Gemini 对照在两种设置中复用，每个开发者—执行模型组合只有一条轨迹。

解释：曲线明显不是逐版上升，一套基准上的改善也可能伴随另一套退步。星号强调最终交付由开发者选择，并不一定是最新版本。不能把图中的每一段上升都直接解释为代码修改带来的确定收益。

Opus 提供了一个较清楚的正例：发现 100 次运行中有 99 次自报成功，实际只有 48 次通过后，它追查到**过早结束**问题，并加入完成检查。这个例子包含具体失败、对应修改和端到端验证，证据比单纯看到分数上涨更完整。

#### 稳定性与最终选择：后续版本可能抹掉已有收益

**多数版本变化不足以证明修改带来了稳定提升，开发者也经常没有选中保留集表现最好的版本**。

64 次切换里，8 次两项基准同时退步，16 次出现单项退步，3 次呈现跨基准取舍，7 次无可测变化，27 次涨分仍处于重复运行噪声范围，只有 2 次存在超出噪声的明确正向证据，另有 1 次没有可执行代码改动。

同一提交的配对成绩本身就可能波动约 **±4.75 个百分点**。因此，单看一次小幅涨分，无法确认是修改有效，还是运行波动。

新增逻辑也存在执行不到的问题：169 个新函数或类中，113 个可以从入口到达，31 个只能通过不会运行的代码到达，25 个没有调用者。Opus 的完成检查有实际路径和案例支持，而 Qwen 的消息清理逻辑则破坏了原本有效的 Gemini 工具结果序列。

图8进一步对照同一版本的反馈成绩和保留集成绩。

![图8：反馈轨迹与保留集成绩](<images/Harness-17.-HarnessDev-16.png>)

> 图8：SWE-Pro-100 反馈轨迹与冻结后的 630 题保留集表现。每列对应一个开发者，叠加可见 100 题反馈轨迹与冻结后的 630 题保留集轨迹。上方为同模型运行，下方为固定 Gemini。星号标记根据 100 题反馈所声明的最终版本，浅色竖线连接同一 Harness 版本的两种成绩。Gemini 对照重复出现仅用于视觉比较。

解释：实线和虚线不总同向变化，开发集上看起来较好的修改可能没有泛化。五条同模型最终版本在保留集上提高 1.43～4.44 个百分点，平均为 3.11；固定 Gemini 后，仅 Opus 改善。64 次可比较切换中，反馈与保留集同向变化只有 34 次，即 53.1%；九个最终版本仅两个恰好是各自轨迹的保留集最优版本。

**根据反馈可以找到有用的修改，但仍可能选错最终版本**。反复按带噪声的分数选版本，可能选中偶然高分，并加重对反馈集的过拟合。

## 5 相关研究

Agent 评测、自动系统设计和 Harness 演化都涉及这类开发工作，但要求交付的产物和验证改进的方式不同。

### Agent 评测与 Harness 开发

- **SWE-bench、GAIA、WebArena、τ-bench、AgentBench**：规范任务、环境与评分，通常在选定 Harness 下评测任务执行能力；
- **Harness-Bench**：关注更换 Harness 如何改变模型表现；
- **Meta-Agent Challenge**：让元 Agent 在沙箱中迭代编写 Agent 产物，再用五个领域的受保护测试评分，与创建阶段最接近；HarnessDev 进一步连接后续维护，区分开发与执行模型，并计入执行成本；
- **HarnessOpt-Bench**：优化器获得种子、评分反馈和固定预算，再由可信环境在隐藏测试划分上评价最终候选。重点是改进给定 Harness，不过接近空白的 GAIA 种子也需要构建。HarnessDev 则把从头创建接到演化，并对每个冻结版本测保留集；
- **Evo-Bench**：从共享 CodeAct 种子出发，固定执行模型，评价最终版本在隔离多领域任务上的表现。HarnessDev 还观察整条轨迹中泛化成绩怎样变化，并比较同模型执行与固定执行模型。

这些评测的起点不同，有的覆盖创建，有的从已有系统开始改进；有的只检查最终版本，有的还检查全部中间版本。

### 自动 Agent 设计与演化

Claude Code、Codex、OpenHands 和 SWE-agent 等人工系统组合了循环、工具、上下文和恢复机制。自动设计方法则在不同层面搜索改进：

- **ADAS、AFlow、MASS、EvoAgentX**：搜索提示词、工作流、操作或 Agent 连接结构，也有方法通过自然语言或代码构建更大范围的 Agent 系统；
- **VeRO**：提供版本快照、预算约束评测和结构化执行轨迹；
- **Meta-Harness**：让代码 Agent 查看过去候选、分数与轨迹，再提出下一个 Harness；
- **Self-Harness**：从失败中推导模型特定修改，并做回归测试，即检查原本正常的能力有没有被改坏；
- **HarnessFix**：结合来源与控制流信息定位修复位置；**DemoEvolve** 在奖励反馈不可靠时引入示范；**HarnessCompass** 处理过拟合和组件干扰；
- **Harness-R1**：训练 Harness 工程师模型，把一批失败转成经过验证的补丁。

其他研究还会在任务之间改进提示词、工作流、记忆、技能、工具和代码。它们主要提出改进方法或基础设施，HarnessDev 则用统一的创建—演化协议，衡量通用模型能够承担多少开发工作。

### 怎样证明演化真的有效

最终涨分可能混合了有效诊断和盲目搜索，需要更细的评测：

- **改动优先级评测**：检查优化器是否知道哪些组件最值得改；
- **Harness Updating Is Not Harness Benefit**：区分“生成了有用修改”和“执行模型能够利用修改”，这也是拆分开发者与执行者的原因；
- **SEAGym**：记录中间快照、成本以及分布内外结果，显示后续更新未必保住泛化收益；
- **等预算比较**：已有研究发现演化可能过拟合搜索基准，也不一定优于更简单的搜索基线。

HarnessDev 据此冻结产物、记录开发轨迹与执行成本，并在后续独立测试泛化。但**与其他搜索策略做严格等预算比较，仍属于未来工作**，现有结果不能证明这套演化流程已是最佳搜索方案。

## 6 讨论、局限与结论

创建结果说明，不同领域对 Harness 工程能力的要求差别很大。写作和机器学习实验中有接近或超过参考系统的结果，代码与搜索仍有较大差距。更换执行模型后，有的系统受益，有的系统退步，反映出系统逻辑与模型行为之间的适配。

演化更难保证持续有效：中间版本的改进可能被后续修改抵消，更新次数增加也不保证最终更好。在固定 Gemini 的对照中，**更换执行模型后，初始成绩会变化，同一项代码修改的效果也会变化**。

### 6.1 局限

- **领域覆盖有限**：四类任务并不覆盖全部真实部署，人工参考也不均衡，更不能保证最优；
- **统一执行仍有残余差异**：模型和 Harness 的交互复杂，固定模型只能减少干扰，不能彻底分离所有因素；
- **行为分析以描述为主**：观察到修改量、测试和表现之间的关系，不等于确定因果；
- **演化样本少**：每个开发者—执行模型组合只有一条轨迹，还有一条主运行设置未完成，不能据此给出稳健的不确定性估计或广泛模型排名；
- **泛化只测到 SWE-Pro**：冻结后保留集没有覆盖其他领域，不能推出跨领域演化已有效；
- **还未验证递归开发**：两阶段都使用固定开发环境，尚未检验演化后的 Harness 能否接替开发环境，再继续开发自身；
- **不等于替代参数训练**：这里积累的是模型外部的系统改进，没有证明这类学习能够替代模型参数训练。

### 6.2 结论

HarnessDev 评测模型创建和维护执行系统的能力。模型能写出实际有用的 Harness，也能根据失败做出局部改进，但仍难以**准确诊断问题、避免改坏已有功能、选出适用于新任务的版本，以及适配其他执行模型**。

模型参数可以积累能力，Harness 也能保存可检查、可测试、可复用的改进。要确认这些修改有效，还得检查新增机制是否真正被调用，以及是否能持续帮助后续任务。

### 伦理与安全

基准使用公开任务与开源 Harness，没有另外收集人类生成内容。自动构建执行系统可能放大不安全工具调用，因此需要约束与运行后审计。本次没有观察到违规，审计产物也作为供他人重复检查的发布内容。

**实验容器主要用于复现，不应直接视为足够的安全隔离**。复用生成的 Harness 时，应把它当作不可信代码，采用比本次实验更严格的隔离措施。

## 总结

HarnessDev 是一篇评测论文，研究大模型能否从弱种子创建 Harness，再根据下游任务反馈持续修改它。论文没有提出新的 Harness 架构或训练算法，主要贡献是把评测对象从单次任务答案换成可运行代码，并通过冻结版本、未见任务和固定执行模型检查修改是否真的有效。

实验给出的答案比较明确：**当前模型已经能写出有用的 Harness，也能完成一些局部修复，但还做不到稳定、通用的 Harness 演化**。连续修改不一定让系统越来越好，反馈集上的涨分到了未见任务上可能缩小，换一个执行模型后也可能消失。代码写得更多、测试跑得更多，同样不保证结果更好。

这个论文的**价值主要在评测方法**。它要求同时检查真实任务结果、执行 token 成本、代码是否真正运行、已有能力有没有退化，以及修改能否迁移。局限也很明显：统一执行只使用 Gemini，演化实验只覆盖代码领域，每个开发者与执行模型组合只有一条演化轨迹，人工系统参考也没有在同一模型和资源条件下重新评测。因此，这些结果可以说明当前模型的 Harness 开发还不稳定，暂时不能据此选出最佳模型、最佳 Harness 架构或可靠的演化方法。

结论：目前没什么卵用。。。。
