> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对《Knowledge Graph Engineering for Multi-Agentic Systems: The Anthropic Playbook》的理解。

![666](<images/Graph-Engineering-Athropic-Playbook-01.png>)

> 这份技术手册近期独立整理，综合了 Anthropic 的 Knowledge Graph Cookbook、Building Effective AI Agents 和 Claude API 文档。它不是 Anthropic 官方发布或背书的论文。整套方案用 Claude 的结构化输出完成实体抽取与实体消歧，再用图结构保存关系、支持多跳查询，并把知识图谱接入多 Agent 系统，作为共享记忆、事实校验层和跨轮次持续更新的世界状态。

## 核心问题

要解决的问题很具体：**多个 Agent 分别处理不同文档、在不同上下文窗口中工作时，怎样把分散事实保存成一个可以共同读取、持续更新、跨文档推理的结构化状态**？

只把各个**工作者 Agent（Worker）** 的长文本结果继续塞给**协调器 Agent（Orchestrator，负责拆任务和汇总结果的中心 Agent）**，会让上下文随着 Agent 数量和任务轮次持续膨胀。普通检索增强生成（Retrieval-Augmented Generation, RAG）可以找出语义相关的文本片段，但遇到“先从文档 A 找到人物，再到文档 B 找到该人物参与的项目，最后从文档 C 找到项目对应地点”这类问题时，检索到片段还不等于已经把事实连接起来。

解决思路是：**把实体保存为节点，把带类型的事实关系保存为边，再让 Agent 查询和更新同一张知识图谱**。完整流程分为四步：

1. 从每篇文档中抽取带类型的实体和主语—谓词—宾语三元组；
2. 把不同文档中的别名合并为规范实体；
3. 组装图结构，并为重要节点生成跨文档摘要；
4. 根据问题取出相关子图，让模型只围绕可追溯的关系进行多跳推理。

## 摘要

多 Agent 系统可以高速生成、评估和协调任务，但每个 Agent 的工作记忆仍受上下文窗口限制。任务一旦需要跨文档连接事实、跨工作者共享状态，或在第二天继续使用前一天积累的信息，单个上下文窗口就不够了。

这套知识图谱流水线省去了专门训练的命名实体识别模型和关系分类器。**结构化输出负责实体与关系抽取，更强的模型负责别名聚类和规范化，处理结果再组装成可查询图**。图中的每条边都保留来源信息，回答多跳问题时可以引用具体关系，结论不再只靠语言模型自行判断。

这里使用的 **Apollo 语料（Apollo corpus）** 由六篇 Wikipedia 摘要组成，主题分别是 Apollo 计划、Apollo 11、Neil Armstrong、Saturn V、Buzz Aldrin 和 Kennedy Space Center，用来演示跨文档实体抽取、别名合并、图组装和多跳查询。抽取结果偏向**高精度、低噪声**：抽取出的实体基本正确，但会漏掉一些不够核心的实体。知识图谱适合这样的取舍，因为一个错误节点通常还会产生多条错误关系，影响往往比漏掉一个边缘节点更大。

## 1 引言

### 1.1 跨文档问题为什么不能只靠上下文

假设一个竞品情报系统把任务分给定价、产品、财务、市场等多个工作者。定价工作者发现某家公司降价 15%，产品工作者发现同一家公司提交了新专利，财务工作者又发现它的研发投入翻倍。最终结论需要把三条事实连起来，但没有任何一个工作者单独看过全部材料。

如果所有工作者只把文本摘要发回协调器，协调器的上下文会随工作者数量线性增长。更麻烦的是，三份报告可能分别使用公司简称和全称。即使文本都放进上下文，模型仍要临时判断这些名称是不是同一个实体。

知识图谱把这个问题拆开处理：

- **实体节点**保存人物、组织、地点、事件或物品；
- **关系边**保存“降低价格”“申请专利”“增加研发投入”等事实；
- **实体消歧**把简称、全称和别名映射到同一规范节点；
- **来源信息**记录每条关系来自哪篇文档，方便后续检查；
- **图遍历**沿节点之间的连接寻找跨文档事实链。

这样，工作者不必反复传递整篇文档或长摘要，只需要读取与任务相关的子图，并把新事实写回图中。

### 1.2 三项主要贡献

主要贡献有三点：

- 给出一条完全由 Claude API 调用和普通图工具组成的知识图谱构建流水线；
- 说明知识图谱怎样嵌入 Anthropic 常见的 Agent 模式，成为 Agent 流程的一部分；
- 用小型 Apollo 语料评估抽取与消歧质量，并给出模型选择、成本控制和生产监控建议。

整条流水线如下。

模型按任务难度分工：**Claude Haiku** 是速度和成本优先的轻量模型，负责高吞吐量抽取；**Claude Sonnet** 的判断与推理能力更强，负责实体消歧、总结和多跳查询。

![](<images/Graph-Engineering-Athropic-Playbook-02.png>)

> 图 1：知识图谱流水线分为抽取、实体消歧、图组装和查询四个阶段。Haiku 负责高吞吐量抽取，Sonnet 负责需要更强判断的消歧、总结和查询；评测结果会反过来推动提示词修改和重新运行。

解释：输入文档先被转成实体和三元组；不同写法的实体随后合并；规范实体与关系进入图结构，并为重要节点生成摘要；查询时只取出问题相关的子图，再生成有事实依据的回答。下方虚线表示这不是一次性流水线：评测发现召回率或精确率有问题后，需要修改抽取提示词，重新运行并观察指标变化。

## 2 背景

### 2.1 经典知识图谱构建流程

传统知识图谱构建通常需要三套能力：

- **命名实体识别（Named Entity Recognition, NER）**，从文本中找出人物、组织、地点等实体；
- **关系分类**，判断两个实体之间是什么关系；
- **实体消歧或实体解析（Entity Resolution）**，判断不同名称是否指向同一个对象。

这些模块经常需要领域标注数据、专门训练和手写规则。换到新领域后，实体类型、关系词和命名方式都会变化，模型与规则也要跟着维护。

传统流水线迁移到新领域时很容易失效：在新闻语料上训练的命名实体识别模型未必能处理法律合同，面向生物医学文献训练的关系分类器也不能直接用于金融文件；按照英文姓名设计的消歧规则，遇到其他文字转写而来的姓名同样容易出错。每次更换领域，通常都要重新准备标注数据、训练模型并进行评测。

这里改用 Claude 提示词完成知识图谱构建，希望让相同的模型、模式和提示词适用于 Claude 能够理解的不同领域。换到新语料后，主要工作变成调整提示词，适配成本也从数周的数据标注和模型训练，缩短为数小时的提示词调试。

### 2.2 Anthropic 的五类 Agent 模式

Anthropic 的工程实践常**把 Agent 系统分成五种可组合模式**：

- **增强型大语言模型（Augmented LLM）**：模型可以调用检索、工具和记忆；
- **提示链（Prompt Chaining）**：前一步输出成为后一步输入，并在步骤之间设置检查；
- **路由（Routing）**：先判断任务类别，再交给合适的模型或专门 Agent；
- **协调器—工作者（Orchestrator–Workers）**：协调器拆分任务，多个工作者并行完成子任务；
- **评估器—优化器（Evaluator–Optimizer）**：一个模型生成内容，另一个模型检查并给出修改反馈。

Anthropic 的内部研究中，在需要从多个独立方向开展工作的任务上，多 Agent 系统相比单 Agent 的表现提高了 **90.2%**，但会消耗 **10～15 倍 token**，因此需要更谨慎地管理上下文。

这些模式解决了任务拆分和协作，**跨 Agent 的持久状态放在哪里**仍要单独处理。如果事实只存在于某个 Agent 的上下文里，Agent 结束后，这部分状态也会消失。知识图谱可以提供一份结构化的共享状态。

### 2.3 知识图谱在 Agent 系统中的三个位置

知识图谱可以在 Agent 系统中承担三种不同角色：

- **共享记忆**：多个工作者直接读写同一张图，协调器不需要保存所有原始结果；
- **事实校验层**：评估器根据图中的关系和来源检查生成内容，而不是只凭语言直觉判断；
- **持久世界状态**：新文档和新事实不断追加到图中，下一轮任务可以从已有状态继续。

知识图谱本身不负责决定应该采取什么行动。它负责保存“当前知道什么、这些事实怎样连接、证据来自哪里”，具体决策仍由 Agent 或人完成。

### 2.4 RAG 和知识图谱各自适合什么问题

RAG 更适合从文本库中找出与问题语义相近的片段。答案位于单篇文档，或者虽然跨文档但不需要连接多层关系时，RAG 简单而有效。

知识图谱更适合下面三类需求：

- **连接事实**：答案需要沿人物、组织、项目、地点等实体连续跳转；
- **共享状态**：多个 Agent 需要访问同一份结构化事实；
- **依据可查**：评估器需要确认某条关系是否存在，以及它来自哪里。

两者不是互相替代。RAG 负责直接检索，知识图谱负责结构推理，大语言模型再综合两边提供的信息。

![](<images/Graph-Engineering-Athropic-Playbook-03.png>)

> 表 6：不同任务对应的基础设施选择。单文档问答可直接使用上下文或 RAG；跨文档单跳问题可使用 RAG 与重排序；跨文档多跳、多人共享状态、事实校验和跨轮次持久记忆更适合知识图谱；简单分类或路由只需要单个 Agent。

解释：知识图谱会带来实体模式、消歧、存储和更新成本，因此不应给所有任务强行加图。判断标准可以压缩成三个词：任务是否需要**连接事实、共享状态、依据校验**。都不需要时，更简单的检索或单 Agent 往往更合适。

## 3 实体与关系抽取

经典流程里，命名实体识别先从文本中标出实体，关系抽取再判断实体对之间的关系。这里把两步合成**每篇文档一次 Claude 调用**：先用 Pydantic 定义好输出形状，再通过结构化输出让模型直接返回可验证的实体列表和关系列表。这样抽取结果不是一段需要再解析的自由文本，而是已经符合模式的 Python 对象。

### 3.1 用结构化模式限定抽取结果

抽取阶段输入一篇文档，输出两组结构化数据：

- **实体**：名称、类型和一句基于当前文档的描述；
- **关系**：来源实体、短谓词和目标实体。

实体类型被限制为人物、组织、地点、事件和物品五类：

```python
EntityType = Literal[
    "PERSON",
    "ORGANIZATION",
    "LOCATION",
    "EVENT",
    "ARTIFACT",
]

class Entity(BaseModel):
    name: str
    type: EntityType
    description: str

class Relation(BaseModel):
    source: str
    predicate: str
    target: str

class ExtractedGraph(BaseModel):
    entities: list[Entity]
    relations: list[Relation]
```

这里的关系采用**主语—谓词—宾语（Subject–Predicate–Object, S–P–O）三元组**。例如：

```text
(Apollo 11) --[commanded by]--> (Neil Armstrong)
```

`source` 是关系起点，`predicate` 是关系类型，`target` 是关系终点。关系有方向，因此“阿姆斯特朗指挥阿波罗 11”和“阿波罗 11 由阿姆斯特朗指挥”在存储时必须统一方向和谓词。

### 3.2 抽取提示词里的四条关键约束

原文提示词可以逐句翻译为：

```text
从下面的文档中抽取一张知识图谱。

<document>
{text}
</document>

指南：
- 只抽取对本文档主题来说核心的实体，跳过偶然提到的实体。
- 为每个实体写一句基于本文档的描述。这些描述稍后会用于消歧名称相似的实体。
- 谓词应该是来自本文的简短动词短语，例如“commanded”“launched from”“part of”。
- 每条关系都必须连接两个你已经抽取出的实体。
```

四条约束分别处理不同问题：

- **只抽核心实体**控制召回率与噪声。改成“抽取所有出现的实体”会提高召回率，也会把大量边缘提及放进图里；
- **补充一句描述**给后面的实体消歧提供上下文，特别适合判断同名实体和非字面别名；
- **使用短谓词**让关系更稳定、更容易查询。“参与了某些相关活动”很难用于推理，“指挥”则明确得多；
- **关系端点必须已抽取**防止悬空边。否则模型可能生成一个关系，却没有生成对应节点。

**抽取提示词实际在决定整张图的精确率和召回率**。大型语料通常更怕错误实体继续产生错误关系，因此偏向“只抽核心实体”；小型语料如果更在意覆盖率，可以放宽这条约束。

### 3.3 结构化输出为什么重要

普通提示词即使要求返回 JSON，也可能出现字段缺失、类型错误、额外说明文字或无法解析的标点。结构化输出通过 `output_format=ExtractedGraph` 把返回格式变成接口约束：

```python
def extract(text: str) -> ExtractedGraph:
    response = client.messages.parse(
        model=EXTRACTION_MODEL,
        max_tokens=2048,
        messages=[{
            "role": "user",
            "content": EXTRACTION_PROMPT.format(text=text),
        }],
        output_format=ExtractedGraph,
    )
    return response.parsed_output
```

返回结果已经是带属性访问的 Python 对象，例如 `result.entities[0].name`，不需要先从自由文本中寻找 JSON，再猜测字段是否存在。

结构化输出带来的变化很实际：**抽取、消歧、组装之间有了可验证的数据边界**。提示词负责语义判断，模式负责结构正确性。

### 3.4 Apollo 语料上的抽取结果

Apollo 语料由六篇 Wikipedia 摘要组成，分别涉及 Apollo 计划、Apollo 11、Neil Armstrong、Saturn V、Buzz Aldrin 和 Kennedy Space Center。Haiku 共抽取出 36 次原始实体和 34 条关系。

![](<images/Graph-Engineering-Athropic-Playbook-04.png>)

> 表 1：六篇 Apollo 文档中的实体与关系抽取数量。Kennedy Space Center 文档抽取出 8 个实体和 10 条关系，是关系最密集的一篇；Neil Armstrong 文档只保留 3 个核心实体和 2 条关系。

解释：不同文档的实体数和关系数并不需要接近。抽取结果取决于文档中的核心事实密度，而不是篇幅。这个阶段仍会把 “Buzz Aldrin” 和 “Edwin Aldrin”、 “Neil Armstrong” 和 “Neil Alden Armstrong” 当成不同名称，下一阶段才负责把它们合并。

## 4 实体消歧

如果直接把抽取结果建图，会让同一人物因为别名而分裂成多个节点。例如 “NASA” 与 “美国国家航空航天局”、“Moon” 与 “the Moon” 可以通过字符串规则处理，但 “Edwin Aldrin” 与 “Buzz Aldrin” 几乎没有字面重合，只看名字写法是否相似，很难判断它们是同一个人。

这里按实体类型分组，再让 **Sonnet 结合名称和描述完成聚类与规范化**。

![](<images/Graph-Engineering-Athropic-Playbook-05.png>)

> 图 2：原始实体先按类型分组，Sonnet 再根据抽取阶段生成的描述进行聚类，并把每组别名映射到一个规范名称。例如 “Edwin Aldrin” 与 “Buzz Aldrin” 合并为 Buzz Aldrin，“Neil Armstrong” 与 “Neil Alden Armstrong” 合并为 Neil Alden Armstrong。

解释：先按类型分组可以避免把同名人物与地点合并，也能缩小模型每次需要比较的候选范围。模型会保留旧名称，并生成“别名到规范名称”的映射，后续关系端点都通过这张映射改写。

### 4.1 消歧结果

Apollo 语料中共有 24 个不同的表面名称，消歧后得到 22 个规范实体。节点数量只减少了两个，但原本分散的关系由此汇聚到了同一个实体上。

例如 Armstrong 的任务关系、登月关系和地点关系如果分别挂在两个名称节点上，多跳查询就会在中间断开。合并后，这些边才会在同一节点相遇。

### 4.2 两类需要重点监控的失败

实体消歧有两种常见失败：

- **遗漏名称**：某个原始名称没有进入任何聚类，也就没有别名映射。建图时它可能直接消失。生产系统应把未匹配名称自动放进单元素聚类；
- **过度合并**：两个相关但不同的实体被合成一个节点，例如把具体任务并入更大的计划。这样会把原本不同的关系错误地集中到一个对象上。

第一种错误造成数据丢失，第二种错误会制造错误关系。两者都不能只靠“消歧后节点变少了”来发现，需要抽样检查别名组和图结构。

### 4.3 描述为实体消歧提供语义证据

实体名称只提供字面信息，抽取阶段的一句话描述则提供语义证据：

- “美国宇航员，参与 Gemini 12 和 Apollo 11”可以帮助识别 Buzz Aldrin；
- “美国宇航员，Apollo 11 指挥官”可以帮助区分 Neil Armstrong；
- 同名实体如果来自不同领域，描述也能阻止错误合并。

这些描述会直接参与别名判断。**有了这层语义信息，实体消歧才能处理没有字面重合的别名**。

### 4.4 消歧提示词的完整约束

原文消歧提示词可以逐句翻译为：

```text
下面是从多篇文档中抽取出的 {entity_type} 实体。其中一些是同一真实世界实体的不同表面形式。

<entities>
{entity_list}
</entities>

对它们进行聚类。每个输入名称必须恰好出现在一个聚类的 aliases 列表中。
确实不同的实体要放进各自的单元素聚类。
使用描述来避免合并那些只是共享同一名称的实体。
canonical 名称应该使用最完整、最不含歧义的形式。
```

输出结构中，每个聚类包含一个 `canonical` 和一组 `aliases`。完整覆盖约束可以防止静默丢失，单元素聚类则让系统在不确定时选择保留，而不是强行合并。

### 4.5 把实体消歧做成可组合 Agent

实体消歧器可以看成一个**无状态的专门 Agent**。它只处理一个很窄的问题：**这一批名称里，哪些其实指向同一个真实对象**。

它的输入和输出都很清楚：

- 输入是一类实体及其描述，例如只输入人物实体，或者只输入组织实体；
- 输出是一组 `canonical` 与 `aliases`，也就是“规范名称”和“属于这个规范名称的所有表面名称”；
- 下游图组装阶段只需要读取这张别名映射，把关系里的 `source` 和 `target` 改写成规范名称。

**无状态**，是因为它每次只依赖当前传入的实体列表和描述，不需要保存长期记忆，也不需要知道整张图的其他部分。

**专门**，是因为它只做实体合并这一件事。这样的边界让它可以被单独评测、单独替换，也更容易并行运行：人物、组织、地点、事件可以分别交给不同调用处理，最后再把各自输出的别名映射合并起来。

放到协调器—工作者模式里，它就可以作为一个独立工作者：抽取工作者先产出原始实体，消歧工作者再把这些实体规范化，图组装阶段最后把所有规范名称和关系写进共享图。

## 5 图组装与实体摘要

消歧完成后，所有关系端点先通过别名映射换成规范名称，再写入 **NetworkX（Python 图分析库）** 的有向多重图 `MultiDiGraph`。

选择**有向多重图（MultiDiGraph）** 有两个原因：

- **关系有方向**，例如“人物指挥任务”和“任务由人物指挥”不能随意互换；
- **同一对实体之间可以同时存在多种关系**，普通简单图会把它们覆盖。

节点保存实体类型、来源文档和出现次数；边保存谓词和来源。Apollo 图最终有 22 个节点、34 条边，全部位于一个弱连通分量中。Apollo program 和 Apollo 11 的度数均为 9，是图中的核心节点。

### 5.1 为重要实体生成跨文档档案

实体摘要会把某个规范实体在不同文档中的提及、相邻关系和来源合在一起，生成跨文档档案。它处理的是同一实体的多处信息，不是重新摘要某一篇文档：

```python
class TimeRange(BaseModel):
    start: str # 年份格式YYYY，未知填写unknown
    end: str # 年份格式YYYY，持续进行填写ongoing
class EntityProfile(BaseModel):
    summary: str # 2至3段事实综述
    key_facts: list[str] # 3至5条可溯源原子事实
    time_range: TimeRange
```

输入包括实体名称与类型、包含该实体的原文片段，以及图中已经知道的关系。输出包括两到三段事实摘要、三到五条可追溯事实和时间范围。

### 5.2 什么时候值得生成摘要

生成摘要需要调用大模型，会比抽取更贵，所以不需要给每个低频节点都做。更合适的触发方式是：

- 节点度数达到阈值，例如 `degree >= 3`；
- 只为度数最高的前若干节点生成摘要。

这样可以把成本留给访问频繁、连接较多的实体。

### 5.3 摘要提示词怎样控制事实质量

原文摘要提示词可以逐句翻译为：

```text
为这个实体生成知识图谱档案。

Entity: {name} ({etype})

Source excerpts mentioning this entity:
{excerpts}

Known relations in the graph:
{relations}

写一段由这些片段综合而成的 2～3 段事实摘要。如果出现矛盾，通过优先采用最具体的说法来解决。包含 3～5 条原子关键事实，每条事实都要可以追溯到来源。时间范围使用 YYYY 或 YYYY-MM 格式。不要编造来源片段没有支持的事实。
```

“优先采用更具体的说法”用于处理信息粒度不同的描述。例如“参与首次登月”和“1969 年在月球行走”并不一定矛盾，后者更具体，适合进入事实档案。“不要补充未支持事实”则限制模型根据常识自行扩写。

### 5.4 实体摘要最终产生了什么

Apollo program 节点汇集六篇文档中的信息，生成的档案覆盖：

- 计划在 1960 年前后的形成；
- 1961 年肯尼迪提出的登月目标；
- 1969 年首次载人登月；
- Saturn V 在 1967～1973 年间的使用；
- Kennedy Space Center 39 号发射台。

没有任何一篇单独文档包含全部信息。实体档案把这些分散事实压缩成可追溯的视图，供下游 Agent 或搜索结果直接使用。

### 5.5 建图后先做三项诊断

执行知识图谱查询前，可以先检查：

- **弱连通分量数量**：弱连通分量就是暂时忽略边的方向后，彼此能连到的一组节点。分量过多说明图被切成很多小岛，可能是实体消歧留下了断裂岛；
- **度分布**：少量高连接节点和大量低连接节点通常符合真实语料；过于平坦可能意味着抽取器没有区分核心与边缘实体；
- **边节点比**：比值低于 1.0 代表知识图谱稀疏，可能存在大量孤立实体；比值高于 2.0 说明连接很密集，可能是信息丰富，也可能是过度抽取，需要结合抽样检查判断。

Apollo 图的边节点比是：

$$
\frac{|E|}{|V|}=\frac{34}{22}\approx 1.55
$$

其中，$|E|$ 是边的数量，$|V|$ 是节点数量。这里的 1.55 位于健康的中间范围。这个指标只能帮助发现异常，不能单独证明图的事实质量。

## 6 多跳查询

知识图谱可以把不同文档中的关系连接成多跳事实链。查询流程先根据问题找到种子实体，取出它周围 $k$ 跳内的子图，把边序列化为三元组，再让 Sonnet 基于这些三元组回答。

![](<images/Graph-Engineering-Athropic-Playbook-06.png>)

> 图 3：问题先触发相关子图选择，子图被序列化为三元组后交给 Sonnet。最终回答引用具体边，例如 Armstrong“在月球行走”、Apollo 11“登陆月球”、Apollo 11“由 Armstrong 指挥”。

解释：模型只会看到与问题相关的小范围关系，无须读取整张图。回答中的每个事实都要能回到某条边，因而可以继续检查边的来源文档。

### 6.1 有图依据与无图依据的回答

不提供图上下文时，模型会依赖预训练知识。对 Apollo 11 这类知名主题，它可能给出内容丰富且大体正确的答案；面对企业私有数据时，模型没有这些背景知识，答案就无法可靠生成。

提供图上下文后，回答会更窄，只覆盖图中确实存在的关系。它可能没有无约束回答那么丰富，但有三个工程优势：

- 每个结论可以追溯到具体关系；
- 不会把模型训练数据中的常识混入私有语料；
- 图中缺少证据时，可以明确说“当前知识图谱没有这项信息”。

### 6.2 子图应该取几跳

$k$ 表示从种子实体向外扩展的跳数：

- $k=1$ 只包含直接邻居，速度快、噪声低，但看不到间接连接；
- $k=2$ 包含邻居的邻居，通常能覆盖常见多跳问题；
- $k\geq3$ 时子图增长很快，容易带来噪声和上下文压力。

在 22 个节点、34 条边的 Apollo 小图中，从核心节点取两跳已经接近整张图。大型图不能直接照搬这个设置，$k$ 继续增大后，**序列化三元组需要先过滤或总结，再交给模型**。

完整查询实现使用**广度优先搜索（Breadth-First Search, BFS）** 收集 $k$ 跳节点：

```python
def serialize_subgraph(graph, center: str, hops: int = 2) -> str:
    visited = {center}
    frontier = {center}

    for _ in range(hops):
        next_frontier = set()
        for node in frontier:
            next_frontier.update(graph.successors(node))
            next_frontier.update(graph.predecessors(node))
        next_frontier -= visited
        visited |= next_frontier
        frontier = next_frontier

    subgraph = graph.subgraph(visited)
    triples = {
        f"({source}) --[{data['predicate']}]--> ({target})"
        for source, target, data in subgraph.edges(data=True)
    }
    return "\n".join(sorted(triples))
```

- `center` 是查询起点；
- `hops` 控制最多扩展几层；
- `visited` 保存已经加入子图的节点；
- `frontier` 保存当前这一层待扩展的节点；
- `successors` 与 `predecessors` 同时读取出边和入边，避免只沿单一方向漏掉关联节点；
- `graph.subgraph(visited)` 构造诱导子图；
- 最后把每条边格式化为可读三元组。

回答函数再把问题和这些三元组一起交给模型。原文查询提示词：

```text
只使用下面的知识图谱回答。引用支持你答案的具体边。

<graph>
{graph_context}
</graph>

Question: {question}
```

这样回答中的事实能回到图里的具体边，而不是只依赖模型自己的判断。

## 7 知识图谱在多 Agent 架构中的位置

不同 Agent 模式会用到知识图谱的不同能力。**表 2 智能体范式与知识图谱嵌入位置对照表**如下：

![](<images/Graph-Engineering-Athropic-Playbook-07.png>)

> 表 2：五类 Agent 模式与知识图谱的结合方式。知识图谱可以作为增强型大语言模型的检索源、提示链之间的检查信号、路由器的分类输入、协调器—工作者系统的共享记忆，以及评估器—优化器系统的事实依据层。

解释：这个表把知识图谱放进五类 Agent 模式里看。增强型大模型把知识图谱当作可调用的检索源，用图遍历处理多跳问题；提示词链式执行把知识图谱当作步骤之间的校验信号，检查新实体是否与已有节点冲突；路由分发把实体类型和节点度数当作分类输入，帮助选择专门 Agent；协调器—工作者系统把知识图谱当作共享记忆，避免所有事实都挤进协调器上下文；评估器—优化器系统则把知识图谱当作事实锚定层，用带来源的边核查生成内容。

### 7.1 协调器—工作者：把图作为共享记忆

多个工作者各自在独立上下文中工作，并直接读取与任务相关的子图。发现新实体和关系后，再把结构化结果写回图中。协调器只负责拆分和协调，不需要把所有工作者的原始报告长期保存在自己的上下文里。

![](<images/Graph-Engineering-Athropic-Playbook-08.png>)

> 图 4：三个工作者围绕同一张知识图谱读取或写入信息，协调器不承担所有事实的中转，因此可以保持较小的上下文。

解释：工作者 A 和工作者 C 只读图中的相关状态，工作者 B 可以同时读取和写入。虚线框中的协调器与事实存储解耦，任务规模扩大时，不会因为每个工作者都回传长文本而线性挤满上下文。

竞争情报示例可以分为四步：

1. 定价、产品、财务和市场工作者并行抽取相同模式的实体与关系；
2. 跨工作者的实体名称统一进入同一个消歧阶段；
3. 所有关系写入同一张带来源信息的共享知识图谱；
4. 战略综合 Agent 通过两跳遍历连接价格、专利和研发投入。

这张共享知识图谱在这里完成了**连接、压缩和依据校验**：别名被连接到同一节点；综合 Agent 只需读取几十条三元组，无须接收五份长报告；每条关系仍能回到来源。

### 7.2 评估器—优化器：把图作为事实校验层

评估器如果没有外部依据，往往只能判断“这段话看起来是否合理”。知识图谱让它检查“这条关系是否存在、方向是否正确、来自哪篇文档”。

例如生成器声称“Armstrong 指挥了 Gemini 12”。图中找不到对应边，却能找到：

```text
(Buzz Aldrin) --[flew on]--> (Gemini 12)
(Neil Alden Armstrong) --[commanded]--> (Apollo 11)
```

评估器因而可以给出具体反馈：Armstrong 没有指挥 Gemini 12；Aldrin 参加了 Gemini 12，Armstrong 指挥的是 Apollo 11。这样的反馈比“事实可能不准确”更容易让生成器修正。

#### 7.2.1 带图依据的评估闭环

带图依据后，评估器—优化器会先把生成内容里的实体拿出来，到图里取相关子图，再用这些边检查每条事实声明。完整流程是：

1. 生成器产生报告或一组事实声明；
2. 图查询阶段找出生成内容里提到的实体，并序列化这些实体附近的 $k$ 跳子图；
3. 评估器同时接收生成内容和图上下文；
4. 每个事实都要与图边核对，没有支持或与图冲突的内容被标记；
5. 生成器根据具体证据改写；
6. 重复直到通过，或把图中完全缺失的关键事实交给人处理。

关键点在最后一步：**图中没有关系，不代表现实中一定不存在**。知识图谱可能只是还没收录这条事实，所以“无证据”更适合进入补充检索或人工确认，而不是直接判定生成器错了。

### 7.3 长期任务状态：让循环第二天接着运行

跨轮次循环需要一份能在上下文清空后继续使用的任务状态。这里的状态不是某个 Agent 的聊天记录，而是一张持续追加的知识图谱：当天新来的文档会被抽取、消歧、写入图里，第二天任务再启动时，Agent 可以继续查询这张图。

新增文档到达后，流程是：

- 先抽取其中实体和关系；
- 新实体与现有规范实体集合做消歧，而不是只和本批新实体比较；
- 新边增量写入图；
- 只有来源集合明显变化的实体才重新生成摘要；
- 状态文件记录哪些文档已经处理过、哪些实体需要重新生成摘要。

这样，图就变成了长期保存的事实状态。Agent 的一次会话会结束，但图中的事实、关系和来源不会跟着消失，下一轮任务还能接着用。

### 7.4 协作式与分层式多 Agent 系统

前面讲的是知识图谱如何嵌入五类经典 Agent 模式，这里继续看它在两种更大的多 Agent 组织方式里怎么用。

在点对点协作系统中，没有一个固定的中心协调器，多个 Agent 直接互相协作。知识图谱可以作为**共享黑板**：所有 Agent 都能读写同一份公共事实，避免信息只停留在某个 Agent 的上下文里。

在分层系统中，上层监督 Agent 会把任务交给不同领域的专门 Agent。这里可以按领域拆成多个子图：

- 各专门 Agent 只维护自己的领域子图；
- 上层**监督 Agent（Supervisor）** 跨子图查询；
- 统一的实体类型、关系谓词和来源字段保证不同子图可以连接。

这种设计减少每个专门 Agent 的搜索范围，同时保留跨领域发现关系的能力。关键是所有子图仍要使用统一的实体类型、关系谓词和来源字段，否则上层监督 Agent 很难把不同子图里的事实重新连起来。

## 8 评估

评估先关注实体抽取，再观察实体消歧后召回是否变化。为了读懂表中的指标，可以使用常见定义：

这里的**金标准（gold standard）** 指人工标注出的标准答案。评估时会把模型抽取出的实体和这份人工答案对照，判断哪些是抽对的、哪些是多抽的、哪些是漏掉的。

$$
\text{Precision}=\frac{TP}{TP+FP}
$$

$$
\text{Recall}=\frac{TP}{TP+FN}
$$

$$
F_1=\frac{2\cdot \text{Precision}\cdot \text{Recall}}
{\text{Precision}+\text{Recall}}
$$

- $TP$ 是正确抽取的实体；
- $FP$ 是抽取出来但不在金标准中的实体；
- $FN$ 是金标准中存在、抽取器却漏掉的实体；
- 精确率关注“抽出的有多少是对的”；
- 召回率关注“应该抽出的有多少被找到”；
- $F_1$ 综合两者。

![](<images/Graph-Engineering-Athropic-Playbook-09.png>)

> 表 3：Apollo 11 与 Neil Armstrong 文档的实体抽取质量。两篇文档的精确率都是 1.00，但召回率分别为 0.55 和 0.38；消歧后的召回率没有继续提高。

解释：抽取器没有制造错误实体，但漏掉了一部分金标准实体。这与“只抽取核心实体”的提示词一致。消歧后召回率没有提高，通常要检查别名映射是否覆盖了消歧器输出的规范名称；如果规范名称不被评分器识别，就可能出现评分伪差。

### 8.1 评估怎样反过来修改提示词

评估校验工具的完整流程为：

```text
修改提示词
→ 重新抽取与消歧
→ 对照人工标注金标准
→ 查看精确率、召回率和 F1
→ 决定是否继续调整
```

这些指标的作用很直接：每次改完提示词后，可以看抽取结果到底是**少漏了一些实体**，还是**多引入了一些错误实体**。也就是说，评估不是只给一个分数，而是帮助判断下一轮提示词应该继续放宽，还是重新收紧。

### 8.2 这些数字在实际系统中意味着什么

错误节点通常比缺失节点更危险。漏掉一个边缘实体，会让部分查询答不出来；错误实体却可能生成多条错误边，在图遍历、实体摘要和评估器校验中持续传播。

Apollo 示例偏向精确率，但召回率仍然需要监控。评估闭环可以帮助团队主动决定取舍：放宽“只抽核心实体”的约束会提高召回率，也会带来更多边缘实体和噪声。

### 8.3 关系评测还需要更细的标准

简单关系评分可以只比较 `(source, target)`，暂时忽略谓词写法。这样能判断两个实体是否被连接，却无法区分“指挥”“参加”“发射自”等关系语义。

更严格的评测应加入**谓词等价类**，把语义相同的表达归为一类，同时保留不同关系之间的区别。否则，一个只连对端点、却写错谓词的关系也可能被算作正确。

## 9 扩展到生产规模

### 9.1 抽取成本

抽取是按文档独立执行的，成本随文档数量近似线性增长，最适合并行和批处理。高吞吐量阶段使用 Haiku，并结合提示缓存和**批处理接口（Batch API）**，可以显著降低成本。批处理接口适合能接受最长 24 小时延迟的任务，价格可以降低 50%。按当时的 Haiku 价格估算，1 万篇、每篇约 2000 token 的文档可以控制在个位数美元量级；实际预算仍应以部署时的模型价格和输入长度重新计算。

### 9.2 大规模实体消歧

不能把上万个实体一次性塞给模型。更可行的流程是：

1. 先按实体类型分组；
2. 再用姓氏、token 重合或向量相似度做廉价候选分块；
3. 每块控制在约 50～100 个候选；
4. 只让 Sonnet 处理块内真正困难的判断。

廉价的字符串方法适合处理拼写错误、大小写变化这类容易判断的情况；昵称、缩写、跨语言名称，以及 Buzz Aldrin 与 Edwin Aldrin 这种非字面别名，更适合交给模型判断。两者结合通常比完全依赖任意一方更稳。

### 9.3 增量更新

新增文档不应触发整张图重建。正确的增量流程是：

- 新文档单独抽取；
- 新实体与已有规范实体集合做消歧；
- 只增加新的节点和边；
- 只有节点来源发生实质变化时才重新生成摘要。

这样，每次更新成本由新增数据量决定，而不是由全部历史语料决定。

### 9.4 存储选择

`NetworkX` 适合原型和中等规模图，几十万条边以内仍可用于本地实验。规模继续扩大后，可以迁移到：

- Neo4j 或 Amazon Neptune 等图数据库；
- PostgreSQL 中的 `entities`、`relations` 和 `aliases` 表；
- 使用递归公共表表达式完成多跳查询。

从 `NetworkX` 切换到属性图数据库或 PostgreSQL 时，抽取和实体消歧代码不需要改变，只替换持久化层。选择图数据库还是关系数据库，主要是基础设施选择，不会改变前面的知识图谱流水线。

### 9.5 长文档怎样分块

固定长度切块可能把一个事实拆开。更合适的是按章节或段落语义分块，并保留一段左右的重叠。重叠区域会产生重复实体和关系，因此需要：

- 先对完全相同的字符串做去重；
- 再进行全局实体消歧；
- 为关系保留来源文档与片段位置。

### 9.6 生产环境要监控什么

至少需要持续观察四项指标：

- **抽取率**：记录每篇文档抽取出的实体和关系数量。突然下降可能表示语料转向了提示词处理不好的领域，突然上升可能表示抽取器开始过度保留边缘实体；
- **消歧压缩比**：用原始表面名称数量除以规范实体数量。接近 1.0 表示语料命名较一致，消歧作用较小；高于 2.0 表示别名变化明显，消歧正在发挥较大作用；
- **图连通性**：同时观察连通分量数量和最大分量大小。断裂分量持续增加，可能表示实体消歧漏掉了跨文档连接；
- **查询延迟**：记录从问题进入到生成有图依据回答的时间。实时应用可以预计算高频种子实体的子图；批处理应用对延迟不太敏感，但更需要关注单次查询成本。

### 9.7 不同阶段怎样选择模型

![](<images/Graph-Engineering-Athropic-Playbook-10.png>)

> 表 4：抽取阶段使用 Haiku，实体消歧、实体摘要和多跳查询使用 Sonnet。抽取更看重吞吐量、结构约束和成本，后三个阶段更依赖复杂判断与跨信息综合。

解释：各阶段不必统一使用最强模型。结构稳定、可并行的抽取适合便宜模型；别名判断、冲突处理和多跳推理中的一次错误会影响更多下游结果，更强的模型应留给这些位置。

## 10 相关工作

### 10.1 与经典 NLP 流水线的关系

这套方法用提示词和结构化模式代替了专门训练的 NER 与关系分类器。领域适配由重新训练模型变成调整提示词，换来了更强的通用性和更低的部署成本，但也放弃了专门模型通过领域标注数据学习细粒度实体类型的部分控制能力。

已有大语言模型实体消歧工作呈现出相似结论：

- 昵称、缩写和跨语言名称等困难情况适合交给大语言模型；
- 拼写错误、大小写变化等简单情况，只看名字写法是否接近就能处理，比调用大语言模型更快、更便宜；
- 更实用的方案是先用廉价确定性信号形成候选块，再让大语言模型处理块内的困难判断。

知识图谱和多 Agent 的结合仍相对少见。较接近的路线包括多智能体强化学习中的共享知识库，以及多个 Agent 通过公共知识仓库通信的**黑板架构（Blackboard Architecture）**。这里进一步把由提示词构建、带来源信息的知识图谱作为黑板的具体实现。

### 10.2 与 RAG 的关系

RAG 强在通过语义相似度找到相关文本片段，适合答案位于单个片段中的直接检索。知识图谱把跨文档共享实体显式表示为节点，适合沿关系连接语义上并不相似的材料。两者是互补关系：**RAG 负责直接检索，知识图谱负责结构推理，大语言模型负责综合两边提供的信息**。

### 10.3 与闭环优化的关系

抽取提示词、实体模式和评分器构成一个闭环工程系统。每次修改提示词后，都可以重新运行金标准评测，观察精确率和召回率怎样变化。提示词是否变好，有了可以直接比较的结果。

### 10.4 结构化输出是整套系统的基础

如果各阶段只能交换自由文本，流水线需要大量解析与容错代码。结构化输出把 LLM 的语义能力放进稳定接口，使抽取器、消歧器、图组装器和查询器可以像普通软件模块一样组合。

## 11 讨论

### 11.1 它替代了什么

这套方案可以替代：

- 为每个领域单独训练的命名实体识别器；
- 专门训练的关系分类器；
- 一部分手写实体消歧规则；
- 围绕不稳定 JSON 输出编写的大量解析代码。

这里说的“训练”不是微调模型参数，而是把过去需要领域标注数据和专门模型学到的输出边界，改由 Pydantic 模式和提示词来约束。Pydantic 模式规定输出结构，提示词规定语义边界。

### 11.2 它没有替代什么

知识图谱负责存储事实，决策仍由 Agent 或人完成。它不会自动知道：

- 哪些来源可信；
- 哪些事实应该影响业务决策；
- 图中缺失的信息是“不存在”还是“尚未收集”；
- 两条冲突信息应采用哪一条。

这些仍需要 Agent 的任务逻辑、外部规则或人工判断。

### 11.3 局限

- **抽取依赖提示词**：改变“核心实体”的定义就会改变整张图；
- **候选分块依赖领域**：姓名、公司和产品适合不同的阻塞规则；
- **语料决定图的边界**：来源有偏差或不完整，图也会继承。对有偏差的图进行多跳推理，不能产生没有偏差的答案。

### 11.4 成本分布

- 抽取调用多，但单次简单，可以用便宜模型；
- 实体消歧调用次数较少，但判断复杂；
- 总结最容易产生长输出，应只对重要节点触发；
- 查询成本随问题数量和子图大小增长。

对于大型语料库，主要成本通常来自逐篇运行的抽取；图被频繁查询后，成本会转向每个问题都要执行的子图查询与生成。Haiku 和 Sonnet 分别处理这两类负载，方便单独优化成本。

### 11.5 生产系统需要哪些纪律

![](<images/Graph-Engineering-Athropic-Playbook-11.png>)

> 表 7：生产就绪检查表包含金标准、别名映射、模式版本、单次抽取上限、消歧兜底、来源追踪、增量更新、连通性监控、总结触发条件和人工抽样十项。每一项缺失都会对应一种可识别的失败。

解释：前两项让提示词修改有反馈；模式版本、抽取上限和消歧兜底防止静默数据损坏；来源、增量更新和连通性监控保持图结构可靠；总结触发与人工抽样控制成本并防止系统规模超过团队理解范围。

落地时可以执行三条简单规则：

- 每天随机抽查若干节点，回到来源文档确认关系；
- 每轮设置最大文档数和实体抽取上限，防止异常批次无限消耗；
- 图结构与抽取模式一起版本化，避免不同提示词版本生成不兼容节点。

### 11.6 后续方向

后续可以继续补足：

- **时间图**：为边增加时间戳，记录事实在什么时间成立，使系统能够回答“2024 年第三季度谁担任这个职位”之类的问题；
- **置信度评分**：为每条边增加抽取置信度，信号可以来自模型自身的不确定性，也可以来自多篇独立文档的交叉支持；
- **图中之图**：不同团队维护各自的领域图，再用一张元图记录领域图之间的连接，在不强行合并不同模式的情况下支持跨团队推理。

## 12 结论

这套方案把知识图谱放进了多 Agent 工程的具体位置：**抽取器把非结构化文档转成事实，消歧器连接跨文档关系，图结构保存共享状态，查询器提供可追溯推理，评测闭环持续约束提示词质量**。

原型只需要 Claude API、Pydantic 和普通图工具，不必先训练专门 NLP 模型，也不必立刻部署图数据库。但进入生产后，金标准、来源追踪、增量更新、连通性监控和人工抽查缺一不可。知识图谱解决的是上下文窗口无法长期保存和连接事实的问题；它能让多 Agent 系统记得更久、共享得更稳，也让事实错误更容易被找到。
