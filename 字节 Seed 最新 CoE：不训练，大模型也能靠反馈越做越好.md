> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Chain-of-Experience for Continual LLM Improvement》的理解。

![](<images/L4-1.-Chain-of-Experience-01.png>)

> 加州大学圣克鲁兹分校与字节跳动 Seed 团队在 最近刚发出的**经验链（Chain-of-Experience, CoE）**：让大语言模型围绕同一个问题反复作答，把每一轮答案和随后的反馈一起保留下来，作为下一轮继续改进的经验。**CoE 不需要训练，也不更新模型参数，却能把原本彼此孤立的多次推理接成一个连续闭环**。数学、代码和知识任务上的结果显示，加入反馈后，正确率更高，整体 API 成本也更低。不过要先划清边界：**这里的“学习”发生在当前上下文中，经验也没有永久写进模型参数**。

## 核心问题

这篇论文要回答的问题是：**大模型完成一次回答后，已经产生了答案、错误、执行结果和评价，为什么下一次尝试还要像第一次一样重新开始？这些交互痕迹能不能直接变成推理时的学习材料**？

并行采样、多次采样取优（Best-of-N）和多数投票会一次生成多个候选，再由验证器选出一个答案。它们利用了更多推理计算，但候选之间通常不会互相学习；一轮结束后，中间经验也随之丢失。Self-Refine（自我改写）、Reflexion（语言反思）和 Self-Debug（自我调试）已经开始让模型根据批评或执行结果反复修正，不过已有研究对不同反馈形式、迭代轮数、成本和模型能力之间的关系缺少统一比较。

CoE 把第 $i$ 轮的**回答 $a_i$** 和**反馈 $f_i$** 组合成一次经验 $e_i=(a_i,f_i)$。下一轮会同时读取原问题和此前积累的经验。推理过程由一次性生成变成了连续循环：**回答—反馈—带着历史重试**。

## 摘要

传统大模型评测把每次推理当成独立事件，忽略模型能否在测试过程中根据交互经验继续改进。经验链把测试时推理改成连续迭代：模型生成答案，环境返回反馈，答案与反馈共同进入后续上下文。

反馈按信息的明确程度分为四种：**无反馈、执行反馈、模型反馈和正确性反馈**。实验涉及数学、代码和知识任务，并使用 GPT-5、o3、Gemini-2.5 Pro、Claude 4.5 Sonnet 等推理模型。相比无反馈迭代，带反馈的 CoE 平均提高 **5.6%**，同时降低 **19%** 的 API 成本；模型自反馈已经能带来明显提升，结合模型反馈与正确性或执行反馈还可以进一步改善部分任务。

后续分析还发现：

- **基础能力更强的模型通常更会利用反馈**，六个任务上的平均皮尔逊相关系数（Pearson correlation coefficient）约为 $0.50$；
- **大部分收益出现在前 20 轮**，继续扩展到 50 轮后提升明显放缓；
- **错误或虚假反馈会伤害性能，但强模型仍有一定恢复能力**；
- **把同一任务的完整经验轨迹压缩成短记忆，反而可能丢掉关键中间推理**；
- 在需要模型训练数据之外知识的任务中，仅靠自反馈无法补充缺失事实，结果甚至可能变差。

# 1 引言

## 1.1 部署后的模型为什么很少从当前经历中学习

人会把每次成功和失败带到下一次决策里。大模型的常规部署方式却不同：训练结束后参数固定，每个问题被当成一个新的独立输入。即使前一次回答已经暴露了错误、收到测试失败信息或得到了环境反馈，这些信息也往往只服务于当前一次输出。

测试时增加计算已经形成两条常见路线：

- **并行搜索与筛选**：生成多条候选答案，再用多数投票、过程验证器或结果验证器选择更好的候选。模型看到了更大的候选空间，但候选之间没有连续积累经验；
- **串行自我修正**：让模型批评并改写自己的答案，或者根据代码执行结果继续调试。它建立了反馈循环，但不同工作使用的反馈、任务和迭代设置并不统一，很难判断模型到底从什么信号中获得提升。

CoE 把关注点放在**前一次尝试留下的内容，能否直接改变后一次尝试**。每轮输出和反馈都保留在同一条经验链中，模型持续读取这条轨迹并重新作答。

图 1 先把主要结果压缩成三个问题：反馈是否提高正确率、是否改善成本，以及更强模型是否更会从反馈中学习。

![](<images/L4-1.-Chain-of-Experience-02.png>)

> 图 1：四款大模型在数学、代码和知识领域四个基准上的汇总结果。上半部分比较带反馈 CoE 与其他测试时增强方法的平均性能；左下比较准确率和 API 成本；右下比较模型基础能力与 CoE 改进能力，两者呈中等到较强的皮尔逊正相关。基线的具体设置见 4.1 节。


## 1.2 主要发现

实验围绕四种反馈方式和多轮经验展开：

- **反馈比单纯重复尝试更重要**：没有反馈时，即使让模型多轮生成，提升也不稳定；自反馈的平均成绩达到 71.0%，比三种跨任务经验基线高出 7～9%，这些基线的总体平均水平约为 62.9%；
- **详细反馈可以同时提高效果和效率**：带反馈 CoE 整体提升 5.6%，API 成本降低 19%。单纯增加 token 无法解释这组结果；
- **不同反馈可以互补**：模型反馈能指出原因和改进方向，正确性或执行反馈能提供更可靠的结果信号，两者结合在部分任务上优于单独使用；
- **完整经验轨迹有保留价值**：在同一任务内加入 DC 或简单记忆（SimpleMem）做压缩和筛选，没有超过直接保留完整经验链；
- **模型能力与学习能力相关**：越强的模型通常越能理解反馈并据此修正，但这种关系在不同任务上的强度不一样。

# 2 相关研究

## 2.1 无需训练的测试时策略

无需重新训练模型的推理增强方法，主要有两条路线：**展开推理过程**、**增加候选搜索**。

- **思维链及其变体**：思维链（Chain-of-Thought, CoT）让模型显式写出中间步骤，随后出现对比思维链、由易到难提示、解释链、笔记链和知识链等任务化变体；
- **树形搜索**：思维树（Tree-of-Thought, ToT）同时探索多条推理路径，并用搜索和评估选择更有希望的分支；
- **验证器筛选**：过程级验证器检查中间步骤，结果级验证器判断最终答案，再从多条并行生成中选出较好的结果。

这些方法很适合“多生成、再筛选”，但一次搜索结束后，候选中出现的错误、修正和评价通常不会继续进入下一轮推理。**CoE 同样不训练模型，区别在于它把反馈放进连续迭代，让后一次回答显式依赖此前全部经验**。

## 2.2 从经验中学习

经验学习可以发生在训练阶段，也可以发生在推理阶段。

**在本文讨论的训练阶段路线中**，强化学习把在线生成、环境奖励和策略更新连接起来。经验通过训练影响模型参数，因此能跨会话保留，但训练成本和系统复杂度都更高。

**推理阶段的跨任务经验**，会把已经解决的问题整理成外部记忆，再用于后续新问题：

- **动态备忘单（DC）** 把过往答案提炼成可复用策略或代码片段；
- **Agent 上下文工程（Agentic Context Engineering, ACE）** 通过“生成—反思—整理”持续更新策略手册；
- Agent KB、ExpeL 等方法也会保存并检索跨任务经验；
- 一些多 Agent 框架还允许不同 Agent 共享集体经验，让一个 Agent 的经历可以被其他 Agent 使用。

**推理阶段的同任务经验。** 这类方法围绕当前问题持续修正：

- Self-Refine 让模型生成反馈并改写当前答案；
- Reflexion 把失败反思写入后续尝试的上下文；
- Self-Debug 根据执行结果修正代码；
- S*、思维迭代（Iteration-of-Thought）和 ReVeal 也会通过多轮生成、验证或内部对话推进同一任务。

近期方法还会利用离线积累的模型经验改进 Agent，并开始把这种基于反馈的迭代方式扩展到真实任务。

CoE 更接近第三类。它没有设计新的复杂记忆结构，重点是建立统一实验框架，系统比较**完整解题历史在不同反馈强度下如何影响测试时改进**。

Self-Refine、Reflexion 和 CoE 的区别，很多人会觉得没区别，其实有，只是不大，可以这样区分：

| 方法              | 保存什么                               | 影响范围                           |
| --------------- | ---------------------------------- | ------------------------------ |
| **Self-Refine** | 当前任务中历轮输出和模型生成的反馈                  | 用这些历史信息继续改写同一个答案，但通常不形成独立记忆    |
| **Reflexion**   | 从失败轨迹中提炼出的语言反思                     | 通常指导同一任务的后续尝试；外部记忆被持久化后可以跨会话保留 |
| **CoE**         | 明确把每轮完整回答和对应反馈记为经验 $e_i=(a_i,f_i)$ | 后续轮次读取完整经验轨迹，继续解决同一个任务         |

可以这样理解：

- **Self-Refine：围绕当前答案连续修改**，前面各轮的输出和反馈都可以进入后续改写，但任务结束后通常不保存；
- **Reflexion：把失败过程压缩成反思记忆**，通常用于同一任务的后续尝试；是否影响其他任务，取决于系统有没有长期保存并重新检索这些反思；
- **CoE：统一组织完整经验轨迹**，把每轮回答和反馈明确组成经验，让后续轮次继续读取和利用。

三个方法都可以不更新模型参数。Self-Refine 重点研究如何反复改写当前答案，Reflexion 把失败提炼成反思记忆，CoE 则统一记录完整的回答—反馈轨迹，并比较不同反馈怎样影响后续推理。Self-Refine 和 CoE 默认只影响当前任务；Reflexion 可以通过外部记忆跨会话保存反思，但原始方法主要用它重新尝试同一个任务。只有系统在新任务中重新检索这些反思，它才可能影响其他任务，**这不是 Reflexion 默认保证的能力**。

# 3 通过 CoE 改进模型

先说明 **CoE 怎样把一次问答变成多轮循环**：模型每次生成回答后接收反馈，再把此前的回答和反馈一起交给下一轮。随后分别介绍**无反馈、执行反馈、模型反馈和正确性反馈**。

CoE 是保存并使用“回答—反馈”历史的循环框架，3.2 中的四种反馈则是 $f_i$ 的四种可选实现。它们不是四个连续步骤，也不要求全部一起使用：代码任务可以使用执行反馈，没有验证器时可以使用模型反馈，有标准答案时可以使用正确性反馈；无反馈主要作为实验对照。

## 3.1 总览：答案和反馈共同组成经验

普通问答中，给定问题 $Q$，模型从条件分布中生成答案 $A$：

$$
A\sim P(A\mid Q).
$$

- $Q$ 是输入问题；
- $A$ 是模型生成的完整回答；
- $P(A\mid Q)$ 表示在问题 $Q$ 条件下生成回答 $A$ 的概率分布。

这个过程只依赖问题本身。为了把环境反应纳入推理，再引入反馈变量 $F$。模型提交答案后，具体反馈 $f$ 由环境分布 $P'$ 产生：

$$
f\sim P'(F\mid Q,A).
$$

- $F$ 是反馈变量，表示环境可能给出的各种反馈，例如程序报错、测试通过率、文字批评或正确与错误标签；
- $f$ 是反馈变量 $F$ 在当前一轮中的具体结果，也就是模型提交答案后实际收到的那一条反馈；
- $P'(F\mid Q,A)$ 是环境的反馈生成机制，描述环境在看到问题 $Q$ 和回答 $A$ 后，各种反馈出现的可能性。代码执行器通常会根据运行结果返回较为确定的报错或测试结果；语言模型评价则可能对同一个答案生成不同的批评；
- 环境可以是代码执行环境、Agent 的内部世界模型或真实交互环境；具体反馈也可以由另一个语言模型生成。

CoE 把单轮问答扩展成连续过程。第 $t$ 轮动作写成：

$$
a_t\sim P(a_t\mid Q,e_0,e_1,\ldots,e_{t-1}).
\tag{1}
$$

其中：

- $t$ 是当前迭代轮次；
- $a_t$ 是第 $t$ 轮生成的回答或动作；
- $e_i=(a_i,f_i)$ 是第 $i$ 轮经验，由当轮回答 $a_i$ 和反馈 $f_i$ 组成；
- $e_0,e_1,\ldots,e_{t-1}$ 是此前积累的完整经验轨迹；
- $P(a_t\mid\cdot)$ 表示模型同时根据原问题和历史经验生成下一次回答。

直观来看，第一次回答只看到问题；第二次回答看到“问题 + 第一次回答 + 第一次反馈”；第三次再加入第二轮回答和反馈。**这里没有梯度更新，所谓改进来自上下文中不断增长的经验轨迹**。

图 2 把这条循环放在同一条信息流里。

![](<images/L4-1.-Chain-of-Experience-03.png>)

> 图 2：大语言模型的迭代改进循环。模型 $\mathcal{M}$ 反复与环境 $\mathcal{E}$ 交互；每一轮根据输入和已经积累的经验生成动作，环境再返回反馈，动作和反馈共同形成新经验。实验比较无反馈、执行反馈、模型反馈和正确性反馈四种设置。

解释：输入先进入模型，模型生成动作；环境接收动作并返回反馈，二者被作为经验。下一轮中，模型读取的不再只有原始输入，还包含此前所有动作与反馈。模型负责利用经验改变后续回答，环境负责提供反馈信号，这两部分反复连接成闭环。

## 3.2 反馈谱系：从没有评价到直接告诉对错

四种反馈的差异主要在于**环境给出的评价信号有多明确**。统一写法是：

$$
a_t\sim P\bigl(a_t\mid Q,(a_0,f_0),\ldots,(a_{t-1},f_{t-1})\bigr).
$$

$f_i$ 决定第 $i$ 轮经验能给后续推理提供多强的指引。其余变量与公式（1）相同。

### 3.2.1 无反馈：只能从自己过去的答案中反思

环境不提供评价：

$$
f_i=\varnothing,
$$

因此经验退化为 $e_i=(a_i,\varnothing)$，下一轮只读取过去的回答：

$$
a_t\sim P(a_t\mid Q,a_0,a_1,\ldots,a_{t-1}).
$$

$\varnothing$ 表示“没有反馈”。这里并非完全不参考历史：模型仍然会看到原问题以及此前生成的所有答案 $a_0,a_1,\ldots,a_{t-1}$，只是看不到环境对这些答案的评价 $f_i$。它可以比较和重新检查过去的回答，却不知道上一轮究竟是对是错，也没有错误位置或执行日志可参考。这个设置用于区分：**提升究竟来自反复查看过去答案，还是来自环境反馈**。

### 3.2.2 执行反馈：让代码和环境直接暴露问题

代码任务可以把回答放进可执行环境 $\mathcal{E}$：

$$
f_i\sim P'(F\mid Q,a_i)=\mathcal{E}(Q,a_i).
$$

- $\mathcal{E}(Q,a_i)$ 表示环境执行当前答案；
- $f_i$ 可以包含执行轨迹、报错信息、运行日志和测试用例结果；
- $P'$ 表示执行环境根据问题和答案生成反馈的条件分布；如果执行过程是确定的，相同输入通常会得到相同反馈。

执行反馈能指出**哪里失败以及怎样失败**。包含执行轨迹、报错或测试结果的反馈，通常比单纯的二元信号提供更多信息。实验还设置了“二元执行反馈”，用来观察简化后的执行结果是否仍然有效。

### 3.2.3 模型反馈：由语言模型给出批评和建议

没有外部执行器或标准答案时，可以让**辅助模型 $\mathcal{M}_{\mathrm{fb}}$ 评价当前回答**：

$$
f_i=\mathcal{M}_{\mathrm{fb}}(Q,a_i).
$$

- $\mathcal{M}_{\mathrm{fb}}$ 是反馈模型，可以与答题模型相同，也可以是另一款模型；
- 输入是问题 $Q$ 和当前回答 $a_i$；
- 输出可以是文字批评、偏好分数或结构化评价。

**模型反馈**是上位概念，只要评价由语言模型生成，都属于模型反馈。反馈模型与答题模型相同时，称为**自反馈**；使用另一款模型评价时，称为**外部模型反馈**。两者都不依赖标准答案或领域验证器，区别在于评价是否由答题模型自己完成。

模型反馈适用范围广，但可靠性取决于反馈模型本身。如果它无法识别当前答案中的错误，就可能返回不准确的评价，使后续修改偏离正确方向。后面的反馈强度实验也显示，**模型评价是否有效，很大程度取决于反馈模型在当前任务上的基础能力**。

### 3.2.4 正确性反馈：用二元信号明确告诉模型对错

如果**存在领域验证器**，可以直接返回当前答案是否正确：

$$
f_i=\mathbf{1}\{a_i\text{ is correct}\}\in\{0,1\}.
$$

- $\mathbf{1}\{\cdot\}$ 是指示函数，条件成立返回 $1$，否则返回 $0$；
- $a_i$ 正确时 $f_i=1$，错误时 $f_i=0$；
- 这个反馈能明确区分成功和失败，却不一定告诉模型错在哪里。

现实任务往往没有便宜、可靠的标准答案，所以它更像一个高信号参考设置，用来估计迭代修正收益的近似上界。这并不意味着真实环境可以随时提供正确性标签。

# 4 实验

## 4.1 实验设置

### 任务与数据集

实验覆盖三类任务，每类选择两个基准：

- **数学**：AIME 2025 包含 30 道竞赛题；OmniMath 从 4,428 道奥数题中抽取 200 道，覆盖不同难度；
- **代码**：LiveCodeBench V6 使用 175 个较新的代码生成样本；LiveBench 的代码子集包含 128 个样本；
- **知识与经验学习**：EvaLearn 包含 648 个样本，用于衡量模型在顺序解题中的学习能力；GPQA Diamond 包含 198 道由博士级专家编写的生物、化学和物理选择题。

AIME 2025 和 GPQA Diamond 使用精确匹配，OmniMath 使用大模型裁判，两个代码任务通过 Python 执行环境检查。每组实验运行三次，报告均值和标准差。

### 模型

实验设置列出的模型包括 GPT-5、GPT-5-mini、o4-mini、o3、o3-mini、Gemini-2.5 Pro 和 Claude 4.5 Sonnet。**PDF v1 的摘要和正文写的是 8 款模型，模型列表却只列出 7 款**。这里按列表记录，不自行补齐缺失模型。

### 基线

基线从“增加当前题推理量”和“利用过去题目的经验”两条路线展开：

- **内置推理强度**：调整 OpenAI 模型的低/高推理强度；Claude 4.5 Sonnet 则分别关闭思考模式和设置 10,000 token 思考预算；
- **少样本上下文学习（In-Context Learning, ICL）**：检索过去最相似的题目与答案，作为示例放进新问题的上下文；
- **动态备忘单（DC）**：从过去解答中总结高层策略或代码片段，形成持续更新的外部备忘单；
- **Agent 上下文工程（ACE）**：通过生成、反思和整理，把提示词逐步更新为可复用策略手册。

ICL、DC 和 ACE 都按语义相似度检索过去解过的问题，并测试 $k\in\{1,5,8,12,15,20\}$ 个相关样本。它们利用的是**其他已完成任务的经验**；CoE 主要利用的是**当前同一问题前几轮产生的答案与反馈**，两者的经验来源并不相同。

## 4.2 随经验扩展

### 4.2.1 性能：带反馈迭代稳定优于无反馈和跨任务记忆

图 3 汇总了五款模型在六个基准上随迭代轮次变化的结果。

![](<images/L4-1.-Chain-of-Experience-04.png>)

> 图 3：五款大模型在六个基准上使用不同生成方式的结果。带正确性/执行反馈、自反馈和二元执行反馈的 CoE 通常优于无反馈、动态备忘单、少样本上下文学习、Agent 上下文工程以及不同推理强度基线。每个结果运行三次，浅色区域表示标准差；箭头标出不同内置推理强度下的模型表现。

解释：每列对应一款模型，每行对应一个任务，横轴是经验迭代轮数。深蓝色正确性或执行反馈通常最高，自反馈次之；灰色无反馈和棕色跨任务经验基线大多较低。很多曲线在前几轮快速上升，之后趋于平缓，说明有效反馈很快被纳入后续推理，继续增加轮数的收益逐渐变小。

主要数字如下：

- 带自反馈的 CoE 相比无反馈平均提高 **5.6%**，正确性或执行反馈平均提高 **11.1%**；
- 两个代码任务中，详细执行反馈平均带来 **8.6%** 提升，自反馈也能带来约 **7.0%**；
- ICL、ACE 和 DC 在六个基准上的平均成绩分别约为 62.1%、64.0% 和 62.7%，低于无反馈迭代的 66.8%；
- 模型自反馈达到 71.0%，最佳反馈达到 79.3%。

在这组推理模型和测试集上，**反馈驱动的连续修正，比仅增加推理强度或检索其他题目的经验更稳定**。这项比较只覆盖当前实验，不能据此判断 DC、ACE 在其他场景中的表现；这里能确认的是，跨任务压缩经验没有稳定超过同题反馈闭环。

### 4.2.2 效率：反馈兼顾了性能与 API 成本

只看正确率还不够，多轮迭代可能用更多 API 调用和 token。图 4 把最佳准确率与完成任务的总 API 成本放在一起。

![](<images/L4-1.-Chain-of-Experience-05.png>)

> 图 4：各模型完成任务的总成本与 20 轮内最佳性能。越靠左上代表成本越低、准确率越高。带反馈的大模型通常用更低成本取得更高分数；没有反馈的迭代大多落在右下区域。图中展示三个基准，六个基准的完整结果位于补充实验。

解释：星形表示正确性或执行反馈，圆点表示自反馈，十字表示二元执行反馈，方块表示无反馈。除了 Gemini 2.5 Pro 的部分结果，带反馈点通常比无反馈方块更靠左上，说明带反馈的方法通常能以更低的累计 API 成本取得更高的准确率。

两个代码任务上，自反馈取得 73.4%，接近执行反馈的 75.0%，API 成本却低 13.4%；与无反馈相比，自反馈成本低约 20%。AIME 2025 和 EvaLearn 上，自反馈也分别降低 47.3% 和 7.0% 的成本，同时提高准确率。Gemini 2.5 Pro 是主要例外，它生成的自反馈更长，收益却较小。

token 分析得到相同趋势：正确性或执行反馈虽然不像 DC 那样极度节省 token，但在相近的多轮 token 预算下取得更高准确率。带反馈的模型把更多计算用于后续修正，生成内容也更简洁。**单纯增加提示词长度无法带来这样的收益**。

### 4.2.3 改进能力：基础越强，通常越会使用反馈

为了区分“初始已经很强”和“经过反馈后提升很多”，改进能力定义为：

$$
\Delta_{\mathcal{M}}=\frac{S_{\max}-S_{\mathrm{base}}}{1-S_{\mathrm{base}}}.
$$

- $\mathcal{M}$ 是被评估的模型；
- $S_{\mathrm{base}}$ 是模型没有反馈时的初始零样本准确率；
- $S_{\max}$ 是模型在自反馈 CoE 中达到的最高准确率；
- 分子 $S_{\max}-S_{\mathrm{base}}$ 是实际提升；
- 分母 $1-S_{\mathrm{base}}$ 是从基础成绩到满分还剩多少提升空间；
- $\Delta_{\mathcal{M}}$ 表示模型填补了剩余错误空间的多大比例。

这种归一化**避免高基础分模型因为“离满分更近”而被低估**。图 5 比较了七款模型的基础能力和归一化改进能力。

![](<images/L4-1.-Chain-of-Experience-06.png>)

> 图 5：模型零样本表现与反馈学习增益之间的皮尔逊相关关系。六个任务均呈正相关，分数由三次运行取平均；代码任务上的相关性最强。

解释：横轴是基础准确率，纵轴是归一化后的改进能力，虚线表示整体趋势。LiveBench Code 的 $r=0.97$，LiveCodeBench V6 的 $r=0.83$，代码任务中基础能力较强的模型也明显更会利用反馈。AIME 2025 和 OmniMath 的相关性较弱，分别为 0.33 和 0.24。六个任务平均约为 0.50，**整体呈正相关，相关强度随任务变化**。基础模型越强，并不代表它一定会按固定比例提升。

## 4.3 实验结论

实验结果可以归纳为三点：

- **反馈是 CoE 取得提升的关键**：只保留同一问题的历史回答、不提供反馈时，模型仍然可以重新检查过去的答案，但提升并不稳定；加入自反馈后，平均成绩比无反馈提高 **5.6%**，正确性反馈或详细执行反馈平均提高 **11.1%**，整体提升最明显；
- **提升并非单纯依靠增加 token**：带反馈的 CoE 在相近的多轮 token 预算下取得了更高准确率，也通常能以更低的累计 API 成本达到更好的结果；
- **模型原有能力会影响它利用经验的效果**：六个任务中，模型的初始能力与后续改进能力整体呈正相关，代码任务上的相关性尤其明显，但不同任务之间仍有较大差异。

因此，CoE 的实验优势主要来自**围绕同一问题保留完整尝试历史，并利用反馈持续修正后续答案**。反馈越具体、越容易验证，模型通常越能从已有经验中找到有效的修改方向。

# 5 进一步讨论与结论

## 5.1 虚假反馈：错误信号会伤害模型，但不一定完全带偏

这里主要说明：**虚假反馈通常会降低模型表现，但模型不会完全照单全收；对多轮答案进行投票，可以减轻部分错误反馈的影响**。

为了测试鲁棒性，模型会连续收到固定的“答案正确”或“答案错误”，无论真实结果如何。虚假反馈在 AIME 2025 上平均降低 7.6%，在 GPQA Diamond 上平均降低 2.6%。强模型更稳定：GPT-5-mini 的降幅只有 2.5% 和 0.6%，o4-mini 则下降 12.8% 和 4.6%。

为减少单轮误导，选择性多数投票（Selective Majority Voting, SelMV-$n$）会从前 $n$ 个有效尝试中提取最终答案，再按多数票决定输出。它利用的是多轮轨迹中较稳定的答案，不让最后一次尝试拥有绝对决定权。

![](<images/L4-1.-Chain-of-Experience-07.png>)

> 表 2：模型在 **20 轮内**持续收到固定“正确”或“错误”反馈时的**最佳表现**。SelMV 从前若干次有效尝试中对最终答案进行多数投票，帮助模型在虚假反馈下维持性能；结果为三次运行均值，粗体表示各设置中的最佳分数。

解释：**虚假反馈通常有害，模型也不会机械服从每一次评价**。SelMV 在部分设置中能够恢复成绩，例如 GPQA Diamond 上，结合固定“错误”反馈的 SelMV 甚至高于普通自反馈。这不代表错误反馈有益。**多轮答案本身带有冗余信号，投票可以抵消一部分误导**。

补充的 50 轮实验还显示，AIME 2025 和 OmniMath 的主要提升都集中在前 20 轮，20～50 轮平均只增加约 2.2% 和 3.5%。经验积累很快进入饱和区，长循环并不自动带来持续线性增长。

## 5.2 模型为什么会从错误改成正确

这里主要说明：**模型从错误改成正确，可能来自遵循反馈、自我反思、重新注意任务要求，也可能只是没有明确原因的随机变化**。

从五款模型、全部任务中收集 6,630 对“错误回答 $\rightarrow$ 正确回答”的相邻轨迹，再用 GPT-5 判断变化原因。改进被分成四类：

- **反馈遵循（Feedback Fidelity）**：明确使用外部反馈给出的提示或纠正；
- **自我反思（Self Reflection）**：主要依靠自身重新推理发现错误；
- **要求回忆（Specification Recall）**：重新注意到题目要求、输出格式或遗漏约束；
- **随机变化（Random）**：找不到明确原因，变化更像随机漂移或文字改写。

自动分类用 100 对样本与两名人工标注者核对，科恩 Kappa 一致性系数（Cohen's kappa）为 $\kappa=0.768$，属于较高一致程度；分歧主要集中在“自我反思”和“随机变化”，因为这两类即使由人判断也不总能清楚分开。

![](<images/L4-1.-Chain-of-Experience-08.png>)

> 图 6：6,630 对从错误变为正确的相邻回答中，不同改进原因所占比例。GPT-5 负责自动分类，比较代码与通用任务在自反馈、执行反馈、正确性反馈和无反馈下的变化来源。

解释：总体约 47.7% 的改进能够直接归因于反馈。代码任务中约 30.0% 的提升来自重新遵守规格或输出要求，说明代码评测对格式、语法和边界条件更敏感。通用任务的自反馈中，68.5% 被归为反馈遵循；无反馈时，69.7% 来自自我反思。模型生成的文字反馈比其他信号更容易触发“按反馈修改”，因为它通常与当前上下文和错误原因贴得更近。

## 5.3 双反馈：原因解释与结果验证可以互补

这里主要说明：**模型反馈可以解释哪里需要修改，正确性或执行反馈可以验证修改结果；两者结合有时效果更好，但收益取决于具体任务**。

**模型反馈包含批评和修改建议，正确性或执行反馈提供更可靠的结果信号**。Claude 4.5 Sonnet 上的双反馈实验把两者放进同一轮经验。

![](<images/L4-1.-Chain-of-Experience-09.png>)

> 表 1：Claude 4.5 Sonnet 在双反馈和任务内经验筛选设置下的结果。双反馈在数学任务中结合模型反馈与正确性信号，在代码任务中结合模型反馈与执行信号；DC 和 SimpleMem 只处理当前任务的历史，不引入其他任务。Acc 是 20 轮内最佳准确率，Best R 是取得最佳成绩的轮次。

解释：AIME 2025 上，双反馈达到 76.7%，高于仅正确性反馈的 70.0% 和仅模型反馈的 60.0%；LiveBench Code 上达到 81.2%，也高于仅执行反馈的 78.1%。OmniMath 上，双反馈 73.5% 略低于仅正确性反馈的 74.5%。**两种反馈能否互补取决于具体任务**。结果信号已经足够强时，额外加入模型反馈未必还能提高成绩。

反馈提供者的能力也很重要。让 GPT-5 与 GPT-5-mini 互相评价时，外部模型反馈在部分 AIME 和代码设置中可以接近或超过正确性/执行反馈；但在更难的 OmniMath 上，基础准确率较低的 GPT-5-mini 无法给 GPT-5 提供足够可靠的判断。模型裁判能否替代验证器，取决于它是否先具备足够的任务能力。

## 5.4 经验筛选：压缩记忆没有超过完整轨迹

这里主要说明：**同一任务连续纠错时，完整保留“回答—反馈—修改”轨迹，比过早筛选和压缩更有效，因为压缩可能丢失关键推理过程**。

DC 和 SimpleMem 在这项实验中只处理当前任务的历史，用来检验“先筛选、再压缩经验”能否优于保留全部轨迹。结果没有支持这一点：

- AIME 2025 上，纯模型反馈为 60.0%，加入 DC 和 SimpleMem 后分别为 50.0% 和 56.7%；
- LiveBench Code 上，纯模型反馈为 57.8%，加入两种记忆方法后分别为 51.6% 和 54.7%；
- OmniMath 上也呈现相同趋势。

这里比较的是**同一任务内的经验处理方式**。完整轨迹保留了每次错误、反馈和修正之间的关系，**压缩摘要可能只留下结论，丢掉模型理解“为什么要改”的中间证据**。这项实验只覆盖短时、多轮修正。在这种设置下，过早压缩未必划算；记忆压缩在其他场景中的效果不在本次比较范围内。

## 5.5 边界：自反馈不能创造模型不知道的事实

这里主要说明：**自反馈只能帮助模型检查和修正已有知识，不能补充模型不知道的外部事实；缺少关键信息时，仍然需要搜索或其他工具**。

BrowseComp-Plus 需要检索模型训练数据之外的信息。多数模型加入自反馈后，表现反而低于无反馈设置。原因很直接：反复检查已有知识可以修正推理，却不能凭空补齐外部事实；如果最初缺少关键证据，自反馈只会围绕不完整信息继续展开。

CoE 还有两项明确限制：

- 当前评测集中在数学、代码和知识任务，尚未验证 CoE 在网页操作、软件工程等长时程交互任务中的实际效果；
- 模型参数没有更新，改进来自上下文中的经验复用。会话结束或经验被移除后，这种能力不会自动永久保留，因此还不能等同于真正的持续学习。

## 总结

CoE 的做法很直接：**把模型已经产生的答案和收到的反馈，交给下一轮继续使用**。它不依赖训练，只要按时间顺序保留动作和反馈，模型就能围绕同一道题继续纠错。

这项工作更像一组**测试时学习的系统实验**。四种反馈被放在同一套设置中比较，效果、成本、模型能力、虚假反馈、反馈来源和经验压缩都纳入了分析。实验阶段表明，大部分提升集中在前期迭代，即使反馈较弱或存在误导，部分模型仍表现出一定的恢复能力。从错误回答转为正确回答的过程主要分为四种模式：遵循反馈、自我反思、重新注意任务要求和随机变化。

但“持续改进”需要谨慎理解。当前 CoE 没有更新权重，也没有建立跨会话的持久记忆，主要完成的是**当前任务上下文中的连续适应**。若要继续走向跨任务、跨会话的长期积累，还需要引入持久外部记忆；若希望模型离开这些上下文后仍保留能力，则需要像文中展望的那样，把经验进一步写入模型参数。
