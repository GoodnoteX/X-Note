> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Harness-of-Harness: Multi-Day Autonomous Software Development with Continual Improvement》的理解。

![](<images/Harness-18.-Harness-of-Harness-Cover.png>)

> 上海人工智能实验室团队在 2026 年 9 月提出 **Harness-of-Harness（HoH）**，在现有编码 Agent 外面增加一层持续开发机制：**外层 HoH 安排规划、开发和独立测试三个角色，依次调用内层 Coding Agent 完成工作，再把测试证据交给下一轮规划**。一次运行中，**外层 HoH 的流程，以及内层 Coding Agent 使用的模型和编码 Harness 都保持固定**；持续更新的是项目代码、开发计划和测试记录。在三个软件开发基准、三组 Harness 与模型搭配中，三轮 HoH 的结果都优于直接使用对应 Harness；在跨多天的案例中，系统完成了 70 轮开发，逐步做出可供人类游玩的第一人称射击游戏。这里的“持续改进”指软件项目在多轮开发中变好，不涉及模型参数训练，也不要求 Agent 修改底层 Harness。论文与项目材料见 [arXiv](https://arxiv.org/abs/2609.01481v1) 和 [项目仓库](https://github.com/Flesymeb/HarnessOfHarness)。


## 核心问题

编码 Agent 已经能写代码、运行测试、修复报错，但让它独立开发一个完整产品，困难会不断累积：以前的需求可能被忘记，已经修好的功能可能再次出错，Agent 也可能反复检查和修补，却迟迟没有做出新的可用功能。

**怎样让一次次编码工作接得起来，既继续增加功能，又记住哪些地方不能改坏**？HoH 保留两样跨轮信息：**当前软件项目**和**经过检查的执行证据**。每轮围绕一个范围有限、可以验收的目标开发，随后独立测试，把已验证的功能和仍待解决的问题一起交给下一轮。

跨多天的开发案例更直观：HoH 从只有需求文档的空工作区开始，先搭建简单原型，再逐轮完善成可玩的游戏。

![HoH 多天自主开发 FPS 游戏的过程](<images/Harness-18.-Harness-of-Harness-01.png>)

> 图1：经过 HoH 的连续迭代，第一人称射击游戏逐步具备连贯故事线、战斗系统、武器与敌人交互系统、玩家引导、屏幕状态显示与菜单、过场动画，以及经过打磨的视觉和音频表现，形成可供人类游玩的体验。游戏、开发轨迹和游玩视频可在 GitHub 中查看。

解释：曲线记录的是**尚未解决的问题数量**，不是游戏质量评分。地图、武器、敌人、菜单和动画陆续接入时，新的缺陷也会被发现，问题数可能随之上升；到了后期，新增功能放缓，更多工作转向修复问题，积压逐渐下降。下方蓝色和紫色标记分别记录新增与关闭的问题。开发在推进，问题数也可能同时增加，只要这些问题被记录下来，后续轮次就能继续处理。图中时间轴延伸到约 80 轮，第5节的具体统计以完成 70 轮时为截点。

## 摘要

HoH 研究的是**自主软件开发**：用户一开始说明想做什么软件，之后由编码 Agent 自己规划、编写和检查，逐步做出完整、可运行的软件，无须人持续指导开发。

HoH 反复调用现有编码 Agent，按**规划—编码—测试**的顺序工作。每轮只做少量能检查清楚的改动，既修复已有问题，也推进新功能。开发者边修改边自测，之后再由独立测试者验收。各角色按规定格式提交计划或报告，自行决定具体做法；需要时再读取历史文档、调用工具和查阅技能，尽量复用已有资源，并用版本记录保存项目变更。

在 GameCraft-Bench、FrontierSWE 和 ProgramBench 三个基准上，三组模型与 Harness 搭配经过三轮 HoH 开发后，成绩都高于直接运行对应 Harness。平均相对提升为 **52.25%**，最大相对提升为 **82.86%**。这些增幅是相对于各自基线计算的，不能当成成功率提高了多少个百分点。跨多天的游戏案例还记录了新功能逐步加入、问题被发现，以及旧功能出错后再次修复的过程。

## 1 引言

编码 Agent 的能力已经从函数补全扩展到阅读大型代码库、处理仓库级问题。不过，实际使用往往仍依赖**人在循环中（Human-in-the-loop）**：人负责拆任务、纠正中途决策、审查代码，在 Agent 卡住后继续给提示。

**自主软件开发**要求 Agent **只接收最开始的高层需求，之后自行完成规划、实现、集成、测试和调试，把一个想法变成完整软件**。

图2对比了这两种开发方式。

![人工持续指导与自主软件开发的区别](<images/Harness-18.-Harness-of-Harness-02.png>)

> 图2：两种不同的软件开发模式。在人在循环中的开发模式下，编码 Agent 在持续的人类监督、指导、审查和干预下生成代码；在自主软件开发模式下，Agent 独立地把高层需求转化为完整、可运行且可部署的软件系统，无须人类继续指导或干预。

解释：传统模式中，人查看运行结果和报错，再给编码 Agent 安排下一步。加上 HoH 后，外层的规划器、开发者和测试者接手这部分工作：先安排任务，再修改项目，测试结果交回规划器，决定下一轮做什么。图中的“产品”是期望交付的结果，不代表只要不断运行，就一定能达到生产可用标准。

开发完整产品需要**连续做出许多决定，并反复修改代码**。多个组件互相依赖，新功能要和已有功能一起正常工作，测试结果也可能说明之前的判断有误。开发过程越长，越容易遇到以下问题：

- **忘记早期要求**：Agent 忘了之前的需求、设计理由或限制，修好一处时可能破坏另一处。
- **旧判断需要修正**：失败尝试和不合适的决定不断积累，新的测试结果也可能推翻早先的假设，后续工作需要据此调整。
- **开发陷入重复**：反复查看和修复同一处，重复验证已完成模块，或者在功能仍有缺失时提前宣布完成。

HoH 要解决的是**怎样接着已有成果继续开发，避免反复修补却没有进展**。它把开发拆成一轮轮的小改动，每轮由三个角色依次完成：

- **项目规划器（Project Planner）**：根据整体需求和上轮测试记录，决定本轮修复什么、增加什么功能，并把任务控制在能够完成和检查的范围内。
- **开发者（Developer）**：实现计划，并在修改过程中持续做针对性测试，及时发现局部错误。
- **独立质量测试者（QA Tester）**：QA 即质量保证（Quality Assurance），根据整体需求和本轮计划，结合运行软件的黑盒测试与查看内部实现的白盒测试，检查功能是否正确、完整、可用；涉及画面和声音时，也检查视觉与音频表现。检查结果交给下一轮规划器。

HoH 规定**每个角色要交出什么，以及用什么记录支撑结果**。计划和报告必须符合指定格式，格式不合要求就重试。至于怎么推理、按什么顺序使用工具、采用哪种实现方法，由各角色在权限范围内决定。

要把这个循环运行很久，还需要控制上下文和工程成本：

- **按需读取历史**：把计划、报告和项目历史存成文件，先让 Agent 看分类目录，需要哪些内容再打开对应文件。这就是**渐进式披露**，可以减少每次放入上下文的历史信息，HoH 没有另建专门的记忆模块。
- **按角色提供工具和技能**：根据角色职责组织可用工具，包括模型上下文协议（Model Context Protocol, MCP）服务、专家模型和领域算法，再用简短的 Markdown 技能文件说明如何使用，需要时读取。
- **优先复用现成资源**：利用已有能力和素材，减少重复实现常规功能的工作。
- **保留可回查的版本历史**：记录每个角色阶段和每轮开发后的项目版本及改动摘要。新修改严重破坏旧功能时，可以恢复到之前验证过的版本；遇到类似问题时，也能回查过去的处理过程。

实验分为两部分。**第一部分做基准测试**：使用任务原本提供的工具，对比加上 HoH 前后，做出的软件质量有没有提高。**第二部分连续多天开发游戏**：额外提供游戏引擎、素材工具、测试技能和版本管理，让 HoH 持续增加功能、修复问题，观察项目能否一步步完善。

## 2 相关工作

### Agent Harness：让模型能工作，再改进它的工作方式

Harness 是模型工作时使用的配套系统，决定给模型看什么、允许它做什么，以及怎样把执行结果交回模型。现在整合进 Harness 的许多能力，最初是分别研究的：

- **提示词与上下文工程**：组织模型每次看到的指令和当前任务信息。
- **外部记忆**：把信息保留下来，供后续交互继续读取。
- **ReAct**：让模型交替思考和执行动作，再根据环境返回的结果继续决策。
- **GPTSwarm**：用图结构表示多个 Agent 如何协作，并优化这种协作方式。

随后，一些方法开始直接改进 Harness 本身：

- **AutoHarness**：根据环境反馈生成实现 Harness 的代码。
- **Meta-Harness**：搜索不同的 Harness 代码方案，寻找更好的实现。
- **Self-Harness**：反复诊断自身 Harness 的问题，再修改它的代码。

HoH 保留已有 Harness，用它反复开发和测试**用户要做的软件项目**。每轮修改的是项目，底层 Harness 的代码保持不变。

### 软件开发 Agent：分工完成项目，再持续修改和完善

软件开发 Agent 已经能处理代码片段生成、仓库问题修复、通用开发和代码重构。有些系统进一步让多个角色合作，完成整个软件：

- **MetaGPT、ChatDev**：预先设置角色和协作流程，让它们分工完成软件开发。
- **AgileCoder、EvoDev**：按开发轮次或功能之间的依赖关系安排工作，逐步增加功能。
- **EvoMAC**：根据测试反馈调整多个 Agent 的工作流程。

评测也开始关注修复单个问题之外的开发能力：

- **SWE-EVO、SlopCodeBench**：检查项目经过长期修改后的表现，以及旧功能是否退化。
- **Commit0、ProjDevBench、ProgramBench、GameCraft-Bench**：检查 Agent 能否从头构建完整的库或项目。
- **FrontierSWE**：覆盖从头实现软件、优化性能，以及研究型任务。

已有编码 Harness 通常围绕一次任务执行组织工作，对多轮修改之间的信息保留支持有限：之前为什么这样设计、哪些功能已经验证、还剩什么问题，未必能充分传给下一轮。HoH 把**已有项目和测试证据一起保留**，让后续开发接着已有代码做，并根据过去的测试结果安排任务和检查新版本。

## 3 Harness-of-Harness

HoH 每轮调用编码 Agent 做规划、开发和测试，完成一组相关改动后，把新版本和测试记录留给下一轮。这样可以逐步增加功能，同时把**已经验证过的功能列为后续修改必须保留的要求**。

### 3.1 问题定义与挑战

用户先给出软件要实现的功能和质量要求，这些要求合起来称为**软件规格**。HoH 的目标是据此做出完整软件。**软件产物（Artifact）** 就是实际交付的项目，包括代码、配置和资源等文件。

固定模型和 Harness 后，整体任务写成：

$$
\operatorname{HoH}_{M,H}:\mathcal{S}\mapsto A.
\tag{1}
$$

其中：

- $\mathcal{S}$ 是**软件规格**，描述用户要什么，以及功能和质量要求。
- $M$ 是承担规划、开发和测试工作的**语言模型**。
- $H$ 是模型使用的**编码 Harness**，为模型提供读文件、改代码、运行程序等能力。
- $\operatorname{HoH}_{M,H}$ 表示在固定 $M$、$H$ 条件下运行的 HoH 开发过程；下标说明其使用的配置。
- $A$ 是最终的软件产物；$\mapsto$ 表示把输入规格映射为输出软件。

这条公式可以读成：**用固定的模型 $M$ 和编码 Harness $H$，把需求 $\mathcal{S}$ 做成软件 $A$**。这里没有训练模型，公式也不保证软件一定达标，结果还需要实际运行和评估。

要完成这项任务，需要处理三类困难：

1. **历史信息容易丢**：开发久了，早期需求、设计理由、失败记录和已经验证的功能可能被忘记，后续修改就少了这些依据。
2. **不知道下一步该改哪里**：用户的整体需求还需要细化，才能确定本轮具体做什么。不同组件又互相依赖，改好一处可能影响另一处，反复修补也会耽误新功能开发。
3. **检查通过不等于软件完整**：不同软件需要检查的功能、交互、配置和资源各不相同。只做少数检查，可能漏掉问题，误把尚未完成的软件当成成品。

### 3.2 框架总览：每轮确定一个可验证的开发目标

决定下一轮任务时，要同时看**用户需求、当前项目和已有测试证据**：需求说明最终要做成什么，当前项目说明已经写到哪里、哪些组件互相依赖，测试证据则说明哪些功能可用、哪里还有问题。

图3说明这些信息怎样在规划器、开发者和测试者之间传递。

![Harness-of-Harness 框架总览](<images/Harness-18.-Harness-of-Harness-03.png>)

> 图3：HoH 对同一个软件项目反复进行规划、开发和 QA 测试。按固定规则运行的控制程序（Runtime）确定每次角色调用的输入并保持其不变，限制操作权限，记录每份证据对应的受测版本，并保存更新后的项目状态。在一次运行中，模型、基础 Harness、角色定义和运行策略保持固定；开发文档、软件产物和执行证据随迭代变化。

解释：规划器根据需求和上轮测试结果写计划，开发者照着计划修改现有项目，测试者再检查新版本，整理出一组测试记录，也就是**证据包**。开发文档会写明**剩余问题、已经通过的功能、本轮任务、必须保留的功能和验收要求**，开发者据此知道要改什么、哪些地方不能改坏。

各角色通过工具和技能完成工作。测试者结合源代码、截图和程序运行记录，检查项目能否构建、功能和交互是否正确，以及数据、依赖、配置、稳定性和完整性。**Runtime 用程序规则管理输入、权限、受测版本和输出格式**，不承担第四个推理角色。图中的引擎和素材工具用于扩展部署；基准实验沿用各基准已有工具，没有额外加入这些能力。

每轮选定一个明确、可验证的开发目标，例如修复问题或增加功能，把完成目标所需的相关修改一起安排进去，无关改动留待后续。测试完成后，再根据结果决定下一轮修什么、加什么，以及哪些已完成的功能需要保护。

一轮开发里要做三种不同的判断，分别交给三个角色：

- **下一步改什么**：规划器结合整个项目的需求和进展安排任务。
- **具体怎么实现**：开发者有代码修改权限，并根据实际代码选择实现方式。
- **实际是否达标**：测试者独立运行和检查修改后的项目，判断是否满足要求。

每轮按这个顺序调用三个角色。**实际运行的是同一组 Harness 与模型的三次独立调用**，只是角色提示词和执行权限不同，并没有使用三个不同模型。

### 3.3 跨轮状态管理：项目和证据都要留下来

只保留代码，下一轮通常只能知道“现在实现了什么”，却不容易知道“为什么这样实现”“哪些失败已经试过”“哪些功能确认过可用”。如果这些信息只在上一次调用的聊天历史中，调用结束后，后续 Agent 就可能需要重新推断。

HoH 在每轮结束时保留两部分信息：

- **软件产物状态 $A_t$**：由**开发者**（Developer）接着上一轮项目 $A_{t-1}$，按照本轮计划修改后产生，包括源码、配置、资源，以及描述项目的元数据。下一轮直接接着这些文件开发。
- **执行证据状态 $\mathcal{E}_t$**：由**测试者**（QA Tester）检查新版本 $A_t$，结合 Runtime 提供的构建和运行检查结果，输出测试报告与证据记录，再由适配程序整理而成。针对这个版本和本轮目标，它记录哪些功能已经验证、哪些还缺少证据、哪些实际运行失败。

**规划器负责制定本轮开发计划 $D_t$**，不直接产生这两项状态。整个过程是：**规划器出计划 → 开发者改项目 → 测试者出证据 → 下一轮规划器据此安排任务**。

两者不能互相替代。代码中存在某个函数，不能证明相应功能实际可用；一份测试报告，也不能替代下一轮需要继续修改的工程文件。

第一轮之前，$A_0$ 表示初始空项目工作区，$\mathcal{E}_0=\varnothing$ 表示还没有上一轮证据。一次循环的状态变化为：

$$
(A_{t-1},\mathcal{E}_{t-1})
\xrightarrow{\text{loop }t\text{ under }\mathcal{S}}
(A_t,\mathcal{E}_t).
\tag{2}
$$

其中：

- $t$ 是当前迭代编号，$t-1$ 是上一轮。
- 左边的 $(A_{t-1},\mathcal{E}_{t-1})$ 是本轮输入：**上轮项目和上轮证据**。
- 箭头上的文字表示，在固定软件规格 $\mathcal{S}$ 下执行第 $t$ 轮规划、开发和测试。
- 右边的 $(A_t,\mathcal{E}_t)$ 是本轮输出：**更新后的项目和针对这个版本的新证据**。
- $\varnothing$ 是空集，表示第一次规划没有可用的历史测试证据。

这两个状态在一轮开发中分别更新：**规划器先根据证据决定目标，开发者再修改项目，测试者最后检查新版本**，得到新的软件与证据。

下一轮接着 $A_t$ 中的代码写，根据 $\mathcal{E}_t$ 决定任务优先级：没解决的问题继续处理，已验证的功能要求保持正常。已有代码和测试结论都能继续使用，减少从头重建和重复摸索。

**规划器每轮都会生成一份开发文档，记为 $D_t$**，说明本轮要做什么、哪些已有功能不能改坏，以及怎样验收。开发者按它修改项目，测试者按它和整体需求检查结果。文档会保存下来方便回查，但下一轮会根据需求和新证据重新制定计划，无须原样沿用旧计划。因此，公式（2）把项目和证据列为跨轮传递的两项状态，计划则在每轮重新生成。

### 3.4 一轮 HoH 如何执行

一轮开发先说清**这次改什么、怎样算完成**，再修改代码，最后独立验收。任务范围明确，出错时就更容易查到相关改动。具体实现仍由开发者决定，因为有些设计要等读过代码、运行过程序之后才能确定。

每次调用的提示词都包含**固定的角色说明和本轮任务信息**，告诉 Agent 要做什么、按什么格式提交结果。Runtime 则实际限制它能读哪些材料、用哪些工具、修改哪些内容，并检查返回格式。Agent 在这些权限内自行决定推理和工具使用过程。

#### 3.4.1 项目规划：任务要小，但相关功能要做完整

规划器需要把需求、故障和保留要求整理成**一个本轮能够实现并验收的目标**。任务拆得太散，可能缺少配套功能，做完仍无法使用；把许多无关目标放在一起，又会扩大修改范围，使失败难以定位。

计划需要同时满足两点：

- **范围有限**：一轮只处理一小组相关任务，不夹带无关重构和临时想到的功能。
- **局部完整**：把实现本轮目标所必需的配套修改一起做完，让修改后的行为能运行、能测试，即使要改多个文件或组件。

这里的“小增量”是**完成一个可以实际检查的开发目标**，既可以增加功能，也可以修复问题。修改几个文件只是实现需要，不能单凭文件数量判断任务范围。

规划器根据整体需求 $\mathcal{S}$ 和上轮证据 $\mathcal{E}_{t-1}$ 排任务，再结合现有项目写出开发文档 $D_t$。**文档至少讲清三件事**：

- **本轮要做的相关任务**及其**预期结果**。
- **已经验证、必须保留的功能**，防止新增功能破坏旧行为。
- **可观察的验收要求**，说明通过什么行为或记录判断本轮是否完成。

**规划器不能修改项目**。算法允许它只读查看现有项目，基准使用的提示模板则进一步限制了可读内容：不直接检查实现代码，依据整理好的文档和证据来规划。

模板要求每轮列出**最多三项可实现目标**，先处理挡住后续开发的问题，以及新改动破坏旧功能的问题，再增加新功能。计划规定要达到什么效果、怎么验收，开发者自行选择算法、工具和调试方法。

#### 3.4.2 软件开发：只有开发者可以改项目，自测贯穿修改过程

开发者接着上轮项目 $A_{t-1}$，完成计划 $D_t$ 中的任务。这个做法称为**从已有产物继续开发**（Warm-start）：保留已有源码、项目结构、配置和资源，在这些文件上继续修改。

HoH 规定**只有开发者能修改项目**，这就是单一写入者规则。规划器负责安排任务，测试者检查提交的版本，都不能顺手改代码。这样就能明确：$A_{t-1}$ 到 $A_t$ 的修改由开发者完成，之后的测试针对 $A_t$。

开发过程采用**测试前移（Shift-left Testing）**，每完成一项有意义的改动，就尽快测试：

1. **修改前检查现状**：先确认目标功能目前表现如何，哪些操作流程原本正常，作为修改后的对照。
2. **完成有意义的修改**：根据计划实现相关功能或修复问题。
3. **重新运行相关流程**：检查修改后的代码和运行结果，并确认相关的旧功能有没有被改坏。

越早发现失败，就越容易查出它与刚才哪项改动有关，并及时修正。具体代码设计、工具调用和调试步骤不被写死，开发者可以随项目情况调整。

**开发者自测用于判断代码是否准备好提交检查，独立 QA 才负责验收**。自测记录和“已经完成”的报告可以帮助验证，但不能直接替代实际产品行为的证据。

#### 3.4.3 独立质量保证：固定受测版本，每项判断都要有依据

不同软件需要检查的功能、交互流程、配置和资源各不相同，还要关注旧功能是否被改坏。测试者根据整体需求 $\mathcal{S}$ 和本轮计划 $D_t$，列出**当前项目需要逐项检查的验收要求**，为不同场景选择合适的测试。

**先固定受测版本，再收集证据**。开发者提交的 $A_t$ 就是待验收的候选版本。测试期间，测试者只能读取和运行这个项目，不能修改其实现，但可以输出测试报告和证据记录。控制程序还会提供按固定规则完成的构建和运行检查结果，保证一份报告里的观察来自同一版本，避免测试者边检查边修复问题。

检查结合两种方式：

- **黑盒测试（Black-box Testing）**：通过正常输入和可见输出检查用户能否使用功能、状态是否正确变化、完整流程能否走通。
- **白盒测试（White-box Testing）**：查看源码、配置、资源绑定、运行状态与日志，用于诊断失败，补充仅靠画面或输出无法确定的内部条件。

两类检查针对同一版本。**只有这个版本的运行和检查记录足以支持某项要求，才把它标为已验证**。代码中有函数、目录里有素材，或者开发者说已经完成，都不足以单独证明功能可用。运行失败、漏做的需求、旧功能被破坏，以及尚无充分证据的情况，都记录下来交给下一轮。

每个验收项都按“列出要求、收集记录、判断是否满足、汇总结果”的顺序处理，写成公式就是：

$$
\begin{aligned}
\mathcal{C}_t &= \operatorname{Claims}(\mathcal{S},D_t),\\
r_i &= \operatorname{Observe}(A_t,c_i),\\
s_i &= \operatorname{Assess}(c_i,r_i),\\
\mathcal{E}_t &= \{(c_i,r_i,s_i)\}_{c_i\in\mathcal{C}_t}.
\end{aligned}
\tag{4}
$$

四行公式依次对应上述四步：

- $\mathcal{C}_t$ 是本轮**要检查的要求集合**。$\operatorname{Claims}$ 根据需求和计划列出这些待验证的功能声明，也就是验收项。
- $c_i$ 是第 $i$ 个验收项，$i$ 编的是检查项目，$t$ 编的是开发轮次。
- $\operatorname{Observe}(A_t,c_i)$ 表示在版本 $A_t$ 上运行或检查第 $i$ 项要求，得到执行记录 $r_i$，例如回放、日志、截图或测试结果；这些是开发循环内可用的记录。
- $\operatorname{Assess}(c_i,r_i)$ 表示对照要求 $c_i$ 和记录 $r_i$，判断是否有足够证据证明它已实现，结果记为 $s_i$。这里的小写 $s_i$ 是检查状态，大写 $\mathcal{S}$ 是整体软件需求。
- $(c_i,r_i,s_i)$ 将**检查内容、支撑记录和判断结果**放在一起。花括号表示集合，下方的 $c_i\in\mathcal{C}_t$ 表示对本轮所有声明汇总。

得到的记录分为两类：

$$
\begin{aligned}
\mathcal{E}^{\mathrm{ver}}_t
&=\{(c_i,r_i,s_i)\in\mathcal{E}_t\mid s_i=\mathrm{verified}\},\\
\mathcal{E}^{\mathrm{gap}}_t
&=\{(c_i,r_i,s_i)\in\mathcal{E}_t\mid s_i=\mathrm{gap}\}.
\end{aligned}
\tag{5}
$$

- 上标 $\mathrm{ver}$ 表示**已验证**，上标 $\mathrm{gap}$ 表示**仍有缺口**，都不是幂运算。
- $\in$ 表示“属于”，竖线 $\mid$ 表示“满足右侧条件”，用于按状态筛选记录。
- $\mathrm{verified}$ 表示已有足够证据证明要求得到满足；$\mathrm{gap}$ 表示仍有待处理的缺口，包括实际错误、未完成要求、旧功能出错的风险和证据不足。缺口也可能只是还没测清楚，不能都当成确定的代码错误。

两类记录合起来构成完整证据包，且同一条记录不会同时归入两类：

$$
\mathcal{E}_t=\mathcal{E}^{\mathrm{ver}}_t\cup\mathcal{E}^{\mathrm{gap}}_t,
\qquad
\mathcal{E}^{\mathrm{ver}}_t\cap\mathcal{E}^{\mathrm{gap}}_t=\varnothing.
\tag{6}
$$

$\cup$ 是并集，表示把两类记录合起来；$\cap$ 是交集，表示两类中共有的记录；$\varnothing$ 表示没有共同记录。也就是说，**同一条记录只归入“已验证”或“有缺口”中的一类**。这只说明记录如何分类，不能保证所有软件行为都已经测过。

例如，按左右键能够移动角色，回放和位置日志也能确认，就把移动功能记为已验证；如果任务结束后没有结果画面，就记为缺口。下一轮计划因此包含：**保持角色移动正常、补上任务完成状态、重新运行到结束并检查结果画面**。

测试者输出文字报告和结构化记录，负责转换格式的适配程序再将它们整理成 $\mathcal{E}_t$。下一轮规划器由此能够查到每项判断的依据，确定该保留什么、继续处理什么。

把规划、开发和测试接起来，就得到算法1中的完整循环：

```text
算法1：Harness-of-Harness

输入：软件规格 S，初始软件产物 A_0，迭代预算 T
固定：模型 M、Harness H、角色的约束
输出：最终软件产物 A_T

E_0 ← 空集
for t = 1, ..., T:
    D_t ← ProjectPlanner(S, E_(t-1); read_only(A_(t-1)))
    A_t ← Developer(A_(t-1); S, D_t)
    E_t ← QATester(read_only(A_t); S, D_t, Runtime.check(A_t))
return A_T
```

其中，$T$ 是允许执行的开发轮数；$D_t$ 是本轮计划；`read_only` 表示只读权限；`Runtime.check(A_t)` 是控制程序对受测版本进行的检查；$\leftarrow$ 表示把返回结果存入左边变量。计划交给开发者和测试者，改好的项目交给测试者，测试记录再交给下一轮规划器。

**完成规定的 $T$ 轮后，算法返回最后一版软件 $A_T$**。它不会依据私有基准分数从历史版本中挑选最好的一版，也不要求所有需求都通过才停止。测试记录用来指导后续开发，预算用完时仍可能有问题尚未解决。

## 4 实验：基准评测

基准实验先比较**同样的模型和 Harness，加上 HoH 后做出的软件是否更好**，再检查哪些方面改善了、多做几轮是否有效，以及更新计划、反馈测试记录和保留已有代码分别有什么作用。

### 4.1 实验设置

**实验保持同组模型、基础 Harness 和任务工具不变，比较加上 HoH 前后的软件质量，并单独统计 token 用量**。开发中的 Agent 只能使用可见的运行证据，不能读取隐藏测试和最终评分。两种方法的调用次数和实际 token 用量并不相同。

#### 三类任务：做游戏、实现与优化软件、重建程序行为

三个基准覆盖不同的软件开发要求：

- **GameCraft-Bench，完整游戏开发基准**：根据自然语言规格构建可玩的 Godot 项目，Godot 是用于制作和运行游戏的引擎。完整基准有 140 个任务、15 个游戏类型；实验使用固定随机种子，按游戏类型进行分层随机抽样，每类抽取 3 个，共 **45 个任务**。分析时进一步合并为动作、时机与节奏、策略、模拟、冒险五组；这是汇报时的粗粒度分类，具体评分仍沿用原基准规则。
- **FrontierSWE，软件工程与开放式优化基准**：从 17 个任务中使用 **15 个任务**，包含 4 个实现任务、9 个性能任务和 2 个研究任务。既有从任务规格构建独立交付物，也有优化已有系统。两个任务因为外部 API 授权和驱动环境要求而未纳入，与模型表现无关。
- **ProgramBench，净室程序重建基准**：只给编译后的可执行程序和文档，要求重建行为与参考程序一致的代码库。“净室”指无法直接阅读原程序源码，需要根据可观察行为重建。评测使用隐藏行为测试，本次使用的任务数未明确列出。

#### 三组 Harness 与模型配置

每组都比较直接使用 Harness 和加入 HoH 后的表现：

| Harness | 版本 | 底层模型 | 推理配置 |
| --- | --- | --- | --- |
| Codex CLI | 0.142.5 | GPT-5.5 | High |
| OpenCode | 1.14.30 | DeepSeek-V4-Pro | 未单列设置 |
| Pi Coding Agent | 0.80.10 | MiniMax-M3 | 客户端 High |

**同一组对比中，模型、Harness 版本、原生设置、任务材料和基准工具保持一致**。基准条件下不额外加入多天游戏案例使用的领域技能、工具扩展和版本管理。

#### 对照方式：开发时使用的反馈与最终评分分开

**Vanilla** 是对照方法，直接调用原生 Harness 完成一次标准开发；**HoH@$T$** 表示完成 $T$ 轮规划—开发—测试，主实验使用 $T=3$。

HoH 每轮都调用规划、开发、测试三个角色。因此，**相同开发轮数不代表相同调用次数或 token 用量**。主实验先比较最终软件，第4.3节再对照反复调用普通编码 Agent 的结果，同时列出资源用量。

开发与最终评测严格分开：

- **开发时的反馈**：Agent 可以读取公开任务材料、运行程序，并使用独立 QA 的检查结果安排下一轮工作。
- **开发后的评分**：等整个开发过程结束，再用基准评估器检查中间版本和最终版本。隐藏测试、私有评分规则和评估器输出都不会交给开发中的 Agent。

HoH 根据循环内可见的运行证据改进项目，不会拿隐藏答案或最终评分指导下一轮修改。

**每个任务在每种实验设置下记录一次有效运行的成绩**。如果因基础设施或模型服务传输故障失败，会重跑替换；这不算对同一任务进行多次独立实验。报告中的平均分是在不同任务之间取平均，并没有通过反复生成同一项目来衡量结果稳定性。

#### 指标：质量与模型调用量分开看

- **GameCraft-Bench 总分（Overall）**：不能编译或运行的项目记 0 分；可运行项目按照核心玩法、内容深度、功能性视觉和美术表现加权评分，使用 0–100 分制。
- **FrontierSWE 平均奖励（Mean Reward）**：用各任务的官方验证器给软件打分，再对 15 个任务取平均。不同任务可能采用不同的分数尺度，这个平均值不能一律看成成功率。
- **FrontierSWE 相对胜率指标（Dominance）**：在每个任务上，计算某种设置相对于随机选取的竞争设置的胜率，再对任务取平均。这里的“比较池”就是一起参加比较的设置集合。主实验包含 3 组模型与 Harness 各自的 Vanilla、HoH@1、HoH@2、HoH@3，共 12 种设置。**对手换了，指标也会变，因此只能在同一个比较池里直接比较**。
- **ProgramBench 平均测试通过率（Avg. Test Pass Rate）**：先算每个任务通过隐藏行为测试的比例，再对任务取平均。通过部分测试也会贡献分数，不等于完整重建成功的任务比例。
- **模型调用量**：累计编码 Harness 调用的输入与输出 token，不包含最终基准评估的 token。输入统计可能包含缓存读取，各服务商口径不同，数据主要用于**同一配置内**比较，不能直接拿不同供应商的 token 总数比较账单。

### 4.2 主要结果

**三轮 HoH 在三个基准、三组配置上的汇总成绩都优于对应 Vanilla，受测的不同基础水平的编码 Agent 都有提升**。但并非每轮都会变好，有些指标会在后续轮次回落。完整结果见表1。

- **不同基础能力的配置**：受测的较强和较弱配置都能获益，部分较弱配置加上 HoH 后，还能在部分任务类别上超过较强配置直接运行的成绩，但不是全面反超。
- **多轮开发的效果**：多做几轮总体上有帮助，但最佳版本不一定在最后一轮。例如，GameCraft-Bench 的三组配置都持续提高到第三轮，ProgramBench 中的 Pi 则在第二轮达到峰值，第三轮有所回落。

![HoH 在三个基准上的主要实验结果](<images/Harness-18.-Harness-of-Harness-04.png>)

> 表1：GameCraft-Bench、FrontierSWE 和 ProgramBench 的主要结果。每个 Harness 分别测试 Vanilla 与 HoH@1–3；同一 Harness、同一指标中，粗体标出最好结果，排序依据未四舍五入的数值，精确相等时使用相同标记。HoH@3 后的绿色小字表示相对 Vanilla 的绝对增量，Dominance 的增量以百分点计。粗斜体区分汇总指标与任务分类。Strat.、Sim.、Adv.、Impl.、Perf. 分别表示策略、模拟、冒险、实现和性能；ProgramBench 的 Pass Rate† 表示平均测试通过率。

解释：**三组模型与 Harness 原本的成绩不同，但加上 HoH 后都有提高**。GameCraft-Bench 中，Codex 从 49.58 提高到 71.52，OpenCode 从 26.90 提高到 48.98，Pi 从 42.16 提高到 58.78。ProgramBench 中，三者分别从 60.41、45.27、35.83 提高到 66.50、57.56、52.68。Pi 的 ProgramBench 成绩在第二轮达到 53.57，第三轮有所回落，说明继续开发也可能把已有表现改差。

#### 不同基础能力的配置都能获益

**HoH 对较强和较弱的起点都有效，收益大小则与任务和配置有关**。

- **Codex + GPT-5.5**：起点较高，三轮后仍取得最高的 GameCraft-Bench 总分 71.52，绝对提高约 21.9 分。
- **OpenCode + DeepSeek-V4-Pro**：GameCraft-Bench 起点最低，但三轮后达到 48.98；在动作、模拟两组中超过 Codex 的 Vanilla，在 FrontierSWE 的性能类任务上也超过 Codex Vanilla。
- **Pi + MiniMax-M3**：FrontierSWE 平均奖励从 0.26 提高到 0.55，ProgramBench 平均测试通过率提高 16.85 个百分点，这两项绝对增量在三组中最大。

这些结果说明 HoH 对不同基础水平的配置都有帮助，但不能保证原先成绩较低的配置加上 HoH 后，就能在所有任务上超过原先更强的配置。

#### 多轮开发总体继续推进，但存在回退

**GameCraft-Bench 三组配置的总分都从第一轮逐步提高到第三轮**。OpenCode 的变化尤其明显：HoH@1 仅比 Vanilla 高 1.71 分，第二轮增量扩大到 13.42 分，第三轮达到 22.08 分。

FrontierSWE 主比较池中，Codex 的 Dominance 从 Vanilla 的 44% 提高到 58%、60%、71%。ProgramBench 中，Codex 和 OpenCode 在第三轮达到最好结果，Pi 则在第二轮达到峰值。

在这些任务上，**多轮开发总体提高了软件质量**，但单个功能和单项指标仍可能在后续修改中变差。

### 4.3 分析与消融实验

下面五项分析分别得到这些结论：

- **游戏质量**：三组配置在玩法、内容、视觉反馈和美术四个维度上的平均分都提高了，改善不只集中在某一方面。
- **十轮开发**：Codex 在 FrontierSWE 上延长开发后仍有收益，但第九轮优于第十轮，最后一版未必最好。
- **重复调用对照**：相同开发轮数下，HoH 得分更高；两轮 HoH 还以更少 token 超过三轮普通续写，收益不能仅用增加开发轮数或 token 消耗来解释。
- **具体游戏案例**：三个展示案例的操作流程、内容或视觉提示更完整、清楚，但不能据此认定所有生成游戏都达到同样品质。
- **消融实验**：去掉计划更新、证据反馈或项目继承中的任一项，平均分都会下降；每轮从头重建还会增加 token 消耗。不过，完整 HoH 并非在每个任务上都最好。

#### 游戏质量：玩法、内容、视觉反馈和美术都得到改善

**玩法、内容、视觉反馈和美术四项平均分都提高了**。图4分别比较这四项成绩，说明总分改善涉及哪些方面。

![HoH 对游戏四种质量维度的影响](<images/Harness-18.-Harness-of-Harness-05.png>)

> 图4：Vanilla 与 HoH@3 在 GameCraft-Bench 四个评分维度上的比较。(a)–(c) 分别为 Codex + GPT-5.5（high）、OpenCode + DeepSeek-V4-Pro、Pi + MiniMax-M3。柱形表示 45 个任务在核心玩法、内容深度、功能性视觉和美术表现上的平均分；误差线为通过自助重采样得到的 95% 置信区间。

解释：**功能性视觉**关注玩家能否看清游戏状态、目标和操作反馈；**美术表现**关注风格是否一致、素材和画面是否精细。前者偏向看得懂、用得顺，后者偏向视觉品质。Codex 的功能性视觉从 48.67 提高到 74.23，美术表现从 45.28 提高到 65.28；三组配置的四项分数都有提升，改进涉及玩法、内容和视觉呈现。

#### 十轮开发：总体收益可以延续，最佳版本未必在最后

**延长到十轮后，Codex 的整体表现继续提高，但第十轮低于第九轮**。这次仍使用相同的 15 个 FrontierSWE 任务，只比较 Codex 的 Vanilla 和 HoH 第1–10轮后的版本，共 11 种设置。

![FrontierSWE 十轮 HoH 开发的相对胜率](<images/Harness-18.-Harness-of-Harness-06.png>)

> 图5：FrontierSWE 上十轮开发的 Dominance 变化。阴影表示正负一个标准误，星号标出成绩最好的版本，虚线表示 Vanilla 基线。

解释：在**这个固定的十轮比较池**中，Vanilla 为 27.33%，HoH@3 为 39.33%，HoH@9 达到 76.00%，HoH@10 为 72.67%。第十轮比第三轮高 33.34 个百分点，比 Vanilla 高 45.34 个百分点。标准误表示图中均值估计的不确定性，不代表每个任务的分数波动范围。

这里的 HoH@3 为 39.33%，主表中却是 71%，原因是**Dominance 的比较对象不同**，不能据此判断第三轮软件退化。引言提到的“22% 到 72.67%”描述迭代过程中的变化，不能把 22% 当成 Vanilla 基线；比较 HoH@10 与直接运行 Harness 的差距时，应使用图5明确标出的 **27.33%**。

#### 重复调用对照：多运行几次解释不了全部收益

**相同开发轮数下，HoH 得分更高；在部分对照中，也用更少 token 超过重复续写**。

对照方法 **Vanilla Continuation（连续开发基线）** 在已有项目上反复调用普通编码 Agent，没有独立规划文档，也没有单独的 QA 角色提供证据。每次追加开发时，使用固定提示：“继续开发并测试当前游戏。”它和 HoH 都接着已有代码做，区别在于怎样安排后续开发。

表2同时列出开发轮数和实际累计 token 用量，方便比较。

![HoH 与 Vanilla 重复开发的轮数和 token 对照](<images/Harness-18.-Harness-of-Harness-07.png>)

> 表2：在 GameCraft-Bench 上，比较 HoH 与重复 Vanilla 开发经过 1、2、3 个开发轮次后的结果。使用 Codex + GPT-5.5（high）；Score 为 45 个任务的平均 Overall 分数，Tokens 为每个任务平均累计使用的编码 Harness token，单位为百万。

解释：三轮 Vanilla Continuation 达到 58.24，使用 6.33M token；三轮 HoH 达到 71.52，使用 8.41M token。这一组是**轮数相同、token 不同**。再看另一组：HoH 两轮用 5.67M token 得到 64.84，超过用了 6.33M token 的三轮续写。这说明 HoH 的提高不能只用“花了更多 token”解释，规划、独立测试和证据反馈的安排有帮助。不过，这些对照的 token 用量仍不完全相同。

#### 具体游戏：操作流程更完整，目标和反馈更清楚

**这些游戏补齐了玩法和操作流程，也让目标与反馈更清楚**。图6对比三个不同类型的任务。

![三种游戏在 Vanilla 和 HoH 三轮后的对照](<images/Harness-18.-Harness-of-Harness-08.png>)

> 图6：使用 Codex + GPT-5.5（high），比较 Vanilla 与 HoH@3 生成的最终游戏。三列分别为 Momentum Lab（基于动量的平台跳跃游戏）、Kitchen Rush（餐厅经营模拟）和 Ant Empire（放置式蚁群经营游戏）；上排为 Vanilla，下排为 HoH@3。编号虚线框对应旁边注释，红叉与绿勾分别表示局限和已实现的功能。

解释：三个例子改进的重点不同：

- **Momentum Lab**：原先是简单几何平台，效果区域相互重叠；改进后地形有了明确主题，终点和墙跳路线也更清楚，总分约从 34.05 提高到 70.61。
- **Kitchen Rush**：原先只有示意性的切菜区域和单一装盘区域；改进后，取料、准备、装盘、取餐和丢弃形成了清楚的流程，总分约从 42.6 提高到 73.38。
- **Ant Empire**：加入了实际生效的特殊蚁种，并显示季节和结果状态，玩家更容易看出蚁群的长期发展，总分从 65.52 提高到 87.88。

截图中的标注与分数共同支持这些具体变化；它们是选取的案例，不代表每个生成游戏都达到同样品质。

#### 消融实验：计划更新、证据反馈和继承项目都有贡献

**去掉任一跨轮机制，平均分都会下降；每轮从头重建还会增加 token 消耗**。

在 45 个 GameCraft-Bench 任务上，固定 Codex + GPT-5.5（high）和三轮预算，分别改变一个机制：

- **不更新计划**：后续轮次继续使用第一轮文档，即 $t>1$ 时 $D_t=D_1$。开发和测试仍进行，项目仍继承，但计划不会随新发现调整。
- **不反馈证据**：QA 仍检查项目，但结果不交给下一轮规划器；规划器仅依据规格重新规划，开发者仍继续修改已有项目。
- **不继承项目**：保留根据证据规划和独立 QA，但每次开发都从初始工作区 $A_0$ 开始，不能接着上轮软件实现往下做。

这三种改法的平均结果见表3。

![HoH 跨轮机制消融实验](<images/Harness-18.-Harness-of-Harness-09.png>)

> 表3：使用 Codex + GPT-5.5（high）的 GameCraft-Bench 消融实验。括号表示相对完整 HoH@3 的分数差；Tokens 为每个任务平均累计 token，单位为百万。

解释：完整 HoH 为 **71.52 分**；不更新计划降至 **63.39**，不反馈证据降至 **65.23**，不继承项目降至 **63.67**。从头重建还把 token 从 **8.41M** 增加到 **11.12M**。保留已有代码、根据新测试结果调整计划，都对平均成绩有帮助；每轮重新做一遍项目还会消耗更多 token。

这里比较的是**平均效果**，完整 HoH 并非在每个任务上都领先。例如 Lighthouse 的“不继承项目”得分为 81.50，略高于完整 HoH 的 80.90。因此，平均分下降不代表所有任务都变差。

在这些基准上，**包含重新规划和独立测试的 HoH 比普通重复续写表现更好，继承项目能减少重复构建，多轮开发仍有收益，但也会回退**。这些结果来自有限任务和单次有效运行，不能保证任意项目都能稳定、持续地改善。

## 5 实验：跨多天自主开发第一人称射击游戏

前面的基准主要比较少量开发轮次后的软件质量。多天案例要检查的是：当功能越来越多、组件互相依赖、未解决的问题不断积累时，HoH 能否接着同一个项目持续开发。

案例项目 **Fusepoint** 是单人叙事型**第一人称射击游戏（First-Person Shooter, FPS）**，从只含产品需求文档的空工作区开始。统计到完成 **70 轮开发**时，观察新功能如何加入、问题如何被发现和修复，以及旧问题是否再次出现。

### 5.1 案例设置：开发什么，人参与到哪一步

#### 任务要求：把多个系统组合成一段可玩的任务

**需求描述了玩家体验与验收条件，具体怎么拆任务、先做什么和怎么测试由 HoH 决定**。

产品需求文档（Product Requirements Document, PRD）要求：

- 完成一场**五分钟的单人拆弹任务**。
- 按顺序占领**两个控制点**，再在最终目标处执行**三个阶段的拆弹操作**。
- 固定 **18 名敌人**，按 **3、5、10** 分布在三个交战区域。
- 提供不同的**成功和引爆结局**。

要让这场任务真正能玩，三维环境、外部素材、任务逻辑、战斗、剧情和界面反馈必须配合起来，运行也要稳定。每次增加新功能，还得检查此前加入的功能有没有被改坏。

#### 模型与人工介入范围

多天案例使用 **Codex CLI + GPT-5.6-Sol，High 推理设置**，与基准实验的 GPT-5.5 不同。

**人类只在网络或 API 不可用时恢复服务**，没有介入规划、实现、调试、测试和验收。这里的自主性指开发决策和执行由系统完成，基础设施仍有人维护。

#### 领域工具和技能：让 Agent 能操作引擎、使用素材并检查成品

复杂游戏开发需要直接接触引擎与运行中的游戏，不能只靠编辑文本。案例配置包括：

- **Godot 4.7 与 Godot MCP**：Godot 用于制作和运行游戏，Godot MCP 让 Agent 通过工具操作引擎、运行项目和调试。前面的 GameCraft 基准实验使用 Godot 4.6.2，版本与这个案例不同。
- **素材生成技能**：规定目标风格、尺寸与文件格式，指导工具获取或生成图像、三维和视频资源。
- **界面与用户体验技能（UI/UX）**：约束视觉呈现和风格一致性。
- **测试技能**：记录适合当前场景的测试注意事项，指导通过 Godot MCP 进行验证和调试。

引入的外部素材采用允许复用的许可，包括 CC0 和 CC BY，并保留来源及需要的署名信息。素材找到了，还要确认能不能合法复用。

#### 验证范围：从有限检查扩展到真实交互

基准实验主要使用任务提供的有限检查，包括截图和**冒烟测试**，即确认系统能否启动、基本路径能否运行的快速检查。

多天案例的测试者还会检查**按键后游戏是否作出相应反应、音频是否正常、三维素材是否正确接入游戏**，把实际游玩时能遇到的问题也纳入测试。

#### 项目记录：同时留下版本、问题和支撑材料

GitHub 用于版本控制和问题跟踪，每轮保留：

- **开发文档**：记录当前目标和验收要求。
- **保留版本的项目工作区**：保存每个版本的源码、资源和配置，方便回查改了什么。
- **问题表与证据包**：记录发现了什么问题、有哪些测试依据、是否已经修好，供后续规划读取。

三个角色通过这些记录接着同一个项目工作，下一轮也能查到已有版本和测试结果。[公开仓库](https://github.com/Flesymeb/HarnessOfHarness)提供项目材料和部分开发记录，方便回查。

### 5.2 多轮开发过程：增加功能、发现问题，再逐步修复

**70 轮开发先做出基础项目，再增加功能，后期更多地修复问题；已经修好的问题也可能再次出现**。

通过 GitHub 的问题记录、代码提交历史和测试证据包，可以跟踪哪些问题是新发现的、哪些经过 QA 确认修好后关闭了、哪些后来又出现，以及当前还有多少问题没解决。这些记录也能与图1中各项功能加入的时间对应起来。

发展过程分为三个阶段：

1. **初始构建，第1–27轮**：先让项目运行起来，做出主要操作流程。越来越多功能能够实际测试后，遗漏的需求和缺陷也随之暴露，待解决的问题增多。
2. **能力扩展，第28–49轮**：继续增加新功能，同时排查和修复问题。各部分逐渐连在一起，一处修改可能影响之前实现的任务状态、战斗、界面或程序运行。
3. **稳定完善，后续轮次**：计划中的功能逐渐接近完成，新增功能放缓，问题修复占比提高，未解决问题数量开始下降。

截至第70轮，**81 个记录问题中关闭了 65 个，仍有 16 个未解决**。另有 **17 个问题曾在关闭后重新打开**，说明后续变更使此前通过验证的行为再次失败；这个数字不能直接与当前未解决数相加。

重新打开的问题会同时保留“这次哪里失败了”和“之前怎样确认正常”的记录，下一轮可以直接安排修复，无须只看当前代码再猜一遍问题经过。

**项目版本保留已经写好的实现，问题历史与证据包保留测试结论**。后续规划可以接着这些成果增加功能、修复错误，并检查旧功能有没有被改坏。不过，游戏已经能玩，不等于全部问题都已清零；单个多天案例也代替不了跨项目的大规模可靠性评测。

## 6 结论与未来工作

HoH 按固定顺序调用现有编码 Agent 做规划、开发和测试，让它从头构建软件，再一轮轮完善。**每轮保留项目文件和测试证据**，下一轮就有代码可接着写，也知道哪里还需要改；现有编码 Harness 的实现保持不变。

三个基准中的三组配置都做出了评分更高的软件，增加开发轮数后也仍有收益。Fusepoint 案例记录了同一个项目在 70 轮里增加功能、旧功能再次出错，再继续修复的过程。

后续计划把 HoH 用到**更多类型的游戏和其他真实软件系统**，检查它能否处理不同开发需求，进一步探索通用的自主软件开发框架。

## 总结

HoH 每轮都留下**当前项目、本轮计划、已经验证的功能和仍未解决的问题**。下一轮规划器据此定任务，开发者接着代码修改，测试者再检查结果，开发就能逐步往前推进。

这套流程能让开发持续推进，但独立 QA 能查到多少问题，仍取决于模型、工具以及实际检查了哪些功能。后续修改也可能破坏旧功能，平均成绩提高不代表每个项目、每一轮都在进步。**HoH 保持模型参数和底层 Harness 不变，通过任务安排、项目复用和测试反馈提高最终软件质量**。
