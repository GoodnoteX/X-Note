> 大家好，这里是Goodnote（好评笔记）。本文详细介绍我们对论文《REACT: SYNERGIZING REASONING AND ACTING IN LANGUAGE MODELS》的理解。

![](<images/L2-1.-ReAct-01.png>)

> 普林斯顿大学与谷歌大脑团队 2023 年发表于 ICLR 的研究《REACT: SYNERGIZING REASONING AND ACTING IN LANGUAGE MODELS》，提出了一种简洁却颠覆认知的解决方案 ——ReAct 协同范式，通过引导大模型交替进行 “推理规划” 与 “外部行动”，彻底破解纯推理的幻觉难题与纯行动的盲目缺陷，大幅提升其在复杂知识任务和交互式决策任务上的表现与可信度。

## 摘要

**背景**  
大型语言模型在语言理解、交互式决策任务中表现亮眼，但模型的推理能力（如思维链提示）与行动能力（如行动计划生成）相关研究长期处于割裂状态。推理与行动研究分离，使得模型无法结合二者优势完成复杂任务，成为性能与能力拓展的核心阻碍。

本文提出了ReAct方法，探索让语言模型以**交错方式**生成**推理轨迹**和**特定任务行动**，实现二者的深度协同：

- 推理轨迹助力模型推导、跟踪和更新行动计划，并处理异常情况；
- 而行动则让模型能够与知识库、环境等外部源交互，获取额外信息。

作者在多种语言任务和决策任务中对ReAct进行验证，ReAct 在多类任务中表现优于当前最优基线模型，同时提升了模型的人类可解释性与可信度。

1. 问答（HotpotQA）、事实验证（Fever）任务中，通过维基百科API交互，解决了思维链推理的幻觉、误差传播问题，生成的任务轨迹更贴近人类思维、易解释；
2. 交互式决策（ALFWorld、WebShop）任务中，仅需1-2个上下文示例提示，成功率就分别比模仿学习、强化学习方法高出34%、10%。

## 1 引言

![](<images/L2-1.-ReAct-02.png>)

> **图1：**  
> （1）四种提示方法求解HotpotQA问答任务（Yang等人，2018）的效果对比，分别为（a）标准提示法、（b）思维链提示法（CoT，仅推理）、（c）仅行动提示法、（d）ReAct提示法（推理+行动）；  
> （2）仅行动提示法（a）与ReAct提示法（b）求解AlfWorld文本游戏任务（Shridhar等人，2020b）的效果对比。在上述两类任务实验中，本文均省略了提示中的上下文示例，仅展示模型生成的任务求解轨迹（行动、想法）以及环境反馈的观测结果（Obs）。

### 研究背景：人类智能的核心机制是什么？

人类智能的一个本质特征，是能够在执行任务时，将**语言推理（内部言语）与具体行动**持续交错。推理用于跟踪进度、调整计划、处理异常和判断是否需要外部信息；行动则反过来为推理提供新的事实依据。

这种双向耦合，使人类即便在**信息不完整、环境不确定**的情况下，仍能稳定推进任务。这一机制支撑了策略制定、自我调节和工作记忆，是高效任务解决的认知基础。“推理 + 行动协同”不是工程巧合，而是**认知层面的关键能力**。

### 现有研究的两条路线：各自有效，但彼此割裂

近期研究显示，大型语言模型在两个方向上都取得了显著进展：

1. **纯推理研究（如思维链）**：大模型可通过多步推理轨迹解算术、常识等问题，但属于静态黑箱，仅依赖内部表征，无外部世界关联，易出现事实幻觉、误差传播，且反应式推理和知识更新能力受限；
2. **纯行动研究**：部分研究将预训练大模型用于交互式环境的行动规划，通过语言先验预测行动，但未让模型对高层目标做抽象推理，也无工作记忆支撑行动，仅少数研究实现了针对空间事实的有限语言推理，尚无通用任务中推理与行动协同结合的研究，也未验证该结合的系统性性能优势。

除少数工作引入了极其有限的语言推理（例如重述空间状态），**目前尚无方法系统性地将推理与行动结合，用于通用任务解决**，也缺乏对这种结合是否带来性能增益的实证验证。

### ReAct方法提出

本文提出**ReAct通用范式**，实现大模型推理与行动能力的结合，适配各类语言推理和决策任务；核心设计为通过提示让大模型交错生成语言推理轨迹和特定任务行动，核心逻辑形成双向协同：**为行动而推理**（通过动态推理制定、维持、调整面向行动的高层计划），**为推理而行动**（与外部环境交互，将额外信息融入推理过程）。

### ReAct 实验结论预览

在问答、事实验证、文本游戏、网页导航四大基准任务中，ReAct展现出显著性能优势，同时实现能力与可解释性的双重提升：

1. **知识推理任务（HotpotQA/Fever）**：调用维基百科API后，性能优于纯行动模型，与思维链模型相当；ReAct+CoT结合方法为最优，可同时利用模型内部知识和外部获取信息；
2. **交互式决策任务（ALFWorld/WebShop）**：仅1-2次示例提示的少样本设置下，性能超过经10³\~10⁵个任务实例训练的模仿/强化学习方法，成功率分别绝对提升34%、10%；
3. **额外验证**：对比仅含行动的基线模型，验证了稀疏、灵活的推理在决策任务中的重要性；
4. **附加价值**：推理与行动的结合让模型在全任务领域更具可解释性、可信度、可诊断性，人类可清晰区分模型内外部知识，通过推理轨迹理解行动决策依据。

### 核心贡献：明确本文的四大研究价值与未来拓展方向

本文的四大核心研究贡献：

1. **方法创新**：提出ReAct这一新颖的基于提示的范式，实现大模型推理与行动的协同，适配通用任务解决；
2. **实验验证**：在多类基准任务中开展大量实验，证实少样本设置下ReAct相较单独推理/单独行动的现有方法具有显著优势；
3. **机理分析**：通过系统性消融实验和分析，阐明行动对推理任务、推理对交互式任务的重要作用；
4. **局限与潜力**：分析了提示设置下ReAct对推理/行动行为支撑有限的局限性，同时通过初步微调实验，验证其在更多训练数据下的性能提升潜力。

未来将ReAct扩展至多任务训练应用、与强化学习等范式结合，有望进一步释放大模型的能力潜力。

## 2 ReAct：实现推理与行动的协同

### 背景问题：Agent 任务求解的核心困境

聚焦智能体与环境交互的通用任务场景：时间步t内，智能体依策略$\pi(a_{t} | c_{t})$，根据上下文$c_{t}$（过往+当前的观测、行动）执行行动$a_{t}$、接收观测$o_{t}$。当$c_{t} \mapsto a_{t}$映射高度隐含、计算量大时，策略学习难度陡增；纯行动智能体因缺乏推理，无法对上下文做复杂分析，易出现行动错误、生成幻觉行为。

### 方法核心：ReAct的行动空间扩展与核心设计

ReAct核心是**扩展智能体行动空间**为$\hat{A}=A \cup L$（A为原行动空间、L为语言空间）：

1. 语言空间的行动$\hat{a}_{t} \in L$即**想法/推理轨迹**，无外部环境影响、无观测反馈，核心作用是通过推理当前上下文$c_{t}$整合信息，更新上下文为后续推理/行动提供支撑；
2. 有效想法具备多重功能，可分解目标、注入常识、提取观测关键信息、跟踪进度、处理异常等。如图1所示，有效的想法包含多种类型，例如：分解任务目标并制定行动计划（2b行动1、1d想法1）、注入与任务解决相关的常识知识（2b行动1）、从观测结果中提取关键信息（1d想法2、4）、跟踪进度并转换行动计划（2b行动8）、处理异常并调整行动计划（1d想法3）等。

### 实验设置：ReAct的模型与提示设计规则

因语言空间具有无限性，模型学习需强语言先验知识支撑，本研究确定以下实验设定：

1. 模型选型：采用冻结大型语言模型为基础模型，主实验使用PaLM-540B，附录补充GPT-3的验证结果；
2. 提示方式：以少样本上下文示例为提示，示例为人类完成任务的完整轨迹，包含行动、想法、环境观测三大核心要素；
3. 差异化生成规则：根据任务类型设计不同的想法-行动生成逻辑，推理核心任务采用**想法-行动-观测**交替生成的密集式推理模式，决策类大行动空间任务则让模型自主决定想法、行动的异步生成时机，仅在关键节点稀疏生成想法。

### 2.4 范式优势：ReAct的四大核心独特特征

得益于决策与推理能力深度整合于单一大语言模型，ReAct形成了区别于传统方法的四大核心优势：

#### A 设计直观、简便

ReAct 提示设计无需特殊格式、定制想法或筛选示例，人类标注者仅需在行动基础上，用语言记录自身思考过程即可。各任务的提示设计细节见第3、4节。

#### B 通用性强、灵活性高

依托灵活的想法空间与想法 - 行动生成格式，ReAct 可适配不同行动空间、推理需求的各类任务，涵盖问答、事实验证、文本游戏、网页导航等。

#### C 性能优异、鲁棒性强

仅通过1-6个上下文示例，ReAct就能对新的任务实例展现出良好的泛化能力，在不同领域中，性能始终优于仅含推理或仅含行动的基线模型。第3节的实验还验证了微调对ReAct的性能提升作用，第4节则证明了ReAct的性能对提示选择具有鲁棒性。

#### D 贴合人类、可被控制

ReAct 让模型的序列决策与推理过程更易解释，人类可轻松核查推理逻辑与事实准确性；还能通过编辑想法，实时控制、修正智能体的行为（详见第4节图5）。

## 3 知识密集型推理任务

本文首先将ReAct应用于多跳问答、事实验证等知识密集型推理任务。如图1(1d)所示，ReAct通过与维基百科API交互，检索信息为推理提供支撑，同时通过推理确定下一步的检索目标，实现了推理与行动的协同。

![](<images/L2-1.-ReAct-03.png>)

### 3.1 实验设置

#### 任务领域

本研究选取两个对知识检索和推理能力要求较高的数据集：

1. **HotpotQA**：多跳问答基准，要求模型对至少两段维基百科文本做关联推理，推导问题答案；
2. **Fever**：事实验证基准，要求模型依据维基百科文本，将声明标注为**支持**/**反驳**/**信息不足**三类结果。

两类任务均采用**仅问题/声明输入**的严苛设置：模型无任何配套支撑段落，仅能依靠自身内部知识，或通过与外部环境交互检索知识完成推理。

#### 行动空间

为模拟人类使用维基百科的交互方式，迫使模型通过**显性语言推理**完成信息检索，设计包含3类核心操作的简易维基百科网页API，作为模型的行动空间：

1. **search[实体]**：检索目标实体，若该实体的维基百科页面存在，返回页面的前5句话；否则，返回维基百科搜索引擎推荐的前5个相似实体；
2. **lookup[字符串]**：模拟浏览器Ctrl+F功能，返回页面中包含该字符串的下一句话；
3. **finish[答案]**：提交最终答案，结束当前任务。

该API仅能基于精确名称检索文本片段，性能远弱于当前最优的词汇/神经检索器，此设计为刻意为之，核心目的是聚焦验证**推理对行动的指导价值**，而非依赖检索器的性能优势。

### 3.2 实验方法

#### ReAct提示设计

**功能：明确ReAct在知识推理任务中的提示构建方式与核心设计**  
针对HotpotQA（6个）、Fever（3个）任务，从训练集随机选样，手动构建ReAct格式轨迹作为少样本提示；轨迹为与图1(d)一致的**密集型想法**，由多步“想法-行动-观测”构成，自由形式想法实现五大核心功能：分解问题、从维基百科观测结果中提取信息、完成常识/算术推理、指导检索重构、整合生成最终答案（附录C见更多细节）。

#### 基线模型

**功能：通过ReAct轨迹消融构建对比基线，为后续性能验证提供参照**  
对ReAct轨迹做系统性消融，构建三类基线模型提示（格式见图1(1a-1c)）：

1. 标准提示（Standard）：移除ReAct轨迹中的所有想法、行动和观测结果；
2. 思维链提示（CoT）：移除行动和观测结果，作为**仅推理**基线模型。我们还构建了自洽性基线模型（CoT-SC）：推理阶段将解码温度设为0.7，采样21条思维链轨迹，采用多数投票的结果作为最终答案，该方法被证实能稳定提升CoT的性能；
3. 仅行动提示（Act）：移除ReAct轨迹中的想法，与WebGPT的工作思路相近（WebGPT通过与互联网交互回答问题），但二者的任务、行动空间不同，且WebGPT采用模仿学习和强化学习，而非提示学习。

#### 内部知识与外部知识的结合

**功能：结合ReAct与CoT-SC各自优势，提出内外部知识融合的切换策略**

后续3.3节的分析将表明：**ReAct的问题解决过程更贴合事实、更具依据，而CoT在构建推理结构方面更准确，但易产生事实或想法的幻觉**。因此，我们提出**将ReAct与CoT-SC结合**，让**模型根据以下启发式规则，决定何时切换方法**：

1. ReAct→CoT-SC：若ReAct在设定步数内未得出答案，则切换至CoT-SC。本研究为HotpotQA和Fever分别设定7步和5步的阈值（实验表明，更多步数无法提升ReAct的性能）；
2. CoT-SC→ReAct：若 n 个CoT-SC样本中，多数答案的出现次数不足n/2（即模型的内部知识无法为任务提供足够可靠的支撑），则切换至ReAct。

#### 微调设置

**功能：解决人工大规模标注的难点，明确ReAct的微调方案与目标**  
由于人工大规模标注推理轨迹和行动的难度较大，本研究采用与Zelikman等人在STaR中相似的自举方法：利用ReAct（及其他基线模型）生成的3000条含正确答案的轨迹，微调较小的语言模型（PaLM-8B/62B），使模型能够根据输入的问题/声明，解码生成完整轨迹（包含所有想法、行动和观测结果）。更多细节见附录B.1。

### 3.3 实验结果与分析

#### ReAct持续优于仅行动模型

**功能：验证推理模块的核心价值，证明推理对行动的指导作用**  
**实验结论**：以PaLM-540B为基础模型，ReAct在HotpotQA和Fever两大任务中的性能均显著优于仅行动模型，尤其在最终答案整合阶段，推理的指导作用尤为突出（见图1(1c-d)）；后续微调实验进一步佐证，推理轨迹能让模型的行动决策更具针对性。

#### ReAct与CoT的对比

**功能：对比ReAct与纯推理基线的性能差异，剖析二者核心优劣势、失败原因，为后续内外部知识融合的改进策略提供实证依据**  
**背景**：ReAct与CoT在不同任务中表现存在差异，ReAct在Fever中性能更优（60.9 vs 56.3），在HotpotQA中略逊于CoT（27.4 vs 29.4），核心原因是Fever的“支持/反驳”声明差异极小，通过行动检索精准、最新的外部知识对任务结果至关重要。  
**方法**：为了更深入地分析ReAct和CoT在HotpotQA任务中的行为差异，作者从二者生成的轨迹中，各随机选取50条含正确答案和50条含错误答案的轨迹（共200个样本），人工标注其成功和失败模式（表2），核心发现如下：  
**实验结论**：

1. **幻觉是CoT的严重问题**：CoT在成功模式中的假阳性率远高于ReAct（14% vs 6%），且幻觉是其最主要的失败原因（占比56%）。相比之下，由于能访问外部知识库，ReAct的问题解决轨迹更贴合实际、以事实为驱动，可信度更高；
2. **推理与行动的交错降低了推理灵活性**：虽然“想法-行动-观测”的步骤交错提升了ReAct的事实依据性和可信度，但这种结构约束降低了模型构建推理步骤的灵活性，导致其推理错误率高于CoT。ReAct存在一种典型的错误模式：重复生成之前的想法和行动，我们将其归为“推理错误”——因为模型无法推理出合理的下一步行动，从而陷入循环；
3. **有效检索是ReAct的关键**：对ReAct而言，通过检索获取有效信息至关重要。23%的错误案例源于无信息价值的检索结果，这会干扰模型的推理过程，使其难以恢复并重构想法。这是模型在**事实性**和**灵活性**之间的必然权衡，也为我们提出的“ReAct与CoT结合”策略提供了依据。

**补充说明**：本研究为所有成败模式均提供了具体示例（见附录E.1），同时发现HotpotQA数据集中部分问题的标注答案存在过时问题（见图4）。

#### ReAct+CoT-SC是提示学习的最优方法

**功能：验证ReAct与CoT-SC融合策略的有效性，证实内外部知识结合对推理任务的核心价值**

![](<images/L2-1.-ReAct-04.png>)

表1显示HotpotQA、Fever的最优提示方法分别为ReAct→CoT-SC、CoT-SC→ReAct；图2可见不同CoT-SC采样数下，两种融合方法均显著且稳定优于单独的CoT-SC，仅需3-5个样本就能达到CoT-SC21个样本的性能。该结果直接证明，**合理结合模型内部知识与外部知识，对提升推理任务性能具有关键价值**。

> 图2：PaLM-540B 模型在不同 CoT‑SC 采样数量下的提示学习结果。

#### ReAct是微调的最优方法

**功能：对比四种方法在提示学习、微调下的性能差异，剖析ReAct微调最优的核心原因，指出ReAct的后续性能提升方向**

![](<images/L2-1.-ReAct-05.png>)

> 图3：在 HotPotQA 数据集上，ReAct（本文方法）与基线模型在**提示学习**和**微调**两种设置下的**性能缩放对比结果**。

**背景**：图3展示标准提示、CoT、仅行动、ReAct四种方法在HotpotQA中提示学习和微调的尺度效应。  
**实验结论**：1. 提示学习阶段，PaLM-8B/62B为基础模型的ReAct性能最差，因从上下文示例中同时学习推理和行动的难度更高；2. 仅用3000个样本微调后，ReAct成为四种方法最优解，微调后的PaLM-8B ReAct性能超所有PaLM-62B提示学习模型，微调后的PaLM-62B ReAct性能超所有PaLM-540B提示学习模型。  
**核心原因**：标准提示、CoT微调性能远逊于ReAct和仅行动模型，前者是让模型记忆可能存在幻觉的知识事实，后者是教模型如何（通过推理）行动、从维基百科获取信息，习得的是更具泛化性的知识推理能力。  
**改进方向**：所有提示学习方法性能与领域内最优方法仍有显著差距（表1），**利用更多人工编写的数据进行微调，是释放ReAct潜力的更优途径**。

## 4 决策任务

本研究还将ReAct应用于两个基于语言的交互式决策任务——ALFWorld和WebShop。这两个任务均具有复杂的环境，要求智能体在稀疏奖励的条件下完成长序列行动，因此亟需通过推理为行动和探索提供有效支撑。

### 4.1 ALFWorld 任务

ALFWorld 是与具身基准 ALFRED 对齐的合成文本游戏，共 6 类任务。智能体需在超过 50 个位置的家居环境中执行长序列文本行动，完成高层目标，对规划、跟踪和探索能力要求高。任务需要常识判断物品位置，适合大模型发挥先验知识。

- **提示设计**：每类任务标注 3 条轨迹，使用**稀疏型想法**，负责目标分解、子目标跟踪、下一步规划、常识推理。
- **评估设置**：在 134 个未见实例上测试，用 2 条轨迹组合出 6 个提示，仅行动基线去掉想法，公平对比。
- **基线**：BUTLER 模仿学习智能体，在 10⁵ 条专家轨迹上训练。

---

### 4.2 WebShop 任务

WebShop 是真实电商交互环境，含 118 万商品、1.2 万人类指令，用于检验 ReAct 在真实噪声环境下的能力。模型需根据用户指令检索、浏览、选择并购买商品。

- **评估指标**：500 条测试指令上的**平均得分**与**任务成功率**。
- **提示设计**：仅行动含检索、选商品、选选项、购买；ReAct 在此基础上增加推理，指导探索、购买时机与属性匹配。
- **基线**：

  - 模仿学习 IL：在 1012 条标注轨迹上训练
  - 模仿学习+强化学习 IL+RL：更多数据训练

### 4.3 实验结果

ReAct在ALFWorld（表3）和WebShop（表4）两个任务中的性能均优于仅行动模型：

- **ALFWorld**：ReAct 平均成功率 71%，远超仅行动（45%）和 BUTLER（37%）；最差结果 48% 也高于两类基线最优值。6 组实验中，ReAct 相对提升 33%\~90%，平均 +62%。仅行动模型无推理，无法做目标拆解与状态跟踪。
- **WebShop**：仅行动已接近 IL/IL+RL；加入稀疏推理后，ReAct 成功率**绝对提升 10%**。推理能在噪声观测与行动间建立桥梁，更准确匹配商品与用户指令。但整体仍明显低于人类水平。

### 4.4 内部推理与外部反馈的价值

ReAct 是首个把 LLM 推理+行动用于闭环交互环境的工作，最接近的前期研究是**内部独白（IM）**。

- IM 只做状态描述和操作复述；
- ReAct 推理更稀疏、更灵活，支持目标分解、常识、规划等多种推理。

为了对比ReAct与IM的差异，同时凸显**内部推理**相对**简单外部反馈反应**的重要性，作者开展了消融实验：采用类IM的密集型外部反馈，构建想法模式（ReAct-IM）。消融实验结论：

**消融实验（ReAct-IM，类 IM 外部反馈式想法）**：  
ReAct 明显优于 ReAct-IM（71% vs 53%），6 类任务中 5 类占优。  
ReAct-IM 缺少高层目标分解与常识推理，无法判断子目标完成与物品位置；这些问题在 ReAct 中都能解决。

## 5 相关研究

### 5.1 语言模型与推理

利用语言模型完成推理的最经典研究是**思维链（CoT）**，该研究揭示了大型语言模型能够为问题解决构建自身“思考流程”的能力。后续相关研究层出不穷，包括：用于解决复杂任务的**由浅入深提示**、**零样本思维链**、**自洽性推理**等。近期有研究系统分析了思维链的构成和结构，发现符号、模式和文本的结合对思维链的有效性至关重要。

也有研究突破简单的提示学习，提出了更复杂的推理架构，例如：**选择-推理**将推理过程分为“选择”和“推理”两步；**STaR**通过在模型自身生成的正确推理依据上微调，实现推理过程的自举；**可信推理**将多步推理分解为三个步骤，每个步骤由专用的语言模型完成。类似的**草稿本**方法，通过在中间计算步骤上微调语言模型，也在多步计算问题中实现了性能提升。

与上述方法不同，ReAct的核心并非孤立、固定的推理，而是将模型的行动及对应的观测结果融入连贯的输入流，让模型的推理更准确，同时能够处理推理之外的任务（如交互式决策）。

### 5.2 语言模型与决策

大型语言模型的能力已突破单纯语言生成，成为交互式环境中的**决策策略模型**，相关研究已形成热点，以下结合核心工作与 ReAct 展开对比，明确 ReAct 的差异化定位：

1. **WebGPT**：核心是利用语言模型与浏览器交互、导航网页，完成ELI5数据集复杂问题的推理作答。与ReAct相比，其核心差距在于**未对推理过程进行显性建模**，无法直观呈现决策逻辑，且依赖昂贵的人类反馈完成强化学习，策略学习成本高。
2. **对话与任务式API调用类研究**（BlenderBot、Sparrow聊天机器人、SimpleTOD任务对话系统等）：这类方法通过训练语言模型完成API调用决策，适用于对话或特定任务场景，但存在两大局限：一是未显性考虑推理过程，决策逻辑不可追溯；二是依赖昂贵的数据集和人类反馈开展策略学习。相比之下，ReAct的策略学习成本显著更低——仅需用语言描述推理过程即可实现（人类反馈可作为补充，本研究将其留作未来工作）。
3. **交互式/具身环境规划决策类研究**（与ReAct最相关）：

   - SayCan：通过提示让语言模型直接预测机器人可执行行动，再结合视觉环境的可用性模型对行动排序，确定最终决策；未体现显性推理，核心依赖视觉反馈与行动排序机制。
   - 内部独白（IM）：通过增加“内部独白”提升性能，本质是注入环境反馈信息，也是首个实现闭环系统的相关研究，ReAct在此基础上进行了拓展。但需明确的是，IM并非真正的“内部思考”（其独白仅局限于环境状态描述和操作复述，缺乏高层推理），这一点已在第4节通过实验与定性分析详细阐述。

此外，已有研究已证实，在交互式决策过程中，将语言作为语义丰富的输入具有显著效果；而随着大型语言模型的发展，越来越多的证据表明，语言作为一种基础认知机制，将**在交互和决策中发挥关键作用**，同时也推动了通用智能体的研究——这也为**ReAct“显性推理+行动协同”的范式**提供了理论与实践支撑，凸显了其研究的合理性与前瞻性。

## 6 结论

本文提出ReAct是一种简洁且有效的方法，实现**大型语言模型中推理与行动能力的协同**。通过在多跳问答、事实验证和交互式决策任务中的大量实验，作者证明ReAct能带来更优的性能，同时生成更易解释的决策轨迹。

尽管本研究的方法简洁，但面对具有大行动空间的复杂任务，ReAct 仍需要更多的示例才能实现良好的学习效果——而这很容易超出上下文学习的输入长度限制。本研究在HotpotQA任务中探索了微调方法，取得了初步的积极结果；而利用更多高质量的人工标注数据进行学习，将是进一步提升ReAct性能的关键。

将ReAct扩展至多任务训练和应用，并与强化学习等互补范式结合，有望打造性能更优的智能体，进一步释放大型语言模型在更多实际场景中的潜力。