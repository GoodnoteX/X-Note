> 大家好，这里是 Goodnote（好评笔记）。本文详细介绍我们对论文《Loop Engineering: The Anthropic Playbook for Designing Systems That Prompt Your Agents》的理解。
> 
> **这份 6 月底流传出来的 Anthropic 内部关于 Loop Engineering 的文档，把 Loop Engineering 从开始出现到爆发，再到有人开始总结范式的整个链路都串起来了。**

![](<images/Pasted image 20260630204140.png>)

> 本文把 Loop Engineering 放进 Prompt Engineering、Context Engineering、Harness Engineering 之后，给出了一个更高层的工程位置：**人不再一轮轮给 Agent 写提示词，而是设计一个会持续发现任务、分发任务、验证结果、保存状态并再次启动的系统**。它真正关心的不是“下一条提示词怎么写”，而是“围绕 Agent 的工作流怎么自己转起来，并且在该停的时候停下来”。

## 核心问题

核心问题非常清楚：**当 Agent 已经可以完成非平凡任务、工具也开始支持定时运行、并行隔离、子 Agent 和外部连接时，人还应该继续站在每一轮提示词输入的入口吗**？

回答是：不应该只停留在手动写提示词。Prompt Engineering 解决的是“这一句话怎么写”，Context Engineering 解决的是“这一轮窗口里放什么”，Harness Engineering 解决的是“单次 Agent 运行要带哪些工具和边界”。Loop Engineering 再往上一层，解决的是：**如何让这个已经配好工具和边界的 Agent 反复运行、自己发现工作、自己交接任务、自己保存状态，并把未完成事项带到下一轮**。

但 loop 不是“全自动乌托邦”，loop 让生成变得接近免费，但判断仍然稀缺。**真正难的不是让 loop 跑起来，而是在 loop 里面放进一个能说“不”的检查机制**。

## 摘要

过去两年里，各种“XX Engineering”概念跟着模型和工具演进不断出现。这里讨论的 Loop Engineering，是 2026 年 6 月前后由 Peter Steinberger、Boris Cherny、Addy Osmani 等人几乎同时提出的工作方式变化，并由 Addy Osmani 正式写成文章命名。

它和前面的提示词、上下文、Harness 不一样。前面几层默认人还坐在键盘前，一轮轮指挥 Agent；Loop Engineering 则把人从内层执行位置拿出来，让人去设计一个能替人给 Agent 写提示词、安排下一步动作的系统。

这里可以把全文主线压成四点：

1. **Loop Engineering 是第四层**：位于 Harness 之上，让单次 Agent 运行变成可重复、可调度、可衔接状态的循环。
2. **一个 loop 的单轮包含五个动作**：发现任务、交接任务、验证结果、保存状态、调度下一轮。
3. **六个部件支撑这五个动作**：自动化、工作树、技能、连接器、子 Agent、外部记忆。
4. **最关键的风险是判断失守**：Agent 自评容易自我认可，所以必须把生成者和评估者分开；否则 loop 会高速积累验证债、理解腐烂、认知屈服和 token 爆炸。

这些已经在 Claude Code、Codex、Stripe Minions 等实践里出现的东西，被整理成了一套可讨论的工程框架。

## Loop Engineering 到底是什么

过去一年里，Prompt Engineering、Context Engineering、Harness Engineering 这些词陆续出现，听起来很容易让人觉得只是又换了个新名词。但这里的 Loop Engineering 确实多了一层不同的东西：它不是让人更会操作 Agent，而是让人设计一个能自己运转、自己推进工作的系统。

### 从“给 Agent 写提示词”到“设计会写提示词的系统”

Addy Osmani 对 Loop Engineering 的定义很短：把自己从“给 Agent 写提示词的人”这个位置替换掉，改为设计那个替人给 Agent 写提示词、安排下一步动作的系统。

这个定义里最重要的是“替代自己”这个动作。过去人像引擎一样，每一步都要给 Agent 输入下一条指令；现在人变成设计引擎的人，写的不再是给 Agent 的一句话，而是一个能自动生成下一条指令、检查结果、决定下一步的系统。

所以 Loop Engineering 的核心不是“提示词写得更长”，而是人的位置变了：**人从循环内部的操作员，变成循环外部的系统设计者**。

### 一周内三个人同时点火

Loop Engineering 不是凭空出来的词。2026 年 6 月的一周里，几条线索几乎同时出现：

- Peter Steinberger 提出，不要继续直接给 coding agent 写提示词，而要设计能替你给它们写提示词的 loop；
- Boris Cherny 也表达了相似变化：他不再直接给 Claude 写提示词，而是让 loop 持续给 Claude 下指令、判断该做什么；
- Addy Osmani 在 6 月 7 日写下《Loop Engineering》，把这些说法收束成一个名字。

这些说法合在一起，指向同一个移动：**设计对象从 Agent 的单次行为，变成驱动 Agent 的完整系统**。就像 “vibe coding” 一样，这个词听起来不够严肃，但它把一种已经发生的工作方式变成了可以讨论的对象。

### 为什么是现在

这个词在 2026 年集中出现，不是因为大家突然爱造新词，而是底层工具过了一个阈值：

- coding agent 已经足够可靠，可以无人看管地完成一些非平凡任务；
- 主流 Harness 开始提供调度、自动化、子 Agent、工作树、连接器等能力；
- 单次 Agent 运行成本下降，反复运行不再显得离谱。

实践通常先于命名。团队先开始写 loop，后来才有人叫它 Loop Engineering；就像很多团队已经把“写代码的 Agent”和“审查代码的 Agent”分开，后来才有人把它命名为生成者 / 评估者分离。

这里给出的判断很重要：下一个工程概念也不一定来自模型发布，而可能来自某个能力便宜到足以被日常组合使用的时刻。

### 它站在 Harness 之上

此前，人坐在键盘前，一行行给 Agent 写提示词。Agent 做完一件事就停下来，等人继续发下一条指令。人就是那个时钟，每一次触发都来自人。

Loop Engineering 中，人设计一个会自动触发的系统：它按时间启动，派出辅助 Agent 去做事，把结果写回状态，再把这些结果作为下一轮输入。Harness 负责组装一次运行：工具、动作、上下文、完成条件；loop 负责让这次运行一轮又一轮地自己发生。

价值也随之改变：过去重要的是“我知道怎么直接指挥 Agent”，现在重要的是“我知道怎么搭 loop，以及怎么在 loop 里面放一个能说‘不’的检查点”。后者才是后面反复回到的难点。

## 从提示词、上下文到 Loop

这些“XX Engineering”不是互相替代，而是层层叠上去。越往上，关注单位越大：从一句话，到一个上下文窗口，到一次 Agent 运行，再到一个会自己反复运行的系统。

表 1 和图 1 把四层关系放在一起。

![](<images/Pasted image 20260630215751.png>)

> 表1：四层栈。每一层关注的对象和核心问题不同：
> 1. Prompt Engineering 关注如何写一句好提示词；
> 2. Context Engineering 关注当前窗口里放什么；
> 3. Harness Engineering 关注如何备齐一次 Agent 运行的条件；
> 4. Loop Engineering 关注如何让 Harness 自己反复运行。

解释：这个表的结论是，Loop Engineering 不是把前几层推翻，而是把它们包起来。没有提示词，Agent 不知道该做什么；没有上下文，Agent 看不到该看的信息；没有 Harness，Agent 没有工具、动作和完成条件；但没有 loop，这些东西只能完成一次运行，结束后仍然等人下一次启动。

![](<images/Pasted image 20260630215806.png>)

> 图1：四层栈。每一层都比下面一层关注更大的范围；Loop Engineering 自动化了 Harness 留下的“等你继续”的部分。

解释：从下往上看，Prompt Engineering 是写给模型的提示词；Context Engineering 是这一轮窗口里放什么；Harness Engineering 是单次运行里的工具、动作和“完成”定义；Loop Engineering 则让 Harness 自己一次次运行。图里“scope grows”说明关注范围不断扩大，“one floor up”说明 loop 坐在 Harness 之上，不是和 Harness 平级。

### 四层分别管什么

**Prompt Engineering** 管的是一句话怎么写，包括措辞、例子、角色、语气。它的边界是一轮交换，最大问题是每一次都默认我们在场等候。

**Context Engineering** 把问题从“我该说什么”推进到“这轮窗口里应该放什么”。它关心检索什么、总结什么、清掉什么过时信息。上下文窗口里如果塞满噪声，再好的提示词也会被浪费。

**Harness Engineering** 管的是一次 Agent 运行需要什么：能用哪些工具、允许哪些动作、什么时候加载上下文、失败后怎么恢复、什么状态算完成。它能准备好一次运行，但不会让这次运行自动重复。

**Loop Engineering** 把“等你继续”这件事自动化。当前三层都具备后，Agent 可以干净地运行一次，但运行结束就停了。Loop 给它加上定时器、并行辅助 Agent、外部状态和下一轮输入，让它从一次任务变成连续工作。

### “上一层”到底多了什么

Harness 和 loop 的差别可以用三个动词概括：

- **按时间运行**：loop 不需要人点按钮，会按计划自己醒来。
- **派出辅助 Agent**：loop 可以拆出子 Agent，一个负责草拟修改，另一个专门挑问题。
- **把结果喂给下一轮**：昨天的发现写进文件，今天早上再读回来继续推进。

这也是为什么它不是“同一个任务多跑几遍”。真正的 loop 有跨对话记忆，上一轮的结果会影响下一轮。

这个细分很重要，因为不同层的错误爆炸半径完全不同。坏提示词往往当场暴露；坏上下文会导致一轮错误答案；坏 Harness 可能让 Agent 改一次文件，但运行结束后人还能看 diff。到了 loop 层，系统可能在你睡觉时运行，把错误写进状态文件，再在第二天当成既成事实继续构建。错误不是一次性的，而会被多轮复制和放大。

### 同一个错误，在不同层的破坏范围不同

用一个例子说明：假设 Agent 误解了某个函数的返回值。

- 在提示词层，它可能只给出一次错误回答，人马上改提示词；
- 在 context 层，错误可能来自窗口里的旧文档，人清掉上下文就能纠正；
- 在 Harness 层，它可能改了一次文件，但运行结束后 diff 仍然可审；
- 在 loop 层，它可能把错误假设写进状态文件，第二天读回来，第三天继续基于它做更多改动。

这里最关键的直觉是：**错误成本取决于它在被发现之前存活了多少轮，而 loop 天生会最大化轮数**。后面的评估者、人类检查点、预算上限，本质上都是为了缩短“错误出现”和“错误被发现”之间的距离。

## 一个 Loop 的五个动作

“Loop”很容易被误解成毫无意义的空转，但这里的每一轮都有具体动作：发现值得做的工作，把任务交给 Agent，验证结果是否正确，保存状态，再决定下一轮怎么启动。

图 2 先给出一轮 loop 的五个动作。

![](<images/Pasted image 20260630220814.png>)

> 图2：单轮 loop 的五个动作。调度会闭合循环，把未完成的一轮送进第二天的运行；验证是其中能说“不”的动作。

解释：这条循环不是“重复执行同一条命令”，而是发现、交接、验证、持久化、调度的闭环。调度让今天没完成的东西进入明天；验证则是整条链路里最关键的刹车点，它负责判断结果能不能继续往后走。

### 一个具体例子：早晨分诊 Loop

Addy Osmani 的早晨分诊 loop 是一个小而完整的例子。早上自动化任务自己启动，分诊 skill 读取昨天失败的 CI、仍然打开的 issue、最近 commit，然后把结果写进 Markdown 文件或 Linear 看板。

对每个值得处理的问题，系统打开一个隔离工作树：一个子 Agent 草拟修复，另一个子 Agent 按项目 skill 和测试做审查。连接器负责自动打开 PR、更新工单；处理不了的东西进入待处理箱等人处理；状态文件保留下来，让第二天继续从这里接上。

这个例子真正重要的是：**没有一步需要人手动推着走，但它又会在该等人的地方停下来**。

### 五个动作分别做什么

表 2 把五个动作映射到早晨分诊 loop。

![](<images/Pasted image 20260630222055.png>)

> 表2：五个动作与早晨分诊 loop 的对应关系：
> 1. 发现负责找到本轮工作，
> 2. 交接负责隔离任务，
> 3. 验证负责换一个 Agent 说“不”，
> 4. 持久化负责把状态写到对话之外，
> 5. 调度负责让循环一轮轮运行。

解释：这个表的结论是，一个 loop 不是只有“Agent 干活”这一段。真正能运行下去的系统，至少要有任务来源、隔离交接、独立审查、外部状态和自动触发。缺其中任何一个，都会在后面的失败模式里变成具体问题。

五个动作可以这样理解：

- **发现（Discovery）**，决定这一轮该做什么。分诊 skill 读取 CI、issue、commit，重点是让 Agent 自己发现工作，而不是每天等人递清单。发现质量决定 loop 上限：如果找出来的都是低价值任务，后面四步做得再漂亮也没意义。
- **交接（Handoff）**，把任务从调度系统交给真正执行的 Agent。每个值得处理的问题都进入独立 `git worktree`，多个 Agent 才不会互相踩文件。任务切得越干净，后面验证和合并越容易。
- **验证（Verification）**，是最容易被偷懒、也最不能被省掉的一步。第一个子 Agent 写修复，第二个子 Agent 用不同指令、甚至不同模型来审查。同一个 Agent 给自己打分会太宽松，专门挑露洞的 Agent 才是那个能说“不”的部件。
- **持久化（Persistence）**，把结果落到对话之外：PR、ticket、待处理箱、状态文件。Loop 的记忆不能只活在上下文窗口里，因为窗口会被刷新，写进 Markdown 或看板的东西才不会丢。
- **调度（Scheduling）**，让一轮运行变成 loop。早晨自动化自己启动，状态文件让未完成发现进入第二天。如果没有自动触发，它就只是你手动跑过的一次任务。

## Loop 的六个部件

五个动作描述“一轮里发生了什么”，六个部件描述“手里必须有什么，loop 才能转起来”。两者是一一对应的：发现依赖 skill，交接依赖工作树，验证依赖子 Agent，持久化依赖记忆和连接器，调度依赖自动化。

表 3 给出六个部件和五个动作的映射。

![](<images/Pasted image 20260630222107.png>)

> 表3：六个部件与五个动作的映射。自动化对应调度，工作树对应交接，技能对应发现，连接器同时支持持久化和发现，子 Agent 对应验证，记忆对应持久化。

解释：这个表的结论是，loop 不是一个单独命令，而是一组基础设施组合。自动化让它动起来，工作树让它并行时不互相打架，技能让它不用每轮重学项目，连接器让它接触外部世界，子 Agent 让它有独立审查，记忆让它跨轮次延续。

### 自动化：让 Loop 自己启动

自动化（Automations）让 loop 挂在时间表或触发器上。没有调度，就只是一次单独运行，不是 loop。

这里有一个很实用的设计原则：自动化应该触发一个命名 skill，而不是在 cron 里粘一大段没人维护的指令。前者可以复用、更新、版本化；后者很容易烂在某个定时任务里，没人记得它为什么这么写。

调度有本地和云端两种：本地调度可以看本机文件、频率更高，但机器必须开着；云端调度可以在人离开后继续运行，但通常间隔更粗，也不一定能看到本地状态。

### 工作树：让并行 Agent 不互相踩文件

工作树（Worktree）是 Git 提供的机制，可以让同一个仓库有多个独立工作目录。它的价值会随着并行度上升而上升。

两个 Agent 同时改同一份文件，麻烦程度和两个工程师同时改同一段代码差不多。工作树解决的是文件层面的机械冲突：每个 Agent 在自己的目录里改，避免把并行变成混乱。

它不能解决人的审查带宽问题。并行 Agent 能跑多少，最终仍然取决于人和评估器能审多少。

### 技能：把项目知识写成可维护的外部知识

技能（Skill）把项目知识固定到一个 `SKILL.md` 文件里。Agent 不需要每一轮重新推断项目规则、构建方式、坑点和团队偏好。

Addy 把 skill 缓解的成本叫 **意图债（intent debt）**：反复解释“这个项目是什么、规则是什么、哪里容易错”的成本。Skill 不是一次性提示词，而是可以复用和维护的项目知识入口。

### 连接器：让 Loop 进入真实工具系统

连接器（Connector）建立在模型上下文协议（Model Context Protocol, MCP）之上，用来把 loop 接到外部世界：issue 系统、数据库、预发布环境 API、Slack、Jira、Sourcegraph 等。

一个只能看文件系统的 loop 很小。连接器决定 loop 的视野半径，也决定它能否从“给建议”变成“真正打开 PR、更新工单、读外部状态并行动”。

### 子 Agent：把写的人和判的人分开

子 Agent（Sub-agent）负责把生成者和评估者拆开。一个 Agent 同时当选手和裁判时，很容易偏向自己。更可靠的方式是保留一个额外 Agent，用不同指令、不同视角，甚至不同模型去检查结果。

这里给一个反直觉的结论：**训练或配置一个独立评估者变挑剔，比让生成者对自己的作品足够苛刻更容易**，因为大模型很容易盲目自信，甚至自负。

### 记忆：上下文会消失，磁盘不会

这里的记忆（Memory）不是模型内部记忆，而是对话之外的持久状态，例如 Markdown 文件、任务看板、PR、待处理的项目列表。

上下文是 Agent 这一轮能看到什么，刷新后就没了；记忆是跨轮次、跨天仍然存在的状态。因为Agent 会忘，仓库不会忘。没有外部记忆，loop 第二天就没法从昨天停止的地方继续。

六个部件都具备后，loop 才有骨架：自动化让它动，工作树让它不打架，技能让它不重复猜，连接器让它看外部，子 Agent 让它能纠错，记忆让它能延续。接下来的难点就变成：同一套部件，在不同人手里可能产生相反结果。

## 为什么要把生成者和评估者分开

Loop 最难的部分不是让 Agent 跑，而是在里面放一个能说“不”的东西。而写的那个Agent，恰恰最不可能说“不”的那一个。

### 它总会夸自己

让 Agent 给自己刚生成的代码打分，它往往会自信地夸自己。Anthropic 工程师 Prithvi Rajasekaran 的观察很直接：这不是模型不聪明，而是“给自己作业打分”的结构性问题。

写代码时，当前上下文里已经塞满了“为什么这么写”的理由。Agent 回头看自己的输出时，看到的不是冷冰冰的结果，而是自己刚刚说服自己的推理链。放到 loop 里，这个问题会被放大：如果每一轮是否足够好都由刚写完的 Agent 判断，loop 就会一轮轮点头，逐渐偏离真实质量。

> 这其实和人类的偏好是一样的，你可以理解一下学生时代是否有和同桌交换试卷检查的事情发生，你自己按照自己做题的思路是很难发现自己的错误的。

### 训练一个审查 Agent，比期待它自省更靠谱

解决方式不是不断提醒生成者“请严格自查”。结构上更稳的做法，是换一个独立评估者。生成者很难跳出自己的视角，但另一个 Agent 可以带着完全不同的指令，从头看这份代码。

这个思路有点像生成对抗网络（Generative Adversarial Network, GAN）：一个负责生成，一个负责挑错。迁移到 coding agent 里，就是一个 Agent 写代码，另一个 Agent 审查代码。

图 3 展示了这个生成者 / 评估者结构。

![](<images/Pasted image 20260630222154.png>)

> 图3：生成者和评估者作为两个独立 Agent。评估者不携带生成者的自我说服，默认持怀疑态度，并通过行动而不只是阅读来判断行为。

解释：生成者负责写代码，评估者接收草稿后默认假设它是坏的。评估者不是只读代码，而是可以通过 MCP 执行动作：点击页面、截图、跑测试。它给出的结果不是“看起来不错”，而是“拒绝并说明原因”或通过检查。这个结构的重点是把生成者和检查者分离。

### 评估者要行动，而不是只读代码

换一个 Agent 还不够，还需要接入各种工具。如果评估者只是读代码，它判断的是“看起来对不对”，不是“运行起来对不对”。

在前端任务里，Prithvi Rajasekaran 把评估者接到 Playwright MCP，让它能打开页面、点击按钮、截图、检查 DOM。判断基础就从“这段 JSX 看起来还行”变成“我点了按钮，页面确实跳转了，这是截图”。

换底层模型也有帮助。同一个模型只换指令，可能保留同样盲点；另一个模型或另一个实例更容易形成不同判断。社区里常见的校准方式是让评估者默认假设代码是坏的，直到被证明没问题，让评估者始终抱着怀疑的态度。

### 用 `/goal` 管住停止条件

Claude Code 的 `/goal` 把这种结构产品化：给 Agent 一个条件，让它持续运行直到条件满足。

评估者设定的大意是：评估者扮演对抗式代码审查者，默认认为代码有问题，不要表扬，按顺序检查能否运行、测试真实输出、作者遗漏的边界情况、行为是否匹配工单，并通过 Playwright MCP 真实操作页面。只有所有检查都通过，才给出 PASS，否则 REJECT 并列出原因。

Evaluator agent 和停止条件大概长这样：

```
# 评估者 Agent（.claude/agents/reviewer.md）

角色：对抗式代码审查者。

假设：在被证明没有问题之前，这段代码就是坏的。

不要表扬。找出哪里会失败。

按顺序检查：

1. 它能运行吗？要执行，不要只读代码。
2. 测试：运行测试，并粘贴真实输出。
3. 作者漏掉的边界情况。
4. 行为是否符合工单要求。

使用 Playwright MCP：打开页面、点击、截图、检查 DOM。判断实际行为，而不是判断意图。

结论：只有每一项检查都成立，才给出 PASS。否则给出 REJECT，并列出每一个原因。

# 停止条件，由一个新的小模型判断

/goal test/auth 里的所有测试都通过，并且 lint 步骤干净

```

这个配置可以拆成几层看：

- **角色先设成对抗式审查者**：不是“帮作者看看有没有问题”，而是默认代码是坏的，目标是找出失败点。
- **检查顺序从运行开始**：先执行，再看测试，再补边界情况，最后对照工单。也就是说，审查不是审美判断，而是行为判断。
- **工具权限给到评估者**：评估者可以用 Playwright MCP 打开页面、点击、截图、检查 DOM，所以它不是只在文本层面点评代码。
- **输出必须能拒绝**：只有所有检查都通过才 PASS，否则要 REJECT 并列出原因。

所以这里的 evaluator 不是“另一个总结 Agent”，而是一个带工具、带默认怀疑立场、能实际行动的审查者。它的工作不是让生成者看起来更严谨，而是把 loop 里最危险的“自我认可”切断。

停止条件也很具体：比如 “`test/auth` 下所有测试通过，并且 lint 干净”。每一轮之后，由一个新的小模型检查条件是否成立；如果不成立，就继续下一轮，而不是把控制权还给人。

这里有两个容易混淆的点：

- `/goal` 是“运行直到条件满足”，关键在停止条件和新模型检查；
- `/loop` 只是按间隔重跑，关键在周期性触发。

整节的结论很硬：**Loop 的下限由评估者决定**。生成者决定它能产出什么，评估者决定它不会放过什么。要让 loop 有能力说“不”，就必须结构性分离生成和判断，让评估者持怀疑态度、通过行动验证，并把最终停止判断交给新模型。

> Codex 通过自动化加智能体配置 达到同样能力;

## 五种最容易失控的 Loop

讲成功 loop 之前，先看失败方式更有用,因为失败比成功更有启发，也更常见。每一种失败，都对应五个动作里某一步被跳过或做坏。

图 4 把五种失败和五个动作对应起来。

![](<images/Pasted image 20260630232302.png>)

> 图4：每一种反模式都对应一个被跳过的动作。五种失败与单轮 loop 的五个动作一一对应。

解释：发现缺失会变成盲目 loop，交接缺失会变成缠绕 loop，验证缺失会变成点头 loop，持久化缺失会变成健忘 loop，调度缺失会变成手动 loop。这个图的价值在于把“loop 坏了”拆成可定位问题：到底是没找任务、没隔离、没检查、没记住，还是没自动启动。

### 点头 Loop：跳过验证

这是最常见的失败。Loop 运行，Agent 写代码，然后同一个 Agent 宣布它写得不错。没有独立检查，每一轮都会产出自我批准的结果，错误以机器速度积累。

症状也很明确：一个 loop 跑了几百轮，从来没有对自己说过“不”。对真实工作负载来说，这几乎不可能，说明根本没有真正检查。修复方式就是上一节的生成者 / 评估者分离。

### 健忘 Loop：跳过持久化

Loop 找到了好任务，也做了，但结果只活在上下文窗口里。窗口刷新后，它忘了自己做过什么。下一轮可能重新发现同一件事，甚至重复修改并和第一次尝试冲突。

症状是没有累计进展：每天早上都像从同一个地方开始。修复方式是把状态写到磁盘、看板或外部系统里。

### 手动 Loop：跳过调度

如果一个系统有四个好动作，但没有自动触发，它就不是 loop，只是人手动运行的脚本。它可能在 demo 当天很惊艳，但在人忘记运行的那天就安静死掉。

修复方式是加入真实触发器：定时器或事件触发，而不是依赖人记得。

### 盲目 Loop：跳过发现

如果每天还是人给 loop 分配任务，比如“修这三个 bug”，那它只自动化了“做”，没有自动化“找。这省下的时间只是比看起来少，其实效果并没有那么好，因为决定做什么往往才是昂贵部分。

修复方式是把发现逻辑写进 skill，让 loop 自己从 CI、issue、commit、待处理箱等来源里发现值得处理的事项。

### 缠绕 Loop：跳过交接

多个 Agent 并行运行，却都改同一个工作目录，最终编辑互相覆盖，合并变得一团糟。这个问题在单 Agent 时不明显，第一次早上五个 Agent 同时跑就会暴露。

修复方式是每个任务一个隔离工作树。

这些失败不是完全独立的。缺验证的团队往往也缺持久化，因为它们都属于“看不见安全价值”的动作。匆忙搭出来的 loop 最容易只装发现和交接，因为这两步能产生可见输出，却跳过验证、持久化、调度这些真正保证安全的部分。

## 三个真实的 Loop Engineering 案例

三个公开案例规模不同，但骨架相同：触发器启动，约束把系统限制在轨道上，最后有人类检查点。“睡觉时运行”不是模型更强，而是这套骨架足够稳。

### 一个人的早晨

Addy Osmani 的早晨分诊 loop 已经在第 3 节讲过。这里再次强调一个细节：自动化调用的是 skill，而不是粘在定时任务里的巨大说明块。

这就是一个人能运行的 loop：一个人、一台机器、每天早上由系统做掉脏活。

### Stripe Minions：每周 1300 个 PR

企业级案例是 Stripe 的 Minions。根据 Stripe 工程师 Steve Kaliski 在 How I AI podcast 的介绍，Minions 每周合并超过 1300 个机器写出的 PR，而且不是人工一行行写出来。

它的触发很轻，可以是在 Slack 里 `@bot`，也可以是加一个 emoji reaction。可靠性来自模型醒来之前的那段流程：确定性的编排器先收集上下文，扫描链接、拉 Jira、找文档，用 Sourcegraph 和 MCP 定位相关代码。关键判断是：**凡是确定性逻辑能解决的事情，就不要交给概率模型**。

图 5 把 Stripe Minions 的流水线画出来。

![](<images/Pasted image 20260701183652.png>)

> 图5：Stripe 的 Minions 流水线。确定性关卡（蓝色）和大语言模型步骤（绿色）相互衔接；凡是可以用规则处理的事情，都被放在概率模型之外。

解释：流程从人类触发开始，确定性编排器先扫描链接、拉取 Jira、用 Sourcegraph 和 MCP 组装上下文；大语言模型拿着材料写代码；硬编码关卡运行 linter，Agent 不能跳过；随后 Agent 修复 lint，再由硬编码步骤执行 git commit；最后仍然进入人类审查。这里最重要的不是“模型很强”，而是模型被夹在确定性步骤之间，不能跳过规则关卡。

Minions 还有几个重要细节：

- 它不是靠更强模型取胜，而是基于开源工具 Goose 的 fork；
- 沙箱运行在 EC2 的 Devbox 上，以“cattle not pets”的方式随时替换环境；
- 上千个 Agent 可以并发运行，但仍然不会互相踩；
- 1300 个 PR 仍然由人类 review。人没有离开，只是从写代码换到了审查位置。

### “睡觉时运行”到底依赖什么

本地 `/loop` 和桌面计划任务都要求机器开着。机器关了，loop 就停了。真正想在人不在、电脑关机时继续跑，就要用 Cloud Routines 或 GitHub Actions 这样的云端调度。

表 4 对比了几种调度方式。

![](<images/Pasted image 20260701222056.png>)

> 表4：调度选项对比。云端、桌面计划任务和 `/loop` 在运行位置、是否要求机器开机、是否要求会话打开、最小间隔、能否看到本地文件上各有取舍。

解释：这个表的结论是，没有一种调度方式全能。云端不要求本机开机，但通常看不到本地文件，最小间隔也更粗；桌面计划任务和 `/loop` 可以更频繁、能看本地状态，但依赖本机和会话。选择调度器不是口味问题，而是看任务是否绑定本地机器。

对一些广泛流传的数字要谨慎，比如“Claude Code 大约 90% 由自己写出”或大型迁移提速。这里更偏向使用一手来源支撑案例，而不是追逐听起来很炸裂的二手数字。

### 实践中如何选调度器

调度器选择可以从一个问题出发：这个 loop 的工作是否粘在本地机器上？

- 如果要每分钟检查本地开发服务器，只能本地跑，因为云端看不到你笔记本上的进程，也不能低于某些云端调度最小间隔。
- 如果要凌晨扫描仓库 issue 并自动开 PR，就不应该绑在笔记本上，因为笔记本会合盖、断电、被带走。

更准确的说法是：本地调度买到的是高频率和本地文件访问，代价是机器必须开着；云端调度买到的是真正无人值守，代价是间隔更粗、每次可能是干净 clone。成熟 loop 往往两者都用：本地做紧密内圈检查，云端做隔夜扫荡。

### 同一种能力，两套工具链

这些命令偏 Claude Code，但能力不是 Claude Code 独有。Codex 也提供类似器官，只是名字不同。

![](<images/Pasted image 20260701222112.png>)

> 表5：同一种能力在 Claude Code 和 Codex 两套工具链中的对应关系。调度、运行直到条件满足、并行隔离、子 Agent、外部连接、显式 skill、关机后运行等能力可以由不同入口实现。

解释：这个表的结论是，Loop Engineering 是一组能力，不是某个产品名字。Claude Code 里可能叫 `/loop`、`/goal`、`.claude/agents/`、Cloud Routines；Codex 里可能对应 Automations、自动重跑加评估、后台 worktree、`.codex/agents/`、MCP connector、`$skill-name`。判断一个工具链能否支持 loop，重点看六个器官是否具备，而不是命令名是不是一样。

## Loop 带来的几张隐形账单

一个会自己运行的 loop，也会自己犯错。越顺滑，错误越安静。四种成本就像不会自己清掉的账单。

### 验证债：通过了，不代表真对了

验证债（verification debt）指的是，PR 被打开和合并节省了时间，但未验证输出也在积累。问题常藏在测试没有覆盖的地方，介于“能跑”和“正确”之间。

防线是独立评估者：必须有一个不同于执行者的 Agent 或机制来审查。

### 理解腐烂：代码库增长，脑中地图没跟上

理解腐烂（comprehension rot）指的是，loop 越快交付你没写过的代码，你脑子里的系统地图越容易落后于真实代码库。

这和我们平时说的“技术债”很接近，但更具体：不是代码本身一定变差，而是 **人的理解速度跟不上代码变化速度**。防线不是把所有代码都读完，那会抵消 loop 的意义；更现实的做法是定期抽样读 loop 输出，并强迫自己解释这些改动。

### 认知屈服：人停止形成自己的判断

认知屈服（cognitive surrender）是态度层面的风险。Loop 越可靠，人越容易不再有自己的意见，直接接受它交回来的东西。

边界很清楚：loop 可以执行，但不能决定。人至少要保留说“这是错的”的能力。

### Token 爆炸：账单会直接打过来

Token 爆炸（token blowout）是唯一会直接体现在账单上的成本。Loop 会不断派出辅助 Agent、重试、跑很多轮，一个 bug 可能整夜空转，最后留下的不是修好的代码，而是不熟悉的账单。

防线是上线前设置硬上限：单次运行预算、每日预算、最大重试次数。预算上限不是单纯省钱，而是熔断器。

图 6 把四种成本的相互强化关系画出来。

![](<images/Pasted image 20260701092722.png>)

> 图6：四种成本会互相强化。未验证输出侵蚀理解，理解下降诱发认知屈服，认知屈服让 loop 跑得更久、花得更多，进而产生更多未验证输出。

解释：验证债、理解腐烂、认知屈服、token 爆炸不是四个孤立风险，而是一条循环。未验证输出越多，人越不理解；越不理解，越容易放弃判断；越放弃判断，loop 越会无人看管地跑下去；跑得越久，成本越高，也产生更多未验证输出。

### 当这些成本开始复利

假设一个 loop 一晚上开了 20 个 PR，测试全绿。表面上很成功。但如果其中 3 个 PR 有测试没覆盖的细微错误，而且没有独立评估者，这就是验证债。

如果人没有读这 20 个 PR，他对代码库的理解落后了 20 个变化，这就是理解腐烂。因为 loop 看起来很顺，人第二天更不想读新一批 PR，这就是认知屈服。再加上 loop 整夜派出辅助 Agent 和重试，账单是预估三倍，这就是 token 爆炸。

这四个成本本质上是一种失败的四种表现形式。共同防线也很清楚：保留一个能说“不”的人，并安装一个不需要人醒着也能运行的检查。

## 继续当工程师，而不是启动按钮

同一个 loop，被两个人搭出来，可能走向完全相反的结果。差别不在 loop 本身，而在使用者带进去的东西。

一个人用 loop 加速自己已经掌握的工作：读代码，有方向感，用 loop 放大已有判断。另一个人用同一个 loop 逃避理解：六个月后，他只是一个自己读不懂的机器守门人。

Loop 是一个放大器。带进去的是理解，它放大理解；带进去的是懒惰，它放大懒惰。**Loop 不是自动保证质量的工具，而是忠实放大使用者判断力的乘法器**。

这也是为什么 judgment 会被反复强调。Loop 让生成变得极其便宜：代码、计划、PR、修复都可以大量产生。稀缺的是判断：哪个方案对、哪条线该停、哪个输出虽然能跑但根上是错的。Loop 可以生成一百个选项，但它只能判断“看起来合理”，而不能真正替代工程判断。

所以 Loop Engineering 没有降低判断的重要性，反而把所有不需要判断的工作剥掉后，让判断成为剩下最值钱的部分。

## 判断力会变得更贵

如果 loop 让生成变便宜、让判断变稀缺，那团队组织方式也会变化。

### 变得充裕的东西

Loop 让代码、计划、修复、PR 变得充裕。一个工程师搭好 loop 后，可以产出过去一个小团队才有的机械产量。

过去占用工程师时间的大量活动，比如打字、样板代码、机械重构，会不断接近零成本。第一反应可能是工程师不值钱了，但真正的变化相反：**工程师价值会向判断力集中**。

### 仍然稀缺的东西

稀缺的是**决定哪些输出值得保留的判断力**。Loop 能生成一百种实现，但它无法真正知道哪一个是对的；它只能知道哪一个看起来合理。

**“看起来合理”和“实际上正确”之间的缝隙，就是工程师发挥关键作用的地方**。

这带来一个不舒服的结论：如果一个工程师的价值主要来自机械劳动，比如打字快、API 记得多、愿意写样板代码，这些价值会被 loop 快速压缩；如果价值来自判断力，loop 会把好判断放大一百倍。工具不是平等抬升所有人，而是放大每个人本来带来的东西。

### 放大器也会放大坏判断

Loop 放大好判断，也会放大坏判断。旧世界里，一个坏决策只会慢慢变成一段手写错误代码，过程足够慢，人可能半路发现。新世界里，坏决策会被机器忠实批量执行，而且机器不会停下来问“这真的对吗”。

Loop 去掉了原来能救人的慢速齿轮。过程不再慢到足以让人中途发现问题，所以“继续当工程师”不是情绪口号，而是操作上的必要条件。

## 别让 Loop 偷走你的判断

如果判断力是稀缺资源，问题就变成：怎么把判断力用在最有效的地方？这里有三条常规纪律。

### 每天留一点时间读它写了什么

对抗理解腐烂的方法，不是读完 loop 产出的所有东西，那样就失去了意义，也不现实，而是每天读一部分有代表性的样本，并强迫自己解释每个改动做了什么、为什么这么做。

解释不了一个改动，就是你的脑子里面关于这个项目的理解已经落后于代码库。这个信号越早出现越好，最好是在安静早晨的一条抽样 PR 里发现，而不是在生产事故里发现。

### 先设预算，再谈自动化

对抗 token 爆炸，要在 loop 第一次无人值守运行前就设好硬上限，而不是等第一张意外账单来了再说。

单次预算、每日预算、最大重试次数，本质上都是断路器。一个没有 cap 的 loop，相当于把花钱权限交给了它自己的 bug。

### 永远保留人工介入的入口

对抗认知屈服，不能只靠态度，要靠结构。Loop 里至少要有一个地方会停下来等人看。

这个暂停点不是因为人每次都会介入，而是因为它让人保留介入能力。把每扇门都焊死的人，真正需要进去那天会发现自己没有钥匙；留一扇门的人，随时还能走进去看 loop 在做什么。

## 先搭一个小到能审的 Loop

Stripe 的流水线是终点，不是起点。第一个 loop 应该小到几乎不像系统：只是一个定时检查某件事的小东西。

### 五步起手式

第一步，跑一个 `/loop`。Claude Code v2.1.72 之后支持按间隔重跑同一任务，它只在当前会话内有效，周期任务 7 天后过期，并且在本地机器运行；机器关了就停。

三个最小用法是：

```bash
/loop 5m check the deploy
/loop check the deploy
/loop
```

第一种是固定每 5 分钟检查一次部署；第二种让 Agent 自己掌握节奏；第三种读取 `.claude/loop.md` 里的默认循环任务。这里要注意，`/loop` 只是让任务按周期继续跑，**它本身不等于完整 loop**。如果没有发现、状态、评估者和人类审查点，它只是一个会重复运行的命令。

第二步，先读 CI 和 issue 做分诊。单纯重跑一行命令不算 loop。让它每天早上看失败 CI、过去 24 小时新 issue、最近 commit，并列出哪些值得处理。发现逻辑应该写在 skill 里，而不是写死在 schedule 里。

这里的分诊 skill 最小形态大概是：

```markdown
# .claude/skills/morning-triage/SKILL.md

名称：morning-triage

触发时机：每天早上由自动化任务调用。

读取：

- 从昨天到现在失败的 CI 运行记录
- 最近 24 小时内新建的 issue
- 自上次运行以来合并的 commit

判断：对每一项内容，判断它是否值得处理。跳过噪声，只保留可执行的发现。

输出：将发现项和状态写入：

./state/triage.md

每个发现项占一行。
```

这段不是为了展示提示词写法，而是强调发现逻辑应该沉淀成可维护的 skill。定时器只负责触发，skill 负责告诉 Agent 读什么、怎么判断、写到哪里。

第三步，加状态文件。不要把结果留在聊天窗口里。每个发现是什么、处理到哪一步，都要写进 Markdown 文件或 Linear board。

状态文件可以很朴素：

```markdown
# ./state/triage.md

| 发现项 | 来源 | 状态 |
|---|---|---|
| auth 测试偶发失败 | CI #4821 | 修复中 |
| 空指针解引用 | issue 92 | PR 已打开 |
| 依赖过期 | commit a3 | 待处理 |

```

它承担的是 loop 的外部记忆。聊天窗口会丢，状态文件不会丢；第二天能不能接着昨天继续，就看这类状态有没有落盘。

第四步，加评估者。这是最关键也最容易跳过的一步。`/goal` 让 Agent 运行直到条件满足，并由不同模型判断条件是否成立。

```bash
/goal all tests in test/auth pass and the lint step is clean
```

这条命令真正重要的是“停止条件明确”。不是让模型自己说“我觉得差不多完成了”，而是把完成写成可以被检查的条件：测试通过，lint 干净。

第五步，用工作树支持并行。每个后台 Agent 一个独立 worktree，避免互相覆盖。

```bash
claude --worktree fix/auth-test "draft the fix"
claude --worktree fix/null-deref "draft the fix"
```

这里的工作树不是可有可无的优化，而是并行 Agent 的基本隔离手段。没有隔离，多个 Agent 同时改同一份工作目录，问题会从“代码是否正确”变成“谁覆盖了谁”。

表 6 是第一个 loop 的检查清单。

![](<images/Pasted image 20260701134731.png>)

> 表6：第一个 loop 的检查清单。需要确认发现源、状态文件、评估者、隔离、token 上限和人类审查点是否都存在。

解释：前两项决定 loop 能不能跑，后四项决定它跑起来后会不会失控。新手最容易只做发现源和状态文件，看起来已经自动化了，但没有独立评估、隔离、预算和人类审查，最后就会变成没人看、没人能停、只会点头的 loop。

### 一个完整但最小的 Loop

一个最小完整 loop 的伪代码结构，包含六个器官：

- **调度**：用 GitHub Actions 的 cron 每天 6 点触发；
- **发现**：workflow 调用 `morning-triage` skill，而不是粘大段说明；
- **持久化**：skill 写入 `./state/triage.md` 并提交回仓库；
- **交接**：每个待处理问题开一个独立 worktree；
- **验证**：`/goal` 停止检查每轮运行，第二个审查 Agent 挑问题；
- **人类审查**：PR 只打开，不自动合并；不确定的事项进入 `./inbox/`。

把它连起来，可以理解为下面这条最小流水线：

```yaml
# 1. 调度 -- 真实触发器  
# (.github/workflows/triage.yml) on:

schedule:  
- cron: '0 6 * * *' # 每天 06:00 云端

# 2. 发现 -- skill，不是一堵提示词墙 run: claude --skill morning-triage

# 3. 持久化 -- 磁盘状态  
# skill 写入 ./state/triage.md 并 commit

# 4. 移交 -- 每个发现一个 worktree  
for finding in $(parse ./state/triage.md); do

claude --worktree "fix/$finding" \ --goal "tests pass and lint is clean" \ "draft a fix for $finding"

done

# 5. 验证 -- 新模型裁决  
# /goal 每轮后检查;第二审查者挑错

# 6. 人工审查 -- 敞开的门  
# 只开 PR，不自动合并;不确定进 ./inbox/

```

这段示例的关键不是命令细节，而是六个器官都齐了。一个很小但六件事齐全的 loop，才是真 loop；缺任何一个，都可能变成第 6 节里的失败模式。

这也是“能跑”和“值得信任”的区别。调度和发现能让 loop 动起来，但状态、隔离、评估者、预算、人类审查点，才决定它是否能长期安全地动下去。

### 安全地扩大 Loop

最小 loop 跑起来后，人很容易想扩大规模：更多发现、更多并行 Agent、更短间隔。更安全的顺序是：**最后再加并行**。

先增加发现范围，再增加并行执行量；先证明评估者真的抓到过错误，再让它去 gate 多个 Agent。Stripe 的案例是终点，不是入口。它可靠是因为确定性关卡被多年打磨，不是因为一开始就大规模启动。

一句话：loop 要先证明自己能停住一个坏 Agent，才有资格运行更多 Agent。

## 一个最小分诊 Skill 长什么样

附录 A 给出更完整且带注释的早晨分诊 skill。它读取 CI、issue、commit 和上一轮状态；判断候选事项是否值得处理；写入状态文件；为每个保留事项生成 worktree 和目标条件；最后声明哪些事情必须停给人处理。

附录里的 skill 原文可以直接看成一个最小可运行模板：

```markdown

# .claude/skills/morning-triage/SKILL.md

---

name: morning-triage

trigger: 每日自动化任务调用

---

## 读取：发现阶段的输入

- 自上次运行以来失败的 CI 记录

- 最近 24 小时内新建的 issue

- 从昨天到现在合并的 commit

- 上一次的 `./state/triage.md`

## 判断：决定上限的部分

对每个候选项，判断：

- 现在是否可执行，还是只是噪声？

- 是否阻塞发布？如果是，标为优先处理

- 是否已经被追踪？如果是，跳过

只保留今天值得开一个 worktree 处理的事项。

## 写入：持久化输出

追加到 `./state/triage.md`：

| finding | source | priority | status |

|---|---|---|---|

提交这个文件，这样明天还能继续读取。

## 交接：准备任务交接

对每个保留下来的发现项，输出一行任务：

worktree=fix/<slug> goal=<stop-condition>

## 停止：你给自己保留的边界

永远不要合并。永远不要删除。任何你没有足够把握的内容，都放进 `./inbox/` 交给人处理，而不是直接做成 PR。

```

这段 skill 的关键不是格式，而是它把一个 loop 最容易含糊的几件事全部写成明确边界：

- **Read**：发现阶段读什么，不靠 Agent 自己猜。它不仅读新的 CI、issue、commit，也读上一轮 `./state/triage.md`，所以发现不是从零开始。
- **Judge**：发现阶段怎么筛。候选事项要判断是不是可行动、是否阻塞发布、是否已经被追踪。这里决定 loop 后面会把算力花在什么地方。
- **Write**：状态写到哪里。`finding/source/priority/status` 这些字段分别记录问题、来源、优先级和处理状态，让第二天能接着昨天的结论继续。
- **Hand off**：如何把候选事项变成可执行任务。每个保留事项都会生成 worktree 名和停止条件，为后面的并行隔离和 `/goal` 做准备。
- **Stop**：哪些事不能自动推进。永不自动合并，永不删除，不确定就进待处理箱给人看。这个边界不是模型自己能悟出来的，必须写进 skill。

这个 skill 的几个部分正好对应 loop 动作：Read 是发现输入，Judge 决定发现质量上限，Write 负责持久化，Hand off 为交接准备任务行，Stop 写入人类边界。最重要的是 Stop：loop 无法自己推断你想在哪些地方保留控制权，必须由工程师写清楚。

如果少了 Stop，loop 会带着它并不真正拥有的信心继续往前合并。这个例子把一个抽象原则落到了文件里：**人类边界也要工程化，不然 loop 默认会继续跑**。

这个附录还有一个容易被忽略的点：Judge 决定发现质量上限。Loop 后面的执行、验证、持久化再强，如果一开始保留下来的都是噪声任务，整个系统还是会把资源花在错误方向上。所以发现阶段不是简单“扫描一下”，而是要明确判断标准：

- 这个候选事项现在是否可行动；
- 它是否阻塞发布，是否应该提优先级；
- 它是否已经被记录，是否应该跳过；
- 它是否值得今天开一个 worktree 处理。

这就是为什么 discovery sets the ceiling。发现阶段给错任务，后面做得越自动化，浪费越稳定。

## 读懂这套 Playbook 的关键词

术语表把全文里的核心概念收束到一起。

![](<images/Pasted image 20260701135810.png>)

> 表7：核心术语表。Loop、Harness、Move、Part、Generator、Evaluator、Worktree、Skill、Connector、Memory、Intent debt、Verification debt 等概念被集中定义。

解释：这个是速查表。Loop 是能发现、执行、验证、持久化并重新调度工作的系统；Harness 是武装单次 Agent 运行的工具包；Move 是单轮五个步骤之一；Part 是实现这些步骤的六个部件之一；Generator 写代码，Evaluator 独立判断；Worktree 给并行 Agent 独立工作目录；Skill 把项目知识固定下来；Connector 连接外部系统；Memory 是对话外的持久状态；Intent debt 是反复解释项目的成本；Verification debt 是“能跑”和“正确”之间积累的未验证输出。

逐项展开后，这些术语之间的关系更清楚：

- **Loop**：一个不把人放在内层循环里的工作系统。它能发现任务、执行任务、验证结果、保存状态，并把未完成工作重新调度到下一轮。
- **Harness**：单次 Agent 运行的工具包，包括工具权限、允许动作、失败恢复和“什么算完成”。Harness 管一次运行，Loop 管多轮运行。
- **Move**：一轮 loop 里的动作，也就是发现、交接、验证、持久化、调度。
- **Part**：实现这些动作的组件，比如自动化、工作树、技能、连接器、子 Agent、外部记忆。
- **Generator**：负责产出代码或方案的 Agent。它的强弱决定 loop 能生成什么。
- **Evaluator**：负责审查结果的独立 Agent。它默认怀疑，通过运行、点击、截图、测试等行动验证结果。它的强弱决定 loop 不会放过什么。
- **Worktree**：Git 的独立工作目录机制。它让每个并行 Agent 在自己的工作副本里改代码，减少文件互相覆盖。
- **Skill**：写在 `SKILL.md` 里的项目知识和任务流程。它让项目规则、坑点、构建方式、筛选标准跨轮次保留下来。
- **Connector**：基于 MCP 的外部系统接口，让 loop 能接触 issue、数据库、Slack、Jira、Sourcegraph、预发布环境等真实工具。
- **Memory**：对话外的持久状态，通常是磁盘文件、看板、PR、待处理箱。它和上下文不一样，上下文是这一轮看到什么，记忆是下一轮还能接着用什么。
- **Intent debt**：意图债，指每次重新解释项目背景、规则、偏好、坑点的成本。Skill 用来偿还这类债。
- **Verification debt**：验证债，指“看起来能跑”到“确实正确”之间累积的未验证输出。独立 evaluator、人类审查和硬编码关卡用来控制这类债。

## 最后，把 Loop 拉回工程判断

整体骨架其实很清楚：Loop Engineering 是提示词、上下文、Harness 之后的第四层，它把人从“做工作”的位置拿出来，让人设计一个能自己运行的系统。

单轮 loop 有五个动作：发现、交接、验证、持久化、调度；这些动作由六个部件实现：自动化、工作树、技能、连接器、子 Agent、记忆。Loop 的失败，基本就是某个动作被跳过。最难的动作是验证，因为 Agent 给自己打分会偏软，可靠修法不是提醒它谦虚，而是结构性换一个评估者：默认怀疑、通过行动验证、由新模型判断明确停止条件。

真实 loop 已经在运行，从个人早晨分诊到 Stripe 每周超过 1300 个机器写 PR。它们可靠不是因为模型更大，而是因为约束更硬。与此同时，loop 会带来四种安静积累的成本：验证债、理解腐烂、认知屈服和 token 爆炸。

最后一句话可以作为全文总结：**停止亲自给 Agent 写提示词，去设计替人给 Agent 写提示词的系统；但要像一个打算继续当工程师的人那样设计它，而不是像一个只想按下启动键的人那样设计它，保持你自己的思考和判断**。

评估者、状态文件、预算上限、人类审查点，本质上都是同一件事的不同实现：让人保留对高速说“yes”的机器说“no”的能力。

### 第一个loop应该怎么实践

最后补了一组采用 loop 的实践提醒，给团队落地时的一些建议。它们不是新机制，但能说明这套操作手册的重心到底在哪里。

- **先从小的 loop 开始**：能活下来的 loop 往往不是一开始就野心很大的系统，而是一个小任务端到端跑通，并且检查机制真的抓到过错误之后，再逐步扩大的系统。
- **工程上的努力优先花在评估者上**：强生成者加弱评估者，会产出自信垃圾；**普通生成者加尖锐评估者，反而会慢慢积累可靠进展**。长期复利来自后者。
- **人类审查点不是临时脚手架**：它不是等 loop 可信之后就可以删掉的东西，而是 loop 保持可信的永久特性。删掉这个点，理解腐烂就会真正开始。
- **预算上限要假设一定会空转**：不要假设 Agent 一定会聪明地自己停下来。真实系统里，总会出现某个 loop 没有产出却持续消耗 token、时间和 API 调用的情况。预算上限决定空转的后果只是日志里的一个小插曲，还是账单上的一行大数字。

核心很简单：loop 的愉悦感会侵蚀纪律。一个“看起来自己会工作”的系统，最容易让人忘掉让它可靠的那些机制。真正的实践手册不只是“如何搭 loop”，而是如何在 loop 越来越顺的时候，仍然能回答：它刚刚做的那件事到底对不对。

## 参考来源

参考来源每一个都对应框架中的一块 Loop 背景：

- **Addy Osmani 的《Loop Engineering》**：提供 Loop Engineering 的命名、组件拆解和“替代自己”的核心表述。
- **Peter Steinberger 关于设计能提示 coding agent 的 loop 的帖子**：提供“不要再直接给 coding agent 写提示词，而要设计能给 Agent 写提示词的 loop”这一传播性很强的表达。
- **Boris Cherny 关于写 loop 来提示 Claude 的公开说法**：提供从直接写提示词转向写 loop 的真实工作方式变化。
- **Prithvi Rajasekaran 的 Anthropic 长运行 Agent 经验**：提供生成者 / 评估者模式，尤其是“Agent 给自己打分会太友好”的关键观察。
- **Steve Kaliski 关于 Stripe Minions 的访谈**：提供企业级 loop 案例，也就是每周超过 1300 个机器写 PR 的流水线。
- **模型上下文协议（MCP）规范**：提供连接器这一层的基础协议背景。
- **Goose 开源 Agent 框架**：提供 Stripe Minions 所基于的工具背景。
- **Claude Code 文档**：提供 `/loop`、`/goal`、worktree、skills、automations 等产品能力背景。
- **HuaShu 的 Orange Books《Loop Engineering: Stop Asking Me What It Is》**：提供会议论文风格综合稿的原始框架来源。

## 总结

Loop Engineering 的价值，不是发明了某个全新算法，而是把已经散落在 Claude Code、Codex、Addy Osmani、Anthropic 长运行 Agent、Stripe Minions 等实践里的工程经验，整理成一套更清楚的层级语言。

它最值得记住的有三点：

1. **Loop Engineering 是 Harness 之上的调度和闭环层**。Harness 让一次 Agent 运行可用，loop 让它持续运行。
2. **Loop 的核心不是自动化，而是带验证的自动化**。没有独立评估者，它只是一个会自我认可的高速生成器。
3. **AI 时代的新工程能力不是“写得更多”，而是“判断得更准”**。生成会变便宜，判断会变稀缺；loop 会放大工程师，也会放大工程师的偷懒。

**这个文章是一份总框架：Addy 的博客负责命名和组件拆解，Boris 的访谈负责说明我们工作方式变化，Business Insider 负责这个词的传播入口，而这份据说是 A 社穿出来的文章，则把它们收束成一套“层级、动作、部件、失败、案例、成本、操作纪律”的完整工程手册。**
